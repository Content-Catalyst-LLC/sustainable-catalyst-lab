# Deploy Lab Backend v0.115.0

Upload `sustainable-catalyst-lab-backend-v0.115.0.zip` and `upgrade_lab_backend_v0_115_0_contabo.sh` to `/tmp`, then run the upgrade script on the Contabo VPS. The deploy preserves compose-managed volumes and `.env.production`, verifies the v0.114 design-system surface, validates all v0.115 statistical-graphics endpoints and behavior fixtures, and treats total route count as informational only.
