from __future__ import annotations

import base64
import io
import hmac
from contextlib import asynccontextmanager
from typing import Any

from fastapi import Depends, FastAPI, Header, HTTPException, Query, Request
from fastapi.middleware.cors import CORSMiddleware
from fastapi.responses import JSONResponse, Response
from reportlab.lib.pagesizes import A4, letter
from reportlab.pdfgen import canvas

from .benchmarks import catalog as benchmark_catalog, resolve as resolve_benchmark, run_benchmark, run_convergence, run_suite
from .compute import ComputeExecutionError, run_compute
from .config import settings
from .extensions import load_legacy_extensions
from .jobs import InvalidJobStateError, PersistentJobQueue, ProjectLimitError, QueueCapacityError
from .precision import policy_catalog, recommend
from .visualization import VisualizationInputError, build_spec as build_visualization_spec, catalog as visualization_catalog, csv_text as visualization_csv
from .datasets import DatasetProfileError, health as dataset_health_payload, profile_dataset
from .data_transformations import DataTransformationError, health as data_transformations_health, join_datasets as join_scientific_datasets, normalize_plan as normalize_data_transformation_plan, policies as data_transformations_policies, transform_dataset as transform_scientific_dataset
from .reproducibility import ReproducibilityError, build_manifest as build_reproducibility_manifest, compare_manifests, environment_fingerprint, health as reproducibility_health_payload, verify_manifest
from .research_provenance import ProvenanceError, health as research_provenance_health, normalize_source, normalize_evidence, verify_record as verify_provenance_record, build_provenance
from .research_quality import QualityReviewError, compare_reviews as compare_quality_reviews, evaluate_review as evaluate_quality_review, health as research_quality_health, normalize_review as normalize_quality_review, policies as research_quality_policies, verify_review as verify_quality_review
from .external_discovery import DiscoveryError, build_openurl as build_discovery_openurl, deduplicate as deduplicate_discovery, health as external_discovery_health, normalize as normalize_discovery, open_access_lookup, provider_catalog as discovery_provider_catalog, search as search_discovery
from .experiment_framework import ExperimentFrameworkError, build_report as build_experiment_report, build_run as build_experiment_run, compare_runs as compare_experiment_runs, health as experiment_framework_health, normalize_protocol, policies as experiment_framework_policies, validate_protocol, verify as verify_experiment_record
from .design_studies import DesignStudyError, analyze_results as analyze_design_results, build_batch as build_design_batch, generate_design, health as design_studies_health, normalize_study, policies as design_studies_policies, recommend_design, verify as verify_design_record
from .advanced_experimental_design import AdvancedExperimentalDesignError, generate_optimal_design, health as advanced_experimental_design_health, normalize_spec as normalize_advanced_experimental_design, policies as advanced_experimental_design_policies, sequential_plan as build_sequential_experiment_plan, verify_record as verify_advanced_experimental_design
from .scientific_workflow_composer import ScientificWorkflowComposerError, catalog as scientific_workflow_catalog, compare_runs as compare_scientific_workflow_runs, health as scientific_workflow_composer_health, normalize_workflow as normalize_scientific_workflow, policies as scientific_workflow_composer_policies, run_workflow as run_scientific_workflow, templates as scientific_workflow_templates
from .model_calibration import ModelCalibrationError, build_report as build_calibration_report, calibrate as calibrate_model, compare_models, health as model_calibration_health, normalize_study as normalize_calibration_study, policies as model_calibration_policies, validate_result as validate_calibration_result, verify as verify_calibration_record
from .model_studio import ModelStudioError, build_bundle as build_model_studio_bundle, health as model_studio_health, normalize_graph as normalize_model_studio_graph, normalize_model as normalize_model_studio_model, policies as model_studio_policies, preview_equation_model as preview_model_studio_equation, validate_equation as validate_model_studio_equation
from .model_diagnostics import ModelDiagnosticsError, compare as compare_scientific_models, cross_validate as cross_validate_scientific_model, diagnose as diagnose_scientific_model, health as model_diagnostics_health, policies as model_diagnostics_policies
from .dynamic_systems import DynamicSystemError, estimate_parameters as estimate_dynamic_system_parameters, health as dynamic_systems_health, normalize_definition as normalize_dynamic_system, policies as dynamic_systems_policies, simulate as simulate_dynamic_system, templates as dynamic_system_templates
from .dynamic_systems_v0540 import DynamicSystemsV0540Error, bifurcation_scan as dynamic_systems_v0540_bifurcation, health as dynamic_systems_v0540_health, normalize_study as normalize_dynamic_systems_v0540, phase_analysis as dynamic_systems_v0540_phase, policies as dynamic_systems_v0540_policies, simulate as simulate_dynamic_systems_v0540
from .response_surfaces import ResponseSurfaceError, explore as explore_response_surface, fit as fit_response_surface, health as response_surfaces_health, normalize_study as normalize_response_surface_study, optimize as optimize_response_surface, policies as response_surfaces_policies
from .graph_studio import GraphStudioError, build_workspace as build_graph_studio_workspace, health as graph_studio_health, normalize_figure as normalize_graph_studio_figure, normalize_graph as normalize_graph_studio_graph, policies as graph_studio_policies
from .visualization_engine_v0730 import VisualizationEngineError, build_workspace as build_visualization_workspace_v0730, health as visualization_engine_health_v0730, normalize_figure as normalize_visualization_figure_v0730, normalize_spec as normalize_visualization_spec_v0730, policies as visualization_engine_policies_v0730
from .visualization_engine_v0740 import VisualizationGrammarError, build_workspace as build_visualization_workspace_v0740, health as visualization_engine_health_v0740, normalize_figure as normalize_visualization_figure_v0740, normalize_spec as normalize_visualization_spec_v0740, policies as visualization_engine_policies_v0740
from .scientific_data_binding_v0750 import ScientificDataBindingError, bind_visualization as bind_visualization_v0750, build_figure as build_bound_figure_v0750, build_workspace as build_bound_workspace_v0750, execute_pipeline as execute_data_pipeline_v0750, health as scientific_data_binding_health_v0750, normalize_binding as normalize_data_binding_v0750, normalize_dataset as normalize_scientific_dataset_v0750, normalize_pipeline as normalize_data_pipeline_v0750, policies as scientific_data_binding_policies_v0750
from .large_data_visualization_v0760 import LargeDataVisualizationError, adapt_dataset as adapt_large_dataset_v0760, bind_visualization as bind_large_visualization_v0760, build_figure as build_large_figure_v0760, build_workspace as build_large_workspace_v0760, health as large_data_visualization_health_v0760, normalize_plan as normalize_adaptive_render_plan_v0760, policies as large_data_visualization_policies_v0760
from .scientific_scene_v0770 import ScientificSceneError, build_figure as build_scientific_scene_figure_v0770, build_workspace as build_scientific_scene_workspace_v0770, health as scientific_scene_health_v0770, normalize_camera as normalize_scientific_scene_camera_v0770, normalize_scene as normalize_scientific_scene_v0770, policies as scientific_scene_policies_v0770, scene_from_dataset as scientific_scene_from_dataset_v0770
from .time_parameter_space_v0780 import TimeParameterSpaceError, build_figure as build_time_parameter_figure_v0780, build_workspace as build_time_parameter_workspace_v0780, health as time_parameter_health_v0780, normalize_axis as normalize_state_axis_v0780, normalize_projection as normalize_4d_projection_v0780, normalize_state_space as normalize_state_space_v0780, policies as time_parameter_policies_v0780, slice_state_space as slice_state_space_v0780, state_space_from_dataset as state_space_from_dataset_v0780
from .linked_views_v0790 import LinkedViewsError, apply_link_event as apply_link_event_v0790, build_workspace as build_linked_workspace_v0790, facet_dataset as facet_dataset_v0790, health as linked_views_health_v0790, normalize_composition as normalize_linked_composition_v0790, normalize_facet as normalize_facet_v0790, normalize_link as normalize_view_link_v0790, normalize_view as normalize_linked_view_v0790, policies as linked_views_policies_v0790
from .spatial_geospatial_raster_v0800 import SpatialVisualizationError, bbox_select as bbox_select_v0800, build_spatial_figure as build_spatial_figure_v0800, build_workspace as build_spatial_workspace_v0800, health as spatial_health_v0800, normalize_crs as normalize_crs_v0800, normalize_raster as normalize_raster_v0800, normalize_vector_layer as normalize_vector_layer_v0800, normalize_viewport as normalize_viewport_v0800, policies as spatial_policies_v0800
from .annotation_measurement_markup_v0810 import ScientificMarkupError, attach_markup as attach_markup_v0810, build_workspace as build_markup_workspace_v0810, compute_measurement as compute_measurement_v0810, health as scientific_markup_health_v0810, normalize_annotation as normalize_annotation_v0810, normalize_markup_layer as normalize_markup_layer_v0810, policies as scientific_markup_policies_v0810
from .uncertainty_ensemble_distribution_v0820 import UncertaintyVisualizationError, attach_uncertainty as attach_uncertainty_v0820, build_workspace as build_uncertainty_workspace_v0820, health as uncertainty_health_v0820, normalize_distribution as normalize_distribution_v0820, normalize_ensemble as normalize_ensemble_v0820, normalize_layer as normalize_uncertainty_layer_v0820, normalize_uncertainty as normalize_uncertainty_v0820, policies as uncertainty_policies_v0820
from .provenance_aware_figures_v0830 import FigureProvenanceError, attach_provenance as attach_provenance_v0830, build_export_manifest as build_export_manifest_v0830, build_workspace as build_provenance_workspace_v0830, health as provenance_health_v0830, normalize_provenance as normalize_provenance_v0830, policies as provenance_policies_v0830, verify_provenance as verify_provenance_v0830
from .gpu_renderer_architecture_v0840 import GPURendererArchitectureError, build_diagnostics as build_renderer_diagnostics_v0840, build_workspace as build_gpu_workspace_v0840, health as gpu_renderer_health_v0840, negotiate_renderer as negotiate_renderer_v0840, normalize_browser_capabilities as normalize_browser_capabilities_v0840, normalize_picking_contract as normalize_picking_v0840, normalize_shader_descriptor as normalize_shader_v0840, plan_buffer as plan_gpu_buffer_v0840, policies as gpu_renderer_policies_v0840, renderer_registry as renderer_registry_v0840
from .webgl2_scientific_renderer_v0850 import WebGL2ScientificRendererError, build_render_plan as build_webgl2_render_plan_v0850, build_workspace as build_webgl2_workspace_v0850, health as webgl2_health_v0850, normalize_camera as normalize_webgl2_camera_v0850, normalize_draw_call as normalize_webgl2_draw_call_v0850, normalize_picking as normalize_webgl2_picking_v0850, policies as webgl2_policies_v0850, renderer_descriptor as webgl2_renderer_descriptor_v0850
from .system_dynamics_feedback_v0860 import SystemDynamicsV0860Error, analyze_feedback as analyze_system_feedback_v0860, analyze_leverage as analyze_system_leverage_v0860, build_workspace as build_system_dynamics_workspace_v0860, health as system_dynamics_health_v0860, normalize_causal_loop as normalize_causal_loop_v0860, normalize_stock_flow as normalize_stock_flow_v0860, policies as system_dynamics_policies_v0860, simulate_stock_flow as simulate_stock_flow_v0860
from .soil_organic_carbon_v0890 import SoilOrganicCarbonV0600Error, build_project_packet as build_soc_project_packet_v0600, calculate_layer_stock as calculate_soc_layer_stock_v0600, calculate_profile_stock as calculate_soc_profile_stock_v0600, health as soil_organic_carbon_health_v0600, policies as soil_organic_carbon_policies_v0600, schema as soil_organic_carbon_schema_v0600
from .soil_carbon_sampling_v0900 import SoilCarbonSamplingV0700Error, build_field_packet as build_soc_field_packet_v0700, build_profile_handoff as build_soc_profile_handoff_v0700, build_sampling_design as build_soc_sampling_design_v0700, calculate_bulk_density as calculate_soc_bulk_density_v0700, health as soc_sampling_health_v0700, normalize_batch as normalize_soc_sample_batch_v0700, normalize_sample as normalize_soc_sample_v0700, policies as soc_sampling_policies_v0700, schema as soc_sampling_schema_v0700
from .soil_carbon_change_v0910 import SoilCarbonChangeV0800Error, build_change_series as build_soc_change_series_v0800, build_project_packet as build_soc_change_project_packet_v0800, compare_profiles as compare_soc_profiles_v0800, health as soc_change_health_v0800, policies as soc_change_policies_v0800, schema as soc_change_schema_v0800
from .soil_carbon_uncertainty_v0920 import SoilCarbonUncertaintyV0900Error, build_project_packet as build_soc_uncertainty_project_packet_v0900, change_uncertainty as calculate_soc_change_uncertainty_v0900, health as soc_uncertainty_health_v0900, policies as soc_uncertainty_policies_v0900, propagate_layer_uncertainty as propagate_soc_layer_uncertainty_v0900, schema as soc_uncertainty_schema_v0900, stratified_estimate as calculate_soc_stratified_estimate_v0900, summarize_replicates as summarize_soc_replicates_v0900
from .soil_carbon_scenarios_v0930 import SoilCarbonScenarioV01000Error, build_project_packet as build_soc_scenario_project_packet_v1000, compare_scenarios as compare_soc_management_scenarios_v1000, health as soc_scenario_health_v1000, policies as soc_scenario_policies_v1000, project_scenario as project_soc_management_scenario_v1000, schema as soc_scenario_schema_v1000, sensitivity_sweep as soc_management_sensitivity_v1000
from .whole_farm_ghg_v0940 import WholeFarmGHGV01100Error, _normalize_gwp_set as normalize_whole_farm_gwp_set_v1100, build_project_packet as build_whole_farm_ghg_project_packet_v1100, calculate_balance as calculate_whole_farm_ghg_balance_v1100, health as whole_farm_ghg_health_v1100, normalize_entry as normalize_whole_farm_ghg_entry_v1100, policies as whole_farm_ghg_policies_v1100, schema as whole_farm_ghg_schema_v1100
from .carbon_mrv_registry_v0950 import CarbonMRVRegistryV01200Error, assess_readiness as assess_carbon_mrv_readiness_v1200, build_project_packet as build_carbon_mrv_project_packet_v1200, compare_methods as compare_carbon_mrv_methods_v1200, get_method as get_carbon_mrv_method_v1200, health as carbon_mrv_registry_health_v1200, list_methods as list_carbon_mrv_methods_v1200, policies as carbon_mrv_registry_policies_v1200, schema as carbon_mrv_registry_schema_v1200
from .carbon_mrv_protocol_v0960 import CarbonMRVProtocolV01300Error, build_project_packet as build_carbon_mrv_protocol_project_packet_v1300, build_protocol as build_carbon_mrv_protocol_v1300, health as carbon_mrv_protocol_health_v1300, policies as carbon_mrv_protocol_policies_v1300, protocol_template as carbon_mrv_protocol_template_v1300, schema as carbon_mrv_protocol_schema_v1300, validate_protocol as validate_carbon_mrv_protocol_v1300
from .carbon_mrv_monitoring_v0970 import CarbonMRVMonitoringV01400Error, allocate_strata as allocate_carbon_mrv_strata_v1400, build_plan as build_carbon_mrv_monitoring_plan_v1400, build_project_packet as build_carbon_mrv_monitoring_project_packet_v1400, health as carbon_mrv_monitoring_health_v1400, plan_template as carbon_mrv_monitoring_template_v1400, policies as carbon_mrv_monitoring_policies_v1400, sample_size_plan as carbon_mrv_sample_size_v1400, schema as carbon_mrv_monitoring_schema_v1400, validate_plan as validate_carbon_mrv_monitoring_plan_v1400
from .carbon_mrv_uncertainty_v0980 import CarbonMRVUncertaintyV01500Error, build_assessment as build_carbon_mrv_uncertainty_assessment_v1500, build_project_packet as build_carbon_mrv_uncertainty_project_packet_v1500, change_detection as carbon_mrv_change_detection_v1500, health as carbon_mrv_uncertainty_health_v1500, policies as carbon_mrv_uncertainty_policies_v1500, required_sample_size as carbon_mrv_detection_sample_size_v1500, schema as carbon_mrv_uncertainty_schema_v1500, uncertainty_budget as carbon_mrv_uncertainty_budget_v1500, validate_assessment as validate_carbon_mrv_uncertainty_assessment_v1500
from .carbon_mrv_verification_ledger_v0990 import CarbonMRVVerificationLedgerV01600Error, build_ledger as build_carbon_mrv_verification_ledger_v1600, build_project_packet as build_carbon_mrv_verification_project_packet_v1600, health as carbon_mrv_verification_health_v1600, normalize_evidence_entry as normalize_carbon_mrv_evidence_entry_v1600, policies as carbon_mrv_verification_policies_v1600, schema as carbon_mrv_verification_schema_v1600, validate_ledger as validate_carbon_mrv_verification_ledger_v1600, verify_chain as verify_carbon_mrv_verification_chain_v1600
from .carbon_mrv_reporting_v01000 import CarbonMRVReportingV01700Error, build_audit_packet as build_carbon_mrv_audit_packet_v1700, build_project_packet as build_carbon_mrv_reporting_project_packet_v1700, build_report as build_carbon_mrv_report_v1700, health as carbon_mrv_reporting_health_v1700, policies as carbon_mrv_reporting_policies_v1700, report_template as carbon_mrv_report_template_v1700, schema as carbon_mrv_reporting_schema_v1700, validate_report as validate_carbon_mrv_report_v1700
from .webgpu_scientific_renderer_v0870 import WebGPUScientificRendererError, build_compute_plan as build_webgpu_compute_plan_v0870, build_render_plan as build_webgpu_render_plan_v0870, build_workspace as build_webgpu_workspace_v0870, health as webgpu_health_v0870, normalize_compute_dispatch as normalize_webgpu_compute_dispatch_v0870, normalize_render_pass as normalize_webgpu_render_pass_v0870, policies as webgpu_policies_v0870, renderer_descriptor as webgpu_renderer_descriptor_v0870
from .advanced_scientific_scene_v0880 import AdvancedScientificSceneError, build_render_plan as build_advanced_scene_render_plan_v0880, build_workspace as build_advanced_scene_workspace_v0880, health as advanced_scene_health_v0880, normalize_camera as normalize_advanced_scene_camera_v0880, normalize_light as normalize_advanced_scene_light_v0880, normalize_material as normalize_advanced_scene_material_v0880, normalize_scene as normalize_advanced_scene_v0880, policies as advanced_scene_policies_v0880, scene_descriptor as advanced_scene_descriptor_v0880
from .probabilistic_analysis import ProbabilisticAnalysisError, analyze as run_probabilistic_analysis, health as probabilistic_analysis_health, normalize_study as normalize_probabilistic_study, policies as probabilistic_analysis_policies
from .correlated_uncertainty import CorrelatedUncertaintyError, analyze as run_correlated_uncertainty, estimate_dependency as estimate_probabilistic_dependency, health as correlated_uncertainty_health, normalize_study as normalize_correlated_uncertainty_study, policies as correlated_uncertainty_policies
from .shared_model_handoff import ModelHandoffError, build_workbench_handoff, health as model_handoff_health, import_workbench_handoff, normalize_shared_model, policies as model_handoff_policies
from .reproducible_model_package import ReproducibleModelPackageError, build_package as build_reproducible_model_package, build_research_bundle as build_model_research_bundle, health as reproducible_model_package_health, policies as reproducible_model_package_policies, registry_projection as reproducible_model_registry_projection, verify_package as verify_reproducible_model_package
from .advanced_statistical_modeling import AdvancedStatisticalModelingError, compare as compare_advanced_statistical_models, cross_validate as cross_validate_advanced_statistical_model, fit as fit_advanced_statistical_model, health as advanced_statistical_modeling_health, normalize_study as normalize_advanced_statistical_study, policies as advanced_statistical_modeling_policies, predict as predict_advanced_statistical_model
from .bayesian_inference import BayesianInferenceError, fit as fit_bayesian_model, health as bayesian_inference_health, normalize_study as normalize_bayesian_study, policies as bayesian_inference_policies, posterior_predictive as run_posterior_predictive
from .distributed_dispatcher import DispatcherError, policies as distributed_dispatcher_policies
from .persistent_dispatch_queue import PersistentDistributedDispatcher
from .worker_agent_runtime import WorkerAgentError, policies as worker_agent_policies
from .artifact_transport import ArtifactStore, ArtifactTransportError
from .workflow_orchestration import WorkflowError, WorkflowOrchestrator, policies as workflow_policies
from .workflow_scheduling import WorkflowScheduleError, WorkflowScheduler, policies as workflow_scheduling_policies, verify_event_signature
from .experiment_campaigns import ExperimentCampaignError, ExperimentCampaignManager, policies as experiment_campaign_policies
from .closed_loop_campaigns import ClosedLoopError, ClosedLoopCampaignManager, policies as closed_loop_policies
from .model_registry import ModelRegistryError, ScientificModelRegistry, capture_environment, policies as model_registry_policies
from .ensemble_uncertainty import EnsembleError, EnsembleStudyManager, policies as ensemble_policies
from .surrogate_reduced_order import SurrogateROMError, SurrogateReducedOrderManager, policies as surrogate_rom_policies
from .team_workspaces import TeamWorkspaceError, TeamWorkspaceManager, policies as team_workspace_policies
from .workspace_reviews import WorkspaceReviewError, WorkspaceReviewManager, policies as workspace_review_policies
from .workspace_versioning import WorkspaceVersionError, WorkspaceVersionManager, policies as workspace_version_policies
from .artifact_repository import ArtifactRepositoryError, ScientificArtifactRepository, policies as artifact_repository_policies
from .institutional_node_federation import InstitutionalNodeError, InstitutionalNodeFederation, policies as institutional_node_policies
from .offline_edge_sync import EdgeSyncError, OfflineEdgeSyncManager, policies as edge_sync_policies
from .publication_studio import PublicationStudioError, ReproducibilityPublicationStudio, policies as publication_studio_policies
from .manuscript_assembly import ManuscriptAssemblyError, ManuscriptAssemblyStudio, policies as manuscript_assembly_policies
from .public_reproduction_portal import PublicReproductionError, PublicReproductionPortal, policies as public_reproduction_policies
from .research_interoperability import InteroperabilityError, ResearchInteroperabilityLayer, policies as research_interoperability_policies
from .typed_cross_product_handoffs import TypedCrossProductHandoffs, policies as typed_cross_product_handoff_policies
from .platform_core_v3_adapter_v01040 import PlatformCoreV3AdapterError, build_contract_product_registration as build_core_v3_contract_registration, build_session_product_binding as build_core_v3_session_product_binding, compatibility_report as core_v3_compatibility_report, health as core_v3_adapter_health, manifest as core_v3_adapter_manifest, normalize_runtime_context as normalize_core_v3_runtime_context, validate_handoff as validate_core_v3_handoff
from .platform_core_v3_object_mapping_v01050 import PlatformCoreV3ObjectMappingError, build_core_object_binding as build_core_v3_object_binding, build_core_object_binding_batch as build_core_v3_object_binding_batch, catalog as core_v3_object_mapping_catalog, health as core_v3_object_mapping_health, map_legacy_typed_handoff as map_core_v3_legacy_typed_handoff, normalize_object as normalize_core_v3_object
from .platform_core_v3_research_context_v01060 import PlatformCoreV3ResearchContextError, build_contextual_handoff_binding as build_core_v3_contextual_handoff_binding, build_contextual_object_binding as build_core_v3_contextual_object_binding, build_core_product_context_binding as build_core_v3_product_context_binding, build_core_session_registration as build_core_v3_session_registration, check_context_continuity as check_core_v3_context_continuity, health as core_v3_research_context_health, manifest as core_v3_research_context_manifest, normalize_research_context as normalize_core_v3_research_context
from .platform_core_v3_execution_lineage_v01070 import PlatformCoreV3ExecutionLineageError, build_core_execution_binding as build_core_v3_execution_binding, build_core_execution_binding_batch as build_core_v3_execution_binding_batch, check_execution_lineage as check_core_v3_execution_lineage, health as core_v3_execution_lineage_health, manifest as core_v3_execution_lineage_manifest, map_legacy_execution as map_core_v3_legacy_execution, normalize_execution as normalize_core_v3_execution
from .platform_core_v3_findings_validation_v01080 import PlatformCoreV3FindingsValidationError, build_core_claim_binding as build_core_v3_claim_binding, build_core_contradiction_binding as build_core_v3_contradiction_binding, build_core_evidence_link_binding as build_core_v3_evidence_link_binding, build_core_finding_binding as build_core_v3_finding_binding, build_core_replication_binding as build_core_v3_replication_binding, build_core_validation_challenge_binding as build_core_v3_validation_challenge_binding, health as core_v3_findings_validation_health, manifest as core_v3_findings_validation_manifest, map_legacy_scientific_claim as map_core_v3_legacy_scientific_claim, normalize_claim as normalize_core_v3_claim, normalize_finding as normalize_core_v3_finding
from .platform_core_v3_visual_scene_v01090 import PlatformCoreV3VisualSceneError, bridge_linked_views as bridge_core_v3_linked_views, bridge_uncertainty_visual as bridge_core_v3_uncertainty_visual, build_core_scene_contract as build_core_v3_scene_contract, build_core_visual_binding as build_core_v3_visual_binding, health as core_v3_visual_scene_health, manifest as core_v3_visual_scene_manifest, map_legacy_scene as map_core_v3_legacy_scene, negotiate_renderer as negotiate_core_v3_renderer, normalize_scene as normalize_core_v3_scene, normalize_visual as normalize_core_v3_visual, renderer_catalog as core_v3_renderer_catalog
from .platform_core_v3_scholarly_package_v01100 import PlatformCoreV3ScholarlyPackageError, bridge_citations as bridge_core_v3_citations, bridge_datasets_notebooks as bridge_core_v3_descriptors, build_core_package_binding as build_core_v3_package_binding, build_provenance_manifest as build_core_v3_provenance_manifest, build_publication_binding as build_core_v3_publication_binding, build_scholarly_interoperability_plan as build_core_v3_scholarly_plan, build_scholarly_package_envelope as build_core_v3_scholarly_package, build_validation_record as build_core_v3_scholarly_validation, health as core_v3_scholarly_package_health, manifest as core_v3_scholarly_package_manifest, map_legacy_package as map_core_v3_legacy_package, normalize_package as normalize_core_v3_package
from .platform_core_v3_scientific_investigation_v01110 import PlatformCoreV3ScientificInvestigationError, bridge_evidence_context as bridge_core_v3_investigation_evidence, bridge_reasoning_context as bridge_core_v3_investigation_reasoning, bridge_scientific_assets as bridge_core_v3_investigation_assets, build_core_investigation_binding as build_core_v3_investigation_binding, build_runtime_integration_plan as build_core_v3_investigation_plan, check_investigation_continuity as check_core_v3_investigation_continuity, health as core_v3_scientific_investigation_health, manifest as core_v3_scientific_investigation_manifest, map_legacy_argumentation_case as map_core_v3_legacy_argumentation_case, map_legacy_scientific_study as map_core_v3_legacy_scientific_study, normalize_investigation as normalize_core_v3_scientific_investigation
from .platform_core_v3_integration_certification_v01120 import PlatformCoreV3IntegrationCertificationError, assess_roundtrip as assess_core_v3_integration_roundtrip, build_case_plans as build_core_v3_certification_cases, build_case_result_plan as build_core_v3_certification_case_result, build_evidence_plan as build_core_v3_certification_evidence, build_exchange_check_plan as build_core_v3_certification_exchange, build_finding_plan as build_core_v3_certification_finding, build_full_submission_plan as build_core_v3_certification_full_plan, build_product_plan as build_core_v3_certification_product, build_reproduction_check_plan as build_core_v3_certification_reproduction, build_run_plan as build_core_v3_certification_run, build_suite_plan as build_core_v3_certification_suite, build_trace_check_plan as build_core_v3_certification_trace, catalog as core_v3_integration_certification_catalog, compatibility_report as core_v3_integration_certification_compatibility, health as core_v3_integration_certification_health, manifest as core_v3_integration_certification_manifest
from .platform_core_v3_production_runtime_v01130 import PlatformCoreV3ProductionRuntimeError, assess_diagnostics as assess_core_v3_production_diagnostics, assess_roundtrip as assess_core_v3_production_roundtrip, build_checkpoint as build_core_v3_production_checkpoint, build_receipt as build_core_v3_production_receipt, build_recovery_plan as build_core_v3_production_recovery, build_retry_plan as build_core_v3_production_retry, build_roundtrip_plan as build_core_v3_production_roundtrip_plan, build_submission_plan as build_core_v3_production_submission_plan, catalog as core_v3_production_runtime_catalog, check_continuity as check_core_v3_production_continuity, check_idempotency as check_core_v3_production_idempotency, check_readiness as check_core_v3_production_readiness, health as core_v3_production_runtime_health, manifest as core_v3_production_runtime_manifest, normalize_operation as normalize_core_v3_production_operation
from .scientific_visualization_design_system_v01140 import VisualizationDesignSystemError, accessibility_metadata as visualization_design_accessibility, apply_profile as visualization_design_apply_profile, audit_figure as visualization_design_audit, build_annotation_plan as visualization_design_annotations, build_export_plan as visualization_design_export_plan, build_publication_figure as visualization_design_publication_figure, catalog as visualization_design_catalog, compose_small_multiples as visualization_design_small_multiples, core_visual_plan as visualization_design_core_visual_plan, design_tokens as visualization_design_tokens, health as visualization_design_health, manifest as visualization_design_manifest, normalize_figure as visualization_design_normalize, renderer_plan as visualization_design_renderer_plan, style_uncertainty as visualization_design_uncertainty
from .advanced_statistical_uncertainty_graphics_v01150 import AdvancedStatisticalGraphicsError, build_calibration_figure as advanced_stats_calibration, build_coefficient_forest as advanced_stats_coefficients, build_coverage_figure as advanced_stats_coverage, build_distribution_figure as advanced_stats_distribution_figure, build_fan_chart as advanced_stats_fan_chart, build_interval_figure as advanced_stats_interval, build_posterior_figure as advanced_stats_posterior, build_publication_figure as advanced_stats_publication, build_qq_figure as advanced_stats_qq, build_residual_diagnostics as advanced_stats_residuals, build_sensitivity_figure as advanced_stats_sensitivity, build_uncertainty_decomposition as advanced_stats_decomposition, catalog as advanced_stats_catalog, compose_statistical_small_multiples as advanced_stats_small_multiples, explicit_kde as advanced_stats_kde, health as advanced_stats_health, manifest as advanced_stats_manifest, normalize_distribution as advanced_stats_normalize_distribution
from .interactive_scientific_dashboards_v01160 import InteractiveDashboardError, accessibility_audit as interactive_dashboards_accessibility, build_filter_state as interactive_dashboards_filter_state, build_publication_dashboard as interactive_dashboards_publication, catalog as interactive_dashboards_catalog, compose_small_multiples as interactive_dashboards_small_multiples, core_visual_plan as interactive_dashboards_core_visual_plan, export_plan as interactive_dashboards_export_plan, health as interactive_dashboards_health, manifest as interactive_dashboards_manifest, normalize_control as interactive_dashboards_normalize_control, normalize_dashboard as interactive_dashboards_normalize, normalize_link as interactive_dashboards_normalize_link, propagate_interaction as interactive_dashboards_propagate, provenance_trace as interactive_dashboards_provenance, restore_plan as interactive_dashboards_restore_plan, schema_info as interactive_dashboards_schema, snapshot_state as interactive_dashboards_snapshot, synchronize_scales as interactive_dashboards_sync_scales
from .advanced_3d_4d_scientific_visualization_v01170 import AdvancedScientificVisualizationError, accessibility_audit as advanced_3d_accessibility, build_core_visual_plan as advanced_3d_core_visual, build_dashboard_panel as advanced_3d_dashboard_panel, build_isosurface_plan as advanced_3d_isosurface, build_mesh as advanced_3d_mesh, build_publication_export as advanced_3d_publication, build_renderer_plan as advanced_3d_renderer, build_scalar_field as advanced_3d_scalar_field, build_slice_plan as advanced_3d_slice, build_surface as advanced_3d_surface, build_temporal_frames as advanced_3d_temporal_frames, build_trajectory as advanced_3d_trajectory, build_uncertainty_geometry as advanced_3d_uncertainty, build_vector_field as advanced_3d_vector_field, build_volume as advanced_3d_volume, catalog as advanced_3d_catalog, health as advanced_3d_health, manifest as advanced_3d_manifest, normalize_camera as advanced_3d_camera, normalize_scene as advanced_3d_normalize, schema_info as advanced_3d_schema
from .visual_research_narrative_figure_composer_v01180 import VisualResearchNarrativeError, accessibility_audit as narrative_accessibility, build_annotation_layer as narrative_annotations, build_caption_package as narrative_caption, build_core_visual_plan as narrative_core_visual, build_export_plan as narrative_export, build_figure_plate as narrative_figure_plate, build_layout_plan as narrative_layout, build_publication_package as narrative_publication, build_reference_panel as narrative_references, build_research_links as narrative_links, build_revision_snapshot as narrative_snapshot, catalog as narrative_catalog, health as narrative_health, manifest as narrative_manifest, normalize_figure_reference as narrative_figure, normalize_narrative as narrative_normalize, normalize_section as narrative_section, schema_info as narrative_schema, trace_provenance as narrative_provenance
from .scientific_figure_intelligence_automatic_layout_v01190 import FigureLayoutIntelligenceError, accessibility_audit as figure_layout_accessibility, analyze_figure as figure_layout_analyze, build_core_visual_plan as figure_layout_core_visual, build_export_plan as figure_layout_export, build_figure_intelligence as figure_layout_intelligence, build_snapshot as figure_layout_snapshot, catalog as figure_layout_catalog, collision_audit as figure_layout_collision_audit, explain_layout as figure_layout_explain, health as figure_layout_health, manifest as figure_layout_manifest, normalize_figure_context as figure_layout_normalize, plan_annotations as figure_layout_annotations, plan_axes as figure_layout_axes, plan_layout as figure_layout_plan, plan_legend as figure_layout_legend, plan_panels as figure_layout_panels, plan_print as figure_layout_print, plan_responsive as figure_layout_responsive, quality_audit as figure_layout_quality, schema_info as figure_layout_schema
from .exploratory_data_analysis_studio_v01200 import EDAStudioError, analyze_correlations as eda_correlations, analyze_distributions as eda_distributions, analyze_missingness as eda_missingness, analyze_outliers as eda_outliers, analyze_pca as eda_pca, analyze_relationship as eda_relationship, build_core_object_plan as eda_core_object, build_export_plan as eda_export, build_snapshot as eda_snapshot, build_studio as eda_studio, build_visualization_plan as eda_visualization, catalog as eda_catalog, compare_groups as eda_groups, health as eda_health, manifest as eda_manifest, normalize_dataset as eda_normalize, plan_transformations as eda_transform_plan, preview_transformations as eda_transform_preview, profile_dataset as eda_profile, schema_info as eda_schema
from .statistical_modeling_diagnostics_studio_v01210 import StatisticalModelingDiagnosticsError, build_core_object_plan as model_diag_core_object, build_export_plan as model_diag_export, build_snapshot as model_diag_snapshot, build_studio as model_diag_studio, build_visualization_plan as model_diag_visualization, catalog as model_diag_catalog, coefficient_report as model_diag_coefficients, effect_report as model_diag_effects, assumption_audit as model_diag_assumptions, compare_models as model_diag_compare, cross_validate_model as model_diag_cross_validate, diagnose_model as model_diag_diagnose, evaluate_predictions as model_diag_evaluate, fit_model as model_diag_fit, health as model_diag_health, manifest as model_diag_manifest, normalize_model_spec as model_diag_normalize, schema_info as model_diag_schema
from .bayesian_analysis_workbench_v01220 import BayesianWorkbenchError, build_core_object_plan as bayes2_core_object, build_export_plan as bayes2_export, build_reproduction_plan as bayes2_reproduction, build_snapshot as bayes2_snapshot, build_visualization_plan as bayes2_visualization, build_workbench as bayes2_workbench, catalog as bayes2_catalog, compare_models as bayes2_compare, convergence_audit as bayes2_convergence, fit_hierarchical_normal as bayes2_hierarchical_fit, fit_posterior as bayes2_fit, health as bayes2_health, hierarchical_summary as bayes2_hierarchical_summary, manifest as bayes2_manifest, normalize_analysis as bayes2_normalize, posterior_predictive_report as bayes2_ppc, prior_posterior_report as bayes2_prior_posterior, probability_report as bayes2_probability, sampler_diagnostics as bayes2_diagnostics, schema_info as bayes2_schema
from .simulation_monte_carlo_research_studio_v01230 import SimulationStudioError, build_compute_budget_plan as sim_mc_budget, build_core_object_plan as sim_mc_core_object, build_execution_lineage_plan as sim_mc_lineage, build_export_plan as sim_mc_export, build_reproduction_plan as sim_mc_reproduction, build_sampling_plan as sim_mc_sampling, build_snapshot as sim_mc_snapshot, build_studio as sim_mc_studio, build_visualization_plan as sim_mc_visualization, catalog as sim_mc_catalog, convergence_report as sim_mc_convergence, health as sim_mc_health, manifest as sim_mc_manifest, normalize_study as sim_mc_normalize, parameter_sweep_plan as sim_mc_sweep_plan, replication_report as sim_mc_replication, run_parameter_sweep as sim_mc_sweep_run, run_scenario_ensemble as sim_mc_scenarios, run_simulation as sim_mc_run, run_uncertainty_propagation as sim_mc_uncertainty, schema_info as sim_mc_schema, threshold_report as sim_mc_threshold
from .sensitivity_global_uncertainty_analysis_studio_v01240 import SensitivityStudioError, build_core_object_plan as sens_core_object, build_execution_lineage_plan as sens_lineage, build_export_plan as sens_export, build_reproduction_plan as sens_reproduction, build_snapshot as sens_snapshot, build_studio as sens_studio, build_visualization_plan as sens_visualization, catalog as sens_catalog, correlation_screening_report as sens_correlation, health as sens_health, interaction_screening_report as sens_interactions, interpretation_boundaries_report as sens_boundaries, manifest as sens_manifest, morris_report as sens_morris, normalize_analysis as sens_normalize, response_surface_plan as sens_surface_plan, response_surface_run as sens_surface_run, schema_info as sens_schema, sensitivity_convergence_report as sens_convergence, sensitivity_replication_report as sens_replication, sobol_report as sens_sobol, variance_decomposition_report as sens_variance
from .causal_research_studio_v01250 import CausalResearchStudioError, balance_report as causal2_balance, build_core_object_plan as causal2_core_object, build_execution_lineage_plan as causal2_lineage, build_export_plan as causal2_export, build_reproduction_plan as causal2_reproduction, build_snapshot as causal2_snapshot, build_studio as causal2_studio, build_visualization_plan as causal2_visualization, catalog as causal2_catalog, counterfactual_report as causal2_counterfactual, dag_adjustment_plan as causal2_adjustment, estimate_did as causal2_did, estimate_its as causal2_its, estimate_matching as causal2_matching, estimate_rd as causal2_rd, estimate_synthetic_control as causal2_synthetic, estimate_weighting as causal2_weighting, fit_propensity as causal2_propensity, governance_review_report as causal2_governance, health as causal2_health, interpretation_boundaries_report as causal2_boundaries, manifest as causal2_manifest, normalize_dag as causal2_dag, normalize_study as causal2_normalize, overlap_report as causal2_overlap, placebo_plan as causal2_placebo, robustness_plan as causal2_robustness, schema_info as causal2_schema
from .spatial_spatiotemporal_research_studio_v01260 import SpatialResearchStudioError, build_core_object_plan as spatial2_core_object, build_execution_lineage_plan as spatial2_lineage, build_export_plan as spatial2_export, build_reproduction_plan as spatial2_reproduction, build_snapshot as spatial2_snapshot, build_spatial_weights as spatial2_weights, build_spatiotemporal_cube as spatial2_cube, build_studio as spatial2_studio, build_visualization_plan as spatial2_visualization, catalog as spatial2_catalog, crs_audit as spatial2_crs_audit, getis_ord_gi_star as spatial2_gistar, global_morans_i as spatial2_moran, health as spatial2_health, hotspot_persistence as spatial2_persistence, interpretation_boundaries_report as spatial2_boundaries, local_morans_i as spatial2_local_moran, manifest as spatial2_manifest, nearest_neighbor_report as spatial2_nearest, normalize_study as spatial2_normalize, raster_change_summary as spatial2_raster_change, raster_zonal_stats as spatial2_zonal, schema_info as spatial2_schema, space_time_autocorrelation as spatial2_space_time, spatial_join_plan as spatial2_join, spatial_lag_estimate as spatial2_lag, spatiotemporal_change_profile as spatial2_change_profile, trajectory_report as spatial2_trajectory
from .scientific_time_series_laboratory_v01270 import TimeSeriesLaboratoryError, autocorrelation as ts_acf, build_core_object_plan as ts_core, build_execution_lineage_plan as ts_lineage, build_export_plan as ts_export, build_reproduction_plan as ts_reproduction, build_snapshot as ts_snapshot, build_studio as ts_studio, build_visualization_plan as ts_visualization, catalog as ts_catalog, change_point_analysis as ts_changepoint, cross_correlation as ts_crosscorr, decompose_series as ts_decompose, difference_series as ts_difference, exponential_smoothing as ts_ets, fit_arima as ts_arima, fit_autoregression as ts_ar, forecast as ts_forecast, frequency_audit as ts_frequency, health as ts_health, interpretation_boundaries_report as ts_boundaries, manifest as ts_manifest, normalize_series as ts_normalize, anomaly_screen as ts_anomaly, partial_autocorrelation as ts_pacf, regime_analysis as ts_regime, residual_diagnostics as ts_residuals, rolling_origin_evaluation as ts_rolling, schema_info as ts_schema, spectral_analysis as ts_spectral, state_space_local_linear_trend as ts_state_space, stationarity_diagnostics as ts_stationarity
from .experimental_design_power_analysis_v01280 import ExperimentalDesignError, adaptive_reestimation_plan as exp_adaptive, blocked_randomization_plan as exp_blocked, build_core_object_plan as exp_core, build_execution_lineage_plan as exp_lineage, build_export_plan as exp_export, build_reproduction_plan as exp_reproduction, build_snapshot as exp_snapshot, build_studio as exp_studio, build_visualization_plan as exp_visualization, catalog as exp_catalog, cluster_design_effect as exp_cluster, design_diagnostics as exp_diagnostics, factorial_design_plan as exp_factorial, health as exp_health, interpretation_boundaries_report as exp_boundaries, manifest as exp_manifest, multiple_testing_plan as exp_multiplicity, normalize_design as exp_normalize, one_proportion_power as exp_one_prop, one_sample_mean_power as exp_one_mean, one_way_anova_power as exp_anova, paired_mean_power as exp_paired, precision_sample_size as exp_precision, randomization_schedule as exp_randomization, schema_info as exp_schema, sequential_design_plan as exp_sequential, simulation_power as exp_simulation, standardized_effect_report as exp_effect, two_proportion_power as exp_two_prop, two_sample_mean_power as exp_two_mean
from .research_reproduction_replication_studio_v01290 import ReproductionReplicationError, artifact_inventory as rr_inventory, build_core_object_plan as rr_core, build_execution_lineage_plan as rr_lineage, build_export_plan as rr_export, build_snapshot as rr_snapshot, build_studio as rr_studio, build_visualization_plan as rr_visualization, catalog as rr_catalog, claim_linkage_report as rr_claims, deviation_register as rr_deviations, environment_lock_plan as rr_environment, execution_reproduction_plan as rr_execution, figure_reproduction_plan as rr_figures, health as rr_health, independent_replication_plan as rr_replication_plan, input_integrity_report as rr_integrity, interpretation_boundaries_report as rr_boundaries, manifest as rr_manifest, method_reconstruction_plan as rr_methods, normalize_study as rr_normalize, package_manifest as rr_package, provenance_reconstruction as rr_provenance, readiness_report as rr_readiness, reproduction_status_report as rr_status, replication_protocol as rr_protocol, replication_result_record as rr_replication_result, reproducibility_matrix as rr_matrix, result_comparison as rr_compare, schema_info as rr_schema, tolerance_profile as rr_tolerance
from .scientific_research_project_studio_v01300 import ResearchProjectStudioError, catalog as project_catalog, claim_evidence_index as project_claim_evidence, component_registry as project_components, core_project_plan as project_core, cross_product_bindings as project_bindings, dataset_index as project_datasets, dependency_graph as project_dependencies, execution_index as project_executions, export_plan as project_export, figure_index as project_figures, handoff_plan as project_handoff, health as project_health, interpretation_boundaries_report as project_boundaries, investigation_index as project_investigations, manifest as project_manifest, manuscript_index as project_manuscripts, milestone_plan as project_milestones, model_index as project_models, normalize_project as project_normalize, provenance_aggregate as project_provenance, publication_package_plan as project_publication, readiness_report as project_readiness, relationship_graph as project_relationships, reproduction_index as project_reproductions, research_session_plan as project_session, research_timeline as project_timeline, revision_plan as project_revision, schema_info as project_schema, build_snapshot as project_snapshot, project_state_report as project_state, workspace_layout as project_workspace, workstream_plan as project_workstreams
from .research_question_hypothesis_workspace_v01310 import ResearchQuestionHypothesisError, assumption_register as rqh_assumptions, build_snapshot as rqh_snapshot, catalog as rqh_catalog, competing_hypothesis_set as rqh_competing, core_object_plan as rqh_core, decision_boundary_plan as rqh_decisions, discriminating_test_plan as rqh_tests, disconfirmation_criteria as rqh_disconfirmation, evidence_link_plan as rqh_evidence, export_plan as rqh_export, falsification_criteria as rqh_falsification, health as rqh_health, hypothesis_registry as rqh_hypotheses, interpretation_boundaries_report as rqh_boundaries, manifest as rqh_manifest, method_eligibility_plan as rqh_methods, normalize_question as rqh_question, observable_registry as rqh_observables, operationalize_variables as rqh_variables, preregistration_deviation as rqh_deviation, preregistration_lock as rqh_lock, project_binding as rqh_project, provenance_aggregate as rqh_provenance, question_registry as rqh_questions, readiness_report as rqh_readiness, revision_plan as rqh_revision, rival_explanations as rqh_rivals, schema_info as rqh_schema, scope_boundary as rqh_scope, session_binding as rqh_session, status_report as rqh_status, visualization_plan as rqh_visual, expected_observation_matrix as rqh_expectations
from .method_selection_intelligence_v01320 import MethodSelectionIntelligenceError, alignment_report as msi_alignment, assumption_matrix as msi_assumptions, build_snapshot as msi_snapshot, catalog as msi_catalog, core_object_plan as msi_core, data_profile as msi_data, design_profile as msi_design, diagnostic_plan as msi_diagnostics, eligibility_matrix as msi_eligibility, estimand_profile as msi_estimand, export_plan as msi_export, handoff_plan as msi_handoff, health as msi_health, incompatibility_report as msi_incompatibilities, interpretation_boundaries_report as msi_boundaries, manifest as msi_manifest, method_candidates as msi_candidates, method_comparison as msi_comparison, method_decision_record as msi_decision, method_registry as msi_methods, normalize_research_context as msi_context, preanalysis_plan as msi_preanalysis, project_binding as msi_project, provenance_aggregate as msi_provenance, readiness_report as msi_readiness, requirement_gap_report as msi_gaps, researcher_shortlist as msi_shortlist, revision_plan as msi_revision, sample_structure_profile as msi_sample, schema_info as msi_schema, sensitivity_plan as msi_sensitivity, session_binding as msi_session, status_report as msi_status, temporal_spatial_profile as msi_temporal_spatial, uncertainty_profile as msi_uncertainty, validation_plan as msi_validation, visualization_plan as msi_visual
from .statistical_assumption_diagnostic_intelligence_v01330 import StatisticalAssumptionDiagnosticIntelligenceError, adjudication_record as sadi_adjudication, assumption_matrix as sadi_matrix, assumption_registry as sadi_assumptions, build_snapshot as sadi_snapshot, catalog as sadi_catalog, causal_overlap_diagnostics as sadi_causal_overlap, convergence_diagnostics as sadi_convergence, core_object_plan as sadi_core, cross_method_synthesis as sadi_synthesis, dependency_graph as sadi_dependencies, diagnostic_registry as sadi_diagnostics, distribution_diagnostics as sadi_distribution, evaluate_assumptions as sadi_evaluate, experimental_design_diagnostics as sadi_experimental, export_plan as sadi_export, handoff_plan as sadi_handoff, health as sadi_health, independence_diagnostics as sadi_independence, influence_diagnostics as sadi_influence, interpretation_boundaries_report as sadi_boundaries, manifest as sadi_manifest, method_impact_report as sadi_method_impact, missingness_diagnostics as sadi_missingness, multicollinearity_diagnostics as sadi_multicollinearity, normalize_context as sadi_context, normalize_diagnostic_observations as sadi_observations, project_binding as sadi_project, provenance_aggregate as sadi_provenance, readiness_report as sadi_readiness, remediation_options as sadi_remediation, residual_diagnostics as sadi_residuals, revision_plan as sadi_revision, robustness_plan as sadi_robustness, schema_info as sadi_schema, sensitivity_plan as sadi_sensitivity, session_binding as sadi_session, simulation_diagnostics as sadi_simulation, spatial_diagnostics as sadi_spatial, status_report as sadi_status, time_series_diagnostics as sadi_time_series, uncertainty_report as sadi_uncertainty, untested_report as sadi_untested, variance_diagnostics as sadi_variance, violation_report as sadi_violations, visualization_plan as sadi_visual
from .evidence_synthesis_intelligence_ii_v01340 import EvidenceSynthesisIntelligenceIIError, adjudication_record as esii_adjudication, assumption_diagnostic_bridge as esii_assumption_bridge, build_snapshot as esii_snapshot, catalog as esii_catalog, claim_synthesis as esii_claim_synthesis, claims_registry as esii_claims, competing_findings_report as esii_competing_findings, contradiction_report as esii_contradictions, core_object_plan as esii_core, coverage_gap_report as esii_coverage_gaps, direction_agreement_report as esii_direction, evidence_matrix as esii_evidence_matrix, export_plan as esii_export, findings_registry as esii_findings, handoff_plan as esii_handoff, health as esii_health, heterogeneity_report as esii_heterogeneity, hypothesis_evidence_map as esii_hypothesis_map, interpretation_boundaries_report as esii_boundaries, leave_one_out_synthesis as esii_leave_one_out, lineage_graph as esii_lineage, manifest as esii_manifest, mixed_methods_plan as esii_mixed_methods, narrative_synthesis_plan as esii_narrative, normalize_context as esii_context, normalize_evidence as esii_evidence, normalize_protocol as esii_protocol, project_binding as esii_project, provenance_aggregate as esii_provenance, publication_bias_diagnostic_plan as esii_publication_bias, qualitative_theme_registry as esii_themes, quantitative_synthesis as esii_quantitative, readiness_report as esii_readiness, replication_synthesis as esii_replication, revision_plan as esii_revision, robustness_plan as esii_robustness, schema_info as esii_schema, sensitivity_plan as esii_sensitivity, session_binding as esii_session, status_report as esii_status, study_characteristics_matrix as esii_study_matrix, subgroup_synthesis as esii_subgroup, uncertainty_synthesis as esii_uncertainty, visualization_plan as esii_visual
from .competing_model_hypothesis_analysis_v01350 import CompetingModelHypothesisAnalysisError, adjudication_record as cmha_adjudication, assumption_compatibility_report as cmha_assumption_compatibility, assumption_state_sensitivity as cmha_assumption_sensitivity, build_snapshot as cmha_snapshot, calibration_report as cmha_calibration, catalog as cmha_catalog, comparison_matrix as cmha_matrix, complexity_report as cmha_complexity, contradiction_matrix as cmha_contradictions, core_object_plan as cmha_core, counterexample_registry as cmha_counterexamples, decision_rule_plan as cmha_decision_rules, diagnostic_bridge as cmha_diagnostic_bridge, discriminating_observation_plan as cmha_discriminating_observation, discriminating_test_plan as cmha_discriminating_test, evidence_alignment_matrix as cmha_evidence_alignment, evidence_exclusion_sensitivity as cmha_evidence_sensitivity, export_plan as cmha_export, handoff_plan as cmha_handoff, health as cmha_health, hypothesis_registry as cmha_hypotheses, hypothesis_revision_plan as cmha_hypothesis_revision, interpretation_boundaries_report as cmha_boundaries, lineage_graph as cmha_lineage, manifest as cmha_manifest, model_fit_evidence_report as cmha_model_fit, model_registry as cmha_models, model_revision_plan as cmha_model_revision, normalize_context as cmha_context, observation_registry as cmha_observations, pairwise_comparison as cmha_pairwise, parsimony_comparison_plan as cmha_parsimony, prediction_observation_matrix as cmha_prediction_matrix, prediction_registry as cmha_predictions, predictive_performance_report as cmha_predictive_performance, project_binding as cmha_project, provenance_aggregate as cmha_provenance, readiness_report as cmha_readiness, replication_bridge as cmha_replication_bridge, researcher_shortlist as cmha_shortlist, revision_plan as cmha_revision, robustness_plan as cmha_robustness, schema_info as cmha_schema, scope_compatibility_report as cmha_scope_compatibility, session_binding as cmha_session, status_report as cmha_status, synthesis_bridge as cmha_synthesis_bridge, visualization_plan as cmha_visual, unexplained_observation_report as cmha_unexplained
from .scientific_visualization_experience_v01351 import ScientificVisualizationExperienceError, accessibility_audit as sve1351_accessibility, analytical_panels_plan as sve1351_panels, build_snapshot as sve1351_snapshot, capability_map as sve1351_capabilities, catalog as sve1351_catalog, compose_workspace as sve1351_workspace, core_visual_object_plan as sve1351_core, evidence_assumption_panel as sve1351_evidence, export_plan as sve1351_export, handoff_plan as sve1351_handoff, health as sve1351_health, interaction_contract as sve1351_interaction, interpretation_boundaries_report as sve1351_boundaries, linked_view_plan as sve1351_linked, manifest as sve1351_manifest, model_architecture_graph as sve1351_model_graph, normalize_experience as sve1351_normalize, presentation_profile as sve1351_profile, primary_figure_plan as sve1351_primary, project_binding as sve1351_project, provenance_aggregate as sve1351_provenance, provenance_pipeline as sve1351_pipeline, quality_audit as sve1351_quality, readiness_report as sve1351_readiness, renderer_stack_plan as sve1351_renderer, responsive_layout_plan as sve1351_responsive, revision_plan as sve1351_revision, schema_info as sve1351_schema, session_binding as sve1351_session, status_report as sve1351_status, visualization_plan as sve1351_visual
from .model_architecture_computational_provenance_graphs_v01352 import ModelArchitectureProvenanceGraphError, assumption_overlay_plan as macpg1352_assumptions, build_core_lineage as macpg1352_core_lineage, build_evidence_lineage as macpg1352_evidence, build_execution_trace as macpg1352_execution, build_model_architecture as macpg1352_architecture, build_provenance_graph as macpg1352_provenance, build_snapshot as macpg1352_snapshot, build_uncertainty_graph as macpg1352_uncertainty, catalog as macpg1352_catalog, core_object_plan as macpg1352_core, cycle_audit as macpg1352_cycles, dependency_graph as macpg1352_dependency, diagnostic_overlay_plan as macpg1352_diagnostics, downstream_trace as macpg1352_downstream, edge_detail as macpg1352_edge, evidence_overlay_plan as macpg1352_evidence_overlay, export_plan as macpg1352_export, graph_statistics as macpg1352_stats, handoff_plan as macpg1352_handoff, health as macpg1352_health, impact_path as macpg1352_impact, interaction_contract as macpg1352_interaction, interpretation_boundaries_report as macpg1352_boundaries, layered_layout as macpg1352_layered, linked_selection_plan as macpg1352_linked, manifest as macpg1352_manifest, node_detail as macpg1352_node, normalize_graph as macpg1352_normalize, orphan_report as macpg1352_orphans, path_trace as macpg1352_path, project_binding as macpg1352_project, radial_layout as macpg1352_radial, readiness_report as macpg1352_readiness, revision_plan as macpg1352_revision, schema_info as macpg1352_schema, session_binding as macpg1352_session, subgraph as macpg1352_subgraph, swimlane_layout as macpg1352_swimlane, timeline_overlay_plan as macpg1352_timeline, upstream_trace as macpg1352_upstream, visual_encoding_plan as macpg1352_encoding
from .multi_view_scientific_analysis_canvas_v01353 import MultiViewScientificAnalysisCanvasError, analysis_layout as mvac1353_analysis_layout, annotation_plan as mvac1353_annotation, brush_plan as mvac1353_brush, build_snapshot as mvac1353_snapshot, catalog as mvac1353_catalog, comparison_layout as mvac1353_comparison_layout, compose_canvas as mvac1353_compose, core_object_plan as mvac1353_core, cursor_sync as mvac1353_cursor, data_table_panel_plan as mvac1353_table, diagnostic_context_plan as mvac1353_diagnostic_context, diagnostics_layout as mvac1353_diagnostics_layout, diagnostics_panel_plan as mvac1353_diagnostics_panel, domain_sync as mvac1353_domain, evidence_context_plan as mvac1353_evidence_context, evidence_layout as mvac1353_evidence_layout, evidence_panel_plan as mvac1353_evidence_panel, export_plan as mvac1353_export, filter_apply as mvac1353_filter_apply, filter_normalize as mvac1353_filter_normalize, filter_share as mvac1353_filter_share, focus_plan as mvac1353_focus, handoff_plan as mvac1353_handoff, health as mvac1353_health, interpretation_boundaries_report as mvac1353_boundaries, manifest as mvac1353_manifest, model_graph_panel_plan as mvac1353_model_graph, narrative_panel_plan as mvac1353_narrative, normalize_canvas as mvac1353_normalize, panel_plan as mvac1353_panel, presentation_layout as mvac1353_presentation_layout, primary_figure_plan as mvac1353_primary, provenance_context_plan as mvac1353_provenance_context, provenance_panel_plan as mvac1353_provenance_panel, readiness_report as mvac1353_readiness, revision_plan as mvac1353_revision, schema_info as mvac1353_schema, selection_clear as mvac1353_selection_clear, selection_link as mvac1353_selection_link, selection_normalize as mvac1353_selection_normalize, uncertainty_context_plan as mvac1353_uncertainty_context, uncertainty_panel_plan as mvac1353_uncertainty_panel
from .interactive_scientific_scene_drilldown_v01354 import InteractiveScientificSceneDrillDownError
from . import interactive_scientific_scene_drilldown_v01354 as scene1354
from .scientific_scene_linking_comparative_context_v01355 import ScientificSceneLinkingComparativeContextError
from . import scientific_scene_linking_comparative_context_v01355 as scene1355
from .reproducible_visual_analysis_sessions_v01356 import ReproducibleVisualAnalysisSessionError
from . import reproducible_visual_analysis_sessions_v01356 as session1356
from .visual_research_narrative_findings_v01357 import VisualResearchNarrativeFindingsError
from . import visual_research_narrative_findings_v01357 as narrative1357
from .research_review_critique_revision_v01358 import ResearchReviewCritiqueRevisionError
from . import research_review_critique_revision_v01358 as review1358
from .graph_studio_canonical_runtime_v013581 import GraphStudioCanonicalRuntimeError
from . import graph_studio_canonical_runtime_v013581 as canonical13581
from .graph_studio_renderer_replacement_v013582 import GraphStudioRendererReplacementError
from . import graph_studio_renderer_replacement_v013582 as renderer13582
from .graph_studio_recovery_v013583 import GraphStudioRecoveryError
from . import graph_studio_recovery_v013583 as recovery13583
from . import graph_studio_bootstrap_finalization_v0135831 as bootstrap135831
from . import graph_studio_live_binding_v013584 as binding13584
from . import graph_studio_provenance_interaction_recovery_v0135841 as provenance135841
from . import graph_studio_native_provenance_v013585 as provenance13585
from . import graph_studio_provenance_authority_v0135851 as provenance135851
from . import graph_studio_incremental_interaction_v0135852 as provenance135852
from . import graph_studio_context_relationships_v0135853 as provenance135853
from . import graph_studio_object_explorer_v013590 as explorer13590
from . import graph_studio_provenance_path_analysis_v0135100 as path135100
from . import graph_studio_competing_paths_v0135110 as paths135110
from . import graph_studio_review_threads_v0135120 as review135120
from . import graph_studio_review_resolution_v0135130 as resolution135130
from . import graph_studio_review_audit_v0135140 as reviewaudit135140
from . import graph_studio_verification_artifacts_v0135150 as verification135150
from .public_research_integrations import IntegrationError, PublicResearchIntegrationGateway, policies as public_research_integration_policies, sdk_manifest as public_research_sdk_manifest, public_api_catalog
from .institutional_governance import InstitutionalGovernanceError, InstitutionalGovernanceManager, policies as institutional_governance_policies
from .security_privacy_hardening import SecurityHardeningError, SecurityPrivacyManager, policies as security_privacy_policies, privacy_scan, privacy_redact
from .multi_instance_operations import MultiInstanceOperationsManager, OperationsError, policies as multi_instance_operations_policies
from .performance_chaos_validation import PerformanceChaosManager, ValidationError as PerformanceValidationError, policies as performance_validation_policies
from .scientific_compute_hardening import ScientificComputeHardeningError, ScientificComputeManager, assess_workload as assess_scientific_workload, dataset_window as scientific_dataset_window, policies as scientific_compute_policies
from .scientific_audit_v0590 import ScientificAuditError, build_redacted_export as build_scientific_redacted_export, data_minimization_review as scientific_data_minimization_review, health as scientific_audit_health, policies as scientific_audit_policies, reproducibility_audit as run_reproducibility_audit_v0590, scan_surface as scan_scientific_audit_surface, scientific_audit as run_scientific_audit_v0590, verify_audit as verify_scientific_audit_v0590
from .integrated_research_beta_v0600 import IntegratedResearchBetaError, beta_readiness as integrated_beta_readiness, build_beta_packet as build_integrated_beta_packet, capability_matrix as integrated_beta_capabilities, health as integrated_beta_health, policies as integrated_beta_policies, research_journey as integrated_research_journey, verify_beta_packet as verify_integrated_beta_packet
from .beta_field_diagnostics_v0601 import BetaFieldDiagnosticsError, analyze_soak as beta_field_analyze_soak, build_diagnostic_packet as build_beta_field_diagnostic_packet, health as beta_field_diagnostics_health, integration_probe as beta_field_integration_probe, policies as beta_field_diagnostics_policies, scenarios as beta_field_diagnostic_scenarios, verify_diagnostic_packet as verify_beta_field_diagnostic_packet
from .scientific_claims_traceability_v0620 import ScientificClaimsTraceabilityError, build_traceability_packet as build_scientific_traceability_packet_v0620, evaluate_matrix as evaluate_scientific_evidence_matrix_v0620, health as scientific_claims_traceability_health_v0620, normalize_claim as normalize_scientific_claim_v0620, normalize_conclusion as normalize_scientific_conclusion_v0620, policies as scientific_claims_traceability_policies_v0620, record_claim_review as record_scientific_claim_review_v0620, record_conclusion_review as record_scientific_conclusion_review_v0620, verify_traceability_packet as verify_scientific_traceability_packet_v0620
from .scientific_literature_provenance_v0630 import ScientificLiteratureProvenanceError, build_provenance_packet as build_scientific_literature_packet_v0630, evaluate_provenance as evaluate_scientific_literature_v0630, health as scientific_literature_health_v0630, normalize_citation_edge as normalize_scientific_citation_edge_v0630, normalize_claim_link as normalize_scientific_claim_link_v0630, normalize_source as normalize_scientific_literature_source_v0630, policies as scientific_literature_policies_v0630, record_source_review as record_scientific_literature_source_review_v0630, verify_provenance_packet as verify_scientific_literature_packet_v0630
from .systematic_evidence_synthesis_v0640 import SystematicEvidenceSynthesisError, build_synthesis_packet as build_evidence_synthesis_packet_v0640, health as evidence_synthesis_health_v0640, meta_analyze as meta_analyze_v0640, normalize_effect as normalize_study_effect_v0640, normalize_protocol as normalize_synthesis_protocol_v0640, policies as evidence_synthesis_policies_v0640, record_synthesis_review as record_synthesis_review_v0640, verify_synthesis_packet as verify_evidence_synthesis_packet_v0640
from .scientific_evidence_grading_v0650 import ScientificEvidenceGradingError, build_consensus_packet as build_scientific_consensus_packet_v0650, evaluate_evidence as evaluate_scientific_evidence_v0650, health as scientific_evidence_grading_health_v0650, normalize_assessment as normalize_scientific_evidence_assessment_v0650, policies as scientific_evidence_grading_policies_v0650, record_boundary_review as record_scientific_boundary_review_v0650, verify_consensus_packet as verify_scientific_consensus_packet_v0650
from .scientific_argumentation_v0660 import ScientificArgumentationError, build_argumentation_packet as build_scientific_argumentation_packet_v0660, evaluate_argumentation as evaluate_scientific_argumentation_v0660, health as scientific_argumentation_health_v0660, normalize_case as normalize_scientific_argumentation_case_v0660, normalize_discriminating_test as normalize_discriminating_test_v0660, normalize_evidence_link as normalize_argument_evidence_link_v0660, normalize_hypothesis as normalize_scientific_hypothesis_v0660, policies as scientific_argumentation_policies_v0660, record_case_review as record_scientific_argumentation_case_review_v0660, record_hypothesis_review as record_scientific_hypothesis_review_v0660, verify_argumentation_packet as verify_scientific_argumentation_packet_v0660
from .causal_inference_v0670 import CausalInferenceError, build_packet as build_causal_inference_packet_v0670, evaluate as evaluate_causal_inference_v0670, health as causal_inference_health_v0670, normalize_design as normalize_causal_design_v0670, normalize_diagnostic as normalize_causal_diagnostic_v0670, normalize_estimate as normalize_causal_estimate_v0670, policies as causal_inference_policies_v0670, record_review as record_causal_review_v0670, verify_packet as verify_causal_inference_packet_v0670
from .hierarchical_modeling_v0680 import HierarchicalModelingError, build_packet as build_hierarchical_modeling_packet_v0680, evaluate as evaluate_hierarchical_modeling_v0680, fit_model as fit_hierarchical_model_v0680, health as hierarchical_modeling_health_v0680, normalize_model as normalize_hierarchical_model_v0680, normalize_unit as normalize_hierarchical_unit_v0680, policies as hierarchical_modeling_policies_v0680, record_review as record_hierarchical_modeling_review_v0680, verify_packet as verify_hierarchical_modeling_packet_v0680
from .scientific_theory_v0690 import ScientificTheoryError, build_graph as build_scientific_theory_graph_v0690, build_packet as build_scientific_theory_packet_v0690, evaluate as evaluate_scientific_theory_v0690, health as scientific_theory_health_v0690, normalize_construct as normalize_theory_construct_v0690, normalize_prediction as normalize_theory_prediction_v0690, normalize_relation as normalize_theory_relation_v0690, normalize_theory as normalize_scientific_theory_v0690, policies as scientific_theory_policies_v0690, record_review as record_scientific_theory_review_v0690, verify_packet as verify_scientific_theory_packet_v0690
from .preregistration_v0700 import PreregistrationError, build_packet as build_preregistration_packet_v0700, evaluate as evaluate_preregistration_v0700, freeze_preregistration as freeze_preregistration_v0700, health as preregistration_health_v0700, normalize_deviation as normalize_preregistration_deviation_v0700, normalize_hypothesis as normalize_registered_hypothesis_v0700, normalize_preregistration as normalize_preregistration_v0700, normalize_question as normalize_research_question_v0700, policies as preregistration_policies_v0700, record_review as record_preregistration_review_v0700, verify_freeze as verify_preregistration_freeze_v0700, verify_packet as verify_preregistration_packet_v0700
from .scientific_study_lifecycle_v0610 import ScientificStudyLifecycleError, build_study_packet as build_scientific_study_packet_v0610, evaluate_lifecycle as evaluate_scientific_study_lifecycle_v0610, health as scientific_study_lifecycle_health_v0610, normalize_study as normalize_scientific_study_v0610, policies as scientific_study_lifecycle_policies_v0610, record_stage_review as record_scientific_study_stage_review_v0610, templates as scientific_study_templates_v0610, verify_study_packet as verify_scientific_study_packet_v0610
from .connected_platform_beta import ConnectedPlatformBetaManager, BetaPlatformError, policies as connected_platform_beta_policies
from .interface_finalization import InterfaceFinalizationManager, InterfaceFinalizationError, policies as interface_finalization_policies
from .public_release_hardening import PublicReleaseHardeningManager, PublicReleaseHardeningError, policies as public_release_hardening_policies
from .connected_platform import ConnectedScientificPlatform, ConnectedPlatformError, policies as connected_platform_policies
from .registry import catalog, resolve
from .schemas import ComputeRequest, ComputeResponse
from .security import require_compute_auth


jobs = PersistentJobQueue()
dispatcher = PersistentDistributedDispatcher(settings.dispatcher_db_path, settings.dispatcher_worker_stale_seconds, settings.dispatcher_default_lease_seconds, settings.dispatcher_max_workers, settings.dispatcher_max_queue_records, settings.dispatcher_max_attempts, settings.dispatcher_history_limit, settings.dispatcher_retry_base_delay_seconds, settings.dispatcher_retry_max_delay_seconds)
artifacts = ArtifactStore(settings.artifact_root, settings.artifact_db_path, settings.artifact_max_bytes, settings.artifact_chunk_bytes, settings.artifact_upload_ttl_seconds, settings.artifact_retention_seconds)
workflows = WorkflowOrchestrator(settings.workflow_db_path, dispatcher, settings.workflow_max_nodes, settings.workflow_max_runs, settings.workflow_history_limit)
workflow_automation = WorkflowScheduler(settings.workflow_schedule_db_path, workflows, settings.workflow_scheduler_poll_seconds, settings.workflow_scheduler_max_catch_up_runs, settings.workflow_scheduler_history_limit)
experiment_campaigns = ExperimentCampaignManager(settings.experiment_campaign_db_path, workflows, settings.experiment_campaign_poll_seconds, settings.experiment_campaign_max_campaigns, settings.experiment_campaign_max_trials, settings.experiment_campaign_history_limit)
closed_loop_campaigns = ClosedLoopCampaignManager(settings.closed_loop_db_path, experiment_campaigns, settings.closed_loop_poll_seconds, settings.closed_loop_max_loops, settings.closed_loop_max_cycles, settings.closed_loop_history_limit, settings.closed_loop_measurement_secret)
model_registry = ScientificModelRegistry(settings.model_registry_db_path, settings.model_registry_max_models, settings.model_registry_max_versions, settings.model_registry_history_limit)
ensemble_studies = EnsembleStudyManager(settings.ensemble_study_db_path, model_registry, dispatcher, settings.ensemble_max_studies, settings.ensemble_max_evaluations, settings.ensemble_history_limit)
surrogate_rom = SurrogateReducedOrderManager(settings.surrogate_rom_db_path, model_registry, settings.surrogate_rom_max_studies, settings.surrogate_rom_max_training_rows, settings.surrogate_rom_max_snapshot_dimensions, settings.surrogate_rom_history_limit)
team_workspaces = TeamWorkspaceManager(settings.team_workspace_db_path, settings.team_workspace_max_workspaces, settings.team_workspace_max_members, settings.team_workspace_history_limit)
workspace_reviews = WorkspaceReviewManager(settings.team_workspace_db_path, settings.team_workspace_history_limit)
workspace_versions = WorkspaceVersionManager(settings.team_workspace_db_path, settings.team_workspace_history_limit)
artifact_repository = ScientificArtifactRepository(settings.artifact_repository_db_path, team_workspaces, artifacts.get, settings.artifact_repository_max_collections, settings.artifact_repository_max_records, settings.artifact_repository_max_manifest_records, settings.artifact_repository_history_limit)
institutional_nodes = InstitutionalNodeFederation(settings.institutional_node_db_path, team_workspaces, resolve, settings.institutional_node_coordinator_secret, settings.institutional_node_max_nodes, settings.institutional_node_max_data_assets, settings.institutional_node_max_requests, settings.institutional_node_history_limit)
edge_sync = OfflineEdgeSyncManager(settings.edge_sync_db_path, team_workspaces, institutional_nodes, settings.edge_sync_max_devices, settings.edge_sync_max_packages, settings.edge_sync_max_changes, settings.edge_sync_max_batch, settings.edge_sync_history_limit)
publication_studio = ReproducibilityPublicationStudio(settings.publication_studio_db_path, team_workspaces, settings.publication_studio_max_packages, settings.publication_studio_max_publications, settings.publication_studio_max_resources, settings.publication_studio_history_limit, workspace_reviews)
manuscript_assembly = ManuscriptAssemblyStudio(settings.manuscript_assembly_db_path, team_workspaces, publication_studio, settings.manuscript_assembly_max_assemblies, settings.manuscript_assembly_max_sections, settings.manuscript_assembly_max_sections_per_assembly, settings.manuscript_assembly_history_limit)
public_reproduction = PublicReproductionPortal(settings.public_reproduction_db_path, team_workspaces, publication_studio, manuscript_assembly, settings.public_reproduction_receipt_secret, settings.public_reproduction_max_records, settings.public_reproduction_max_challenges, settings.public_reproduction_challenge_ttl_seconds, settings.public_reproduction_history_limit)
research_interoperability = ResearchInteroperabilityLayer(settings.interoperability_db_path, team_workspaces, settings.interoperability_receipt_secret, settings.interoperability_max_profiles, settings.interoperability_max_handoffs, settings.interoperability_history_limit)
typed_cross_product_handoffs = TypedCrossProductHandoffs(research_interoperability)
public_research_integrations = PublicResearchIntegrationGateway(settings.public_integration_db_path, team_workspaces, settings.webhook_signing_secret, settings.webhook_delivery_enabled, settings.public_integration_persistent_disk_mounted, settings.public_integration_max_subscriptions, settings.public_integration_max_deliveries)
institutional_governance = InstitutionalGovernanceManager(settings.institutional_governance_db_path, team_workspaces, settings.institutional_governance_persistent_disk_mounted, settings.institutional_governance_max_institutions, settings.institutional_governance_max_principals, settings.institutional_governance_history_limit)
security_privacy = SecurityPrivacyManager(settings.security_privacy_db_path, settings.secret_master_key, settings.secret_previous_master_keys, settings.audit_signing_secret, settings.security_privacy_persistent_disk_mounted, settings.security_privacy_max_secrets, settings.security_privacy_max_credentials, settings.security_privacy_history_limit, settings.service_credential_ttl_days)
multi_instance_operations = MultiInstanceOperationsManager(
    settings.multi_instance_operations_db_path,
    settings.multi_instance_backup_root,
    settings.instance_id,
    settings.instance_name,
    settings.instance_environment,
    settings.instance_region,
    settings.instance_public_url,
    settings.backup_signing_secret,
    settings.multi_instance_persistent_disk_mounted,
    {
        "jobs": settings.job_db_path,
        "dispatcher": settings.dispatcher_db_path,
        "artifacts": settings.artifact_db_path,
        "workflows": settings.workflow_db_path,
        "workflow-schedules": settings.workflow_schedule_db_path,
        "experiment-campaigns": settings.experiment_campaign_db_path,
        "closed-loop": settings.closed_loop_db_path,
        "model-registry": settings.model_registry_db_path,
        "ensemble-studies": settings.ensemble_study_db_path,
        "surrogate-rom": settings.surrogate_rom_db_path,
        "team-workspaces": settings.team_workspace_db_path,
        "artifact-repository": settings.artifact_repository_db_path,
        "institutional-nodes": settings.institutional_node_db_path,
        "edge-sync": settings.edge_sync_db_path,
        "publication-studio": settings.publication_studio_db_path,
        "manuscript-assembly": settings.manuscript_assembly_db_path,
        "public-reproduction": settings.public_reproduction_db_path,
        "interoperability": settings.interoperability_db_path,
        "public-integrations": settings.public_integration_db_path,
        "institutional-governance": settings.institutional_governance_db_path,
        "security-privacy": settings.security_privacy_db_path,
        "request-replay": settings.security_replay_db_path,
        "multi-instance-operations": settings.multi_instance_operations_db_path,
        "performance-validation": settings.performance_validation_db_path,
        "platform-beta": settings.platform_beta_db_path,
        "interface-finalization": settings.interface_finalization_db_path,
        "public-release-hardening": settings.public_release_hardening_db_path,
        "connected-platform": settings.stable_platform_db_path,
    },
    settings.artifact_root,
    settings.multi_instance_max_backups,
    settings.multi_instance_max_bundle_bytes,
    settings.multi_instance_history_limit,
    settings.recovery_rpo_hours,
    settings.recovery_rto_minutes,
)
performance_validation = PerformanceChaosManager(
    settings.performance_validation_db_path,
    settings.performance_validation_persistent_disk_mounted,
    settings.performance_validation_max_concurrency,
    settings.performance_validation_max_iterations,
    settings.performance_validation_history_limit,
    settings.performance_validation_default_p95_ms,
    settings.performance_validation_default_error_rate_ppm / 1_000_000.0,
)
scientific_compute = ScientificComputeManager(
    settings.scientific_compute_db_path,
    {
        "workflow.run": run_scientific_workflow,
        "statistics.fit": fit_advanced_statistical_model,
        "bayesian.fit": fit_bayesian_model,
        "probabilistic.analyze": run_probabilistic_analysis,
        "correlated-uncertainty.analyze": run_correlated_uncertainty,
        "dynamic-systems.simulate": simulate_dynamic_systems_v0540,
        "experimental-design.generate": generate_optimal_design,
        "data-transformations.run": transform_scientific_dataset,
    },
    max_workers=settings.scientific_compute_workers,
    max_queued=settings.scientific_compute_max_queued,
    cache_ttl_seconds=settings.scientific_compute_cache_ttl_seconds,
    max_cache_records=settings.scientific_compute_max_cache_records,
    max_result_bytes=settings.scientific_compute_max_result_bytes,
)
connected_platform_beta = ConnectedPlatformBetaManager(
    settings.platform_beta_db_path,
    settings.platform_beta_persistent_disk_mounted,
    settings.platform_beta_telemetry_enabled,
    settings.platform_beta_history_limit,
    settings.platform_beta_max_records,
)
interface_finalization = InterfaceFinalizationManager(
    settings.interface_finalization_db_path,
    settings.interface_finalization_persistent_disk_mounted,
    settings.interface_offline_shell_enabled,
    settings.interface_finalization_history_limit,
    settings.interface_max_snapshot_assets,
    settings.interface_max_queue_records,
)
public_release_hardening = PublicReleaseHardeningManager(
    settings.public_release_hardening_db_path,
    settings.public_release_hardening_persistent_disk_mounted,
    settings.public_release_migration_execution_enabled,
    settings.public_release_hardening_history_limit,
)
connected_platform = ConnectedScientificPlatform(
    settings.stable_platform_db_path,
    settings.stable_platform_persistent_disk_mounted,
    settings.stable_platform_history_limit,
)


@asynccontextmanager
async def lifespan(application: FastAPI):
    application.state.extensions = (
        load_legacy_extensions(application)
        if settings.extension_loading
        else {"loaded": [], "failed": {}}
    )
    jobs.start()
    workflow_automation.start()
    experiment_campaigns.start()
    closed_loop_campaigns.start()
    yield
    closed_loop_campaigns.stop()
    experiment_campaigns.stop()
    workflow_automation.stop()
    jobs.stop()


app = FastAPI(
    title=settings.service_name,
    version=settings.version,
    docs_url="/docs",
    redoc_url="/redoc",
    lifespan=lifespan,
)

@app.middleware("http")
async def security_response_headers(request: Request, call_next):
    response = await call_next(request)
    response.headers.setdefault("X-Content-Type-Options", "nosniff")
    response.headers.setdefault("X-Frame-Options", "DENY")
    response.headers.setdefault("Referrer-Policy", "no-referrer")
    response.headers.setdefault("Permissions-Policy", "camera=(), microphone=(), geolocation=()")
    response.headers.setdefault("Cache-Control", "no-store")
    response.headers.setdefault("Content-Security-Policy", "default-src 'none'; frame-ancestors 'none'; base-uri 'none'; form-action 'none'")
    return response

app.add_middleware(
    CORSMiddleware,
    allow_origins=[],
    allow_credentials=False,
    allow_methods=["GET", "POST", "PATCH", "DELETE"],
    allow_headers=["*"],
)


@app.middleware("http")
async def request_limits(request: Request, call_next):
    if request.method in {"POST", "PUT", "PATCH"}:
        body = await request.body()
        if "/artifacts/uploads/" in request.url.path and request.url.path.endswith("/chunks"):
            limit = settings.artifact_chunk_bytes
        elif request.url.path.startswith("/v1/surrogate-rom/"):
            limit = settings.surrogate_rom_max_request_bytes
        elif request.url.path.startswith("/v1/compute-hardening/v0580/"):
            limit = settings.scientific_compute_max_request_bytes
        else:
            limit = settings.max_request_bytes
        if len(body) > limit:
            return JSONResponse(
                status_code=413,
                content={"detail": "Request exceeds the configured size limit."},
            )
    return await call_next(request)


def execute_or_http(payload: ComputeRequest, auth: dict[str, str]) -> ComputeResponse:
    try:
        return run_compute(payload, auth)
    except ComputeExecutionError as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc


def job_payload(source: dict[str, Any]) -> ComputeRequest:
    return ComputeRequest(
        method=str(source.get("methodId") or source.get("method") or ""),
        version=source.get("version"),
        inputs=source.get("inputs") or {},
        parameters=source.get("parameters") or {},
        units=source.get("units") or {},
        project_id=source.get("project_id") or source.get("projectId"),
        requested_outputs=source.get("requested_outputs") or source.get("requestedOutputs") or ["summary", "values"],
        random_seed=source.get("random_seed") if source.get("random_seed") is not None else source.get("randomSeed"),
        governance=source.get("governance") or {},
    )


@app.get("/health")
def health():
    queue_state = jobs.queue_status()
    return {
        "ok": True,
        "status": "ready",
        "service": settings.service_name,
        "version": settings.version,
        "architecture": "python-compute-core",
        "authMode": settings.auth_mode,
        "methodCount": len(catalog()),
        "benchmarkCount": len(benchmark_catalog()),
        "solverGovernance": {"version": "0.27.3", "profiles": 4, "referenceComparison": True, "unitAwareValidation": True},
        "scientificVisualization": {"version": "0.27.4", "profiles": len(visualization_catalog()["profiles"]), "formats": ["svg", "png", "csv", "json"]},
        "projectWorkspace": {"version": "0.28.0", "contextEnvelope": True, "serverBackedStorage": False},
        "datasetRegistry": {"version": "0.28.1", "profiling": True, "formats": ["csv", "json", "geojson", "netcdf", "tabular"], "serverBackedRegistry": False},
        "reproducibility": {"version": "0.28.2", "manifests": True, "verification": True, "comparison": True, "serverBackedRegistry": False},
        "researchProvenance": {"version":"0.29.0","sources":True,"evidence":True,"citations":True,"assumptions":True,"limitations":True},
        "researchQuality": {"version":"0.29.1","methodReview":True,"benchmarkCoverage":True,"calibration":True,"approvalWorkflow":True,"deprecationHistory":True},
        "externalDiscovery": {"version":"0.29.2","providers":["crossref","openalex","datacite"],"worldCatHandoff":True,"openUrlHandoff":True,"deduplication":True,"sourceImport":True},
        "experimentFramework": {"version":"0.30.0","protocols":True,"runHistories":True,"replications":True,"comparisons":True,"reports":True},
        "designStudies": {"version":"0.30.1","factorial":True,"latinHypercube":True,"responseSurfaces":True,"sensitivity":True,"batchPlans":True},
        "modelCalibration": {"version":"0.30.2","parameterFitting":True,"holdoutValidation":True,"confidenceIntervals":True,"residualDiagnostics":True,"modelComparison":True},
        "distributedDispatcher": {"version":"0.31.0","capabilityDiscovery":True,"workloadRouting":True,"signedDispatchContracts":True,"leases":True},
        "persistentQueueInfrastructure": {"version":"0.31.1","storage":"sqlite-wal","persistentWorkerRegistry":True,"persistentWorkloadQueue":True,"leaseRecovery":True},
        "secureWorkerAgent": {"version":"0.32.1","pullBased":True,"workerScopedCredentials":True,"signedContractVerification":True,"registeredMethodsOnly":True,"leaseRenewal":True,"completionReceipts":True,"artifactTransport":True},
        "artifactTransport": {"version":"0.31.3","contentAddressed":True,"resumable":True,"sha256Verification":True,"resultArtifacts":True,"checkpointArtifacts":True,"retentionControls":True},
        "dispatcherOperations": {"version":"0.31.4","failureClassification":True,"boundedRetries":True,"deadLetterRecovery":True,"operatorReplay":True,"queueMetrics":True,"databaseDiagnostics":True},
        "workflowOrchestration": {"version":"0.32.1","typedDefinitions":True,"dagValidation":True,"dependencyAwareScheduling":True,"parallelReadyNodes":True,"resultBindings":True,"artifactHandoffs":True,"runProvenance":True,"declarativeConditions":True,"checkpointHistory":True,"partialRecovery":True,"recoveryLineage":True},
        "workflowAutomation": {"version":"0.32.2","intervalSchedules":True,"cronUtc":True,"oneTimeSchedules":True,"authenticatedEvents":True,"eventDeduplication":True,"misfireRecovery":True,"concurrencyControls":True},
        "experimentCampaigns": {"version":"0.33.1","adaptiveSequentialDesign":True,"workflowBackedTrials":True,"gaussianProcessSurrogate":True,"predictiveUncertainty":True,"activeLearning":True,"resourceAwareSearch":True,"costAdjustedAcquisition":True},
        "closedLoopCampaigns": {"version":"0.33.2","simulationLoops":True,"instrumentMeasurementIngestion":True,"hybridCampaigns":True,"safetyInterlocks":True,"operatorCommandApproval":True,"checkpointedCycles":True,"directDeviceExecution":False},
        "scientificModelRegistry": {"version":"0.34.0","immutableVersions":True,"environmentCapture":True,"dependencyLocking":True,"reproductionManifests":True,"promotionWorkflow":True,"deprecationHistory":True},
        "ensembleSimulation": {"version":"0.34.1","registeredModelMembers":True,"weightedEnsembles":True,"monteCarlo":True,"latinHypercube":True,"sobolDesign":True,"globalSensitivity":True,"uncertaintyIntervals":True},
        "surrogateReducedOrder": {"version":"0.34.2","surrogateTraining":True,"gaussianProcess":True,"polynomialRidge":True,"radialBasis":True,"properOrthogonalDecomposition":True,"hybridRom":True,"validationMetrics":True,"modelRegistryIntegration":True},
        "sharedResearchWorkspaces": {"version":"0.35.0","roleBasedMembership":True,"singleUseInvitations":True,"governedResourceLinks":True,"ownershipTransfer":True,"archiveWithoutDeletion":True},
        "workspaceReviewSignoff": {"version":"0.35.1","appendOnlyComments":True,"reviewAssignments":True,"approvalGates":True,"optimisticConcurrency":True,"immutableDecisions":True,"immutableScientificSignoff":True},
        "workspaceVersionControl": {"version":"0.35.2","immutableSnapshots":True,"namedBranches":True,"threeWayMerge":True,"pathLevelConflicts":True,"protectedBranches":True,"signedApprovalGate":True,"historyRewrite":False},
        "scientificArtifactRepository": {"version":"0.36.0","governedCollections":True,"immutableArtifactVersions":True,"transportIntegrityBinding":True,"manifestFederation":True,"deltaSync":True,"tombstones":True,"conflictResolution":True,"automaticRemoteCallbacks":False},
        "reproducibilityPublicationStudio": {"version":"0.37.0","immutablePackages":True,"logicalBundles":True,"manifestVerification":True,"citationCff":True,"markdownHtmlJson":True,"scientificSignoffGate":True,"embeddedRestrictedData":False},
        "manuscriptAssemblyStudio": {"version":"0.37.1","reusableSections":True,"structuredMethodsNarrative":True,"outputOnlyNotebooks":True,"markdownHtmlJats":True,"bibtex":True,"immutableAssemblies":True,"revisionLineage":True,"arbitraryCode":False},
        "publicReproductionPortal": {"version":"0.37.2","publicVerificationRecords":True,"safeManifestViews":True,"nonceChallenges":True,"signedReceipts":True,"tamperDetection":True,"publicCodeExecution":False,"embeddedRestrictedData":False},
        "researchInteroperability": {"version":"0.38.0","typedCrossProductHandoffs":True,"canonicalEnvelopes":True,"contractNegotiation":True,"capabilityNegotiation":True,"idempotentImports":True,"signedReceipts":True,"directRemoteCallbacks":False,"embeddedRestrictedData":False},
        "typedCrossProductResearchHandoffs": {"version":"0.38.1","executableAdapterRegistry":True,"productPairRoutePlanning":True,"contractInference":True,"profileAwareSealing":True,"remoteCallbacks":False,"embeddedRestrictedData":False},
        "platformCoreV3RuntimeAdapter": {"version":"0.104.0","requiredCoreRelease":"3.0.0","productRef":"product:sustainable-catalyst-lab","runtimeContract":"sc.research.unified-runtime-contract.v1","unifiedRuntimeContract":"sc.research.unified-research-scientific-investigation-runtime.v1","capabilityRegistration":True,"sessionContextNormalization":True,"handoffValidation":True,"automaticCoreCalls":False,"automaticExecution":False},
        "platformCoreV3ObjectMapping": {"version":"0.105.0","requiredCoreRelease":"3.0.0","mappingCount":20,"referenceFirst":True,"legacyTypedHandoffBridge":True,"automaticCoreSubmission":False,"objectPayloadCopy":False},
        "publicResearchIntegrations": {"version":"0.38.2","stableApiCatalog":True,"scopedAuthentication":True,"signedWebhooks":True,"ssrfProtection":True,"signedExpiringEmbeds":True,"pythonSdk":True,"typescriptSdk":True,"browserEmbedSdk":True,"outboundDeliveryEnabled":settings.webhook_delivery_enabled},
        "institutionalGovernance": {"version":"0.39.0","institutions":True,"organizationalUnits":True,"humanAndServicePrincipals":True,"roleBindings":True,"workspaceGovernance":True,"classification":True,"retention":True,"approvals":True,"policyEvaluation":True,"secretStorage":True,"singleSignOn":False},
        "securityPrivacyHardening": {"version":"0.39.1","aes256GcmSecrets":True,"credentialHashing":True,"requestNonceReplayProtection":True,"signedAuditChains":True,"privacyScanning":True,"privacyRequests":True},
        "multiInstanceOperations": {"version":"0.39.2","stableInstanceIdentity":True,"consistentSqliteSnapshots":True,"signedBackupManifests":True,"stagedRestore":True,"idempotentMigrations":True,"signedCrossInstanceTransfers":True,"rpoRtoDrills":True,"activeFileOverwriteByApi":False},
        "performanceLoadChaosValidation": {"version":"0.39.3","loadProfiles":True,"latencyPercentiles":True,"throughputMeasurement":True,"performanceBudgets":True,"safeChaosScenarios":True,"capacityReports":True,"productionChaos":False,"externalTraffic":False},
        "connectedScientificResearchPlatformBeta": {"version":"0.40.0","releaseStage":"beta","institutionalCohorts":True,"guidedResearchProjects":True,"privacyMinimizedTelemetry":True,"feedbackOperations":True,"knownLimitations":True,"supportPathways":True,"releaseReadinessGate":True,"generalAvailabilityClaim":False},
        "accessibilityMobileOfflineInterfaceFinalization": {"version":"0.40.1","responsiveAudits":True,"accessibilityPreferences":True,"browserLocalSnapshots":True,"idempotentOfflineQueue":True,"explicitConflictReconciliation":True,"optInOfflineShell":True,"restrictedDataMayBeCached":False},
        "migrationCompatibilityPublicReleaseHardening": {"version":"0.40.2","upgradeAssessment":True,"compatibilityMatrices":True,"deprecationRegistry":True,"cleanInstallCertification":True,"rollbackEvidence":True,"releaseCandidateGate":True,"forcePushPermitted":False},
        "connectedScientificResearchComputePlatform": {"version":"1.0.0","releaseStage":"general-availability","stableContracts":True,"upgradeCertification":True,"productionAttestation":True,"incidentReadiness":True,"supportLifecycle":True},
        "soilOrganicCarbon": {"domainVersion":"0.13.0","labReleaseVersion":"0.96.0","fixedDepthStock":True,"profileAggregation":True,"samplingFieldMeasurement":True,"samplingPlanRegistry":True,"bulkDensityCoreCalculation":True,"chainOfCustodyMetadata":True,"stockChangeModel":True,"spatialVariabilityUncertainty":True,"managementScenarioStudio":True,"multiScenarioComparison":True,"userSuppliedSensitivitySweep":True,"wholeFarmGhgBalance":True,"explicitGwpFactors":True,"activityFactorAccounting":True,"socStockChangeIntegration":True,"carbonMrvMethodRegistry":True,"mrvDocumentationReadiness":True,"mrvProtocolBuilder":True,"mrvProtocolStructuralValidation":True,"mrvProtocolProjectHandoff":True,"libraryMethodologyCrosswalk":True,"projectPacketHandoff":True,"equivalentSoilMass":False,"sampleSizeAdequacyInference":False,"spatialRandomization":False,"scenarioForecastInference":False,"causalAttributionInference":False,"automaticEmissionFactorInference":False,"automaticGwpInference":False,"automaticMrvMethodSelection":False,"methodologyEligibilityInference":False,"co2eInference":False,"creditEligibilityInference":False},
        "extensionLoading": settings.extension_loading,
        "extensions": getattr(app.state, "extensions", {"loaded": [], "failed": {}}),
        "queue": {
            "persistent": queue_state["persistent"],
            "storage": queue_state["storage"],
            "schema": queue_state["schema"],
            "deploymentDurability": "persistent-disk" if settings.dispatcher_persistent_disk_mounted else "instance-local",
            "durabilityWarning": None if settings.dispatcher_persistent_disk_mounted else "SQLite queue files are instance-local unless a persistent disk is mounted at /app/data.",
            "activeWorkers": queue_state["activeWorkers"],
            "workerCapacity": queue_state["workerCapacity"],
            "queued": queue_state["counts"]["queued"] + queue_state["counts"]["retrying"],
            "paused": queue_state["counts"]["paused"],
            "checkpointRecovery": queue_state["checkpointRecovery"],
            "priorityScheduling": queue_state["priorityScheduling"],
            "maxActiveJobsPerProject": queue_state["maxActiveJobsPerProject"],
            "cache": queue_state["cache"],
        },
    }


@app.get("/version")
def version():
    return {"version": settings.version, "service": settings.service_name}


@app.get("/v1/capabilities", dependencies=[Depends(require_compute_auth)])
def capabilities():
    return {
        "schema": "sc-lab-compute-capabilities/1.4",
        "version": settings.version,
        "executionTargets": ["python-core-cpu", "isolated-process-worker", "browser-web-worker", "local-python", "raspberry-pi", "institutional-node"],
        "modes": ["synchronous", "persistent-queued", "checkpointed-queued", "cached-queued"],
        "packages": ["numpy", "scipy", "pandas", "sympy", "reportlab"],
        "numericalMethods": {"registeredOnly": True, "differentialEquations": True, "optimization": True, "signalProcessing": True, "uncertainty": True, "sensitivity": True, "parameterSweeps": True, "benchmarkLibrary": True, "knownAnswerFixtures": True, "convergenceDiagnostics": True, "solverGovernance": True, "precisionProfiles": True, "conditionDiagnostics": True, "referenceMethodComparisons": True, "scientificVisualization": True, "accessibleSvg": True, "exportFormats": ["svg", "png", "csv", "json"]},
        "benchmarks": {"count": len(benchmark_catalog()), "crossImplementation": ["python-core", "analytic-reference", "browser-reference"], "toleranceControls": True, "unitAssertions": True},
        "security": {
            "authMode": settings.auth_mode,
            "arbitraryCode": False,
            "registeredMethodsOnly": True,
            "requestLimitBytes": settings.max_request_bytes,
            "hardWorkerTermination": True,
        },
        "jobs": {
            "persistent": True,
            "storage": "sqlite-wal",
            "states": ["queued", "running", "paused", "completed", "failed", "cancelled", "timed_out", "retrying"],
            "workerCount": settings.job_workers,
            "maxQueuedJobs": settings.max_queued_jobs,
            "defaultTimeoutSeconds": settings.default_job_timeout_seconds,
            "maxTimeoutSeconds": settings.max_job_timeout_seconds,
            "defaultAttempts": settings.default_job_attempts,
            "maxAttempts": settings.max_job_attempts,
            "deduplication": True,
            "restartRecovery": True,
            "checkpointRecovery": True,
            "partialResultInspection": True,
            "priorityScheduling": True,
            "resultCaching": True,
            "maxActiveJobsPerProject": settings.max_active_jobs_per_project,
        },
        "solverGovernance": {"version": "0.27.3", "profiles": 4, "manualSolverSelection": True, "automaticRecommendations": True, "unitAwareValidation": True, "referenceMethodComparisons": True, "floatingPointReporting": True},
        "scientificVisualization": {"version": "0.27.4", "profiles": len(visualization_catalog()["profiles"]), "serverNormalizedSpecs": True, "accessibleDescriptions": True, "tabularFallback": True},
        "projectWorkspace": {"version": "0.28.0", "projectContextAccepted": True, "serverBackedStorage": False},
        "datasetRegistry": {"version": "0.28.1", "profileEndpoint": True, "dataDictionary": True, "unitMetadata": True, "lineage": True, "serverBackedRegistry": False},
        "reproducibility": {"version": "0.28.2", "frozenManifests": True, "environmentFingerprint": True, "verification": True, "comparison": True, "portableBundles": True, "serverBackedRegistry": False},
        "researchQuality": {"version":"0.29.1","reviewNormalization":True,"policyEvaluation":True,"hashVerification":True,"revisionComparison":True,"serverBackedRegistry":False},
        "externalDiscovery": {"version":"0.29.2","liveProviders":["crossref","openalex","datacite"],"handoffs":["worldcat","google-scholar","openurl"],"deduplication":True,"sourceImport":True,"arbitraryRemoteFetch":False},
        "experimentFramework": {"version":"0.30.0","protocolNormalization":True,"readinessValidation":True,"runManifests":True,"replicationComparison":True,"reportBuilder":True,"serverBackedRegistry":False},
        "designStudies": {"version":"0.30.1","designGeneration":True,"responseSurfaceAnalysis":True,"sensitivityRanking":True,"optimalDesignRecommendation":True,"queueBatchPlans":True,"serverBackedRegistry":False},
        "persistentQueueInfrastructure": {"version":"0.31.1","storage":"sqlite-wal","persistentWorkerRegistry":True,"persistentWorkloadQueue":True,"leaseRecovery":True,"horizontalCoordinatorSafeClaims":True,"centralHistory":True},
        "secureWorkerAgent": {"version":"0.32.1","pullBased":True,"workerScopedCredentials":True,"credentialRotation":True,"credentialRevocation":True,"signedContractVerification":True,"registeredMethodsOnly":True,"leaseRenewal":True,"completionReceipts":True,"artifactTransport":True},
        "artifactTransport": {"version":"0.31.3","contentAddressed":True,"resumableChunks":True,"sha256Verification":True,"deduplication":True,"inputMaterialization":True,"resultExternalization":True,"checkpointTransport":True,"retentionControls":True},
        "workflowOrchestration": {"version":"0.32.1","typedDefinitions":True,"dagValidation":True,"dependencyAwareScheduling":True,"parallelReadyNodes":True,"resultBindings":True,"artifactHandoffs":True,"runProvenance":True,"serverBackedRegistry":True,"declarativeConditions":True,"checkpointHistory":True,"partialRecovery":True,"recoveryLineage":True},
        "experimentCampaigns": {"version":"0.33.1","adaptiveSequentialDesign":True,"gaussianProcessSurrogate":True,"activeLearning":True,"resourceAwareSearch":True},
        "closedLoopCampaigns": {"version":"0.33.2","simulation":True,"instrument":True,"hybrid":True,"signedMeasurements":True,"safetyInterlocks":True,"operatorApproval":True,"directDeviceExecution":False},
        "scientificModelRegistry": {"version":"0.34.0","modelVersioning":True,"environmentCapture":True,"dependencyLocks":True,"reproductionVerification":True,"promotionChannels":["candidate","production","archived"],"arbitraryCode":False},
        "ensembleSimulation": {"version":"0.34.1","weightedRegisteredModels":True,"samplingDesigns":["monte-carlo","latin-hypercube","sobol","saltelli-sobol"],"uncertaintyPropagation":True,"globalSensitivity":True,"arbitraryCode":False},
        "surrogateReducedOrder": {"version":"0.34.2","algorithms":["polynomial-ridge","radial-basis","gaussian-process"],"properOrthogonalDecomposition":True,"hybridReducedOrderModels":True,"holdoutValidation":True,"errorBounds":True,"registryPublication":True,"arbitraryCode":False},
        "platformCoreV3RuntimeAdapter": {"version":"0.104.0","requiredCoreRelease":"3.0.0","productRef":"product:sustainable-catalyst-lab","runtimeBindingRef":"lab:runtime:scientific-compute:0.104.0","contractRegistration":True,"sessionProductBinding":True,"runtimeContextNormalization":True,"handoffValidation":True,"compatibilityChecks":True,"automaticSubmission":False,"arbitraryCode":False},
        "platformCoreV3ObjectMapping": {"version":"0.105.0","requiredCoreRelease":"3.0.0","mappingCount":20,"coreObjectBindingPath":"/v1/research/unified-runtime/object-bindings","referenceFirst":True,"legacyTypedHandoffBridge":True,"automaticSubmission":False,"arbitraryCode":False},
        "provenanceSchema": "sc-lab-compute-provenance/1.1",
        "methodCount": len(catalog()),
        "legacyExtensions": getattr(app.state, "extensions", {"loaded": [], "failed": {}}),
    }


@app.get("/v1/governance/health")
def governance_health():
    catalog = policy_catalog()
    return {"ok": True, "status": "ready", "version": "0.27.3", "serviceVersion": settings.version, "architecture": "solver-precision-governance", "profileCount": len(catalog["profiles"]), "governedMethodCount": len(catalog["solvers"]), "unitAwareValidation": True, "referenceComparison": True}


@app.get("/v1/governance/policies", dependencies=[Depends(require_compute_auth)])
def governance_policies():
    return policy_catalog()


@app.post("/v1/governance/recommend")
def governance_recommend(payload: ComputeRequest, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        method_record = resolve(payload.method)
        return {"ok": True, "version": "0.27.3", "recommendation": recommend(payload, method_record), "request": payload.model_dump(mode="json", by_alias=True)}
    except KeyError as exc:
        raise HTTPException(status_code=404, detail=str(exc)) from exc
    except ValueError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.post("/v1/governance/compare", response_model=ComputeResponse)
def governance_compare(payload: ComputeRequest, auth: dict[str, str] = Depends(require_compute_auth)):
    data = payload.model_dump(mode="json", by_alias=True)
    data.setdefault("governance", {})["referenceComparison"] = True
    return execute_or_http(ComputeRequest.model_validate(data), auth)


@app.get("/v1/visualization/health")
def visualization_health():
    catalog_data = visualization_catalog()
    return {"ok": True, "status": "ready", "version": "0.27.4", "serviceVersion": settings.version, "architecture": "governed-numerical-visualization", "profileCount": len(catalog_data["profiles"]), "formats": catalog_data["formats"], "accessibleSvg": True, "tabularFallback": True}


@app.get("/v1/visualization/profiles", dependencies=[Depends(require_compute_auth)])
def visualization_profiles():
    return visualization_catalog()


@app.post("/v1/visualization/spec")
def visualization_spec(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return build_visualization_spec(payload)
    except VisualizationInputError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.post("/v1/visualization/csv")
def visualization_csv_export(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        spec = build_visualization_spec(payload)
    except VisualizationInputError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc
    return {"ok": True, "version": "0.27.4", "mimeType": "text/csv", "filename": "scientific-visualization.csv", "text": visualization_csv(spec), "spec": spec}


@app.get("/v1/datasets/health")
def datasets_health():
    body = dataset_health_payload()
    body["serviceVersion"] = settings.version
    return body


@app.post("/v1/datasets/profile")
def datasets_profile(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return profile_dataset(payload)
    except DatasetProfileError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.get("/v1/datasets/v0550/health")
def data_transformations_health_route():
    body = data_transformations_health(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/datasets/v0550/policies")
def data_transformations_policies_route():
    return data_transformations_policies()

@app.post("/v1/datasets/v0550/normalize")
def data_transformations_normalize_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return {"ok": True, "version": "0.55.0", "plan": normalize_data_transformation_plan(payload.get("plan") or payload)}
    except DataTransformationError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/datasets/v0550/transform")
def data_transformations_transform_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return transform_scientific_dataset(payload)
    except DataTransformationError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/datasets/v0550/join")
def data_transformations_join_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return join_scientific_datasets(payload)
    except DataTransformationError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc



@app.get("/v1/reproducibility/health")
def reproducibility_health():
    body = reproducibility_health_payload(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/reproducibility/environment", dependencies=[Depends(require_compute_auth)])
def reproducibility_environment():
    return {"ok": True, "version": "0.28.2", "environment": environment_fingerprint()}

@app.post("/v1/reproducibility/manifest")
def reproducibility_manifest(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return build_reproducibility_manifest(payload)
    except ReproducibilityError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/reproducibility/verify")
def reproducibility_verify(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return verify_manifest(payload)
    except ReproducibilityError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/reproducibility/compare")
def reproducibility_compare(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return compare_manifests(payload)
    except ReproducibilityError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.get("/v1/research-provenance/health")
def research_provenance_health_route():
    body=research_provenance_health(); body["serviceVersion"]=settings.version; return body

@app.post("/v1/research-provenance/sources/normalize")
def research_source_normalize(payload: dict[str, Any], auth: dict[str,str]=Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"source":normalize_source(payload)}
    except ProvenanceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/research-provenance/evidence/normalize")
def research_evidence_normalize(payload: dict[str, Any], auth: dict[str,str]=Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"evidence":normalize_evidence(payload)}
    except ProvenanceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/research-provenance/verify")
def research_provenance_verify_route(payload: dict[str, Any], auth: dict[str,str]=Depends(require_compute_auth)):
    del auth; return verify_provenance_record(payload)

@app.post("/v1/research-provenance/build")
def research_provenance_build_route(payload: dict[str, Any], auth: dict[str,str]=Depends(require_compute_auth)):
    del auth; return {"ok":True,"provenance":build_provenance(payload)}


@app.get("/v1/research-quality/health")
def research_quality_health_route():
    body = research_quality_health()
    body["serviceVersion"] = settings.version
    return body


@app.get("/v1/research-quality/policies")
def research_quality_policies_route():
    body = research_quality_policies()
    body["serviceVersion"] = settings.version
    return body


@app.post("/v1/research-quality/reviews/normalize")
def research_quality_normalize_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return {"ok": True, "review": normalize_quality_review(payload)}
    except QualityReviewError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.post("/v1/research-quality/reviews/evaluate")
def research_quality_evaluate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return evaluate_quality_review(payload)
    except QualityReviewError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.post("/v1/research-quality/reviews/verify")
def research_quality_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return verify_quality_review(payload)


@app.post("/v1/research-quality/reviews/compare")
def research_quality_compare_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return compare_quality_reviews(payload)
    except QualityReviewError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.get("/v1/discovery/health")
def discovery_health_route():
    body=external_discovery_health(); body["serviceVersion"]=settings.version; return body


@app.get("/v1/discovery/providers")
def discovery_providers_route():
    body=discovery_provider_catalog(); body["serviceVersion"]=settings.version; return body


@app.post("/v1/discovery/search")
def discovery_search_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return search_discovery(payload)
    except DiscoveryError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.post("/v1/discovery/normalize")
def discovery_normalize_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return normalize_discovery(payload)
    except DiscoveryError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.post("/v1/discovery/deduplicate")
def discovery_deduplicate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return deduplicate_discovery(payload)
    except DiscoveryError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.post("/v1/discovery/open-access")
def discovery_open_access_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return open_access_lookup(payload)
    except DiscoveryError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.post("/v1/discovery/openurl")
def discovery_openurl_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return build_discovery_openurl(payload)
    except DiscoveryError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.get("/v1/methods", dependencies=[Depends(require_compute_auth)])
def methods():
    return {"schema": "sc-lab-method-registry/1.0", "version": settings.version, "methods": catalog()}


@app.get("/v1/methods/{method_id}", dependencies=[Depends(require_compute_auth)])
def method(method_id: str):
    try:
        return resolve(method_id).public()
    except KeyError as exc:
        raise HTTPException(status_code=404, detail=str(exc)) from exc


@app.get("/v1/benchmarks", dependencies=[Depends(require_compute_auth)])
def benchmarks():
    return {"schema": "sc-lab-numerical-benchmark-catalog/1.0", "version": settings.version, "benchmarkCount": len(benchmark_catalog()), "benchmarks": benchmark_catalog()}


@app.get("/v1/benchmarks/{benchmark_id}", dependencies=[Depends(require_compute_auth)])
def benchmark(benchmark_id: str):
    try:
        return resolve_benchmark(benchmark_id).public()
    except KeyError as exc:
        raise HTTPException(status_code=404, detail=str(exc)) from exc


@app.post("/v1/benchmarks/run")
def benchmark_run(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    benchmark_id = str(payload.get("benchmarkId") or payload.get("benchmark_id") or "")
    if not benchmark_id:
        raise HTTPException(status_code=422, detail="benchmarkId is required.")
    try:
        return run_benchmark(benchmark_id, lambda request: run_compute(request, auth))
    except KeyError as exc:
        raise HTTPException(status_code=404, detail=str(exc)) from exc
    except ComputeExecutionError as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc


@app.post("/v1/benchmarks/run-suite")
def benchmark_suite(payload: dict[str, Any] | None = None, auth: dict[str, str] = Depends(require_compute_auth)):
    benchmark_ids = None
    if isinstance(payload, dict) and isinstance(payload.get("benchmarkIds"), list):
        benchmark_ids = [str(value) for value in payload["benchmarkIds"][:100]]
    try:
        return run_suite(lambda request: run_compute(request, auth), benchmark_ids)
    except KeyError as exc:
        raise HTTPException(status_code=404, detail=str(exc)) from exc


@app.post("/v1/benchmarks/convergence")
def benchmark_convergence(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    benchmark_id = str(payload.get("benchmarkId") or payload.get("benchmark_id") or "")
    if not benchmark_id:
        raise HTTPException(status_code=422, detail="benchmarkId is required.")
    try:
        return run_convergence(benchmark_id, lambda request: run_compute(request, auth))
    except KeyError as exc:
        raise HTTPException(status_code=404, detail=str(exc)) from exc
    except ValueError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc
    except ComputeExecutionError as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc


@app.post("/v1/compute/run", response_model=ComputeResponse)
def compute_run(payload: ComputeRequest, auth: dict[str, str] = Depends(require_compute_auth)):
    return execute_or_http(payload, auth)


@app.get("/v1/languages", dependencies=[Depends(require_compute_auth)])
def languages():
    return {
        "languages": [
            {"id": "python", "status": "native", "version": "3"},
            {"id": "javascript", "status": "browser-fallback"},
            {"id": "typescript", "status": "source-only"},
            {"id": "r", "status": "source-only"},
            {"id": "julia", "status": "source-only"},
            {"id": "rust", "status": "source-only"},
            {"id": "go", "status": "source-only"},
            {"id": "c", "status": "source-only"},
            {"id": "cpp", "status": "source-only"},
            {"id": "fortran", "status": "source-only"},
            {"id": "sql", "status": "source-only"},
            {"id": "haskell", "status": "source-only"},
        ]
    }


@app.post("/v1/validate")
def validate(payload: ComputeRequest, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        method_record = resolve(payload.method)
    except KeyError as exc:
        raise HTTPException(status_code=404, detail=str(exc)) from exc
    return {"ok": True, "method": method_record.public(), "request": payload.model_dump()}


@app.post("/v1/execute")
def execute_compat(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    language = str(payload.get("language") or "python").lower()
    if language != "python":
        raise HTTPException(
            status_code=422,
            detail=f"v0.26.1 executes Python natively; {language} remains a browser/source fallback.",
        )
    request = job_payload(payload)
    result = execute_or_http(request, auth)
    return {
        "ok": True,
        "methodId": payload.get("methodId") or request.method,
        "language": "python",
        "outputs": result.outputs,
        "result": result.model_dump(mode="json", by_alias=True),
        "provenance": result.provenance.model_dump(mode="json", by_alias=True),
    }


@app.post("/v1/compare")
def compare_compat(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    languages_requested = [str(v).lower() for v in payload.get("languages") or []]
    request = job_payload(payload)
    result = execute_or_http(request, auth)
    return {
        "ok": True,
        "methodId": payload.get("methodId"),
        "referenceLanguage": "python",
        "reference": result.outputs,
        "runs": [{"language": "python", "status": "completed", "outputs": result.outputs}]
        if "python" in languages_requested
        else [],
        "unsupported": [lang for lang in languages_requested if lang != "python"],
        "provenance": result.provenance.model_dump(mode="json", by_alias=True),
    }


@app.post("/v1/jobs", status_code=202)
def create_job(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    operation = str(payload.get("operation") or "core_run")
    if operation in {"execute", "core_run"}:
        source = payload.get("execute") or payload.get("request") or payload
    elif operation == "compare":
        source = payload.get("compare") or {}
    else:
        raise HTTPException(status_code=422, detail="Job operation must be execute, core_run, or compare.")

    try:
        request = job_payload(source)
    except Exception as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

    idempotency_key = str(payload.get("idempotencyKey") or payload.get("idempotency_key") or "") or None
    timeout_seconds = payload.get("timeoutSeconds") or payload.get("timeout_seconds") or source.get("timeoutSeconds")
    max_attempts = payload.get("maxAttempts") or payload.get("max_attempts")
    priority = payload.get("priority")
    cache_mode = payload.get("cacheMode") or payload.get("cache_mode") or "use"
    try:
        record, deduplicated = jobs.submit(
            operation=operation,
            payload=request.model_dump(mode="json"),
            auth=auth,
            timeout_seconds=int(timeout_seconds) if timeout_seconds is not None else None,
            max_attempts=int(max_attempts) if max_attempts is not None else None,
            idempotency_key=idempotency_key,
            priority=int(priority) if priority is not None else None,
            cache_mode=str(cache_mode),
        )
    except (QueueCapacityError, ProjectLimitError) as exc:
        raise HTTPException(status_code=429, detail=str(exc)) from exc
    except ValueError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc
    record["deduplicated"] = deduplicated
    return record


@app.get("/v1/jobs", dependencies=[Depends(require_compute_auth)])
def list_jobs(
    status: str | None = Query(default=None),
    project_id: str | None = Query(default=None, max_length=128),
    limit: int = Query(default=50, ge=1, le=200),
    offset: int = Query(default=0, ge=0),
):
    try:
        result = jobs.list(status=status, project_id=project_id, limit=limit, offset=offset)
    except ValueError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc
    return {"schema": "sc-lab-compute-job-list/1.0", **result}


@app.get("/v1/jobs/{job_id}", dependencies=[Depends(require_compute_auth)])
def get_job(job_id: str):
    job = jobs.get(job_id)
    if not job:
        raise HTTPException(status_code=404, detail="Compute job not found.")
    return job


@app.delete("/v1/jobs/{job_id}", dependencies=[Depends(require_compute_auth)])
def cancel_job_delete(job_id: str):
    job = jobs.cancel(job_id)
    if not job:
        raise HTTPException(status_code=404, detail="Compute job not found.")
    return job


@app.post("/v1/jobs/{job_id}/cancel", dependencies=[Depends(require_compute_auth)])
def cancel_job(job_id: str):
    job = jobs.cancel(job_id)
    if not job:
        raise HTTPException(status_code=404, detail="Compute job not found.")
    return job


@app.post("/v1/jobs/{job_id}/retry", dependencies=[Depends(require_compute_auth)])
def retry_job(job_id: str):
    try:
        job = jobs.retry(job_id)
    except InvalidJobStateError as exc:
        raise HTTPException(status_code=409, detail=str(exc)) from exc
    if not job:
        raise HTTPException(status_code=404, detail="Compute job not found.")
    return job


@app.post("/v1/jobs/{job_id}/pause", dependencies=[Depends(require_compute_auth)])
def pause_job(job_id: str):
    try:
        job = jobs.pause(job_id)
    except InvalidJobStateError as exc:
        raise HTTPException(status_code=409, detail=str(exc)) from exc
    if not job:
        raise HTTPException(status_code=404, detail="Compute job not found.")
    return job


@app.post("/v1/jobs/{job_id}/resume", dependencies=[Depends(require_compute_auth)])
def resume_job(job_id: str):
    try:
        job = jobs.resume(job_id)
    except InvalidJobStateError as exc:
        raise HTTPException(status_code=409, detail=str(exc)) from exc
    if not job:
        raise HTTPException(status_code=404, detail="Compute job not found.")
    return job


@app.get("/v1/jobs/{job_id}/checkpoints", dependencies=[Depends(require_compute_auth)])
def job_checkpoints(job_id: str, limit: int = Query(default=20, ge=1, le=200)):
    result = jobs.checkpoints(job_id, limit=limit)
    if not result:
        raise HTTPException(status_code=404, detail="Compute job not found.")
    return result


@app.get("/v1/cache/status", dependencies=[Depends(require_compute_auth)])
def cache_status():
    return jobs.cache_status()


@app.delete("/v1/cache", dependencies=[Depends(require_compute_auth)])
def cache_purge():
    return jobs.purge_cache()


@app.get("/v1/workers", dependencies=[Depends(require_compute_auth)])
def workers():
    return jobs.workers_status()


@app.get("/v1/queue/status", dependencies=[Depends(require_compute_auth)])
def queue_status():
    return jobs.queue_status()


@app.post("/v1/reports/validate")
def report_validate(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    errors = []
    if not str(payload.get("title") or "").strip():
        errors.append("title is required")
    analyses = payload.get("analyses")
    if not isinstance(analyses, list) or not analyses:
        errors.append("at least one analysis is required")
    return {"ok": not errors, "valid": not errors, "errors": errors, "schema": "sc-lab-report-validation/1.0"}


@app.post("/v1/reports/pdf")
def report_pdf(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    validation = report_validate(payload, auth)
    if not validation["valid"]:
        raise HTTPException(status_code=422, detail=validation["errors"])
    buffer = io.BytesIO()
    pagesize = A4 if payload.get("pageSize") == "A4" else letter
    pdf = canvas.Canvas(buffer, pagesize=pagesize)
    _, height = pagesize
    pdf.setTitle(str(payload.get("title")))
    pdf.setFont("Helvetica-Bold", 16)
    pdf.drawString(54, height - 54, str(payload.get("title"))[:100])
    y = height - 86
    pdf.setFont("Helvetica", 10)
    for index, analysis in enumerate(payload.get("analyses") or [], 1):
        line = f"{index}. {analysis.get('title') or analysis.get('method') or 'Analysis'}"
        pdf.drawString(54, y, line[:120])
        y -= 16
        if y < 54:
            pdf.showPage()
            y = height - 54
            pdf.setFont("Helvetica", 10)
    pdf.save()
    raw = buffer.getvalue()
    return {
        "ok": True,
        "mimeType": "application/pdf",
        "filename": "sustainable-catalyst-lab-report.pdf",
        "base64": base64.b64encode(raw).decode("ascii"),
        "sizeBytes": len(raw),
    }


@app.post("/v1/handoffs/decision-studio/validate")
def handoff_validate(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    packet = payload.get("packet")
    valid = isinstance(packet, dict) and bool(packet)
    return {
        "ok": valid,
        "valid": valid,
        "errors": [] if valid else ["packet must be a non-empty object"],
        "schema": "sc-lab-decision-handoff-validation/1.0",
    }


@app.get("/v1/experiments/health")
def experiments_health_route():
    body=experiment_framework_health(); body["serviceVersion"]=settings.version; return body

@app.get("/v1/experiments/policies")
def experiments_policies_route():
    body=experiment_framework_policies(); body["serviceVersion"]=settings.version; return body

@app.post("/v1/experiments/protocols/normalize")
def experiments_protocol_normalize_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return normalize_protocol(payload)
    except ExperimentFrameworkError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/experiments/protocols/validate")
def experiments_protocol_validate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return validate_protocol(payload)
    except ExperimentFrameworkError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/experiments/runs/build")
def experiments_run_build_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_experiment_run(payload)
    except ExperimentFrameworkError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/experiments/runs/compare")
def experiments_runs_compare_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return compare_experiment_runs(payload)
    except ExperimentFrameworkError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/experiments/reports/build")
def experiments_report_build_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_experiment_report(payload)
    except ExperimentFrameworkError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/experiments/verify")
def experiments_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return verify_experiment_record(payload)
    except ExperimentFrameworkError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.get("/v1/design-studies/health")
def design_studies_health_route():
    body=design_studies_health(); body["serviceVersion"]=settings.version; return body

@app.get("/v1/design-studies/policies")
def design_studies_policies_route():
    body=design_studies_policies(); body["serviceVersion"]=settings.version; return body

@app.post("/v1/design-studies/normalize")
def design_studies_normalize_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return normalize_study(payload)
    except DesignStudyError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/design-studies/generate")
def design_studies_generate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return generate_design(payload)
    except DesignStudyError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/design-studies/analyze")
def design_studies_analyze_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return analyze_design_results(payload)
    except DesignStudyError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/design-studies/recommend")
def design_studies_recommend_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return recommend_design(payload)
    except DesignStudyError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/design-studies/batches/build")
def design_studies_batch_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_design_batch(payload)
    except DesignStudyError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/design-studies/verify")
def design_studies_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return verify_design_record(payload)
    except DesignStudyError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.get("/v1/design-studies/v0560/health")
def advanced_experimental_design_health_route():
    body = advanced_experimental_design_health(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/design-studies/v0560/policies")
def advanced_experimental_design_policies_route():
    return advanced_experimental_design_policies()

@app.post("/v1/design-studies/v0560/normalize")
def advanced_experimental_design_normalize_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok": True, "version": "0.56.0", "spec": normalize_advanced_experimental_design(payload.get("spec") or payload)}
    except AdvancedExperimentalDesignError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/design-studies/v0560/optimal-design")
def advanced_experimental_design_generate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return generate_optimal_design(payload)
    except AdvancedExperimentalDesignError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/design-studies/v0560/sequential-plan")
def advanced_experimental_design_sequential_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return build_sequential_experiment_plan(payload)
    except AdvancedExperimentalDesignError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/design-studies/v0560/verify")
def advanced_experimental_design_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return verify_advanced_experimental_design(payload)
    except AdvancedExperimentalDesignError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc



# v0.66.0 Competing Hypotheses & Scientific Argumentation
@app.get("/v1/scientific-argumentation/v0660/health")
def scientific_argumentation_v0660_health_route():
    body=scientific_argumentation_health_v0660(); body["serviceVersion"]=settings.version; return body

@app.get("/v1/scientific-argumentation/v0660/policies")
def scientific_argumentation_v0660_policies_route():
    return scientific_argumentation_policies_v0660()

@app.post("/v1/scientific-argumentation/v0660/normalize-case")
def scientific_argumentation_v0660_normalize_case_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"case":normalize_scientific_argumentation_case_v0660(payload.get("case") if isinstance(payload,dict) else {})}
    except ScientificArgumentationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-argumentation/v0660/normalize-hypothesis")
def scientific_argumentation_v0660_normalize_hypothesis_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"hypothesis":normalize_scientific_hypothesis_v0660(payload.get("hypothesis") if isinstance(payload,dict) else {})}
    except ScientificArgumentationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-argumentation/v0660/normalize-evidence-link")
def scientific_argumentation_v0660_normalize_evidence_link_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"evidenceLink":normalize_argument_evidence_link_v0660(payload.get("evidenceLink") if isinstance(payload,dict) else {})}
    except ScientificArgumentationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-argumentation/v0660/normalize-test")
def scientific_argumentation_v0660_normalize_test_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"test":normalize_discriminating_test_v0660(payload.get("test") if isinstance(payload,dict) else {})}
    except ScientificArgumentationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-argumentation/v0660/review-hypothesis")
def scientific_argumentation_v0660_review_hypothesis_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return record_scientific_hypothesis_review_v0660(payload)
    except ScientificArgumentationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-argumentation/v0660/review-case")
def scientific_argumentation_v0660_review_case_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return record_scientific_argumentation_case_review_v0660(payload)
    except ScientificArgumentationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-argumentation/v0660/evaluate")
def scientific_argumentation_v0660_evaluate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return evaluate_scientific_argumentation_v0660(payload)
    except ScientificArgumentationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-argumentation/v0660/packet")
def scientific_argumentation_v0660_packet_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return build_scientific_argumentation_packet_v0660(payload)
    except ScientificArgumentationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-argumentation/v0660/verify")
def scientific_argumentation_v0660_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return verify_scientific_argumentation_packet_v0660(payload)
    except ScientificArgumentationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


# v0.67.0 Causal Inference & Quasi-Experimental Methods
@app.get("/v1/causal-inference/v0670/health")
def causal_inference_v0670_health_route():
    body=causal_inference_health_v0670(); body["serviceVersion"]=settings.version; return body

@app.get("/v1/causal-inference/v0670/policies")
def causal_inference_v0670_policies_route():
    return causal_inference_policies_v0670()

@app.post("/v1/causal-inference/v0670/normalize-design")
def causal_inference_v0670_normalize_design_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"design":normalize_causal_design_v0670(payload.get("design") if isinstance(payload,dict) else {})}
    except CausalInferenceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/causal-inference/v0670/normalize-estimate")
def causal_inference_v0670_normalize_estimate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"estimate":normalize_causal_estimate_v0670(payload.get("estimate") if isinstance(payload,dict) else {})}
    except CausalInferenceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/causal-inference/v0670/normalize-diagnostic")
def causal_inference_v0670_normalize_diagnostic_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"diagnostic":normalize_causal_diagnostic_v0670(payload.get("diagnostic") if isinstance(payload,dict) else {})}
    except CausalInferenceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/causal-inference/v0670/review")
def causal_inference_v0670_review_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return record_causal_review_v0670(payload)
    except CausalInferenceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/causal-inference/v0670/evaluate")
def causal_inference_v0670_evaluate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return evaluate_causal_inference_v0670(payload)
    except CausalInferenceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/causal-inference/v0670/packet")
def causal_inference_v0670_packet_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return build_causal_inference_packet_v0670(payload)
    except CausalInferenceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/causal-inference/v0670/verify")
def causal_inference_v0670_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return verify_causal_inference_packet_v0670(payload)
    except CausalInferenceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

# v0.68.0 Hierarchical, Multilevel & Cross-Study Modeling
@app.get("/v1/hierarchical-modeling/v0680/health")
def hierarchical_modeling_v0680_health_route():
    body=hierarchical_modeling_health_v0680(); body["serviceVersion"]=settings.version; return body

@app.get("/v1/hierarchical-modeling/v0680/policies")
def hierarchical_modeling_v0680_policies_route():
    return hierarchical_modeling_policies_v0680()

@app.post("/v1/hierarchical-modeling/v0680/normalize-model")
def hierarchical_modeling_v0680_normalize_model_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"model":normalize_hierarchical_model_v0680(payload.get("model") if isinstance(payload,dict) else {})}
    except HierarchicalModelingError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/hierarchical-modeling/v0680/normalize-unit")
def hierarchical_modeling_v0680_normalize_unit_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"unit":normalize_hierarchical_unit_v0680(payload.get("unit") if isinstance(payload,dict) else {})}
    except HierarchicalModelingError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/hierarchical-modeling/v0680/fit")
def hierarchical_modeling_v0680_fit_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return fit_hierarchical_model_v0680(payload)
    except HierarchicalModelingError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/hierarchical-modeling/v0680/review")
def hierarchical_modeling_v0680_review_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return record_hierarchical_modeling_review_v0680(payload)
    except HierarchicalModelingError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/hierarchical-modeling/v0680/evaluate")
def hierarchical_modeling_v0680_evaluate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return evaluate_hierarchical_modeling_v0680(payload)
    except HierarchicalModelingError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/hierarchical-modeling/v0680/packet")
def hierarchical_modeling_v0680_packet_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return build_hierarchical_modeling_packet_v0680(payload)
    except HierarchicalModelingError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/hierarchical-modeling/v0680/verify")
def hierarchical_modeling_v0680_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return verify_hierarchical_modeling_packet_v0680(payload)
    except HierarchicalModelingError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

# v0.69.0 Scientific Theory & Conceptual Model Workspace
@app.get("/v1/scientific-theory/v0690/health")
def scientific_theory_v0690_health_route():
    body=scientific_theory_health_v0690(); body["serviceVersion"]=settings.version; return body

@app.get("/v1/scientific-theory/v0690/policies")
def scientific_theory_v0690_policies_route():
    return scientific_theory_policies_v0690()

@app.post("/v1/scientific-theory/v0690/normalize-theory")
def scientific_theory_v0690_normalize_theory_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"theory":normalize_scientific_theory_v0690(payload.get("theory") if isinstance(payload,dict) else {})}
    except ScientificTheoryError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-theory/v0690/normalize-construct")
def scientific_theory_v0690_normalize_construct_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"construct":normalize_theory_construct_v0690(payload.get("construct") if isinstance(payload,dict) else {})}
    except ScientificTheoryError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-theory/v0690/normalize-relation")
def scientific_theory_v0690_normalize_relation_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"relation":normalize_theory_relation_v0690(payload.get("relation") if isinstance(payload,dict) else {})}
    except ScientificTheoryError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-theory/v0690/normalize-prediction")
def scientific_theory_v0690_normalize_prediction_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"prediction":normalize_theory_prediction_v0690(payload.get("prediction") if isinstance(payload,dict) else {})}
    except ScientificTheoryError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-theory/v0690/graph")
def scientific_theory_v0690_graph_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return build_scientific_theory_graph_v0690(payload)
    except ScientificTheoryError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-theory/v0690/review")
def scientific_theory_v0690_review_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return record_scientific_theory_review_v0690(payload)
    except ScientificTheoryError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-theory/v0690/evaluate")
def scientific_theory_v0690_evaluate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return evaluate_scientific_theory_v0690(payload)
    except ScientificTheoryError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-theory/v0690/packet")
def scientific_theory_v0690_packet_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return build_scientific_theory_packet_v0690(payload)
    except ScientificTheoryError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-theory/v0690/verify")
def scientific_theory_v0690_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return verify_scientific_theory_packet_v0690(payload)
    except ScientificTheoryError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

# v0.70.0 Research Questions, Hypothesis Registry & Preregistration
@app.get("/v1/preregistration/v0700/health")
def preregistration_v0700_health_route():
    body=preregistration_health_v0700(); body["serviceVersion"]=settings.version; return body

@app.get("/v1/preregistration/v0700/policies")
def preregistration_v0700_policies_route():
    return preregistration_policies_v0700()

@app.post("/v1/preregistration/v0700/normalize-question")
def preregistration_v0700_normalize_question_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"question":normalize_research_question_v0700(payload.get("question") if isinstance(payload,dict) else {})}
    except PreregistrationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/preregistration/v0700/normalize-hypothesis")
def preregistration_v0700_normalize_hypothesis_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"hypothesis":normalize_registered_hypothesis_v0700(payload.get("hypothesis") if isinstance(payload,dict) else {})}
    except PreregistrationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/preregistration/v0700/normalize-preregistration")
def preregistration_v0700_normalize_preregistration_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"preregistration":normalize_preregistration_v0700(payload.get("preregistration") if isinstance(payload,dict) else {})}
    except PreregistrationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/preregistration/v0700/normalize-deviation")
def preregistration_v0700_normalize_deviation_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"deviation":normalize_preregistration_deviation_v0700(payload.get("deviation") if isinstance(payload,dict) else {})}
    except PreregistrationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/preregistration/v0700/freeze")
def preregistration_v0700_freeze_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return freeze_preregistration_v0700(payload)
    except PreregistrationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/preregistration/v0700/verify-freeze")
def preregistration_v0700_verify_freeze_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return verify_preregistration_freeze_v0700(payload)
    except PreregistrationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/preregistration/v0700/review")
def preregistration_v0700_review_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return record_preregistration_review_v0700(payload)
    except PreregistrationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/preregistration/v0700/evaluate")
def preregistration_v0700_evaluate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return evaluate_preregistration_v0700(payload)
    except PreregistrationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/preregistration/v0700/packet")
def preregistration_v0700_packet_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return build_preregistration_packet_v0700(payload)
    except PreregistrationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/preregistration/v0700/verify")
def preregistration_v0700_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return verify_preregistration_packet_v0700(payload)
    except PreregistrationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

# v0.65.0 Scientific Evidence Grading, Contradiction Analysis & Consensus Boundaries
@app.get("/v1/evidence-grading/v0650/health")
def scientific_evidence_grading_v0650_health_route():
    body=scientific_evidence_grading_health_v0650(); body["serviceVersion"]=settings.version; return body

@app.get("/v1/evidence-grading/v0650/policies")
def scientific_evidence_grading_v0650_policies_route():
    return scientific_evidence_grading_policies_v0650()

@app.post("/v1/evidence-grading/v0650/normalize-assessment")
def scientific_evidence_grading_v0650_normalize_assessment_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"assessment":normalize_scientific_evidence_assessment_v0650(payload.get("assessment") if isinstance(payload,dict) else {})}
    except ScientificEvidenceGradingError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/evidence-grading/v0650/review")
def scientific_evidence_grading_v0650_review_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return record_scientific_boundary_review_v0650(payload)
    except ScientificEvidenceGradingError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/evidence-grading/v0650/evaluate")
def scientific_evidence_grading_v0650_evaluate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return evaluate_scientific_evidence_v0650(payload)
    except ScientificEvidenceGradingError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/evidence-grading/v0650/packet")
def scientific_evidence_grading_v0650_packet_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return build_scientific_consensus_packet_v0650(payload)
    except ScientificEvidenceGradingError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/evidence-grading/v0650/verify")
def scientific_evidence_grading_v0650_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return verify_scientific_consensus_packet_v0650(payload)
    except ScientificEvidenceGradingError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc




# v0.64.0 Replication, Systematic Evidence Synthesis & Meta-Analysis
@app.get("/v1/evidence-synthesis/v0640/health")
def evidence_synthesis_v0640_health_route():
    body=evidence_synthesis_health_v0640(); body["serviceVersion"]=settings.version; return body

@app.get("/v1/evidence-synthesis/v0640/policies")
def evidence_synthesis_v0640_policies_route():
    return evidence_synthesis_policies_v0640()

@app.post("/v1/evidence-synthesis/v0640/normalize-protocol")
def evidence_synthesis_v0640_normalize_protocol_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"protocol":normalize_synthesis_protocol_v0640(payload.get("protocol") if isinstance(payload,dict) else {})}
    except SystematicEvidenceSynthesisError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/evidence-synthesis/v0640/normalize-effect")
def evidence_synthesis_v0640_normalize_effect_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok":True,"effect":normalize_study_effect_v0640(payload.get("effect") if isinstance(payload,dict) else {})}
    except SystematicEvidenceSynthesisError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/evidence-synthesis/v0640/review")
def evidence_synthesis_v0640_review_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return record_synthesis_review_v0640(payload)
    except SystematicEvidenceSynthesisError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/evidence-synthesis/v0640/meta-analysis")
def evidence_synthesis_v0640_meta_analysis_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return meta_analyze_v0640(payload)
    except SystematicEvidenceSynthesisError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/evidence-synthesis/v0640/packet")
def evidence_synthesis_v0640_packet_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return build_evidence_synthesis_packet_v0640(payload)
    except SystematicEvidenceSynthesisError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/evidence-synthesis/v0640/verify")
def evidence_synthesis_v0640_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return verify_evidence_synthesis_packet_v0640(payload)
    except SystematicEvidenceSynthesisError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

# v0.63.0 Scientific Literature, Citation Graph & Source-to-Claim Provenance

@app.get("/v1/scientific-literature/v0630/health")
def scientific_literature_v0630_health_route():
    body = scientific_literature_health_v0630(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/scientific-literature/v0630/policies")
def scientific_literature_v0630_policies_route():
    return scientific_literature_policies_v0630()

@app.post("/v1/scientific-literature/v0630/normalize-source")
def scientific_literature_v0630_normalize_source_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok": True, "source": normalize_scientific_literature_source_v0630(payload.get("source") if isinstance(payload, dict) else {})}
    except ScientificLiteratureProvenanceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-literature/v0630/review-source")
def scientific_literature_v0630_review_source_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return record_scientific_literature_source_review_v0630(payload)
    except ScientificLiteratureProvenanceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-literature/v0630/normalize-claim-link")
def scientific_literature_v0630_normalize_claim_link_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok": True, "link": normalize_scientific_claim_link_v0630(payload.get("link") if isinstance(payload, dict) else {})}
    except ScientificLiteratureProvenanceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-literature/v0630/normalize-citation-edge")
def scientific_literature_v0630_normalize_citation_edge_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok": True, "edge": normalize_scientific_citation_edge_v0630(payload.get("edge") if isinstance(payload, dict) else {})}
    except ScientificLiteratureProvenanceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-literature/v0630/evaluate")
def scientific_literature_v0630_evaluate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return evaluate_scientific_literature_v0630(payload)
    except ScientificLiteratureProvenanceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-literature/v0630/packet")
def scientific_literature_v0630_packet_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return build_scientific_literature_packet_v0630(payload)
    except ScientificLiteratureProvenanceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-literature/v0630/verify")
def scientific_literature_v0630_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return verify_scientific_literature_packet_v0630(payload)
    except ScientificLiteratureProvenanceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


# v0.62.0 Scientific Claims, Evidence Matrix & Conclusion Traceability

@app.get("/v1/scientific-claims/v0620/health")
def scientific_claims_v0620_health_route():
    body = scientific_claims_traceability_health_v0620(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/scientific-claims/v0620/policies")
def scientific_claims_v0620_policies_route():
    return scientific_claims_traceability_policies_v0620()

@app.post("/v1/scientific-claims/v0620/normalize-claim")
def scientific_claims_v0620_normalize_claim_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok": True, "claim": normalize_scientific_claim_v0620(payload.get("claim") if isinstance(payload, dict) else {})}
    except ScientificClaimsTraceabilityError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-claims/v0620/review-claim")
def scientific_claims_v0620_review_claim_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return record_scientific_claim_review_v0620(payload)
    except ScientificClaimsTraceabilityError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-claims/v0620/normalize-conclusion")
def scientific_claims_v0620_normalize_conclusion_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok": True, "conclusion": normalize_scientific_conclusion_v0620(payload.get("conclusion") if isinstance(payload, dict) else {})}
    except ScientificClaimsTraceabilityError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-claims/v0620/review-conclusion")
def scientific_claims_v0620_review_conclusion_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return record_scientific_conclusion_review_v0620(payload)
    except ScientificClaimsTraceabilityError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-claims/v0620/evaluate")
def scientific_claims_v0620_evaluate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return evaluate_scientific_evidence_matrix_v0620(payload)
    except ScientificClaimsTraceabilityError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-claims/v0620/packet")
def scientific_claims_v0620_packet_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return build_scientific_traceability_packet_v0620(payload)
    except ScientificClaimsTraceabilityError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-claims/v0620/verify")
def scientific_claims_v0620_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return verify_scientific_traceability_packet_v0620(payload)
    except ScientificClaimsTraceabilityError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


# v0.61.0 End-to-End Scientific Study & Research Project Lifecycle

@app.get("/v1/scientific-studies/v0610/health")
def scientific_study_lifecycle_v0610_health_route():
    body = scientific_study_lifecycle_health_v0610(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/scientific-studies/v0610/policies")
def scientific_study_lifecycle_v0610_policies_route():
    return scientific_study_lifecycle_policies_v0610()

@app.get("/v1/scientific-studies/v0610/templates")
def scientific_study_lifecycle_v0610_templates_route():
    return scientific_study_templates_v0610()

@app.post("/v1/scientific-studies/v0610/normalize")
def scientific_study_lifecycle_v0610_normalize_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok": True, "study": normalize_scientific_study_v0610(payload.get("study") if isinstance(payload, dict) else {})}
    except ScientificStudyLifecycleError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-studies/v0610/evaluate")
def scientific_study_lifecycle_v0610_evaluate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return evaluate_scientific_study_lifecycle_v0610(payload)
    except (ScientificStudyLifecycleError, IntegratedResearchBetaError) as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-studies/v0610/review")
def scientific_study_lifecycle_v0610_review_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return record_scientific_study_stage_review_v0610(payload)
    except ScientificStudyLifecycleError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-studies/v0610/packet")
def scientific_study_lifecycle_v0610_packet_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return build_scientific_study_packet_v0610(payload)
    except (ScientificStudyLifecycleError, IntegratedResearchBetaError) as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-studies/v0610/verify")
def scientific_study_lifecycle_v0610_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return verify_scientific_study_packet_v0610(payload)
    except ScientificStudyLifecycleError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


# v0.60.1 Beta Field Diagnostics, Integration Soak & Runtime Repair

@app.get("/v1/beta-diagnostics/v0601/health")
def beta_field_diagnostics_v0601_health_route():
    body = beta_field_diagnostics_health(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/beta-diagnostics/v0601/policies")
def beta_field_diagnostics_v0601_policies_route():
    return beta_field_diagnostics_policies()

@app.get("/v1/beta-diagnostics/v0601/scenarios")
def beta_field_diagnostics_v0601_scenarios_route():
    return beta_field_diagnostic_scenarios()

@app.post("/v1/beta-diagnostics/v0601/probe")
def beta_field_diagnostics_v0601_probe_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return beta_field_integration_probe(payload)
    except BetaFieldDiagnosticsError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/beta-diagnostics/v0601/soak")
def beta_field_diagnostics_v0601_soak_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return beta_field_analyze_soak(payload)
    except BetaFieldDiagnosticsError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/beta-diagnostics/v0601/packet")
def beta_field_diagnostics_v0601_packet_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return build_beta_field_diagnostic_packet(payload)
    except BetaFieldDiagnosticsError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/beta-diagnostics/v0601/verify")
def beta_field_diagnostics_v0601_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return verify_beta_field_diagnostic_packet(payload)
    except BetaFieldDiagnosticsError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


# v0.60.0 Integrated Scientific Research Beta

@app.get("/v1/integrated-research/v0600/health")
def integrated_research_beta_v0600_health_route():
    body = integrated_beta_health(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/integrated-research/v0600/policies")
def integrated_research_beta_v0600_policies_route():
    return integrated_beta_policies()

@app.get("/v1/integrated-research/v0600/capabilities")
def integrated_research_beta_v0600_capabilities_route():
    return integrated_beta_capabilities()

@app.post("/v1/integrated-research/v0600/journey")
def integrated_research_beta_v0600_journey_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return integrated_research_journey(payload)
    except IntegratedResearchBetaError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/integrated-research/v0600/readiness")
def integrated_research_beta_v0600_readiness_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return integrated_beta_readiness(payload)
    except IntegratedResearchBetaError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/integrated-research/v0600/packet")
def integrated_research_beta_v0600_packet_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return build_integrated_beta_packet(payload)
    except IntegratedResearchBetaError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/integrated-research/v0600/verify")
def integrated_research_beta_v0600_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return verify_integrated_beta_packet(payload)
    except IntegratedResearchBetaError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


# v0.59.0 Security, Privacy, Reproducibility & Scientific Audit

@app.get("/v1/scientific-audit/v0590/health")
def scientific_audit_v0590_health_route():
    body = scientific_audit_health(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/scientific-audit/v0590/policies")
def scientific_audit_v0590_policies_route():
    return scientific_audit_policies()

@app.post("/v1/scientific-audit/v0590/scan")
def scientific_audit_v0590_scan_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return scan_scientific_audit_surface(payload.get("target") if "target" in payload else payload)
    except ScientificAuditError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-audit/v0590/minimize")
def scientific_audit_v0590_minimize_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return scientific_data_minimization_review(payload)
    except ScientificAuditError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-audit/v0590/redact-export")
def scientific_audit_v0590_redact_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return build_scientific_redacted_export(payload.get("target") if "target" in payload else payload)
    except ScientificAuditError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-audit/v0590/reproducibility")
def scientific_audit_v0590_reproducibility_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return run_reproducibility_audit_v0590(payload)
    except ScientificAuditError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-audit/v0590/audit")
def scientific_audit_v0590_audit_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return run_scientific_audit_v0590(payload)
    except ScientificAuditError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/scientific-audit/v0590/verify")
def scientific_audit_v0590_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return verify_scientific_audit_v0590(payload.get("report") if isinstance(payload.get("report"), dict) else payload)
    except ScientificAuditError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


# v0.58.0 Large-Model, Large-Dataset & Compute Hardening

@app.get("/v1/compute-hardening/v0580/health")
def scientific_compute_hardening_health_route():
    body = scientific_compute.health(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/compute-hardening/v0580/policies")
def scientific_compute_hardening_policies_route():
    return scientific_compute_policies()

@app.post("/v1/compute-hardening/v0580/assess")
def scientific_compute_hardening_assess_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return assess_scientific_workload(payload)
    except ScientificComputeHardeningError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/compute-hardening/v0580/dataset-window")
def scientific_compute_hardening_dataset_window_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return scientific_dataset_window(payload)
    except ScientificComputeHardeningError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/compute-hardening/v0580/run")
def scientific_compute_hardening_run_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return scientific_compute.execute(payload.get("operation"), payload.get("payload") or {}, use_cache=bool(payload.get("useCache", True)))
    except ScientificComputeHardeningError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/compute-hardening/v0580/jobs")
def scientific_compute_hardening_submit_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return scientific_compute.submit(payload.get("operation"), payload.get("payload") or {})
    except ScientificComputeHardeningError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.get("/v1/compute-hardening/v0580/jobs")
def scientific_compute_hardening_jobs_route(limit: int = Query(30, ge=1, le=200)):
    return scientific_compute.list_jobs(limit)

@app.get("/v1/compute-hardening/v0580/jobs/{job_id}")
def scientific_compute_hardening_job_route(job_id: str):
    try: return {"ok": True, "version": "0.58.0", "job": scientific_compute.status(job_id)}
    except ScientificComputeHardeningError as exc: raise HTTPException(status_code=404, detail=str(exc)) from exc

@app.get("/v1/compute-hardening/v0580/jobs/{job_id}/result")
def scientific_compute_hardening_job_result_route(job_id: str):
    try: return scientific_compute.result(job_id)
    except ScientificComputeHardeningError as exc: raise HTTPException(status_code=404, detail=str(exc)) from exc

@app.post("/v1/compute-hardening/v0580/jobs/{job_id}/cancel")
def scientific_compute_hardening_job_cancel_route(job_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return scientific_compute.cancel(job_id)
    except ScientificComputeHardeningError as exc: raise HTTPException(status_code=404, detail=str(exc)) from exc

@app.get("/v1/compute-hardening/v0580/cache")
def scientific_compute_hardening_cache_route():
    return scientific_compute.cache_stats()

@app.post("/v1/compute-hardening/v0580/cache/clear")
def scientific_compute_hardening_cache_clear_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return scientific_compute.clear_cache()


# v0.57.0 Scientific Workflow Composer

@app.get("/v1/workflows/v0570/health")
def scientific_workflow_composer_health_route():
    body = scientific_workflow_composer_health(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/workflows/v0570/policies")
def scientific_workflow_composer_policies_route():
    return scientific_workflow_composer_policies()

@app.get("/v1/workflows/v0570/catalog")
def scientific_workflow_composer_catalog_route():
    return scientific_workflow_catalog()

@app.get("/v1/workflows/v0570/templates")
def scientific_workflow_composer_templates_route():
    return scientific_workflow_templates()

@app.post("/v1/workflows/v0570/normalize")
def scientific_workflow_composer_normalize_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return {"ok": True, "version": "0.57.0", "workflow": normalize_scientific_workflow(payload)}
    except ScientificWorkflowComposerError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/workflows/v0570/run")
def scientific_workflow_composer_run_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return run_scientific_workflow(payload)
    except ScientificWorkflowComposerError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/workflows/v0570/compare")
def scientific_workflow_composer_compare_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return compare_scientific_workflow_runs(payload)
    except ScientificWorkflowComposerError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.get("/v1/model-studio/health")
def model_studio_health_route():
    return model_studio_health()

@app.get("/v1/model-studio/policies")
def model_studio_policies_route():
    return model_studio_policies()

@app.post("/v1/model-studio/models/normalize")
def model_studio_model_normalize_route(payload: dict[str, Any]):
    try: return {"ok": True, "model": normalize_model_studio_model(payload)}
    except ModelStudioError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/graphs/normalize")
def model_studio_graph_normalize_route(payload: dict[str, Any]):
    try: return {"ok": True, "graph": normalize_model_studio_graph(payload)}
    except ModelStudioError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/bundles/build")
def model_studio_bundle_build_route(payload: dict[str, Any]):
    try: return build_model_studio_bundle(payload)
    except ModelStudioError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/equations/validate")
def model_studio_equation_validate_route(payload: dict[str, Any]):
    try: return validate_model_studio_equation(payload)
    except ModelStudioError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/equations/preview")
def model_studio_equation_preview_route(payload: dict[str, Any]):
    try: return preview_model_studio_equation(payload)
    except ModelStudioError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.get("/v1/model-handoff/health")
def model_handoff_health_route():
    return model_handoff_health()

@app.get("/v1/model-handoff/policies")
def model_handoff_policies_route():
    return model_handoff_policies()

@app.post("/v1/model-handoff/models/normalize")
def model_handoff_model_normalize_route(payload: dict[str, Any]):
    try: return {"ok": True, "model": normalize_shared_model(payload)}
    except ModelHandoffError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-handoff/outbound/workbench")
def model_handoff_outbound_workbench_route(payload: dict[str, Any]):
    try: return build_workbench_handoff(payload)
    except ModelHandoffError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-handoff/inbound/workbench")
def model_handoff_inbound_workbench_route(payload: dict[str, Any]):
    try: return import_workbench_handoff(payload)
    except ModelHandoffError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.get("/v1/model-packages/health")
def reproducible_model_package_health_route():
    return reproducible_model_package_health()

@app.get("/v1/model-packages/policies")
def reproducible_model_package_policies_route():
    return reproducible_model_package_policies()

@app.post("/v1/model-packages/build")
def reproducible_model_package_build_route(payload: dict[str, Any]):
    try: return build_reproducible_model_package(payload)
    except ReproducibleModelPackageError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-packages/verify")
def reproducible_model_package_verify_route(payload: dict[str, Any]):
    try: return verify_reproducible_model_package(payload)
    except ReproducibleModelPackageError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-packages/research-bundle")
def reproducible_model_package_bundle_route(payload: dict[str, Any]):
    try: return build_model_research_bundle(payload)
    except ReproducibleModelPackageError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-packages/registry-projection")
def reproducible_model_package_registry_projection_route(payload: dict[str, Any]):
    try:
        package = payload.get("package") if isinstance(payload.get("package"), dict) else build_reproducible_model_package(payload)["package"]
        return {"ok": True, "projection": reproducible_model_registry_projection(package)}
    except ReproducibleModelPackageError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-packages/register")
def reproducible_model_package_register_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        package = payload.get("package") if isinstance(payload.get("package"), dict) else build_reproducible_model_package(payload)["package"]
        verified = verify_reproducible_model_package(package)
        if not verified.get("ok"):
            raise ReproducibleModelPackageError("Reproducible package integrity verification failed before registry registration.")
        projection = reproducible_model_registry_projection(package)
        registered = model_registry.register(projection, auth.get("subject", "operator"))
        return {"ok": True, "package": package, "verification": verified, "registry": registered}
    except ReproducibleModelPackageError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc
    except ModelRegistryError as exc: raise _model_registry_http_error(exc) from exc

@app.get("/v1/model-studio/diagnostics/health")
def model_studio_diagnostics_health_route():
    return model_diagnostics_health()

@app.get("/v1/model-studio/diagnostics/policies")
def model_studio_diagnostics_policies_route():
    return model_diagnostics_policies()

@app.post("/v1/model-studio/diagnostics/run")
def model_studio_diagnostics_run_route(payload: dict[str, Any]):
    try: return diagnose_scientific_model(payload)
    except ModelDiagnosticsError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/cross-validation/run")
def model_studio_cross_validation_run_route(payload: dict[str, Any]):
    try: return cross_validate_scientific_model(payload)
    except ModelDiagnosticsError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/comparison/run")
def model_studio_comparison_run_route(payload: dict[str, Any]):
    try: return compare_scientific_models(payload)
    except ModelDiagnosticsError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.get("/v1/model-studio/dynamic-systems/health")
def model_studio_dynamic_systems_health_route():
    return dynamic_systems_health()

@app.get("/v1/model-studio/dynamic-systems/policies")
def model_studio_dynamic_systems_policies_route():
    return dynamic_systems_policies()

@app.get("/v1/model-studio/dynamic-systems/templates")
def model_studio_dynamic_systems_templates_route():
    return dynamic_system_templates()

@app.post("/v1/model-studio/dynamic-systems/normalize")
def model_studio_dynamic_systems_normalize_route(payload: dict[str, Any]):
    try: return {"ok": True, "system": normalize_dynamic_system(payload.get("system") or payload)}
    except DynamicSystemError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/dynamic-systems/simulate")
def model_studio_dynamic_systems_simulate_route(payload: dict[str, Any]):
    try: return simulate_dynamic_system(payload)
    except DynamicSystemError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/dynamic-systems/estimate")
def model_studio_dynamic_systems_estimate_route(payload: dict[str, Any]):
    try: return estimate_dynamic_system_parameters(payload)
    except DynamicSystemError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.get("/v1/model-studio/dynamic-systems/v0540/health")
def model_studio_dynamic_systems_v0540_health_route():
    return dynamic_systems_v0540_health()

@app.get("/v1/model-studio/dynamic-systems/v0540/policies")
def model_studio_dynamic_systems_v0540_policies_route():
    return dynamic_systems_v0540_policies()

@app.post("/v1/model-studio/dynamic-systems/v0540/normalize")
def model_studio_dynamic_systems_v0540_normalize_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "study": normalize_dynamic_systems_v0540(payload.get("study") or payload)}
    except DynamicSystemsV0540Error as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/dynamic-systems/v0540/simulate")
def model_studio_dynamic_systems_v0540_simulate_route(payload: dict[str, Any]):
    try:
        return simulate_dynamic_systems_v0540(payload)
    except DynamicSystemsV0540Error as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/dynamic-systems/v0540/bifurcation")
def model_studio_dynamic_systems_v0540_bifurcation_route(payload: dict[str, Any]):
    try:
        return dynamic_systems_v0540_bifurcation(payload)
    except DynamicSystemsV0540Error as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/dynamic-systems/v0540/phase")
def model_studio_dynamic_systems_v0540_phase_route(payload: dict[str, Any]):
    try:
        return dynamic_systems_v0540_phase(payload)
    except DynamicSystemsV0540Error as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.get("/v1/model-studio/response-surfaces/health")
def model_studio_response_surfaces_health_route():
    return response_surfaces_health()

@app.get("/v1/model-studio/response-surfaces/policies")
def model_studio_response_surfaces_policies_route():
    return response_surfaces_policies()

@app.post("/v1/model-studio/response-surfaces/normalize")
def model_studio_response_surfaces_normalize_route(payload: dict[str, Any]):
    try: return {"ok": True, "study": normalize_response_surface_study(payload.get("study") or payload)}
    except ResponseSurfaceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/response-surfaces/fit")
def model_studio_response_surfaces_fit_route(payload: dict[str, Any]):
    try: return fit_response_surface(payload)
    except ResponseSurfaceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/response-surfaces/explore")
def model_studio_response_surfaces_explore_route(payload: dict[str, Any]):
    try: return explore_response_surface(payload)
    except ResponseSurfaceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/response-surfaces/optimize")
def model_studio_response_surfaces_optimize_route(payload: dict[str, Any]):
    try: return optimize_response_surface(payload)
    except ResponseSurfaceError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc



@app.get("/v1/model-studio/statistics/health")
def model_studio_statistics_health_route():
    return advanced_statistical_modeling_health()

@app.get("/v1/model-studio/statistics/policies")
def model_studio_statistics_policies_route():
    return advanced_statistical_modeling_policies()

@app.post("/v1/model-studio/statistics/normalize")
def model_studio_statistics_normalize_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "study": normalize_advanced_statistical_study(payload.get("study") or payload)}
    except AdvancedStatisticalModelingError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/statistics/fit")
def model_studio_statistics_fit_route(payload: dict[str, Any]):
    try:
        return fit_advanced_statistical_model(payload)
    except AdvancedStatisticalModelingError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/statistics/predict")
def model_studio_statistics_predict_route(payload: dict[str, Any]):
    try:
        return predict_advanced_statistical_model(payload)
    except AdvancedStatisticalModelingError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/statistics/cross-validate")
def model_studio_statistics_cross_validate_route(payload: dict[str, Any]):
    try:
        return cross_validate_advanced_statistical_model(payload)
    except AdvancedStatisticalModelingError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/statistics/compare")
def model_studio_statistics_compare_route(payload: dict[str, Any]):
    try:
        return compare_advanced_statistical_models(payload)
    except AdvancedStatisticalModelingError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.get("/v1/model-studio/bayesian/health")
def model_studio_bayesian_health_route():
    return bayesian_inference_health()

@app.get("/v1/model-studio/bayesian/policies")
def model_studio_bayesian_policies_route():
    return bayesian_inference_policies()

@app.post("/v1/model-studio/bayesian/normalize")
def model_studio_bayesian_normalize_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "study": normalize_bayesian_study(payload.get("study") or payload)}
    except BayesianInferenceError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/bayesian/fit")
def model_studio_bayesian_fit_route(payload: dict[str, Any]):
    try:
        return fit_bayesian_model(payload)
    except BayesianInferenceError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-studio/bayesian/posterior-predictive")
def model_studio_bayesian_posterior_predictive_route(payload: dict[str, Any]):
    try:
        return run_posterior_predictive(payload)
    except BayesianInferenceError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.get("/v1/model-studio/probabilistic/v0530/health")
def model_studio_correlated_uncertainty_health_route():
    return correlated_uncertainty_health()

@app.get("/v1/model-studio/probabilistic/v0530/policies")
def model_studio_correlated_uncertainty_policies_route():
    return correlated_uncertainty_policies()

@app.post("/v1/model-studio/probabilistic/v0530/normalize")
def model_studio_correlated_uncertainty_normalize_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "study": normalize_correlated_uncertainty_study(payload.get("study") or payload)}
    except CorrelatedUncertaintyError as exc:
        raise HTTPException(status_code=exc.status_code, detail=str(exc)) from exc

@app.post("/v1/model-studio/probabilistic/v0530/analyze")
def model_studio_correlated_uncertainty_analyze_route(payload: dict[str, Any]):
    try:
        return run_correlated_uncertainty(payload.get("study") if isinstance(payload.get("study"), dict) else payload)
    except CorrelatedUncertaintyError as exc:
        raise HTTPException(status_code=exc.status_code, detail=str(exc)) from exc

@app.post("/v1/model-studio/probabilistic/v0530/estimate-dependency")
def model_studio_correlated_uncertainty_estimate_route(payload: dict[str, Any]):
    try:
        return estimate_probabilistic_dependency(payload)
    except CorrelatedUncertaintyError as exc:
        raise HTTPException(status_code=exc.status_code, detail=str(exc)) from exc

@app.get("/v1/model-studio/probabilistic/health")
def model_studio_probabilistic_health_route():
    return probabilistic_analysis_health()

@app.get("/v1/model-studio/probabilistic/policies")
def model_studio_probabilistic_policies_route():
    return probabilistic_analysis_policies()

@app.post("/v1/model-studio/probabilistic/normalize")
def model_studio_probabilistic_normalize_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "study": normalize_probabilistic_study(payload.get("study") or payload)}
    except ProbabilisticAnalysisError as exc:
        raise HTTPException(status_code=exc.status_code, detail=str(exc)) from exc

@app.post("/v1/model-studio/probabilistic/analyze")
def model_studio_probabilistic_analyze_route(payload: dict[str, Any]):
    try:
        return run_probabilistic_analysis(payload.get("study") if isinstance(payload.get("study"), dict) else payload)
    except ProbabilisticAnalysisError as exc:
        raise HTTPException(status_code=exc.status_code, detail=str(exc)) from exc

@app.get("/v1/graph-studio/health")
def graph_studio_health_route():
    return graph_studio_health()

@app.get("/v1/graph-studio/policies")
def graph_studio_policies_route():
    return graph_studio_policies()

@app.post("/v1/graph-studio/graphs/normalize")
def graph_studio_graph_normalize_route(payload: dict[str, Any]):
    try: return {"ok": True, "graph": normalize_graph_studio_graph(payload.get("graph") or payload)}
    except GraphStudioError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/graph-studio/figures/normalize")
def graph_studio_figure_normalize_route(payload: dict[str, Any]):
    try: return {"ok": True, "figure": normalize_graph_studio_figure(payload.get("figure") or payload)}
    except GraphStudioError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/graph-studio/workspaces/build")
def graph_studio_workspace_build_route(payload: dict[str, Any]):
    try: return build_graph_studio_workspace(payload)
    except GraphStudioError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.get("/v1/visualization/v0740/health")
def visualization_engine_v0740_health_route():
    return visualization_engine_health_v0740()

@app.get("/v1/visualization/v0740/policies")
def visualization_engine_v0740_policies_route():
    return visualization_engine_policies_v0740()

@app.post("/v1/visualization/v0740/specs/normalize")
def visualization_engine_v0740_spec_normalize_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "spec": normalize_visualization_spec_v0740(payload.get("spec") or payload.get("graph") or payload)}
    except VisualizationGrammarError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0740/figures/normalize")
def visualization_engine_v0740_figure_normalize_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "figure": normalize_visualization_figure_v0740(payload.get("figure") or payload)}
    except VisualizationGrammarError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0740/workspaces/build")
def visualization_engine_v0740_workspace_build_route(payload: dict[str, Any]):
    try:
        return build_visualization_workspace_v0740(payload)
    except VisualizationGrammarError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.get("/v1/visualization/v0750/health")
def scientific_data_binding_v0750_health_route():
    return scientific_data_binding_health_v0750()

@app.get("/v1/visualization/v0750/policies")
def scientific_data_binding_v0750_policies_route():
    return scientific_data_binding_policies_v0750()

@app.post("/v1/visualization/v0750/datasets/normalize")
def scientific_data_binding_v0750_dataset_normalize_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "dataset": normalize_scientific_dataset_v0750(payload.get("dataset") or payload)}
    except ScientificDataBindingError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0750/pipelines/normalize")
def scientific_data_binding_v0750_pipeline_normalize_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "pipeline": normalize_data_pipeline_v0750(payload.get("pipeline") or payload)}
    except ScientificDataBindingError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0750/pipelines/execute")
def scientific_data_binding_v0750_pipeline_execute_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "result": execute_data_pipeline_v0750(payload)}
    except ScientificDataBindingError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0750/bindings/normalize")
def scientific_data_binding_v0750_binding_normalize_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "binding": normalize_data_binding_v0750(payload.get("binding") or payload)}
    except ScientificDataBindingError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0750/specs/bind")
def scientific_data_binding_v0750_spec_bind_route(payload: dict[str, Any]):
    try:
        return bind_visualization_v0750(payload)
    except ScientificDataBindingError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0750/figures/build")
def scientific_data_binding_v0750_figure_build_route(payload: dict[str, Any]):
    try:
        return build_bound_figure_v0750(payload)
    except ScientificDataBindingError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0750/workspaces/build")
def scientific_data_binding_v0750_workspace_build_route(payload: dict[str, Any]):
    try:
        return build_bound_workspace_v0750(payload)
    except ScientificDataBindingError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.get("/v1/visualization/v0760/health")
def large_data_visualization_v0760_health_route():
    return large_data_visualization_health_v0760()

@app.get("/v1/visualization/v0760/policies")
def large_data_visualization_v0760_policies_route():
    return large_data_visualization_policies_v0760()

@app.post("/v1/visualization/v0760/plans/normalize")
def large_data_visualization_v0760_plan_normalize_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "plan": normalize_adaptive_render_plan_v0760(payload.get("renderPlan") or payload.get("plan") or payload, kind=str(payload.get("kind") or "line-scatter"), row_count=int(payload.get("rowCount") or 0))}
    except LargeDataVisualizationError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0760/datasets/adapt")
def large_data_visualization_v0760_dataset_adapt_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "result": adapt_large_dataset_v0760(payload)}
    except LargeDataVisualizationError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0760/specs/bind")
def large_data_visualization_v0760_spec_bind_route(payload: dict[str, Any]):
    try:
        return bind_large_visualization_v0760(payload)
    except LargeDataVisualizationError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0760/figures/build")
def large_data_visualization_v0760_figure_build_route(payload: dict[str, Any]):
    try:
        return build_large_figure_v0760(payload)
    except LargeDataVisualizationError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0760/workspaces/build")
def large_data_visualization_v0760_workspace_build_route(payload: dict[str, Any]):
    try:
        return build_large_workspace_v0760(payload)
    except LargeDataVisualizationError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.get("/v1/visualization/v0770/health")
def scientific_scene_v0770_health_route():
    return scientific_scene_health_v0770()

@app.get("/v1/visualization/v0770/policies")
def scientific_scene_v0770_policies_route():
    return scientific_scene_policies_v0770()

@app.post("/v1/visualization/v0770/cameras/normalize")
def scientific_scene_v0770_camera_normalize_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "camera": normalize_scientific_scene_camera_v0770(payload.get("camera") or payload)}
    except ScientificSceneError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0770/scenes/normalize")
def scientific_scene_v0770_scene_normalize_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "scene": normalize_scientific_scene_v0770(payload.get("scene") or payload)}
    except ScientificSceneError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0770/scenes/from-dataset")
def scientific_scene_v0770_scene_from_dataset_route(payload: dict[str, Any]):
    try:
        return scientific_scene_from_dataset_v0770(payload)
    except ScientificSceneError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0770/figures/build")
def scientific_scene_v0770_figure_build_route(payload: dict[str, Any]):
    try:
        return build_scientific_scene_figure_v0770(payload)
    except ScientificSceneError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0770/workspaces/build")
def scientific_scene_v0770_workspace_build_route(payload: dict[str, Any]):
    try:
        return build_scientific_scene_workspace_v0770(payload)
    except ScientificSceneError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.get("/v1/visualization/v0780/health")
def time_parameter_space_v0780_health_route():
    return time_parameter_health_v0780()

@app.get("/v1/visualization/v0780/policies")
def time_parameter_space_v0780_policies_route():
    return time_parameter_policies_v0780()

@app.post("/v1/visualization/v0780/axes/normalize")
def time_parameter_space_v0780_axis_normalize_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "axis": normalize_state_axis_v0780(payload.get("axis") or payload)}
    except TimeParameterSpaceError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0780/projections/normalize")
def time_parameter_space_v0780_projection_normalize_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "projection": normalize_4d_projection_v0780(payload.get("projection") or payload)}
    except TimeParameterSpaceError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0780/state-spaces/normalize")
def time_parameter_space_v0780_state_normalize_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "stateSpace": normalize_state_space_v0780(payload.get("stateSpace") or payload)}
    except TimeParameterSpaceError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0780/state-spaces/from-dataset")
def time_parameter_space_v0780_state_dataset_route(payload: dict[str, Any]):
    try:
        return state_space_from_dataset_v0780(payload)
    except TimeParameterSpaceError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0780/state-spaces/slice")
def time_parameter_space_v0780_slice_route(payload: dict[str, Any]):
    try:
        return slice_state_space_v0780(payload)
    except TimeParameterSpaceError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0780/figures/build")
def time_parameter_space_v0780_figure_route(payload: dict[str, Any]):
    try:
        return build_time_parameter_figure_v0780(payload)
    except TimeParameterSpaceError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0780/workspaces/build")
def time_parameter_space_v0780_workspace_route(payload: dict[str, Any]):
    try:
        return build_time_parameter_workspace_v0780(payload)
    except TimeParameterSpaceError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.get("/v1/visualization/v0790/health")
def linked_views_v0790_health_route(): return linked_views_health_v0790()

@app.get("/v1/visualization/v0790/policies")
def linked_views_v0790_policies_route(): return linked_views_policies_v0790()

@app.post("/v1/visualization/v0790/views/normalize")
def linked_views_v0790_view_route(payload: dict[str, Any]):
    try: return {"ok": True, "view": normalize_linked_view_v0790(payload.get("view") or payload)}
    except LinkedViewsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0790/links/normalize")
def linked_views_v0790_link_route(payload: dict[str, Any]):
    try: return {"ok": True, "link": normalize_view_link_v0790(payload.get("link") or payload)}
    except LinkedViewsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0790/facets/normalize")
def linked_views_v0790_facet_route(payload: dict[str, Any]):
    try: return {"ok": True, "facet": normalize_facet_v0790(payload.get("facet") or payload)}
    except LinkedViewsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0790/facets/build")
def linked_views_v0790_facet_build_route(payload: dict[str, Any]):
    try: return facet_dataset_v0790(payload)
    except LinkedViewsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0790/compositions/normalize")
def linked_views_v0790_composition_route(payload: dict[str, Any]):
    try: return {"ok": True, "composition": normalize_linked_composition_v0790(payload.get("composition") or payload)}
    except LinkedViewsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0790/interactions/propagate")
def linked_views_v0790_propagate_route(payload: dict[str, Any]):
    try: return apply_link_event_v0790(payload)
    except LinkedViewsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0790/workspaces/build")
def linked_views_v0790_workspace_route(payload: dict[str, Any]):
    try: return build_linked_workspace_v0790(payload)
    except LinkedViewsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc


@app.get("/v1/visualization/v0730/health")
def visualization_engine_v0730_health_route():
    return visualization_engine_health_v0730()

@app.get("/v1/visualization/v0730/policies")
def visualization_engine_v0730_policies_route():
    return visualization_engine_policies_v0730()

@app.post("/v1/visualization/v0730/specs/normalize")
def visualization_engine_v0730_spec_normalize_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "spec": normalize_visualization_spec_v0730(payload.get("spec") or payload.get("graph") or payload)}
    except VisualizationEngineError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0730/figures/normalize")
def visualization_engine_v0730_figure_normalize_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "figure": normalize_visualization_figure_v0730(payload.get("figure") or payload)}
    except VisualizationEngineError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/visualization/v0730/workspaces/build")
def visualization_engine_v0730_workspace_build_route(payload: dict[str, Any]):
    try:
        return build_visualization_workspace_v0730(payload)
    except VisualizationEngineError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.get("/v1/model-calibration/health")
def model_calibration_health_route():
    return model_calibration_health()

@app.get("/v1/model-calibration/policies")
def model_calibration_policies_route():
    return model_calibration_policies()

@app.post("/v1/model-calibration/normalize")
def model_calibration_normalize_route(payload: dict[str, Any]):
    try: return {"ok": True, "study": normalize_calibration_study(payload)}
    except ModelCalibrationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-calibration/calibrate")
def model_calibration_calibrate_route(payload: dict[str, Any]):
    try: return calibrate_model(payload)
    except ModelCalibrationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-calibration/validate")
def model_calibration_validate_route(payload: dict[str, Any]):
    try: return validate_calibration_result(payload)
    except ModelCalibrationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-calibration/compare")
def model_calibration_compare_route(payload: dict[str, Any]):
    try: return compare_models(payload)
    except ModelCalibrationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-calibration/reports/build")
def model_calibration_report_route(payload: dict[str, Any]):
    try: return build_calibration_report(payload)
    except ModelCalibrationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc

@app.post("/v1/model-calibration/verify")
def model_calibration_verify_route(payload: dict[str, Any]):
    try: return verify_calibration_record(payload)
    except ModelCalibrationError as exc: raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.get("/v1/dispatcher/health")
def distributed_dispatcher_health_route():
    body=dispatcher.health(); body["serviceVersion"]=settings.version; return body

@app.get("/v1/dispatcher/policies")
def distributed_dispatcher_policies_route():
    body=distributed_dispatcher_policies(); body["serviceVersion"]=settings.version; return body

@app.get("/v1/dispatcher/workers")
def distributed_dispatcher_workers_route(active_only: bool = Query(False, alias="activeOnly"), auth: dict[str,str]=Depends(require_compute_auth)):
    del auth; return dispatcher.list(active_only)

@app.post("/v1/dispatcher/workers/register")
def distributed_dispatcher_register_route(payload: dict[str,Any], auth: dict[str,str]=Depends(require_compute_auth)):
    del auth
    try: return dispatcher.register(payload)
    except DispatcherError as exc: raise HTTPException(status_code=422,detail=str(exc)) from exc

@app.post("/v1/dispatcher/workers/{worker_id}/heartbeat")
def distributed_dispatcher_heartbeat_route(worker_id: str, payload: dict[str,Any], auth: dict[str,str]=Depends(require_compute_auth)):
    del auth
    try: return dispatcher.heartbeat(worker_id,payload)
    except DispatcherError as exc: raise HTTPException(status_code=404 if "not found" in str(exc).lower() else 422,detail=str(exc)) from exc

@app.post("/v1/dispatcher/route")
def distributed_dispatcher_route_route(payload: dict[str,Any], auth: dict[str,str]=Depends(require_compute_auth)):
    del auth
    try: return dispatcher.route(payload)
    except DispatcherError as exc: raise HTTPException(status_code=422,detail=str(exc)) from exc

@app.post("/v1/dispatcher/contracts/build")
def distributed_dispatcher_contract_build_route(payload: dict[str,Any], auth: dict[str,str]=Depends(require_compute_auth)):
    del auth
    try: return dispatcher.build_contract(payload,settings.dispatcher_contract_secret)
    except DispatcherError as exc: raise HTTPException(status_code=422,detail=str(exc)) from exc

@app.post("/v1/dispatcher/contracts/verify")
def distributed_dispatcher_contract_verify_route(payload: dict[str,Any], auth: dict[str,str]=Depends(require_compute_auth)):
    del auth; return dispatcher.verify_contract(payload,settings.dispatcher_contract_secret)

@app.post("/v1/dispatcher/contracts/{contract_id}/acknowledge")
def distributed_dispatcher_contract_ack_route(contract_id: str, payload: dict[str,Any], auth: dict[str,str]=Depends(require_compute_auth)):
    del auth
    try: return dispatcher.acknowledge(contract_id,str(payload.get("workerId") or ""))
    except DispatcherError as exc: raise HTTPException(status_code=422,detail=str(exc)) from exc

@app.post("/v1/dispatcher/contracts/{contract_id}/complete")
def distributed_dispatcher_contract_complete_route(contract_id: str, payload: dict[str,Any], auth: dict[str,str]=Depends(require_compute_auth)):
    del auth
    try: return dispatcher.complete(contract_id,payload,str(payload.get("workerId") or ""))
    except DispatcherError as exc: raise HTTPException(status_code=422,detail=str(exc)) from exc


@app.get("/v1/dispatcher/queue/status")
def dispatcher_queue_status_route(auth: dict[str,str]=Depends(require_compute_auth)):
    del auth; return dispatcher.status()

@app.post("/v1/dispatcher/queue/enqueue")
def dispatcher_queue_enqueue_route(payload: dict[str,Any], auth: dict[str,str]=Depends(require_compute_auth)):
    del auth
    try: return dispatcher.enqueue(payload)
    except DispatcherError as exc: raise HTTPException(status_code=422,detail=str(exc)) from exc

@app.post("/v1/dispatcher/leases/claim")
def dispatcher_lease_claim_route(payload: dict[str,Any], auth: dict[str,str]=Depends(require_compute_auth)):
    del auth
    try: return dispatcher.claim(payload,settings.dispatcher_contract_secret)
    except DispatcherError as exc: raise HTTPException(status_code=422,detail=str(exc)) from exc

@app.get("/v1/dispatcher/leases")
def dispatcher_leases_route(limit:int=Query(100,ge=1,le=500), auth: dict[str,str]=Depends(require_compute_auth)):
    del auth; return dispatcher.leases(limit)

@app.post("/v1/dispatcher/leases/{lease_id}/renew")
def dispatcher_lease_renew_route(lease_id:str,payload:dict[str,Any],auth:dict[str,str]=Depends(require_compute_auth)):
    del auth
    try:return dispatcher.renew(lease_id,payload)
    except DispatcherError as exc: raise HTTPException(status_code=422,detail=str(exc)) from exc

@app.post("/v1/dispatcher/leases/{lease_id}/release")
def dispatcher_lease_release_route(lease_id:str,payload:dict[str,Any],auth:dict[str,str]=Depends(require_compute_auth)):
    del auth
    try:return dispatcher.release(lease_id,payload)
    except DispatcherError as exc: raise HTTPException(status_code=422,detail=str(exc)) from exc

@app.post("/v1/dispatcher/recovery/run")
def dispatcher_recovery_route(auth:dict[str,str]=Depends(require_compute_auth)):
    del auth; return dispatcher.recover()

@app.get("/v1/dispatcher/history")
def dispatcher_history_route(limit:int=Query(100,ge=1,le=1000),auth:dict[str,str]=Depends(require_compute_auth)):
    del auth; return dispatcher.history(limit)

# v0.31.4 Dispatcher Operations, Dead-Letter Recovery, and Observability

@app.get("/v1/dispatcher/operations/health")
def dispatcher_operations_health_route(auth:dict[str,str]=Depends(require_compute_auth)):
    del auth; return dispatcher.operations_health()

@app.get("/v1/dispatcher/operations/policies")
def dispatcher_operations_policies_route(auth:dict[str,str]=Depends(require_compute_auth)):
    del auth; return dispatcher.failure_policies()

@app.get("/v1/dispatcher/operations/metrics")
def dispatcher_operations_metrics_route(auth:dict[str,str]=Depends(require_compute_auth)):
    del auth; return dispatcher.operations_metrics()

@app.get("/v1/dispatcher/operations/diagnostics")
def dispatcher_operations_diagnostics_route(auth:dict[str,str]=Depends(require_compute_auth)):
    del auth; return dispatcher.diagnostics()

@app.get("/v1/dispatcher/dead-letters")
def dispatcher_dead_letters_route(limit:int=Query(100,ge=1,le=1000), failure_class:str=Query("",alias="failureClass"), auth:dict[str,str]=Depends(require_compute_auth)):
    del auth; return dispatcher.dead_letters(limit,failure_class)

@app.post("/v1/dispatcher/dead-letters/replay")
def dispatcher_dead_letters_replay_route(payload:dict[str,Any],auth:dict[str,str]=Depends(require_compute_auth)):
    del auth
    try:return dispatcher.replay_many(payload)
    except DispatcherError as exc: raise HTTPException(status_code=422,detail=str(exc)) from exc

@app.get("/v1/dispatcher/queue/{queue_id}")
def dispatcher_queue_item_route(queue_id:str,auth:dict[str,str]=Depends(require_compute_auth)):
    del auth
    try:return dispatcher.queue_item(queue_id)
    except DispatcherError as exc: raise HTTPException(status_code=404,detail=str(exc)) from exc

@app.get("/v1/dispatcher/queue/{queue_id}/timeline")
def dispatcher_queue_timeline_route(queue_id:str,limit:int=Query(250,ge=1,le=1000),auth:dict[str,str]=Depends(require_compute_auth)):
    del auth
    try:return dispatcher.timeline(queue_id,limit)
    except DispatcherError as exc: raise HTTPException(status_code=404,detail=str(exc)) from exc

@app.post("/v1/dispatcher/queue/{queue_id}/replay")
def dispatcher_queue_replay_route(queue_id:str,payload:dict[str,Any] | None=None,auth:dict[str,str]=Depends(require_compute_auth)):
    del auth
    try:return dispatcher.replay(queue_id,payload or {})
    except DispatcherError as exc: raise HTTPException(status_code=422,detail=str(exc)) from exc

@app.post("/v1/dispatcher/queue/{queue_id}/cancel")
def dispatcher_queue_cancel_route(queue_id:str,payload:dict[str,Any] | None=None,auth:dict[str,str]=Depends(require_compute_auth)):
    del auth
    try:return dispatcher.cancel_queue_item(queue_id,payload or {})
    except DispatcherError as exc: raise HTTPException(status_code=422,detail=str(exc)) from exc

# v0.31.3 Distributed Artifact, Result, and Checkpoint Transport

def _artifact_http_error(exc: ArtifactTransportError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)


@app.get("/v1/artifacts/health")
def artifact_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = artifacts.health()
    body["serviceVersion"] = settings.version
    body["deploymentDurability"] = "persistent-disk" if settings.artifact_persistent_disk_mounted else "instance-local"
    body["durabilityWarning"] = None if settings.artifact_persistent_disk_mounted else "Artifact files are instance-local unless a persistent disk is mounted."
    return body


@app.get("/v1/artifacts/policies")
def artifact_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return artifacts.policies()


@app.get("/v1/artifacts")
def artifact_list_route(project_id: str = Query("", alias="projectId"), queue_id: str = Query("", alias="queueId"), kind: str = Query(""), limit: int = Query(100, ge=1, le=1000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return artifacts.list(project_id, queue_id, kind, limit)


@app.get("/v1/artifacts/uploads")
def artifact_sessions_route(limit: int = Query(100, ge=1, le=1000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return artifacts.sessions(limit)


@app.post("/v1/artifacts/uploads")
def artifact_create_upload_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return artifacts.create_upload(payload)
    except ArtifactTransportError as exc:
        raise _artifact_http_error(exc) from exc


@app.post("/v1/artifacts/uploads/{session_id}/chunks")
async def artifact_append_chunk_route(session_id: str, request: Request, offset: int = Query(..., ge=0), x_sc_lab_chunk_sha256: str = Header(default=""), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return artifacts.append_chunk(session_id, offset, await request.body(), chunk_sha256=x_sc_lab_chunk_sha256)
    except ArtifactTransportError as exc:
        raise _artifact_http_error(exc) from exc


@app.post("/v1/artifacts/uploads/{session_id}/finalize")
def artifact_finalize_route(session_id: str, payload: dict[str, Any] | None = None, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return artifacts.finalize(session_id, payload or {})
    except ArtifactTransportError as exc:
        raise _artifact_http_error(exc) from exc


@app.get("/v1/artifacts/{artifact_id}")
def artifact_metadata_route(artifact_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return artifacts.get(artifact_id)
    except ArtifactTransportError as exc:
        raise _artifact_http_error(exc) from exc


@app.get("/v1/artifacts/{artifact_id}/content")
def artifact_content_route(artifact_id: str, offset: int = Query(0, ge=0), length: int | None = Query(None, ge=0), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        metadata, data = artifacts.read(artifact_id, offset, length)
    except ArtifactTransportError as exc:
        raise _artifact_http_error(exc) from exc
    headers = {"X-SC-Lab-Artifact-SHA256": metadata["sha256"], "X-SC-Lab-Artifact-Size": str(metadata["sizeBytes"]), "Content-Disposition": f'attachment; filename="{metadata["filename"]}"'}
    return Response(content=data, media_type=metadata["mediaType"], headers=headers)


@app.post("/v1/artifacts/manifest")
def artifact_manifest_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        ids = payload.get("artifactIds") if isinstance(payload.get("artifactIds"), list) else []
        return artifacts.manifest([str(item) for item in ids])
    except ArtifactTransportError as exc:
        raise _artifact_http_error(exc) from exc


@app.post("/v1/artifacts/cleanup")
def artifact_cleanup_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return artifacts.cleanup_retention()


@app.delete("/v1/artifacts/{artifact_id}")
def artifact_delete_route(artifact_id: str, reason: str = Query("operator deletion"), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return artifacts.delete(artifact_id, reason)
    except ArtifactTransportError as exc:
        raise _artifact_http_error(exc) from exc


# v0.31.3 Secure Worker Agent Runtime and Artifact Transport

def _require_worker_enrollment(presented: str) -> None:
    expected = settings.worker_enrollment_token
    if expected:
        if not presented or not hmac.compare_digest(presented, expected):
            raise HTTPException(status_code=401, detail="Worker enrollment token is invalid.")
        return
    if settings.environment == "production" and not settings.allow_open_worker_enrollment:
        raise HTTPException(status_code=503, detail="Worker enrollment is disabled until SC_LAB_WORKER_ENROLLMENT_TOKEN is configured.")


def _authenticate_worker(worker_id: str, credential: str) -> dict[str, Any]:
    try:
        return dispatcher.authenticate_worker(worker_id, credential)
    except DispatcherError as exc:
        raise HTTPException(status_code=401, detail=str(exc)) from exc


@app.get("/v1/worker-agent/health")
def worker_agent_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    credentials = dispatcher.credential_status()
    return {
        "ok": True,
        "status": "ready",
        "version": settings.version,
        "architecture": "secure-pull-based-worker-agent-runtime",
        "enrollmentTokenConfigured": bool(settings.worker_enrollment_token),
        "openEnrollment": not bool(settings.worker_enrollment_token) and (settings.environment != "production" or settings.allow_open_worker_enrollment),
        "contractSecretConfigured": bool(settings.dispatcher_contract_secret),
        "credentials": credentials,
        "policies": worker_agent_policies(bool(settings.worker_enrollment_token), bool(settings.dispatcher_contract_secret)),
    }


@app.get("/v1/worker-agent/policies")
def worker_agent_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return worker_agent_policies(bool(settings.worker_enrollment_token), bool(settings.dispatcher_contract_secret))


@app.get("/v1/worker-agent/credentials/status")
def worker_agent_credentials_status_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return dispatcher.credential_status()


@app.post("/v1/worker-agent/enroll")
def worker_agent_enroll_route(payload: dict[str, Any], x_sc_lab_worker_enrollment: str = Header(default="")):
    _require_worker_enrollment(x_sc_lab_worker_enrollment)
    try:
        return dispatcher.enroll(payload)
    except DispatcherError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.post("/v1/worker-agent/{worker_id}/heartbeat")
def worker_agent_heartbeat_route(worker_id: str, payload: dict[str, Any], x_sc_lab_worker_credential: str = Header(default="")):
    _authenticate_worker(worker_id, x_sc_lab_worker_credential)
    try:
        return dispatcher.heartbeat(worker_id, payload)
    except DispatcherError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.post("/v1/worker-agent/{worker_id}/claim")
def worker_agent_claim_route(worker_id: str, payload: dict[str, Any], x_sc_lab_worker_credential: str = Header(default="")):
    _authenticate_worker(worker_id, x_sc_lab_worker_credential)
    try:
        return dispatcher.claim({"workerId": worker_id, "leaseSeconds": payload.get("leaseSeconds") or settings.dispatcher_default_lease_seconds}, settings.dispatcher_contract_secret)
    except DispatcherError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.post("/v1/worker-agent/{worker_id}/contracts/{contract_id}/acknowledge")
def worker_agent_acknowledge_route(worker_id: str, contract_id: str, x_sc_lab_worker_credential: str = Header(default="")):
    _authenticate_worker(worker_id, x_sc_lab_worker_credential)
    try:
        return dispatcher.acknowledge(contract_id, worker_id)
    except DispatcherError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.post("/v1/worker-agent/{worker_id}/contracts/{contract_id}/complete")
def worker_agent_complete_route(worker_id: str, contract_id: str, payload: dict[str, Any], x_sc_lab_worker_credential: str = Header(default="")):
    _authenticate_worker(worker_id, x_sc_lab_worker_credential)
    try:
        return dispatcher.complete(contract_id, payload, worker_id)
    except DispatcherError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.post("/v1/worker-agent/{worker_id}/leases/{lease_id}/renew")
def worker_agent_renew_route(worker_id: str, lease_id: str, payload: dict[str, Any], x_sc_lab_worker_credential: str = Header(default="")):
    _authenticate_worker(worker_id, x_sc_lab_worker_credential)
    try:
        return dispatcher.renew(lease_id, {"workerId": worker_id, "leaseSeconds": payload.get("leaseSeconds") or settings.dispatcher_default_lease_seconds})
    except DispatcherError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.post("/v1/worker-agent/{worker_id}/leases/{lease_id}/release")
def worker_agent_release_route(worker_id: str, lease_id: str, payload: dict[str, Any], x_sc_lab_worker_credential: str = Header(default="")):
    _authenticate_worker(worker_id, x_sc_lab_worker_credential)
    try:
        return dispatcher.release(lease_id, {"workerId": worker_id, "reason": payload.get("reason"), "requeue": payload.get("requeue", True)})
    except DispatcherError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.post("/v1/worker-agent/{worker_id}/credential/rotate")
def worker_agent_rotate_credential_route(worker_id: str, x_sc_lab_worker_credential: str = Header(default="")):
    _authenticate_worker(worker_id, x_sc_lab_worker_credential)
    try:
        return dispatcher.rotate_worker_credential(worker_id)
    except DispatcherError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc


@app.post("/v1/worker-agent/{worker_id}/artifacts/uploads")
def worker_artifact_create_upload_route(worker_id: str, payload: dict[str, Any], x_sc_lab_worker_credential: str = Header(default="")):
    _authenticate_worker(worker_id, x_sc_lab_worker_credential)
    try:
        return artifacts.create_upload(payload, worker_id)
    except ArtifactTransportError as exc:
        raise _artifact_http_error(exc) from exc


@app.post("/v1/worker-agent/{worker_id}/artifacts/uploads/{session_id}/chunks")
async def worker_artifact_append_chunk_route(worker_id: str, session_id: str, request: Request, offset: int = Query(..., ge=0), x_sc_lab_chunk_sha256: str = Header(default=""), x_sc_lab_worker_credential: str = Header(default="")):
    _authenticate_worker(worker_id, x_sc_lab_worker_credential)
    try:
        return artifacts.append_chunk(session_id, offset, await request.body(), worker_id, x_sc_lab_chunk_sha256)
    except ArtifactTransportError as exc:
        raise _artifact_http_error(exc) from exc


@app.post("/v1/worker-agent/{worker_id}/artifacts/uploads/{session_id}/finalize")
def worker_artifact_finalize_route(worker_id: str, session_id: str, payload: dict[str, Any] | None = None, x_sc_lab_worker_credential: str = Header(default="")):
    _authenticate_worker(worker_id, x_sc_lab_worker_credential)
    try:
        return artifacts.finalize(session_id, payload or {}, worker_id)
    except ArtifactTransportError as exc:
        raise _artifact_http_error(exc) from exc


@app.get("/v1/worker-agent/{worker_id}/artifacts/{artifact_id}")
def worker_artifact_metadata_route(worker_id: str, artifact_id: str, x_sc_lab_worker_credential: str = Header(default="")):
    _authenticate_worker(worker_id, x_sc_lab_worker_credential)
    try:
        metadata = artifacts.get(artifact_id)
    except ArtifactTransportError as exc:
        raise _artifact_http_error(exc) from exc
    if metadata.get("workerId") != worker_id and not dispatcher.artifact_access_allowed(worker_id, artifact_id):
        raise HTTPException(status_code=403, detail="Worker does not have an active artifact grant.")
    return metadata


@app.get("/v1/worker-agent/{worker_id}/artifacts/{artifact_id}/content")
def worker_artifact_content_route(worker_id: str, artifact_id: str, offset: int = Query(0, ge=0), length: int | None = Query(None, ge=0), x_sc_lab_worker_credential: str = Header(default="")):
    _authenticate_worker(worker_id, x_sc_lab_worker_credential)
    try:
        metadata = artifacts.get(artifact_id)
        if metadata.get("workerId") != worker_id and not dispatcher.artifact_access_allowed(worker_id, artifact_id):
            raise HTTPException(status_code=403, detail="Worker does not have an active artifact grant.")
        metadata, data = artifacts.read(artifact_id, offset, length)
    except ArtifactTransportError as exc:
        raise _artifact_http_error(exc) from exc
    headers = {"X-SC-Lab-Artifact-SHA256": metadata["sha256"], "X-SC-Lab-Artifact-Size": str(metadata["sizeBytes"])}
    return Response(content=data, media_type=metadata["mediaType"], headers=headers)


@app.post("/v1/worker-agent/{worker_id}/revoke")
def worker_agent_revoke_route(worker_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return dispatcher.revoke_worker(worker_id, str(payload.get("reason") or "revoked by coordinator"))
    except DispatcherError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from exc



# v0.32.1 Workflow Checkpoints, Conditional Execution, and Partial Recovery

def _workflow_http_error(exc: WorkflowError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)


@app.get("/v1/workflows/health")
def workflow_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = workflows.health()
    body["serviceVersion"] = settings.version
    body["deploymentDurability"] = "persistent-disk" if settings.workflow_persistent_disk_mounted else "instance-local"
    body["durabilityWarning"] = None if settings.workflow_persistent_disk_mounted else "Workflow records are instance-local unless a persistent disk is mounted."
    return body


@app.get("/v1/workflows/policies")
def workflow_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return workflow_policies(settings.workflow_max_nodes, settings.workflow_max_runs)


@app.post("/v1/workflows/validate")
def workflow_validate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflows.validate(payload)
    except WorkflowError as exc:
        raise _workflow_http_error(exc) from exc


@app.post("/v1/workflows")
def workflow_save_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflows.save(payload)
    except WorkflowError as exc:
        raise _workflow_http_error(exc) from exc


@app.get("/v1/workflows")
def workflow_list_route(project_id: str = Query("", alias="projectId"), limit: int = Query(100, ge=1, le=1000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return workflows.list(project_id, limit)


@app.get("/v1/workflows/{workflow_id}")
def workflow_get_route(workflow_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflows.get(workflow_id)
    except WorkflowError as exc:
        raise _workflow_http_error(exc) from exc


@app.post("/v1/workflows/{workflow_id}/runs")
def workflow_start_run_route(workflow_id: str, payload: dict[str, Any] | None = None, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflows.start_run(workflow_id, payload or {})
    except WorkflowError as exc:
        raise _workflow_http_error(exc) from exc


@app.get("/v1/workflow-runs")
def workflow_runs_route(workflow_id: str = Query("", alias="workflowId"), project_id: str = Query("", alias="projectId"), status: str = Query(""), limit: int = Query(100, ge=1, le=1000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflows.runs(workflow_id, project_id, status, limit)
    except WorkflowError as exc:
        raise _workflow_http_error(exc) from exc


@app.get("/v1/workflow-runs/{run_id}")
def workflow_run_route(run_id: str, reconcile: bool = Query(True), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflows.run(run_id, reconcile=reconcile)
    except WorkflowError as exc:
        raise _workflow_http_error(exc) from exc


@app.post("/v1/workflow-runs/{run_id}/reconcile")
def workflow_reconcile_route(run_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflows.reconcile(run_id)
    except WorkflowError as exc:
        raise _workflow_http_error(exc) from exc


@app.post("/v1/workflow-runs/{run_id}/cancel")
def workflow_cancel_route(run_id: str, payload: dict[str, Any] | None = None, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflows.cancel(run_id, payload or {})
    except WorkflowError as exc:
        raise _workflow_http_error(exc) from exc


@app.post("/v1/workflow-runs/{run_id}/recovery-plan")
def workflow_recovery_plan_route(run_id: str, payload: dict[str, Any] | None = None, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflows.recovery_plan(run_id, payload or {})
    except WorkflowError as exc:
        raise _workflow_http_error(exc) from exc


@app.post("/v1/workflow-runs/{run_id}/recover")
def workflow_recover_route(run_id: str, payload: dict[str, Any] | None = None, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflows.recover(run_id, payload or {})
    except WorkflowError as exc:
        raise _workflow_http_error(exc) from exc


@app.post("/v1/workflow-runs/{run_id}/nodes/{node_id}/restart")
def workflow_restart_node_route(run_id: str, node_id: str, payload: dict[str, Any] | None = None, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflows.restart_node(run_id, node_id, payload or {})
    except WorkflowError as exc:
        raise _workflow_http_error(exc) from exc


@app.get("/v1/workflow-runs/{run_id}/nodes/{node_id}/checkpoints")
def workflow_checkpoints_route(run_id: str, node_id: str, limit: int = Query(100, ge=1, le=1000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflows.checkpoints(run_id, node_id, limit)
    except WorkflowError as exc:
        raise _workflow_http_error(exc) from exc


@app.post("/v1/workflow-runs/{run_id}/nodes/{node_id}/checkpoints")
def workflow_record_checkpoint_route(run_id: str, node_id: str, payload: dict[str, Any] | None = None, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflows.record_checkpoint(run_id, node_id, payload or {})
    except WorkflowError as exc:
        raise _workflow_http_error(exc) from exc


@app.get("/v1/workflow-runs/{run_id}/timeline")
def workflow_timeline_route(run_id: str, limit: int = Query(500, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflows.timeline(run_id, limit)
    except WorkflowError as exc:
        raise _workflow_http_error(exc) from exc

# v0.32.2 Scheduled and Event-Driven Research Runs

def _workflow_schedule_http_error(exc: WorkflowScheduleError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)


@app.get("/v1/workflow-automation/health")
def workflow_automation_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = workflow_automation.health()
    body["serviceVersion"] = settings.version
    body["deploymentDurability"] = "persistent-disk" if settings.workflow_schedule_persistent_disk_mounted else "instance-local"
    body["durabilityWarning"] = None if settings.workflow_schedule_persistent_disk_mounted else "Workflow schedules and event receipts are instance-local unless a persistent disk is mounted."
    body["eventHmacConfigured"] = bool(settings.workflow_event_secret)
    return body


@app.get("/v1/workflow-automation/policies")
def workflow_automation_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return workflow_scheduling_policies(settings.workflow_scheduler_max_catch_up_runs)


@app.post("/v1/workflow-automation/schedules/validate")
def workflow_schedule_validate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflow_automation.validate(payload)
    except WorkflowScheduleError as exc:
        raise _workflow_schedule_http_error(exc) from exc


@app.post("/v1/workflow-automation/schedules")
def workflow_schedule_save_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflow_automation.save(payload)
    except WorkflowScheduleError as exc:
        raise _workflow_schedule_http_error(exc) from exc


@app.get("/v1/workflow-automation/schedules")
def workflow_schedule_list_route(workflow_id: str = Query("", alias="workflowId"), enabled: bool | None = Query(None), limit: int = Query(100, ge=1, le=1000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return workflow_automation.list(workflow_id, enabled, limit)


@app.get("/v1/workflow-automation/schedules/{schedule_id}")
def workflow_schedule_get_route(schedule_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflow_automation.get(schedule_id)
    except WorkflowScheduleError as exc:
        raise _workflow_schedule_http_error(exc) from exc


@app.delete("/v1/workflow-automation/schedules/{schedule_id}")
def workflow_schedule_delete_route(schedule_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflow_automation.delete(schedule_id)
    except WorkflowScheduleError as exc:
        raise _workflow_schedule_http_error(exc) from exc


@app.post("/v1/workflow-automation/schedules/{schedule_id}/enable")
def workflow_schedule_enable_route(schedule_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflow_automation.set_enabled(schedule_id, True)
    except WorkflowScheduleError as exc:
        raise _workflow_schedule_http_error(exc) from exc


@app.post("/v1/workflow-automation/schedules/{schedule_id}/disable")
def workflow_schedule_disable_route(schedule_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflow_automation.set_enabled(schedule_id, False)
    except WorkflowScheduleError as exc:
        raise _workflow_schedule_http_error(exc) from exc


@app.post("/v1/workflow-automation/schedules/{schedule_id}/trigger")
def workflow_schedule_trigger_route(schedule_id: str, payload: dict[str, Any] | None = None, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflow_automation.trigger(schedule_id, payload or {})
    except WorkflowScheduleError as exc:
        raise _workflow_schedule_http_error(exc) from exc


@app.post("/v1/workflow-automation/tick")
def workflow_schedule_tick_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return workflow_automation.tick()


@app.get("/v1/workflow-automation/firings")
def workflow_schedule_firings_route(schedule_id: str = Query("", alias="scheduleId"), status: str = Query(""), limit: int = Query(100, ge=1, le=1000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return workflow_automation.firings(schedule_id, status, limit)
    except WorkflowScheduleError as exc:
        raise _workflow_schedule_http_error(exc) from exc


@app.get("/v1/workflow-automation/events")
def workflow_events_list_route(event_type: str = Query("", alias="eventType"), limit: int = Query(100, ge=1, le=1000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return workflow_automation.events(event_type, limit)


@app.post("/v1/workflow-automation/events")
async def workflow_event_ingest_route(
    request: Request,
    x_sc_lab_event_timestamp: str | None = Header(default=None),
    x_sc_lab_event_signature: str | None = Header(default=None),
    auth: dict[str, str] = Depends(require_compute_auth),
):
    del auth
    try:
        payload = await request.json()
    except Exception as exc:
        raise HTTPException(status_code=400, detail="A JSON event payload is required.") from exc
    if not isinstance(payload, dict):
        raise HTTPException(status_code=422, detail="Event payload must be a JSON object.")
    if settings.workflow_event_secret and not verify_event_signature(payload, x_sc_lab_event_timestamp or "", x_sc_lab_event_signature or "", settings.workflow_event_secret, settings.workflow_event_signature_tolerance_seconds):
        raise HTTPException(status_code=401, detail="Invalid or expired workflow event signature.")
    try:
        return workflow_automation.ingest_event(payload)
    except WorkflowScheduleError as exc:
        raise _workflow_schedule_http_error(exc) from exc

# v0.33.1 Bayesian Optimization, Active Learning, and Resource-Aware Search

def _experiment_campaign_http_error(exc: ExperimentCampaignError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)


@app.get("/v1/experiment-campaigns/health")
def experiment_campaign_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = experiment_campaigns.health()
    body["serviceVersion"] = settings.version
    body["deploymentDurability"] = "persistent-disk" if settings.experiment_campaign_persistent_disk_mounted else "instance-local"
    body["durabilityWarning"] = None if settings.experiment_campaign_persistent_disk_mounted else "Experiment campaigns and trial histories are instance-local unless a persistent disk is mounted."
    return body


@app.get("/v1/experiment-campaigns/policies")
def experiment_campaign_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return experiment_campaign_policies(settings.experiment_campaign_max_trials, settings.experiment_campaign_max_campaigns)


@app.post("/v1/experiment-campaigns/validate")
def experiment_campaign_validate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return experiment_campaigns.validate(payload)
    except ExperimentCampaignError as exc:
        raise _experiment_campaign_http_error(exc) from exc


@app.post("/v1/experiment-campaigns")
def experiment_campaign_save_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return experiment_campaigns.save(payload)
    except ExperimentCampaignError as exc:
        raise _experiment_campaign_http_error(exc) from exc


@app.get("/v1/experiment-campaigns")
def experiment_campaign_list_route(project_id: str = Query("", alias="projectId"), status: str = Query(""), limit: int = Query(100, ge=1, le=1000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return experiment_campaigns.list(project_id, status, limit)
    except ExperimentCampaignError as exc:
        raise _experiment_campaign_http_error(exc) from exc


@app.get("/v1/experiment-campaigns/{campaign_id}")
def experiment_campaign_get_route(campaign_id: str, reconcile: bool = Query(False), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return experiment_campaigns.get(campaign_id, reconcile=reconcile)
    except ExperimentCampaignError as exc:
        raise _experiment_campaign_http_error(exc) from exc


@app.post("/v1/experiment-campaigns/{campaign_id}/start")
def experiment_campaign_start_route(campaign_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return experiment_campaigns.start_campaign(campaign_id)
    except ExperimentCampaignError as exc:
        raise _experiment_campaign_http_error(exc) from exc


@app.post("/v1/experiment-campaigns/{campaign_id}/pause")
def experiment_campaign_pause_route(campaign_id: str, payload: dict[str, Any] | None = None, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return experiment_campaigns.pause(campaign_id, str((payload or {}).get("reason") or "operator pause"))
    except ExperimentCampaignError as exc:
        raise _experiment_campaign_http_error(exc) from exc


@app.post("/v1/experiment-campaigns/{campaign_id}/resume")
def experiment_campaign_resume_route(campaign_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return experiment_campaigns.resume(campaign_id)
    except ExperimentCampaignError as exc:
        raise _experiment_campaign_http_error(exc) from exc


@app.post("/v1/experiment-campaigns/{campaign_id}/advance")
def experiment_campaign_advance_route(campaign_id: str, payload: dict[str, Any] | None = None, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        count = (payload or {}).get("count")
        return experiment_campaigns.advance(campaign_id, int(count) if count is not None else None)
    except (ExperimentCampaignError, ValueError) as exc:
        if isinstance(exc, ExperimentCampaignError):
            raise _experiment_campaign_http_error(exc) from exc
        raise HTTPException(status_code=422, detail="count must be an integer") from exc


@app.post("/v1/experiment-campaigns/{campaign_id}/reconcile")
def experiment_campaign_reconcile_route(campaign_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return experiment_campaigns.reconcile(campaign_id, auto_advance=True)
    except ExperimentCampaignError as exc:
        raise _experiment_campaign_http_error(exc) from exc


@app.post("/v1/experiment-campaigns/{campaign_id}/cancel")
def experiment_campaign_cancel_route(campaign_id: str, payload: dict[str, Any] | None = None, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return experiment_campaigns.cancel(campaign_id, str((payload or {}).get("reason") or "operator cancellation"))
    except ExperimentCampaignError as exc:
        raise _experiment_campaign_http_error(exc) from exc


@app.post("/v1/experiment-campaigns/{campaign_id}/observations")
def experiment_campaign_observe_route(campaign_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return experiment_campaigns.observe(campaign_id, payload)
    except ExperimentCampaignError as exc:
        raise _experiment_campaign_http_error(exc) from exc


@app.get("/v1/experiment-campaigns/{campaign_id}/surrogate")
def experiment_campaign_surrogate_route(campaign_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return experiment_campaigns.surrogate(campaign_id)
    except ExperimentCampaignError as exc:
        raise _experiment_campaign_http_error(exc) from exc


@app.post("/v1/experiment-campaigns/{campaign_id}/proposal-preview")
def experiment_campaign_proposal_preview_route(campaign_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return experiment_campaigns.preview_proposal(campaign_id)
    except ExperimentCampaignError as exc:
        raise _experiment_campaign_http_error(exc) from exc


@app.get("/v1/experiment-campaigns/{campaign_id}/trials")
def experiment_campaign_trials_route(campaign_id: str, status: str = Query(""), limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return experiment_campaigns.trials(campaign_id, status, limit)
    except ExperimentCampaignError as exc:
        raise _experiment_campaign_http_error(exc) from exc


@app.get("/v1/experiment-campaigns/{campaign_id}/timeline")
def experiment_campaign_timeline_route(campaign_id: str, limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return experiment_campaigns.timeline(campaign_id, limit)
    except ExperimentCampaignError as exc:
        raise _experiment_campaign_http_error(exc) from exc


@app.post("/v1/experiment-campaigns/tick")
def experiment_campaign_tick_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return experiment_campaigns.tick()


# v0.34.0 Scientific Model Registry and Environment Reproduction
def _model_registry_http_error(exc: ModelRegistryError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)


def _validate_model_registry_reference(payload: dict[str, Any]) -> None:
    source = payload.get("model") if isinstance(payload.get("model"), dict) else payload
    model_type = str(source.get("type") or "registered-method")
    if model_type == "registered-method":
        method_id = str(source.get("methodId") or "").strip()
        try:
            resolve(method_id)
        except KeyError as exc:
            raise ModelRegistryError(str(exc), 422) from exc
    elif model_type == "workflow":
        workflow_id = str(source.get("workflowId") or "").strip()
        try:
            workflows.get(workflow_id)
        except WorkflowError as exc:
            raise ModelRegistryError(f"Unknown registered workflow: {workflow_id}", 422) from exc

@app.get("/v1/model-registry/health")
def model_registry_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    body = model_registry.health(); body["serviceVersion"] = settings.version; body["deploymentDurability"] = "persistent-disk" if settings.model_registry_persistent_disk_mounted else "instance-local"; body["durabilityWarning"] = None if settings.model_registry_persistent_disk_mounted else "Model registry records are instance-local unless a persistent disk is mounted."; return body

@app.get("/v1/model-registry/policies")
def model_registry_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    body = model_registry_policies(settings.model_registry_max_models, settings.model_registry_max_versions); body["serviceVersion"] = settings.version; return body

@app.post("/v1/model-registry/environments/capture")
def model_registry_capture_environment_route(payload: dict[str, Any] | None = None, auth: dict[str, str] = Depends(require_compute_auth)):
    try: return {"ok": True, "environment": capture_environment(payload or {})}
    except ModelRegistryError as exc: raise _model_registry_http_error(exc) from exc

@app.post("/v1/model-registry/environments/compare")
def model_registry_compare_environment_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return model_registry.compare_environments(payload)
    except ModelRegistryError as exc: raise _model_registry_http_error(exc) from exc

@app.post("/v1/model-registry/validate")
def model_registry_validate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        _validate_model_registry_reference(payload)
        return model_registry.validate(payload)
    except ModelRegistryError as exc: raise _model_registry_http_error(exc) from exc

@app.post("/v1/model-registry/models")
def model_registry_register_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        _validate_model_registry_reference(payload)
        return model_registry.register(payload, auth.get("subject", "operator"))
    except ModelRegistryError as exc: raise _model_registry_http_error(exc) from exc

@app.get("/v1/model-registry/models")
def model_registry_list_route(project_id: str = Query("", alias="projectId"), channel: str = Query(""), limit: int = Query(100, ge=1, le=1000), auth: dict[str, str] = Depends(require_compute_auth)):
    try: return model_registry.list(project_id, channel, limit)
    except ModelRegistryError as exc: raise _model_registry_http_error(exc) from exc

@app.get("/v1/model-registry/models/{model_id}")
def model_registry_get_route(model_id: str, version: str = Query(""), auth: dict[str, str] = Depends(require_compute_auth)):
    try: return model_registry.get(model_id, version)
    except ModelRegistryError as exc: raise _model_registry_http_error(exc) from exc

@app.post("/v1/model-registry/models/{model_id}/{model_version}/promote")
def model_registry_promote_route(model_id: str, model_version: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return model_registry.promote(model_id, model_version, str(payload.get("channel") or "candidate"), auth.get("subject", "operator"), str(payload.get("reason") or ""))
    except ModelRegistryError as exc: raise _model_registry_http_error(exc) from exc

@app.post("/v1/model-registry/models/{model_id}/{model_version}/deprecate")
def model_registry_deprecate_route(model_id: str, model_version: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return model_registry.deprecate(model_id, model_version, auth.get("subject", "operator"), str(payload.get("reason") or ""))
    except ModelRegistryError as exc: raise _model_registry_http_error(exc) from exc

@app.get("/v1/model-registry/models/{model_id}/reproduction")
def model_registry_reproduction_route(model_id: str, version: str = Query(""), auth: dict[str, str] = Depends(require_compute_auth)):
    try: return model_registry.reproduction_manifest(model_id, version)
    except ModelRegistryError as exc: raise _model_registry_http_error(exc) from exc

@app.post("/v1/model-registry/reproduction/verify")
def model_registry_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return model_registry.verify_reproduction(payload)
    except ModelRegistryError as exc: raise _model_registry_http_error(exc) from exc

@app.get("/v1/model-registry/models/{model_id}/timeline")
def model_registry_timeline_route(model_id: str, limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    return model_registry.timeline(model_id, limit)

# v0.34.1 Ensemble Simulation, Global Sensitivity, and Uncertainty
def _ensemble_http_error(exc: EnsembleError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/ensemble-studies/health")
def ensemble_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = ensemble_studies.health(); body["serviceVersion"] = settings.version; body["deploymentDurability"] = "persistent-disk" if settings.ensemble_persistent_disk_mounted else "instance-local"; body["durabilityWarning"] = None if settings.ensemble_persistent_disk_mounted else "Ensemble study records are instance-local unless a persistent disk is mounted."; return body

@app.get("/v1/ensemble-studies/policies")
def ensemble_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = ensemble_policies(settings.ensemble_max_studies, settings.ensemble_max_evaluations); body["serviceVersion"] = settings.version; return body

@app.post("/v1/ensemble-studies/validate")
def ensemble_validate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return ensemble_studies.validate(payload)
    except EnsembleError as exc: raise _ensemble_http_error(exc) from exc

@app.post("/v1/ensemble-studies")
def ensemble_create_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return ensemble_studies.create(payload, auth.get("subject", "operator"))
    except EnsembleError as exc: raise _ensemble_http_error(exc) from exc

@app.get("/v1/ensemble-studies")
def ensemble_list_route(project_id: str = Query("", alias="projectId"), status: str = Query(""), limit: int = Query(100, ge=1, le=1000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return ensemble_studies.list(project_id, status, limit)
    except EnsembleError as exc: raise _ensemble_http_error(exc) from exc

@app.get("/v1/ensemble-studies/{study_id}")
def ensemble_get_route(study_id: str, reconcile: bool = Query(True), evaluation_limit: int = Query(5000, alias="evaluationLimit", ge=1, le=200000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return ensemble_studies.get(study_id, reconcile, evaluation_limit)
    except EnsembleError as exc: raise _ensemble_http_error(exc) from exc

@app.post("/v1/ensemble-studies/{study_id}/start")
def ensemble_start_route(study_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    try: return ensemble_studies.start(study_id, auth.get("subject", "operator"))
    except EnsembleError as exc: raise _ensemble_http_error(exc) from exc

@app.post("/v1/ensemble-studies/{study_id}/reconcile")
def ensemble_reconcile_route(study_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return ensemble_studies.reconcile(study_id)
    except EnsembleError as exc: raise _ensemble_http_error(exc) from exc

@app.post("/v1/ensemble-studies/{study_id}/evaluations/{evaluation_id}/result")
def ensemble_record_result_route(study_id: str, evaluation_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return ensemble_studies.record_result(study_id, evaluation_id, payload, auth.get("subject", "operator"))
    except EnsembleError as exc: raise _ensemble_http_error(exc) from exc

@app.post("/v1/ensemble-studies/{study_id}/cancel")
def ensemble_cancel_route(study_id: str, payload: dict[str, Any] | None = None, auth: dict[str, str] = Depends(require_compute_auth)):
    body = payload or {}
    try: return ensemble_studies.cancel(study_id, auth.get("subject", "operator"), str(body.get("reason") or "operator cancellation"))
    except EnsembleError as exc: raise _ensemble_http_error(exc) from exc

@app.get("/v1/ensemble-studies/{study_id}/timeline")
def ensemble_timeline_route(study_id: str, limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return ensemble_studies.timeline(study_id, limit)
    except EnsembleError as exc: raise _ensemble_http_error(exc) from exc

# v0.34.2 Surrogate Models and Reduced-Order Analysis

def _surrogate_rom_http_error(exc: SurrogateROMError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/surrogate-rom/health")
def surrogate_rom_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = surrogate_rom.health(); body["serviceVersion"] = settings.version; body["deploymentDurability"] = "persistent-disk" if settings.surrogate_rom_persistent_disk_mounted else "instance-local"; body["durabilityWarning"] = None if settings.surrogate_rom_persistent_disk_mounted else "Surrogate and ROM records are instance-local unless a persistent disk is mounted."; return body

@app.get("/v1/surrogate-rom/policies")
def surrogate_rom_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = surrogate_rom_policies(settings.surrogate_rom_max_studies, settings.surrogate_rom_max_training_rows, settings.surrogate_rom_max_snapshot_dimensions, settings.surrogate_rom_max_request_bytes); body["serviceVersion"] = settings.version; return body

@app.post("/v1/surrogate-rom/validate")
def surrogate_rom_validate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return surrogate_rom.validate(payload)
    except SurrogateROMError as exc: raise _surrogate_rom_http_error(exc) from exc

@app.post("/v1/surrogate-rom/studies")
def surrogate_rom_train_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return surrogate_rom.train(payload, auth.get("subject", "operator"))
    except SurrogateROMError as exc: raise _surrogate_rom_http_error(exc) from exc

@app.get("/v1/surrogate-rom/studies")
def surrogate_rom_list_route(project_id: str = Query("", alias="projectId"), status: str = Query(""), limit: int = Query(100, ge=1, le=1000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return surrogate_rom.list(project_id, status, limit)
    except SurrogateROMError as exc: raise _surrogate_rom_http_error(exc) from exc

@app.get("/v1/surrogate-rom/studies/{study_id}")
def surrogate_rom_get_route(study_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return surrogate_rom.get(study_id)
    except SurrogateROMError as exc: raise _surrogate_rom_http_error(exc) from exc

@app.post("/v1/surrogate-rom/studies/{study_id}/predict")
def surrogate_rom_predict_route(study_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return surrogate_rom.predict(study_id, payload)
    except SurrogateROMError as exc: raise _surrogate_rom_http_error(exc) from exc

@app.post("/v1/surrogate-rom/studies/{study_id}/register")
def surrogate_rom_register_route(study_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return surrogate_rom.register_model(study_id, payload, auth.get("subject", "operator"))
    except SurrogateROMError as exc: raise _surrogate_rom_http_error(exc) from exc

@app.get("/v1/surrogate-rom/studies/{study_id}/timeline")
def surrogate_rom_timeline_route(study_id: str, limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return surrogate_rom.timeline(study_id, limit)
    except SurrogateROMError as exc: raise _surrogate_rom_http_error(exc) from exc

# v0.35.0 Shared Research Projects and Team Workspaces

def _team_workspace_actor(auth: dict[str, str]) -> tuple[str, str]:
    return (auth.get("actor") or auth.get("subject") or auth.get("client") or "operator", auth.get("actorName") or auth.get("client") or "Workspace member")

def _team_workspace_http_error(exc: TeamWorkspaceError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/team-workspaces/health")
def team_workspace_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = team_workspaces.health(); body["serviceVersion"] = settings.version; body["deploymentDurability"] = "persistent-disk" if settings.team_workspace_persistent_disk_mounted else "instance-local"; body["durabilityWarning"] = None if settings.team_workspace_persistent_disk_mounted else "Shared workspace records are instance-local unless a persistent disk is mounted."; return body

@app.get("/v1/team-workspaces/policies")
def team_workspace_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = team_workspace_policies(); body["serviceVersion"] = settings.version; return body

@app.post("/v1/team-workspaces")
def team_workspace_create_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, name = _team_workspace_actor(auth)
    try: return team_workspaces.create(payload, actor, name)
    except TeamWorkspaceError as exc: raise _team_workspace_http_error(exc) from exc

@app.get("/v1/team-workspaces")
def team_workspace_list_route(status: str = Query(""), limit: int = Query(100, ge=1, le=1000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return team_workspaces.list(actor, status or None, limit)
    except TeamWorkspaceError as exc: raise _team_workspace_http_error(exc) from exc

@app.post("/v1/team-workspaces/invitations/accept")
def team_workspace_accept_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, name = _team_workspace_actor(auth)
    try: return team_workspaces.accept_invitation(payload, actor, name)
    except TeamWorkspaceError as exc: raise _team_workspace_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}")
def team_workspace_get_route(workspace_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return team_workspaces.get(workspace_id, actor)
    except TeamWorkspaceError as exc: raise _team_workspace_http_error(exc) from exc

@app.patch("/v1/team-workspaces/{workspace_id}")
def team_workspace_update_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return team_workspaces.update(workspace_id, payload, actor)
    except TeamWorkspaceError as exc: raise _team_workspace_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/archive")
def team_workspace_archive_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return team_workspaces.archive(workspace_id, actor, str(payload.get("reason") or ""))
    except TeamWorkspaceError as exc: raise _team_workspace_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/invitations")
def team_workspace_invite_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return team_workspaces.invite(workspace_id, payload, actor)
    except TeamWorkspaceError as exc: raise _team_workspace_http_error(exc) from exc

@app.patch("/v1/team-workspaces/{workspace_id}/members/{member_id}")
def team_workspace_member_role_route(workspace_id: str, member_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return team_workspaces.set_member_role(workspace_id, member_id, payload, actor)
    except TeamWorkspaceError as exc: raise _team_workspace_http_error(exc) from exc

@app.delete("/v1/team-workspaces/{workspace_id}/members/{member_id}")
def team_workspace_remove_member_route(workspace_id: str, member_id: str, reason: str = Query(""), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return team_workspaces.remove_member(workspace_id, member_id, actor, reason)
    except TeamWorkspaceError as exc: raise _team_workspace_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/ownership")
def team_workspace_transfer_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return team_workspaces.transfer_ownership(workspace_id, payload, actor)
    except TeamWorkspaceError as exc: raise _team_workspace_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/resources")
def team_workspace_link_resource_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return team_workspaces.link_resource(workspace_id, payload, actor)
    except TeamWorkspaceError as exc: raise _team_workspace_http_error(exc) from exc

@app.delete("/v1/team-workspaces/{workspace_id}/resources/{link_id}")
def team_workspace_unlink_resource_route(workspace_id: str, link_id: str, reason: str = Query(""), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return team_workspaces.unlink_resource(workspace_id, link_id, actor, reason)
    except TeamWorkspaceError as exc: raise _team_workspace_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/authorize")
def team_workspace_authorize_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return team_workspaces.authorize(workspace_id, payload, actor)
    except TeamWorkspaceError as exc: raise _team_workspace_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/timeline")
def team_workspace_timeline_route(workspace_id: str, limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return team_workspaces.timeline(workspace_id, actor, limit)
    except TeamWorkspaceError as exc: raise _team_workspace_http_error(exc) from exc


# v0.35.1 Review, Comments, Approvals, and Scientific Sign-Off
def _workspace_review_http_error(exc: WorkspaceReviewError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/workspace-reviews/health")
def workspace_review_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    body = workspace_reviews.health(); body["serviceVersion"] = settings.version; body["deploymentDurability"] = "persistent-disk" if settings.team_workspace_persistent_disk_mounted else "instance-local"; return body

@app.get("/v1/workspace-reviews/policies")
def workspace_review_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    body = workspace_review_policies(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/team-workspaces/{workspace_id}/review-threads")
def workspace_review_threads_list_route(workspace_id: str, status: str = Query(""), resourceLinkId: str = Query(""), limit: int = Query(200, ge=1, le=1000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_reviews.list_threads(workspace_id, actor, status or None, resourceLinkId or None, limit)
    except WorkspaceReviewError as exc: raise _workspace_review_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/review-threads")
def workspace_review_thread_create_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_reviews.create_thread(workspace_id, payload, actor)
    except WorkspaceReviewError as exc: raise _workspace_review_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/review-threads/{thread_id}")
def workspace_review_thread_get_route(workspace_id: str, thread_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_reviews.get_thread(workspace_id, thread_id, actor)
    except WorkspaceReviewError as exc: raise _workspace_review_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/review-threads/{thread_id}/comments")
def workspace_review_comment_create_route(workspace_id: str, thread_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_reviews.add_comment(workspace_id, thread_id, payload, actor)
    except WorkspaceReviewError as exc: raise _workspace_review_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/review-comments/{comment_id}/withdraw")
def workspace_review_comment_withdraw_route(workspace_id: str, comment_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_reviews.withdraw_comment(workspace_id, comment_id, payload, actor)
    except WorkspaceReviewError as exc: raise _workspace_review_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/review-threads/{thread_id}/resolve")
def workspace_review_thread_resolve_route(workspace_id: str, thread_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_reviews.set_thread_status(workspace_id, thread_id, payload, actor, "resolved")
    except WorkspaceReviewError as exc: raise _workspace_review_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/review-threads/{thread_id}/reopen")
def workspace_review_thread_reopen_route(workspace_id: str, thread_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_reviews.set_thread_status(workspace_id, thread_id, payload, actor, "open")
    except WorkspaceReviewError as exc: raise _workspace_review_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/review-assignments")
def workspace_review_assignments_list_route(workspace_id: str, status: str = Query(""), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_reviews.list_assignments(workspace_id, actor, status or None)
    except WorkspaceReviewError as exc: raise _workspace_review_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/review-assignments")
def workspace_review_assignment_create_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_reviews.create_assignment(workspace_id, payload, actor)
    except WorkspaceReviewError as exc: raise _workspace_review_http_error(exc) from exc

@app.patch("/v1/team-workspaces/{workspace_id}/review-assignments/{assignment_id}")
def workspace_review_assignment_update_route(workspace_id: str, assignment_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_reviews.update_assignment(workspace_id, assignment_id, payload, actor)
    except WorkspaceReviewError as exc: raise _workspace_review_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/approval-requests")
def workspace_review_approval_list_route(workspace_id: str, status: str = Query(""), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_reviews.list_approvals(workspace_id, actor, status or None)
    except WorkspaceReviewError as exc: raise _workspace_review_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/approval-requests")
def workspace_review_approval_create_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_reviews.create_approval(workspace_id, payload, actor)
    except WorkspaceReviewError as exc: raise _workspace_review_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/approval-requests/{approval_id}")
def workspace_review_approval_get_route(workspace_id: str, approval_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_reviews.get_approval(workspace_id, approval_id, actor)
    except WorkspaceReviewError as exc: raise _workspace_review_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/approval-requests/{approval_id}/evaluate")
def workspace_review_approval_evaluate_route(workspace_id: str, approval_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_reviews.evaluate_approval(workspace_id, approval_id, actor)
    except WorkspaceReviewError as exc: raise _workspace_review_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/approval-requests/{approval_id}/decisions")
def workspace_review_decision_create_route(workspace_id: str, approval_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_reviews.decide(workspace_id, approval_id, payload, actor)
    except WorkspaceReviewError as exc: raise _workspace_review_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/approval-requests/{approval_id}/cancel")
def workspace_review_approval_cancel_route(workspace_id: str, approval_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_reviews.cancel_approval(workspace_id, approval_id, payload, actor)
    except WorkspaceReviewError as exc: raise _workspace_review_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/approval-requests/{approval_id}/signoff")
def workspace_review_signoff_route(workspace_id: str, approval_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_reviews.signoff(workspace_id, approval_id, payload, actor)
    except WorkspaceReviewError as exc: raise _workspace_review_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/review-timeline")
def workspace_review_timeline_route(workspace_id: str, limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_reviews.timeline(workspace_id, actor, limit)
    except WorkspaceReviewError as exc: raise _workspace_review_http_error(exc) from exc

# v0.35.2 Version History, Branching, Merge, and Conflict Resolution
def _workspace_version_http_error(exc: WorkspaceVersionError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/workspace-versions/health")
def workspace_version_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    body = workspace_versions.health(); body["serviceVersion"] = settings.version; body["deploymentDurability"] = "persistent-disk" if settings.team_workspace_persistent_disk_mounted else "instance-local"; return body

@app.get("/v1/workspace-versions/policies")
def workspace_version_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    body = workspace_version_policies(); body["serviceVersion"] = settings.version; return body

@app.post("/v1/team-workspaces/{workspace_id}/versions/bootstrap")
def workspace_version_bootstrap_route(workspace_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_versions.bootstrap(workspace_id, actor)
    except WorkspaceVersionError as exc: raise _workspace_version_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/branches")
def workspace_version_branches_list_route(workspace_id: str, status: str = Query(""), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_versions.list_branches(workspace_id, actor, status or None)
    except WorkspaceVersionError as exc: raise _workspace_version_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/branches")
def workspace_version_branch_create_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_versions.create_branch(workspace_id, payload, actor)
    except WorkspaceVersionError as exc: raise _workspace_version_http_error(exc) from exc

@app.patch("/v1/team-workspaces/{workspace_id}/branches/{branch_ref:path}")
def workspace_version_branch_update_route(workspace_id: str, branch_ref: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_versions.set_branch(workspace_id, branch_ref, payload, actor)
    except WorkspaceVersionError as exc: raise _workspace_version_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/branches/{branch_ref:path}/snapshots")
def workspace_version_snapshots_list_route(workspace_id: str, branch_ref: str, limit: int = Query(200, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_versions.list_snapshots(workspace_id, branch_ref, actor, limit)
    except WorkspaceVersionError as exc: raise _workspace_version_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/branches/{branch_ref:path}/snapshots")
def workspace_version_snapshot_create_route(workspace_id: str, branch_ref: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_versions.create_snapshot(workspace_id, branch_ref, payload, actor)
    except WorkspaceVersionError as exc: raise _workspace_version_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/branches/{branch_ref:path}/restore/{snapshot_id}")
def workspace_version_restore_route(workspace_id: str, branch_ref: str, snapshot_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_versions.restore_snapshot(workspace_id, branch_ref, snapshot_id, payload, actor)
    except WorkspaceVersionError as exc: raise _workspace_version_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/snapshots/{snapshot_id}")
def workspace_version_snapshot_get_route(workspace_id: str, snapshot_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_versions.get_snapshot(workspace_id, snapshot_id, actor)
    except WorkspaceVersionError as exc: raise _workspace_version_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/version-compare")
def workspace_version_compare_route(workspace_id: str, fromSnapshotId: str = Query(""), toSnapshotId: str = Query(""), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_versions.compare(workspace_id, fromSnapshotId or None, toSnapshotId or None, actor)
    except WorkspaceVersionError as exc: raise _workspace_version_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/merge-requests")
def workspace_version_merges_list_route(workspace_id: str, status: str = Query(""), limit: int = Query(200, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_versions.list_merge_requests(workspace_id, actor, status or None, limit)
    except WorkspaceVersionError as exc: raise _workspace_version_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/merge-requests")
def workspace_version_merge_create_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_versions.create_merge_request(workspace_id, payload, actor)
    except WorkspaceVersionError as exc: raise _workspace_version_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/merge-requests/{merge_id}")
def workspace_version_merge_get_route(workspace_id: str, merge_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_versions.get_merge_request(workspace_id, merge_id, actor)
    except WorkspaceVersionError as exc: raise _workspace_version_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/merge-requests/{merge_id}/conflicts/{conflict_id}/resolve")
def workspace_version_conflict_resolve_route(workspace_id: str, merge_id: str, conflict_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_versions.resolve_conflict(workspace_id, merge_id, conflict_id, payload, actor)
    except WorkspaceVersionError as exc: raise _workspace_version_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/merge-requests/{merge_id}/approval")
def workspace_version_approval_attach_route(workspace_id: str, merge_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_versions.attach_approval(workspace_id, merge_id, payload, actor)
    except WorkspaceVersionError as exc: raise _workspace_version_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/merge-requests/{merge_id}/finalize")
def workspace_version_merge_finalize_route(workspace_id: str, merge_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_versions.finalize_merge(workspace_id, merge_id, payload, actor)
    except WorkspaceVersionError as exc: raise _workspace_version_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/merge-requests/{merge_id}/cancel")
def workspace_version_merge_cancel_route(workspace_id: str, merge_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_versions.cancel_merge(workspace_id, merge_id, payload, actor)
    except WorkspaceVersionError as exc: raise _workspace_version_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/version-timeline")
def workspace_version_timeline_route(workspace_id: str, limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return workspace_versions.timeline(workspace_id, actor, limit)
    except WorkspaceVersionError as exc: raise _workspace_version_http_error(exc) from exc

# v0.36.0 Scientific Artifact Repository and Data Federation

def _artifact_repository_http_error(exc: ArtifactRepositoryError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/artifact-repository/health")
def artifact_repository_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = artifact_repository.health()
    body["serviceVersion"] = settings.version
    body["deploymentDurability"] = "persistent-disk" if settings.artifact_repository_persistent_disk_mounted else "instance-local"
    body["durabilityWarning"] = None if settings.artifact_repository_persistent_disk_mounted else "Artifact repository metadata is instance-local unless a persistent disk is mounted."
    return body

@app.get("/v1/artifact-repository/policies")
def artifact_repository_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = artifact_repository_policies(settings.artifact_repository_max_collections, settings.artifact_repository_max_records, settings.artifact_repository_max_manifest_records)
    body["serviceVersion"] = settings.version
    return body

@app.get("/v1/team-workspaces/{workspace_id}/artifact-collections")
def artifact_repository_collections_route(workspace_id: str, status: str = Query(""), limit: int = Query(100, ge=1, le=1000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return artifact_repository.list_collections(workspace_id, actor, status, limit)
    except ArtifactRepositoryError as exc: raise _artifact_repository_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/artifact-collections")
def artifact_repository_collection_create_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return artifact_repository.create_collection(workspace_id, payload, actor)
    except ArtifactRepositoryError as exc: raise _artifact_repository_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/artifact-collections/{collection_id}")
def artifact_repository_collection_get_route(workspace_id: str, collection_id: str, include_artifacts: bool = Query(True, alias="includeArtifacts"), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return artifact_repository.get_collection(workspace_id, collection_id, actor, include_artifacts)
    except ArtifactRepositoryError as exc: raise _artifact_repository_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/artifact-collections/{collection_id}/archive")
def artifact_repository_collection_archive_route(workspace_id: str, collection_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return artifact_repository.archive_collection(workspace_id, collection_id, payload, actor)
    except ArtifactRepositoryError as exc: raise _artifact_repository_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/artifact-collections/{collection_id}/artifacts")
def artifact_repository_register_route(workspace_id: str, collection_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return artifact_repository.register_artifact(workspace_id, collection_id, payload, actor)
    except ArtifactRepositoryError as exc: raise _artifact_repository_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/repository-artifacts/{artifact_id}/verify")
def artifact_repository_verify_route(workspace_id: str, artifact_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return artifact_repository.verify_artifact(workspace_id, artifact_id, actor)
    except ArtifactRepositoryError as exc: raise _artifact_repository_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/artifact-collections/{collection_id}/manifest")
def artifact_repository_manifest_route(workspace_id: str, collection_id: str, node_id: str = Query("local-node", alias="nodeId"), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return artifact_repository.export_manifest(workspace_id, collection_id, actor, node_id)
    except ArtifactRepositoryError as exc: raise _artifact_repository_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/federated-sources")
def artifact_repository_sources_route(workspace_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return artifact_repository.list_sources(workspace_id, actor)
    except ArtifactRepositoryError as exc: raise _artifact_repository_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/federated-sources")
def artifact_repository_source_create_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return artifact_repository.register_source(workspace_id, payload, actor)
    except ArtifactRepositoryError as exc: raise _artifact_repository_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/federated-sources/{source_id}/sync/{collection_id}")
def artifact_repository_sync_route(workspace_id: str, source_id: str, collection_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return artifact_repository.sync_manifest(workspace_id, source_id, collection_id, payload, actor)
    except ArtifactRepositoryError as exc: raise _artifact_repository_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/federation-sync-runs")
def artifact_repository_sync_history_route(workspace_id: str, source_id: str = Query("", alias="sourceId"), limit: int = Query(200, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return artifact_repository.sync_history(workspace_id, actor, source_id, limit)
    except ArtifactRepositoryError as exc: raise _artifact_repository_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/federation-conflicts")
def artifact_repository_conflicts_route(workspace_id: str, status: str = Query("open"), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return artifact_repository.list_conflicts(workspace_id, actor, status)
    except ArtifactRepositoryError as exc: raise _artifact_repository_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/federation-conflicts/{conflict_id}/resolve")
def artifact_repository_conflict_resolve_route(workspace_id: str, conflict_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return artifact_repository.resolve_conflict(workspace_id, conflict_id, payload, actor)
    except ArtifactRepositoryError as exc: raise _artifact_repository_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/artifact-repository-timeline")
def artifact_repository_timeline_route(workspace_id: str, limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return artifact_repository.timeline(workspace_id, actor, limit)
    except ArtifactRepositoryError as exc: raise _artifact_repository_http_error(exc) from exc


# v0.36.1 Institutional Node Federation and Local-Data Execution

def _institutional_node_http_error(exc: InstitutionalNodeError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/institutional-nodes/health")
def institutional_node_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = institutional_nodes.health(); body["serviceVersion"] = settings.version; body["deploymentDurability"] = "persistent-disk" if settings.institutional_node_persistent_disk_mounted else "instance-local"; body["durabilityWarning"] = None if settings.institutional_node_persistent_disk_mounted else "Institutional node and local-execution records are instance-local unless a persistent disk is mounted."; return body

@app.get("/v1/institutional-nodes/policies")
def institutional_node_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = institutional_node_policies(settings.institutional_node_max_nodes, settings.institutional_node_max_data_assets, settings.institutional_node_max_requests); body["serviceVersion"] = settings.version; return body

@app.get("/v1/team-workspaces/{workspace_id}/institutional-nodes")
def institutional_node_list_route(workspace_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return institutional_nodes.list_nodes(workspace_id, actor)
    except InstitutionalNodeError as exc: raise _institutional_node_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/institutional-nodes")
def institutional_node_register_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return institutional_nodes.register_node(workspace_id, payload, actor)
    except InstitutionalNodeError as exc: raise _institutional_node_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/institutional-nodes/{node_id}/status")
def institutional_node_status_route(workspace_id: str, node_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return institutional_nodes.set_node_status(workspace_id, node_id, str(payload.get("status") or ""), actor, str(payload.get("reason") or ""))
    except InstitutionalNodeError as exc: raise _institutional_node_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/institutional-nodes/{node_id}/data-assets")
def institutional_node_data_list_route(workspace_id: str, node_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return institutional_nodes.list_data_assets(workspace_id, node_id, actor)
    except InstitutionalNodeError as exc: raise _institutional_node_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/institutional-nodes/{node_id}/data-assets")
def institutional_node_data_register_route(workspace_id: str, node_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return institutional_nodes.register_data_asset(workspace_id, node_id, payload, actor)
    except InstitutionalNodeError as exc: raise _institutional_node_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/local-executions")
def institutional_execution_list_route(workspace_id: str, node_id: str = Query("", alias="nodeId"), status: str = Query(""), limit: int = Query(200, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return institutional_nodes.list_executions(workspace_id, actor, node_id, status, limit)
    except InstitutionalNodeError as exc: raise _institutional_node_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/local-executions")
def institutional_execution_create_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return institutional_nodes.create_execution(workspace_id, payload, actor)
    except InstitutionalNodeError as exc: raise _institutional_node_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/local-executions/{request_id}/cancel")
def institutional_execution_cancel_route(workspace_id: str, request_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return institutional_nodes.cancel_execution(workspace_id, request_id, actor, str(payload.get("reason") or ""))
    except InstitutionalNodeError as exc: raise _institutional_node_http_error(exc) from exc

@app.post("/v1/institutional-nodes/{node_id}/executions/claim")
def institutional_execution_claim_route(node_id: str, x_sc_lab_node_secret: str = Header("", alias="X-SC-Lab-Node-Secret"), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return institutional_nodes.claim_execution(node_id, x_sc_lab_node_secret)
    except InstitutionalNodeError as exc: raise _institutional_node_http_error(exc) from exc

@app.post("/v1/institutional-nodes/{node_id}/executions/{request_id}/complete")
def institutional_execution_complete_route(node_id: str, request_id: str, payload: dict[str, Any], x_sc_lab_node_secret: str = Header("", alias="X-SC-Lab-Node-Secret"), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return institutional_nodes.complete_execution(node_id, request_id, payload, x_sc_lab_node_secret)
    except InstitutionalNodeError as exc: raise _institutional_node_http_error(exc) from exc

@app.post("/v1/institutional-nodes/verify-envelope")
def institutional_execution_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return institutional_nodes.verify_envelope(payload)
    except InstitutionalNodeError as exc: raise _institutional_node_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/institutional-node-timeline")
def institutional_node_timeline_route(workspace_id: str, limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return institutional_nodes.timeline(workspace_id, actor, limit)
    except InstitutionalNodeError as exc: raise _institutional_node_http_error(exc) from exc

# v0.33.2 Closed-Loop Simulation and Instrument Campaigns

@app.get("/v1/closed-loop-campaigns/health")
def closed_loop_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = closed_loop_campaigns.health(); body["serviceVersion"] = settings.version; body["deploymentDurability"] = "persistent-disk" if settings.closed_loop_persistent_disk_mounted else "instance-local"; return body

@app.get("/v1/closed-loop-campaigns/policies")
def closed_loop_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = closed_loop_policies(settings.closed_loop_max_loops, settings.closed_loop_max_cycles); body["serviceVersion"] = settings.version; return body

@app.post("/v1/closed-loop-campaigns/validate")
def closed_loop_validate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return closed_loop_campaigns.validate(payload)
    except ClosedLoopError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/closed-loop-campaigns")
def closed_loop_save_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return closed_loop_campaigns.save(payload)
    except ClosedLoopError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.get("/v1/closed-loop-campaigns")
def closed_loop_list_route(project_id: str = Query("", alias="projectId"), status: str = Query(""), limit: int = Query(100, ge=1, le=1000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return closed_loop_campaigns.list(project_id, status, limit)
    except ClosedLoopError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.get("/v1/closed-loop-campaigns/{loop_id}")
def closed_loop_get_route(loop_id: str, reconcile: bool = Query(False), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return closed_loop_campaigns.get(loop_id, reconcile)
    except ClosedLoopError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/closed-loop-campaigns/{loop_id}/start")
def closed_loop_start_route(loop_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return closed_loop_campaigns.start_loop(loop_id)
    except ClosedLoopError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/closed-loop-campaigns/{loop_id}/pause")
def closed_loop_pause_route(loop_id: str, payload: dict[str, Any] | None = None, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return closed_loop_campaigns.pause(loop_id, str((payload or {}).get("reason") or "operator pause"))
    except ClosedLoopError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/closed-loop-campaigns/{loop_id}/resume")
def closed_loop_resume_route(loop_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return closed_loop_campaigns.resume(loop_id)
    except ClosedLoopError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/closed-loop-campaigns/{loop_id}/reconcile")
def closed_loop_reconcile_route(loop_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return closed_loop_campaigns.reconcile(loop_id)
    except ClosedLoopError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/closed-loop-campaigns/{loop_id}/cancel")
def closed_loop_cancel_route(loop_id: str, payload: dict[str, Any] | None = None, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return closed_loop_campaigns.cancel(loop_id, str((payload or {}).get("reason") or "operator cancellation"))
    except ClosedLoopError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/closed-loop-campaigns/{loop_id}/emergency-stop")
def closed_loop_emergency_stop_route(loop_id: str, payload: dict[str, Any] | None = None, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return closed_loop_campaigns.emergency_stop(loop_id, str((payload or {}).get("reason") or "operator emergency stop"))
    except ClosedLoopError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/closed-loop-campaigns/{loop_id}/commands/preview")
def closed_loop_preview_route(loop_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return closed_loop_campaigns.preview_command(loop_id)
    except ClosedLoopError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/closed-loop-campaigns/{loop_id}/commands/issue")
def closed_loop_issue_route(loop_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return closed_loop_campaigns.issue_next_command(loop_id)
    except ClosedLoopError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.get("/v1/closed-loop-campaigns/{loop_id}/commands")
def closed_loop_commands_route(loop_id: str, status: str = Query(""), limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return closed_loop_campaigns.commands(loop_id, status, limit)
    except ClosedLoopError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/closed-loop-campaigns/{loop_id}/commands/{command_id}/approve")
def closed_loop_approve_route(loop_id: str, command_id: str, payload: dict[str, Any] | None = None, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = payload or {}
    try: return closed_loop_campaigns.approve_command(loop_id, command_id, str(body.get("operator") or "operator"), str(body.get("reason") or "approved"))
    except ClosedLoopError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/closed-loop-campaigns/{loop_id}/commands/{command_id}/dispatch")
def closed_loop_dispatch_route(loop_id: str, command_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return closed_loop_campaigns.dispatch_command(loop_id, command_id)
    except ClosedLoopError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/closed-loop-campaigns/{loop_id}/measurements")
def closed_loop_measurement_route(loop_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return closed_loop_campaigns.ingest_measurement(loop_id, payload)
    except ClosedLoopError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.get("/v1/closed-loop-campaigns/{loop_id}/measurements")
def closed_loop_measurements_route(loop_id: str, limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return closed_loop_campaigns.measurements(loop_id, limit)
    except ClosedLoopError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.get("/v1/closed-loop-campaigns/{loop_id}/timeline")
def closed_loop_timeline_route(loop_id: str, limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return closed_loop_campaigns.timeline(loop_id, limit)
    except ClosedLoopError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/closed-loop-campaigns/tick")
def closed_loop_tick_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return closed_loop_campaigns.tick()



def _publication_http_error(exc: PublicationStudioError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/publication-studio/health")
def publication_studio_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body=publication_studio.health(); body["serviceVersion"]=settings.version; body["deploymentDurability"]="persistent-disk" if settings.publication_studio_persistent_disk_mounted else "instance-local"; body["durabilityWarning"]=None if settings.publication_studio_persistent_disk_mounted else "Reproducibility packages and publication records are instance-local unless a persistent disk is mounted."; return body

@app.get("/v1/publication-studio/policies")
def publication_studio_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body=publication_studio_policies(settings.publication_studio_max_packages,settings.publication_studio_max_publications,settings.publication_studio_max_resources); body["serviceVersion"]=settings.version; return body

@app.get("/v1/team-workspaces/{workspace_id}/reproducibility-packages")
def publication_packages_list_route(workspace_id: str,status: str=Query(""),limit: int=Query(100,ge=1,le=1000),auth: dict[str,str]=Depends(require_compute_auth)):
    actor,_=_team_workspace_actor(auth)
    try:return publication_studio.list_packages(workspace_id,actor,status,limit)
    except PublicationStudioError as exc:raise _publication_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/reproducibility-packages")
def publication_package_create_route(workspace_id: str,payload: dict[str,Any],auth: dict[str,str]=Depends(require_compute_auth)):
    actor,_=_team_workspace_actor(auth)
    try:return publication_studio.create_package(workspace_id,payload,actor)
    except PublicationStudioError as exc:raise _publication_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/reproducibility-packages/{package_id}")
def publication_package_get_route(workspace_id: str,package_id: str,auth: dict[str,str]=Depends(require_compute_auth)):
    actor,_=_team_workspace_actor(auth)
    try:return publication_studio.get_package(workspace_id,package_id,actor)
    except PublicationStudioError as exc:raise _publication_http_error(exc) from exc

@app.patch("/v1/team-workspaces/{workspace_id}/reproducibility-packages/{package_id}")
def publication_package_update_route(workspace_id: str,package_id: str,payload: dict[str,Any],auth: dict[str,str]=Depends(require_compute_auth)):
    actor,_=_team_workspace_actor(auth)
    try:return publication_studio.update_package(workspace_id,package_id,payload,actor)
    except PublicationStudioError as exc:raise _publication_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/reproducibility-packages/{package_id}/seal")
def publication_package_seal_route(workspace_id: str,package_id: str,auth: dict[str,str]=Depends(require_compute_auth)):
    actor,_=_team_workspace_actor(auth)
    try:return publication_studio.seal_package(workspace_id,package_id,actor)
    except PublicationStudioError as exc:raise _publication_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/reproducibility-packages/{package_id}/verify")
def publication_package_verify_route(workspace_id: str,package_id: str,auth: dict[str,str]=Depends(require_compute_auth)):
    actor,_=_team_workspace_actor(auth)
    try:return publication_studio.verify_package(workspace_id,package_id,actor)
    except PublicationStudioError as exc:raise _publication_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/research-publications")
def publications_list_route(workspace_id: str,status: str=Query(""),limit: int=Query(100,ge=1,le=1000),auth: dict[str,str]=Depends(require_compute_auth)):
    actor,_=_team_workspace_actor(auth)
    try:return publication_studio.list_publications(workspace_id,actor,status,limit)
    except PublicationStudioError as exc:raise _publication_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/research-publications")
def publication_create_route(workspace_id: str,payload: dict[str,Any],auth: dict[str,str]=Depends(require_compute_auth)):
    actor,_=_team_workspace_actor(auth)
    try:return publication_studio.create_publication(workspace_id,payload,actor)
    except PublicationStudioError as exc:raise _publication_http_error(exc) from exc

@app.patch("/v1/team-workspaces/{workspace_id}/research-publications/{publication_id}")
def publication_update_route(workspace_id: str,publication_id: str,payload: dict[str,Any],auth: dict[str,str]=Depends(require_compute_auth)):
    actor,_=_team_workspace_actor(auth)
    try:return publication_studio.update_publication(workspace_id,publication_id,payload,actor)
    except PublicationStudioError as exc:raise _publication_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/research-publications/{publication_id}/render")
def publication_render_route(workspace_id: str,publication_id: str,auth: dict[str,str]=Depends(require_compute_auth)):
    actor,_=_team_workspace_actor(auth)
    try:return publication_studio.render_publication(workspace_id,publication_id,actor)
    except PublicationStudioError as exc:raise _publication_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/research-publications/{publication_id}/ready")
def publication_ready_route(workspace_id: str,publication_id: str,auth: dict[str,str]=Depends(require_compute_auth)):
    actor,_=_team_workspace_actor(auth)
    try:return publication_studio.mark_ready(workspace_id,publication_id,actor)
    except PublicationStudioError as exc:raise _publication_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/research-publications/{publication_id}/publish")
def publication_publish_route(workspace_id: str,publication_id: str,payload: dict[str,Any],auth: dict[str,str]=Depends(require_compute_auth)):
    actor,_=_team_workspace_actor(auth)
    try:return publication_studio.publish(workspace_id,publication_id,payload,actor)
    except PublicationStudioError as exc:raise _publication_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/publication-timeline")
def publication_timeline_route(workspace_id: str,limit: int=Query(500,ge=1,le=5000),auth: dict[str,str]=Depends(require_compute_auth)):
    actor,_=_team_workspace_actor(auth)
    try:return publication_studio.timeline(workspace_id,actor,limit)
    except PublicationStudioError as exc:raise _publication_http_error(exc) from exc

# v0.37.1 Manuscript, Report, Notebook, and Methods Assembly

def _manuscript_assembly_http_error(exc: ManuscriptAssemblyError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/manuscript-assembly/health")
def manuscript_assembly_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = manuscript_assembly.health(); body["serviceVersion"] = settings.version; body["deploymentDurability"] = "persistent-disk" if settings.manuscript_assembly_persistent_disk_mounted else "instance-local"; body["durabilityWarning"] = None if settings.manuscript_assembly_persistent_disk_mounted else "Assembly sections, drafts, and renders are instance-local unless a persistent disk is mounted."; return body

@app.get("/v1/manuscript-assembly/policies")
def manuscript_assembly_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = manuscript_assembly_policies(settings.manuscript_assembly_max_assemblies, settings.manuscript_assembly_max_sections, settings.manuscript_assembly_max_sections_per_assembly); body["serviceVersion"] = settings.version; return body

@app.get("/v1/team-workspaces/{workspace_id}/assembly-sections")
def assembly_sections_list_route(workspace_id: str, kind: str = Query(""), limit: int = Query(200, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return manuscript_assembly.list_sections(workspace_id, actor, kind, limit)
    except ManuscriptAssemblyError as exc: raise _manuscript_assembly_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/assembly-sections")
def assembly_section_create_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return manuscript_assembly.create_section(workspace_id, payload, actor)
    except ManuscriptAssemblyError as exc: raise _manuscript_assembly_http_error(exc) from exc

@app.patch("/v1/team-workspaces/{workspace_id}/assembly-sections/{section_id}")
def assembly_section_update_route(workspace_id: str, section_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return manuscript_assembly.update_section(workspace_id, section_id, payload, actor)
    except ManuscriptAssemblyError as exc: raise _manuscript_assembly_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/research-assemblies")
def research_assemblies_list_route(workspace_id: str, status: str = Query(""), limit: int = Query(200, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return manuscript_assembly.list_assemblies(workspace_id, actor, status, limit)
    except ManuscriptAssemblyError as exc: raise _manuscript_assembly_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/research-assemblies")
def research_assembly_create_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return manuscript_assembly.create_assembly(workspace_id, payload, actor)
    except ManuscriptAssemblyError as exc: raise _manuscript_assembly_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/research-assemblies/{assembly_id}")
def research_assembly_get_route(workspace_id: str, assembly_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return manuscript_assembly.get_assembly(workspace_id, assembly_id, actor)
    except ManuscriptAssemblyError as exc: raise _manuscript_assembly_http_error(exc) from exc

@app.patch("/v1/team-workspaces/{workspace_id}/research-assemblies/{assembly_id}")
def research_assembly_update_route(workspace_id: str, assembly_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return manuscript_assembly.update_assembly(workspace_id, assembly_id, payload, actor)
    except ManuscriptAssemblyError as exc: raise _manuscript_assembly_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/research-assemblies/{assembly_id}/methods")
def research_assembly_methods_route(workspace_id: str, assembly_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return manuscript_assembly.generate_methods(workspace_id, assembly_id, actor)
    except ManuscriptAssemblyError as exc: raise _manuscript_assembly_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/research-assemblies/{assembly_id}/validate")
def research_assembly_validate_route(workspace_id: str, assembly_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return manuscript_assembly.validate(workspace_id, assembly_id, actor)
    except ManuscriptAssemblyError as exc: raise _manuscript_assembly_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/research-assemblies/{assembly_id}/render")
def research_assembly_render_route(workspace_id: str, assembly_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return manuscript_assembly.render(workspace_id, assembly_id, actor)
    except ManuscriptAssemblyError as exc: raise _manuscript_assembly_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/research-assemblies/{assembly_id}/seal")
def research_assembly_seal_route(workspace_id: str, assembly_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return manuscript_assembly.seal(workspace_id, assembly_id, actor)
    except ManuscriptAssemblyError as exc: raise _manuscript_assembly_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/research-assemblies/{assembly_id}/revise")
def research_assembly_revise_route(workspace_id: str, assembly_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return manuscript_assembly.revise(workspace_id, assembly_id, payload, actor)
    except ManuscriptAssemblyError as exc: raise _manuscript_assembly_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/assembly-timeline")
def research_assembly_timeline_route(workspace_id: str, limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return manuscript_assembly.timeline(workspace_id, actor, limit)
    except ManuscriptAssemblyError as exc: raise _manuscript_assembly_http_error(exc) from exc

def _edge_sync_http_error(exc: EdgeSyncError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/edge-sync/health")
def edge_sync_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    body = edge_sync.health(); body["serviceVersion"] = settings.version; body["deploymentDurability"] = "persistent-disk" if settings.edge_sync_persistent_disk_mounted else "instance-local"; body["durabilityWarning"] = None if settings.edge_sync_persistent_disk_mounted else "Offline package, device, synchronization, and conflict records are instance-local unless a persistent disk is mounted."; return body

@app.get("/v1/edge-sync/policies")
def edge_sync_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    body = edge_sync_policies(settings.edge_sync_max_devices, settings.edge_sync_max_packages, settings.edge_sync_max_changes, settings.edge_sync_max_batch); body["serviceVersion"] = settings.version; return body

@app.get("/v1/team-workspaces/{workspace_id}/edge-devices")
def edge_device_list_route(workspace_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return edge_sync.list_devices(workspace_id, actor)
    except EdgeSyncError as exc: raise _edge_sync_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/edge-devices")
def edge_device_enroll_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return edge_sync.enroll_device(workspace_id, payload, actor)
    except EdgeSyncError as exc: raise _edge_sync_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/edge-devices/{device_id}/status")
def edge_device_status_route(workspace_id: str, device_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return edge_sync.set_device_status(workspace_id, device_id, str(payload.get("status") or ""), actor, str(payload.get("reason") or ""))
    except EdgeSyncError as exc: raise _edge_sync_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/offline-packages")
def edge_package_list_route(workspace_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return edge_sync.list_packages(workspace_id, actor)
    except EdgeSyncError as exc: raise _edge_sync_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/offline-packages")
def edge_package_create_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return edge_sync.create_package(workspace_id, payload, actor)
    except EdgeSyncError as exc: raise _edge_sync_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/offline-packages/{package_id}/seal")
def edge_package_seal_route(workspace_id: str, package_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return edge_sync.seal_package(workspace_id, package_id, actor)
    except EdgeSyncError as exc: raise _edge_sync_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/offline-packages/{package_id}/assign/{device_id}")
def edge_package_assign_route(workspace_id: str, package_id: str, device_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return edge_sync.assign_package(workspace_id, package_id, device_id, actor)
    except EdgeSyncError as exc: raise _edge_sync_http_error(exc) from exc

@app.get("/v1/edge-devices/{device_id}/offline-packages/{package_id}")
def edge_package_pull_route(device_id: str, package_id: str, x_sc_lab_edge_secret: str = Header("", alias="X-SC-Lab-Edge-Secret"), auth: dict[str, str] = Depends(require_compute_auth)):
    try: return edge_sync.pull_package(device_id, package_id, x_sc_lab_edge_secret)
    except EdgeSyncError as exc: raise _edge_sync_http_error(exc) from exc

@app.post("/v1/edge-devices/{device_id}/offline-packages/{package_id}/sync-sessions")
def edge_sync_begin_route(device_id: str, package_id: str, payload: dict[str, Any], x_sc_lab_edge_secret: str = Header("", alias="X-SC-Lab-Edge-Secret"), auth: dict[str, str] = Depends(require_compute_auth)):
    try: return edge_sync.begin_sync(device_id, package_id, payload, x_sc_lab_edge_secret)
    except EdgeSyncError as exc: raise _edge_sync_http_error(exc) from exc

@app.post("/v1/edge-devices/{device_id}/sync-sessions/{session_id}/changes")
def edge_sync_push_route(device_id: str, session_id: str, payload: dict[str, Any], x_sc_lab_edge_secret: str = Header("", alias="X-SC-Lab-Edge-Secret"), x_sc_lab_sync_token: str = Header("", alias="X-SC-Lab-Sync-Token"), auth: dict[str, str] = Depends(require_compute_auth)):
    try: return edge_sync.push_changes(device_id, session_id, payload, x_sc_lab_edge_secret, x_sc_lab_sync_token)
    except EdgeSyncError as exc: raise _edge_sync_http_error(exc) from exc

@app.get("/v1/edge-devices/{device_id}/sync-sessions/{session_id}/changes")
def edge_sync_pull_route(device_id: str, session_id: str, since: int = Query(0, ge=0), limit: int = Query(500, ge=1, le=5000), x_sc_lab_edge_secret: str = Header("", alias="X-SC-Lab-Edge-Secret"), x_sc_lab_sync_token: str = Header("", alias="X-SC-Lab-Sync-Token"), auth: dict[str, str] = Depends(require_compute_auth)):
    try: return edge_sync.pull_changes(device_id, session_id, since, x_sc_lab_edge_secret, x_sc_lab_sync_token, limit)
    except EdgeSyncError as exc: raise _edge_sync_http_error(exc) from exc

@app.post("/v1/edge-devices/{device_id}/sync-sessions/{session_id}/complete")
def edge_sync_complete_route(device_id: str, session_id: str, payload: dict[str, Any], x_sc_lab_edge_secret: str = Header("", alias="X-SC-Lab-Edge-Secret"), x_sc_lab_sync_token: str = Header("", alias="X-SC-Lab-Sync-Token"), auth: dict[str, str] = Depends(require_compute_auth)):
    try: return edge_sync.complete_sync(device_id, session_id, payload, x_sc_lab_edge_secret, x_sc_lab_sync_token)
    except EdgeSyncError as exc: raise _edge_sync_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/edge-sync-conflicts")
def edge_conflict_list_route(workspace_id: str, status: str = Query("open"), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return edge_sync.list_conflicts(workspace_id, actor, status)
    except EdgeSyncError as exc: raise _edge_sync_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/edge-sync-conflicts/{conflict_id}/resolve")
def edge_conflict_resolve_route(workspace_id: str, conflict_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return edge_sync.resolve_conflict(workspace_id, conflict_id, payload, actor)
    except EdgeSyncError as exc: raise _edge_sync_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/edge-sync-sessions")
def edge_session_list_route(workspace_id: str, limit: int = Query(200, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return edge_sync.list_sessions(workspace_id, actor, limit)
    except EdgeSyncError as exc: raise _edge_sync_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/edge-sync-timeline")
def edge_timeline_route(workspace_id: str, limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor, _ = _team_workspace_actor(auth)
    try: return edge_sync.timeline(workspace_id, actor, limit)
    except EdgeSyncError as exc: raise _edge_sync_http_error(exc) from exc


# v0.37.2 Public Reproduction and Verification Portal
def _public_reproduction_http_error(exc: PublicReproductionError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/public-reproduction/health")
def public_reproduction_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    body = public_reproduction.health(); body["serviceVersion"] = settings.version; body["deploymentDurability"] = "persistent-disk" if settings.public_reproduction_persistent_disk_mounted else "instance-local"; body["durabilityWarning"] = None if settings.public_reproduction_persistent_disk_mounted else "Public records and verification receipts are instance-local unless a persistent disk is mounted."; return body

@app.get("/v1/public-reproduction/policies")
def public_reproduction_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    body = public_reproduction_policies(settings.public_reproduction_max_records, settings.public_reproduction_max_challenges, settings.public_reproduction_challenge_ttl_seconds); body["serviceVersion"] = settings.version; return body

@app.get("/v1/team-workspaces/{workspace_id}/public-reproduction-records")
def public_reproduction_records_list_route(workspace_id: str, status: str = Query(""), limit: int = Query(200, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return public_reproduction.list_records(workspace_id, actor, status, limit)
    except PublicReproductionError as exc: raise _public_reproduction_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/public-reproduction-records")
def public_reproduction_record_create_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return public_reproduction.create_record(workspace_id, payload, actor)
    except PublicReproductionError as exc: raise _public_reproduction_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/public-reproduction-records/{record_id}")
def public_reproduction_record_get_route(workspace_id: str, record_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return public_reproduction.get_record(workspace_id, record_id, actor)
    except PublicReproductionError as exc: raise _public_reproduction_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/public-reproduction-records/{record_id}/publish")
def public_reproduction_record_publish_route(workspace_id: str, record_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return public_reproduction.publish_record(workspace_id, record_id, actor)
    except PublicReproductionError as exc: raise _public_reproduction_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/public-reproduction-records/{record_id}/withdraw")
def public_reproduction_record_withdraw_route(workspace_id: str, record_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return public_reproduction.withdraw_record(workspace_id, record_id, payload, actor)
    except PublicReproductionError as exc: raise _public_reproduction_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/public-reproduction-records/{record_id}/challenges")
def public_reproduction_challenges_list_route(workspace_id: str, record_id: str, limit: int = Query(200, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return public_reproduction.list_challenges(workspace_id, record_id, actor, limit)
    except PublicReproductionError as exc: raise _public_reproduction_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/public-reproduction-timeline")
def public_reproduction_timeline_route(workspace_id: str, limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return public_reproduction.timeline(workspace_id, actor, limit)
    except PublicReproductionError as exc: raise _public_reproduction_http_error(exc) from exc

@app.get("/v1/public/reproduction/{slug}")
def public_reproduction_public_record_route(slug: str):
    try: return public_reproduction.public_record(slug)
    except PublicReproductionError as exc: raise _public_reproduction_http_error(exc) from exc

@app.get("/v1/public/reproduction/{slug}/manifest")
def public_reproduction_public_manifest_route(slug: str):
    try: return public_reproduction.public_manifest(slug)
    except PublicReproductionError as exc: raise _public_reproduction_http_error(exc) from exc

@app.post("/v1/public/reproduction/{slug}/challenges")
def public_reproduction_issue_challenge_route(slug: str, payload: dict[str, Any] | None = None):
    try: return public_reproduction.issue_challenge(slug, payload or {})
    except PublicReproductionError as exc: raise _public_reproduction_http_error(exc) from exc

@app.post("/v1/public/reproduction/challenges/{challenge_id}/submit")
def public_reproduction_submit_challenge_route(challenge_id: str, payload: dict[str, Any]):
    try: return public_reproduction.submit_challenge(challenge_id, payload)
    except PublicReproductionError as exc: raise _public_reproduction_http_error(exc) from exc

@app.get("/v1/public/reproduction/receipts/{receipt_hash}")
def public_reproduction_receipt_route(receipt_hash: str):
    try: return public_reproduction.public_receipt(receipt_hash)
    except PublicReproductionError as exc: raise _public_reproduction_http_error(exc) from exc




# v0.38.2 Public API, Webhooks, Embeds, and Research SDK
def _public_research_http_error(exc: IntegrationError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

def require_public_integration_auth(
    x_sc_lab_api_key: str = Header("", alias="X-SC-Lab-API-Key"),
    x_sc_lab_key: str = Header("", alias="X-SC-Lab-Key"),
    x_sc_lab_actor: str = Header("integration-client", alias="X-SC-Lab-Actor"),
) -> dict[str, Any]:
    key = settings.public_api_key
    if not key:
        raise HTTPException(status_code=503, detail="Protected research integrations require SC_LAB_PUBLIC_API_KEY or SC_LAB_COMPUTE_API_KEY configuration.")
    supplied_key = x_sc_lab_api_key or x_sc_lab_key
    if not hmac.compare_digest(supplied_key, key):
        raise HTTPException(status_code=401, detail="A valid Sustainable Catalyst Lab integration API key is required.")
    scopes = {item.strip() for item in settings.public_api_scopes.split(",") if item.strip()}
    return {"actor_id": x_sc_lab_actor.strip() or "integration-client", "scopes": scopes}

def _require_scope(auth: dict[str, Any], scope: str):
    if scope not in auth.get("scopes", set()): raise HTTPException(status_code=403, detail=f"The {scope} integration scope is required.")

@app.get("/v1/public-research-api")
def public_research_api_catalog_route():
    body = public_api_catalog(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/research-integrations/sdk")
def public_research_sdk_route():
    return public_research_sdk_manifest()

@app.get("/v1/research-integrations/health")
def public_research_integrations_health_route(auth: dict[str, Any] = Depends(require_public_integration_auth)):
    _require_scope(auth, "research:read"); body = public_research_integrations.health(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/research-integrations/policies")
def public_research_integrations_policies_route():
    return public_research_integration_policies(settings.webhook_delivery_enabled, settings.public_integration_persistent_disk_mounted)

@app.get("/v1/team-workspaces/{workspace_id}/webhooks")
def public_research_webhooks_list_route(workspace_id: str, status: str = Query(""), auth: dict[str, Any] = Depends(require_public_integration_auth)):
    _require_scope(auth, "webhooks:read")
    try: return public_research_integrations.list_webhooks(workspace_id, auth["actor_id"], status)
    except IntegrationError as exc: raise _public_research_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/webhooks")
def public_research_webhook_register_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, Any] = Depends(require_public_integration_auth)):
    _require_scope(auth, "webhooks:write")
    try: return public_research_integrations.register_webhook(workspace_id, payload, auth["actor_id"])
    except IntegrationError as exc: raise _public_research_http_error(exc) from exc

@app.patch("/v1/team-workspaces/{workspace_id}/webhooks/{subscription_id}")
def public_research_webhook_update_route(workspace_id: str, subscription_id: str, payload: dict[str, Any], auth: dict[str, Any] = Depends(require_public_integration_auth)):
    _require_scope(auth, "webhooks:write")
    try: return public_research_integrations.update_webhook(workspace_id, subscription_id, payload, auth["actor_id"])
    except IntegrationError as exc: raise _public_research_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/webhook-events")
def public_research_webhook_emit_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, Any] = Depends(require_public_integration_auth)):
    _require_scope(auth, "webhooks:emit")
    try: return public_research_integrations.emit_event(workspace_id, payload, auth["actor_id"])
    except IntegrationError as exc: raise _public_research_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/webhook-deliveries")
def public_research_webhook_deliveries_route(workspace_id: str, status: str = Query(""), limit: int = Query(200, ge=1, le=2000), auth: dict[str, Any] = Depends(require_public_integration_auth)):
    _require_scope(auth, "webhooks:read")
    try: return public_research_integrations.list_deliveries(workspace_id, auth["actor_id"], status, limit)
    except IntegrationError as exc: raise _public_research_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/webhook-deliveries/{delivery_id}/dispatch")
def public_research_webhook_dispatch_route(workspace_id: str, delivery_id: str, auth: dict[str, Any] = Depends(require_public_integration_auth)):
    _require_scope(auth, "webhooks:emit")
    try: return public_research_integrations.dispatch_delivery(workspace_id, delivery_id, auth["actor_id"])
    except IntegrationError as exc: raise _public_research_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/research-embeds")
def public_research_embed_create_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, Any] = Depends(require_public_integration_auth)):
    _require_scope(auth, "embeds:write")
    try: return public_research_integrations.create_embed(workspace_id, payload, auth["actor_id"])
    except IntegrationError as exc: raise _public_research_http_error(exc) from exc

@app.get("/v1/public/research-embeds/{token}")
def public_research_embed_verify_route(token: str):
    try: return public_research_integrations.verify_embed(token)
    except IntegrationError as exc: raise _public_research_http_error(exc) from exc

# v0.104.0 Platform Core v3 Runtime Adapter & Capability Registration
def _platform_core_v3_adapter_http_error(exc: PlatformCoreV3AdapterError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/platform-core-v3-adapter/health")
def platform_core_v3_adapter_health_route():
    body = core_v3_adapter_health(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/platform-core-v3-adapter/manifest")
def platform_core_v3_adapter_manifest_route():
    body = core_v3_adapter_manifest(); body["serviceVersion"] = settings.version; return body

@app.post("/v1/platform-core-v3-adapter/registrations/runtime-contract")
def platform_core_v3_contract_registration_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_core_v3_contract_registration(payload)
    except PlatformCoreV3AdapterError as exc: raise _platform_core_v3_adapter_http_error(exc) from exc

@app.post("/v1/platform-core-v3-adapter/registrations/session")
def platform_core_v3_session_registration_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_core_v3_session_product_binding(payload)
    except PlatformCoreV3AdapterError as exc: raise _platform_core_v3_adapter_http_error(exc) from exc

@app.post("/v1/platform-core-v3-adapter/runtime-context/normalize")
def platform_core_v3_runtime_context_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return normalize_core_v3_runtime_context(payload)
    except PlatformCoreV3AdapterError as exc: raise _platform_core_v3_adapter_http_error(exc) from exc

@app.post("/v1/platform-core-v3-adapter/handoffs/validate")
def platform_core_v3_handoff_validation_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return validate_core_v3_handoff(payload)
    except PlatformCoreV3AdapterError as exc: raise _platform_core_v3_adapter_http_error(exc) from exc

@app.post("/v1/platform-core-v3-adapter/compatibility/check")
def platform_core_v3_compatibility_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return core_v3_compatibility_report(payload)
    except PlatformCoreV3AdapterError as exc: raise _platform_core_v3_adapter_http_error(exc) from exc

# v0.105.0 Canonical Research Object Mapping
def _platform_core_v3_object_mapping_http_error(exc: PlatformCoreV3ObjectMappingError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/platform-core-v3-objects/health")
def platform_core_v3_object_mapping_health_route():
    body = core_v3_object_mapping_health(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/platform-core-v3-objects/catalog")
def platform_core_v3_object_mapping_catalog_route(auth: dict[str, str] = Depends(require_compute_auth)):
    body = core_v3_object_mapping_catalog(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/platform-core-v3-objects/schema")
def platform_core_v3_object_mapping_schema_route():
    return {
        "ok": True,
        "version": "0.105.0",
        "mappingSchema": "sc-lab-platform-core-v3-object-mapping/0.105.0",
        "objectSchema": "sc-lab-canonical-research-object/0.105.0",
        "bindingSchema": "sc-lab-platform-core-v3-object-binding/0.105.0",
        "batchSchema": "sc-lab-platform-core-v3-object-binding-batch/0.105.0",
        "legacyBridgeSchema": "sc-lab-platform-core-v3-legacy-object-bridge/0.105.0",
        "coreObjectBindingPath": "/v1/research/unified-runtime/object-bindings",
    }

@app.post("/v1/platform-core-v3-objects/normalize")
def platform_core_v3_object_normalize_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return normalize_core_v3_object(payload)
    except PlatformCoreV3ObjectMappingError as exc: raise _platform_core_v3_object_mapping_http_error(exc) from exc

@app.post("/v1/platform-core-v3-objects/bind")
def platform_core_v3_object_bind_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_core_v3_object_binding(payload)
    except PlatformCoreV3ObjectMappingError as exc: raise _platform_core_v3_object_mapping_http_error(exc) from exc

@app.post("/v1/platform-core-v3-objects/batch")
def platform_core_v3_object_batch_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_core_v3_object_binding_batch(payload)
    except PlatformCoreV3ObjectMappingError as exc: raise _platform_core_v3_object_mapping_http_error(exc) from exc

@app.post("/v1/platform-core-v3-objects/legacy-handoff/map")
def platform_core_v3_legacy_handoff_object_map_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return map_core_v3_legacy_typed_handoff(payload)
    except PlatformCoreV3ObjectMappingError as exc: raise _platform_core_v3_object_mapping_http_error(exc) from exc

# v0.106.0 Unified Project & Research Session Context
def _platform_core_v3_research_context_http_error(exc: PlatformCoreV3ResearchContextError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/platform-core-v3-context/health")
def platform_core_v3_research_context_health_route():
    body = core_v3_research_context_health(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/platform-core-v3-context/manifest")
def platform_core_v3_research_context_manifest_route(auth: dict[str, str] = Depends(require_compute_auth)):
    body = core_v3_research_context_manifest(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/platform-core-v3-context/schema")
def platform_core_v3_research_context_schema_route():
    return {
        "ok": True,
        "version": "0.106.0",
        "contextSchema": "sc-lab-platform-core-v3-research-context/0.106.0",
        "minimumCoreRelease": "3.0.0",
        "sessionPath": "/v1/research/unified-runtime/sessions",
        "productBindingPath": "/v1/research/unified-runtime/product-bindings",
        "objectBindingPath": "/v1/research/unified-runtime/object-bindings",
        "handoffBindingPath": "/v1/research/unified-runtime/handoff-bindings",
    }

@app.post("/v1/platform-core-v3-context/normalize")
def platform_core_v3_research_context_normalize_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return normalize_core_v3_research_context(payload)
    except PlatformCoreV3ResearchContextError as exc: raise _platform_core_v3_research_context_http_error(exc) from exc

@app.post("/v1/platform-core-v3-context/sessions/build")
def platform_core_v3_research_session_build_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_core_v3_session_registration(payload)
    except PlatformCoreV3ResearchContextError as exc: raise _platform_core_v3_research_context_http_error(exc) from exc

@app.post("/v1/platform-core-v3-context/products/bind")
def platform_core_v3_product_context_bind_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_core_v3_product_context_binding(payload)
    except PlatformCoreV3ResearchContextError as exc: raise _platform_core_v3_research_context_http_error(exc) from exc

@app.post("/v1/platform-core-v3-context/objects/bind")
def platform_core_v3_contextual_object_bind_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_core_v3_contextual_object_binding(payload)
    except (PlatformCoreV3ResearchContextError, PlatformCoreV3ObjectMappingError) as exc:
        detail = getattr(exc, "detail", str(exc)); status = getattr(exc, "status_code", 400); raise HTTPException(status_code=status, detail=detail) from exc

@app.post("/v1/platform-core-v3-context/handoffs/bind")
def platform_core_v3_contextual_handoff_bind_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_core_v3_contextual_handoff_binding(payload)
    except PlatformCoreV3ResearchContextError as exc: raise _platform_core_v3_research_context_http_error(exc) from exc

@app.post("/v1/platform-core-v3-context/continuity/check")
def platform_core_v3_context_continuity_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return check_core_v3_context_continuity(payload)
    except PlatformCoreV3ResearchContextError as exc: raise _platform_core_v3_research_context_http_error(exc) from exc

# v0.107.0 Scientific Execution Lineage Bridge
def _platform_core_v3_execution_lineage_http_error(exc: PlatformCoreV3ExecutionLineageError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/platform-core-v3-executions/health")
def platform_core_v3_execution_lineage_health_route():
    body = core_v3_execution_lineage_health(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/platform-core-v3-executions/manifest")
def platform_core_v3_execution_lineage_manifest_route(auth: dict[str, str] = Depends(require_compute_auth)):
    body = core_v3_execution_lineage_manifest(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/platform-core-v3-executions/schema")
def platform_core_v3_execution_lineage_schema_route():
    return {
        "ok": True,
        "version": "0.107.0",
        "executionSchema": "sc-lab-scientific-execution-lineage/0.107.0",
        "minimumCoreRelease": "3.0.0",
        "coreExecutionBindingPath": "/v1/research/unified-runtime/execution-bindings",
        "referenceFirst": True,
        "automaticCoreSubmission": False,
        "automaticScientificExecution": False,
    }

@app.post("/v1/platform-core-v3-executions/normalize")
def platform_core_v3_execution_normalize_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return normalize_core_v3_execution(payload)
    except PlatformCoreV3ExecutionLineageError as exc: raise _platform_core_v3_execution_lineage_http_error(exc) from exc

@app.post("/v1/platform-core-v3-executions/bind")
def platform_core_v3_execution_bind_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_core_v3_execution_binding(payload)
    except PlatformCoreV3ExecutionLineageError as exc: raise _platform_core_v3_execution_lineage_http_error(exc) from exc

@app.post("/v1/platform-core-v3-executions/batch")
def platform_core_v3_execution_batch_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_core_v3_execution_binding_batch(payload)
    except PlatformCoreV3ExecutionLineageError as exc: raise _platform_core_v3_execution_lineage_http_error(exc) from exc

@app.post("/v1/platform-core-v3-executions/lineage/check")
def platform_core_v3_execution_lineage_check_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return check_core_v3_execution_lineage(payload)
    except PlatformCoreV3ExecutionLineageError as exc: raise _platform_core_v3_execution_lineage_http_error(exc) from exc

@app.post("/v1/platform-core-v3-executions/legacy-run/map")
def platform_core_v3_legacy_execution_map_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return map_core_v3_legacy_execution(payload)
    except PlatformCoreV3ExecutionLineageError as exc: raise _platform_core_v3_execution_lineage_http_error(exc) from exc

# v0.108.0 Findings, Claims, Evidence & Validation Bridge
def _platform_core_v3_findings_validation_http_error(exc: PlatformCoreV3FindingsValidationError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/platform-core-v3-research-intelligence/health")
def platform_core_v3_findings_validation_health_route():
    body = core_v3_findings_validation_health(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/platform-core-v3-research-intelligence/manifest")
def platform_core_v3_findings_validation_manifest_route(auth: dict[str, str] = Depends(require_compute_auth)):
    body = core_v3_findings_validation_manifest(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/platform-core-v3-research-intelligence/schema")
def platform_core_v3_findings_validation_schema_route():
    return {
        "ok": True,
        "version": "0.108.0",
        "bridgeSchema": "sc-lab-platform-core-v3-findings-claims-evidence-validation/0.108.0",
        "minimumCoreRelease": "3.0.0",
        "findingClaimEvidenceContract": "sc.research.finding-claim-evidence.v1",
        "validationChallengeContract": "sc.research.validation-challenge.v1",
        "automaticCoreSubmission": False,
        "automaticScientificCertification": False,
    }

@app.post("/v1/platform-core-v3-research-intelligence/findings/normalize")
def platform_core_v3_finding_normalize_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return normalize_core_v3_finding(payload)
    except PlatformCoreV3FindingsValidationError as exc: raise _platform_core_v3_findings_validation_http_error(exc) from exc

@app.post("/v1/platform-core-v3-research-intelligence/findings/bind")
def platform_core_v3_finding_bind_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_core_v3_finding_binding(payload)
    except PlatformCoreV3FindingsValidationError as exc: raise _platform_core_v3_findings_validation_http_error(exc) from exc

@app.post("/v1/platform-core-v3-research-intelligence/claims/normalize")
def platform_core_v3_claim_normalize_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return normalize_core_v3_claim(payload)
    except PlatformCoreV3FindingsValidationError as exc: raise _platform_core_v3_findings_validation_http_error(exc) from exc

@app.post("/v1/platform-core-v3-research-intelligence/claims/bind")
def platform_core_v3_claim_bind_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_core_v3_claim_binding(payload)
    except PlatformCoreV3FindingsValidationError as exc: raise _platform_core_v3_findings_validation_http_error(exc) from exc

@app.post("/v1/platform-core-v3-research-intelligence/evidence-links/bind")
def platform_core_v3_evidence_link_bind_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_core_v3_evidence_link_binding(payload)
    except PlatformCoreV3FindingsValidationError as exc: raise _platform_core_v3_findings_validation_http_error(exc) from exc

@app.post("/v1/platform-core-v3-research-intelligence/contradictions/bind")
def platform_core_v3_contradiction_bind_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_core_v3_contradiction_binding(payload)
    except PlatformCoreV3FindingsValidationError as exc: raise _platform_core_v3_findings_validation_http_error(exc) from exc

@app.post("/v1/platform-core-v3-research-intelligence/validation/bind")
def platform_core_v3_validation_bind_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_core_v3_validation_challenge_binding(payload)
    except PlatformCoreV3FindingsValidationError as exc: raise _platform_core_v3_findings_validation_http_error(exc) from exc

@app.post("/v1/platform-core-v3-research-intelligence/replications/bind")
def platform_core_v3_replication_bind_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_core_v3_replication_binding(payload)
    except PlatformCoreV3FindingsValidationError as exc: raise _platform_core_v3_findings_validation_http_error(exc) from exc

@app.post("/v1/platform-core-v3-research-intelligence/legacy-claim/map")
def platform_core_v3_legacy_claim_map_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return map_core_v3_legacy_scientific_claim(payload)
    except PlatformCoreV3FindingsValidationError as exc: raise _platform_core_v3_findings_validation_http_error(exc) from exc


# v0.109.0 Visual Reasoning & Scientific Scene Bridge
def _platform_core_v3_visual_scene_http_error(exc: PlatformCoreV3VisualSceneError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/platform-core-v3-visual-scene/health")
def platform_core_v3_visual_scene_health_route():
    body = core_v3_visual_scene_health(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/platform-core-v3-visual-scene/manifest")
def platform_core_v3_visual_scene_manifest_route(auth: dict[str, str] = Depends(require_compute_auth)):
    body = core_v3_visual_scene_manifest(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/platform-core-v3-visual-scene/schema")
def platform_core_v3_visual_scene_schema_route():
    return {
        "ok": True,
        "version": "0.109.0",
        "bridgeSchema": "sc-lab-platform-core-v3-visual-reasoning-scientific-scene/0.109.0",
        "minimumCoreRelease": "3.0.0",
        "coreVisualSceneContract": "sc.visual-runtime.scene.v1",
        "coreUnifiedVisualReasoningContract": "sc.visual-runtime.unified-reasoning.v1",
        "coreVisualBindingPath": "/v1/research/unified-runtime/visual-bindings",
        "automaticCoreSubmission": False,
        "automaticRenderingByCore": False,
    }

@app.post("/v1/platform-core-v3-visual-scene/visuals/normalize")
def platform_core_v3_visual_normalize_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return normalize_core_v3_visual(payload)
    except PlatformCoreV3VisualSceneError as exc: raise _platform_core_v3_visual_scene_http_error(exc) from exc

@app.post("/v1/platform-core-v3-visual-scene/visuals/bind")
def platform_core_v3_visual_bind_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_core_v3_visual_binding(payload)
    except PlatformCoreV3VisualSceneError as exc: raise _platform_core_v3_visual_scene_http_error(exc) from exc

@app.post("/v1/platform-core-v3-visual-scene/scenes/normalize")
def platform_core_v3_scene_normalize_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return normalize_core_v3_scene(payload)
    except PlatformCoreV3VisualSceneError as exc: raise _platform_core_v3_visual_scene_http_error(exc) from exc

@app.post("/v1/platform-core-v3-visual-scene/scenes/bridge")
def platform_core_v3_scene_bridge_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return build_core_v3_scene_contract(payload)
    except PlatformCoreV3VisualSceneError as exc: raise _platform_core_v3_visual_scene_http_error(exc) from exc

@app.post("/v1/platform-core-v3-visual-scene/linked-views/bridge")
def platform_core_v3_linked_views_bridge_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return bridge_core_v3_linked_views(payload)
    except PlatformCoreV3VisualSceneError as exc: raise _platform_core_v3_visual_scene_http_error(exc) from exc

@app.post("/v1/platform-core-v3-visual-scene/uncertainty/bridge")
def platform_core_v3_uncertainty_visual_bridge_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return bridge_core_v3_uncertainty_visual(payload)
    except PlatformCoreV3VisualSceneError as exc: raise _platform_core_v3_visual_scene_http_error(exc) from exc

@app.get("/v1/platform-core-v3-visual-scene/renderers/catalog")
def platform_core_v3_renderer_catalog_route(auth: dict[str, str] = Depends(require_compute_auth)):
    try: return core_v3_renderer_catalog()
    except PlatformCoreV3VisualSceneError as exc: raise _platform_core_v3_visual_scene_http_error(exc) from exc

@app.post("/v1/platform-core-v3-visual-scene/renderers/negotiate")
def platform_core_v3_renderer_negotiate_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return negotiate_core_v3_renderer(payload)
    except PlatformCoreV3VisualSceneError as exc: raise _platform_core_v3_visual_scene_http_error(exc) from exc

@app.post("/v1/platform-core-v3-visual-scene/legacy-scene/map")
def platform_core_v3_legacy_scene_map_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return map_core_v3_legacy_scene(payload)
    except PlatformCoreV3VisualSceneError as exc: raise _platform_core_v3_visual_scene_http_error(exc) from exc

# v0.38.1 Typed Cross-Product Research Handoffs
@app.get("/v1/typed-cross-product-handoffs/health")
def typed_cross_product_handoffs_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    body = typed_cross_product_handoffs.health(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/typed-cross-product-handoffs/policies")
def typed_cross_product_handoffs_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    body = typed_cross_product_handoff_policies(); body["serviceVersion"] = settings.version; return body

@app.get("/v1/typed-cross-product-handoffs/adapters")
def typed_cross_product_handoffs_adapters_route(product_id: str = Query("", alias="productId"), auth: dict[str, str] = Depends(require_compute_auth)):
    try: return typed_cross_product_handoffs.catalog(product_id)
    except InteroperabilityError as exc: raise _research_interoperability_http_error(exc) from exc

@app.get("/v1/typed-cross-product-handoffs/routes")
def typed_cross_product_handoffs_routes_route(source_product: str = Query("", alias="sourceProduct"), target_product: str = Query("", alias="targetProduct"), entity_type: str = Query("", alias="entityType"), auth: dict[str, str] = Depends(require_compute_auth)):
    try: return typed_cross_product_handoffs.route_catalog(source_product, target_product, entity_type)
    except InteroperabilityError as exc: raise _research_interoperability_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/typed-research-handoffs/plan")
def typed_cross_product_handoff_plan_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return typed_cross_product_handoffs.plan(workspace_id, payload, actor)
    except InteroperabilityError as exc: raise _research_interoperability_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/typed-research-handoffs")
def typed_cross_product_handoff_create_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return typed_cross_product_handoffs.create(workspace_id, payload, actor)
    except InteroperabilityError as exc: raise _research_interoperability_http_error(exc) from exc

# v0.38.0 Sustainable Catalyst Research Interoperability Layer
def _research_interoperability_http_error(exc: InteroperabilityError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/research-interoperability/health")
def research_interoperability_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    body = research_interoperability.health(); body["serviceVersion"] = settings.version; body["deploymentDurability"] = "persistent-disk" if settings.interoperability_persistent_disk_mounted else "instance-local"; body["durabilityWarning"] = None if settings.interoperability_persistent_disk_mounted else "Interoperability profiles, handoffs, and receipts are instance-local unless a persistent disk is mounted."; return body

@app.get("/v1/research-interoperability/policies")
def research_interoperability_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    body = research_interoperability_policies(settings.interoperability_max_profiles, settings.interoperability_max_handoffs); body["serviceVersion"] = settings.version; return body

@app.get("/v1/team-workspaces/{workspace_id}/interoperability-profiles")
def research_interoperability_profiles_list_route(workspace_id: str, status: str = Query(""), limit: int = Query(200, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return research_interoperability.list_profiles(workspace_id, actor, status, limit)
    except InteroperabilityError as exc: raise _research_interoperability_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/interoperability-profiles")
def research_interoperability_profile_register_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return research_interoperability.register_profile(workspace_id, payload, actor)
    except InteroperabilityError as exc: raise _research_interoperability_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/interoperability-negotiate")
def research_interoperability_negotiate_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return research_interoperability.negotiate(workspace_id, payload, actor)
    except InteroperabilityError as exc: raise _research_interoperability_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/research-handoffs")
def research_interoperability_handoffs_list_route(workspace_id: str, status: str = Query(""), direction: str = Query(""), limit: int = Query(200, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return research_interoperability.list_handoffs(workspace_id, actor, status, direction, limit)
    except InteroperabilityError as exc: raise _research_interoperability_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/research-handoffs")
def research_interoperability_handoff_create_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return research_interoperability.create_handoff(workspace_id, payload, actor)
    except InteroperabilityError as exc: raise _research_interoperability_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/research-handoffs/{handoff_id}")
def research_interoperability_handoff_get_route(workspace_id: str, handoff_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return research_interoperability.get_handoff(workspace_id, handoff_id, actor)
    except InteroperabilityError as exc: raise _research_interoperability_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/research-handoffs/{handoff_id}/seal")
def research_interoperability_handoff_seal_route(workspace_id: str, handoff_id: str, payload: dict[str, Any] | None = None, auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return research_interoperability.seal_handoff(workspace_id, handoff_id, payload or {}, actor)
    except InteroperabilityError as exc: raise _research_interoperability_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/research-handoffs/{handoff_id}/bundle")
def research_interoperability_handoff_bundle_route(workspace_id: str, handoff_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return research_interoperability.export_bundle(workspace_id, handoff_id, actor)
    except InteroperabilityError as exc: raise _research_interoperability_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/research-handoffs/import")
def research_interoperability_import_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return research_interoperability.import_envelope(workspace_id, payload, actor)
    except InteroperabilityError as exc: raise _research_interoperability_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/research-handoffs/{handoff_id}/receipts")
def research_interoperability_receipt_route(workspace_id: str, handoff_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return research_interoperability.record_receipt(workspace_id, handoff_id, payload, actor)
    except InteroperabilityError as exc: raise _research_interoperability_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/research-handoff-receipts/{receipt_hash}")
def research_interoperability_receipt_verify_route(workspace_id: str, receipt_hash: str, auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return research_interoperability.verify_receipt(workspace_id, receipt_hash, actor)
    except InteroperabilityError as exc: raise _research_interoperability_http_error(exc) from exc

@app.post("/v1/team-workspaces/{workspace_id}/research-handoffs/{handoff_id}/withdraw")
def research_interoperability_withdraw_route(workspace_id: str, handoff_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return research_interoperability.withdraw(workspace_id, handoff_id, payload, actor)
    except InteroperabilityError as exc: raise _research_interoperability_http_error(exc) from exc

@app.get("/v1/team-workspaces/{workspace_id}/interoperability-timeline")
def research_interoperability_timeline_route(workspace_id: str, limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    actor = auth.get("actor_id") or auth.get("actor") or "system"
    try: return research_interoperability.timeline(workspace_id, actor, limit)
    except InteroperabilityError as exc: raise _research_interoperability_http_error(exc) from exc


# v0.39.0 Institutional Administration, Identity, and Governance

def _institutional_governance_http_error(exc: InstitutionalGovernanceError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)


def _institutional_actor(auth: dict[str, str]) -> str:
    return auth.get("actor_id") or auth.get("actor") or "system"


@app.get("/v1/institutional-governance/health")
def institutional_governance_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = institutional_governance.health()
    body["serviceVersion"] = settings.version
    body["deploymentDurability"] = "persistent-disk" if settings.institutional_governance_persistent_disk_mounted else "instance-local"
    body["durabilityWarning"] = None if settings.institutional_governance_persistent_disk_mounted else "Institutional governance records are instance-local unless a persistent disk is mounted."
    return body


@app.get("/v1/institutional-governance/policies")
def institutional_governance_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = institutional_governance_policies(settings.institutional_governance_persistent_disk_mounted)
    body["serviceVersion"] = settings.version
    return body


@app.get("/v1/institutions")
def institutional_governance_institutions_list_route(auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.list_institutions(_institutional_actor(auth))
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


@app.post("/v1/institutions")
def institutional_governance_institution_create_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.create_institution(payload, _institutional_actor(auth))
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


@app.get("/v1/institutions/{institution_id}")
def institutional_governance_institution_get_route(institution_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.get_institution(institution_id, _institutional_actor(auth))
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


@app.get("/v1/institutions/{institution_id}/units")
def institutional_governance_units_list_route(institution_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.list_units(institution_id, _institutional_actor(auth))
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


@app.post("/v1/institutions/{institution_id}/units")
def institutional_governance_unit_create_route(institution_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.create_unit(institution_id, payload, _institutional_actor(auth))
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


@app.get("/v1/institutions/{institution_id}/principals")
def institutional_governance_principals_list_route(institution_id: str, status: str = Query(""), auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.list_principals(institution_id, _institutional_actor(auth), status)
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


@app.post("/v1/institutions/{institution_id}/principals")
def institutional_governance_principal_create_route(institution_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.register_principal(institution_id, payload, _institutional_actor(auth))
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


@app.get("/v1/institutions/{institution_id}/role-bindings")
def institutional_governance_bindings_list_route(institution_id: str, principal_id: str = Query("", alias="principalId"), workspace_id: str = Query("", alias="workspaceId"), auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.list_bindings(institution_id, _institutional_actor(auth), principal_id, workspace_id)
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


@app.post("/v1/institutions/{institution_id}/role-bindings")
def institutional_governance_binding_create_route(institution_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.grant_role(institution_id, payload, _institutional_actor(auth))
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


@app.delete("/v1/institutions/{institution_id}/role-bindings/{binding_id}")
def institutional_governance_binding_revoke_route(institution_id: str, binding_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.revoke_role(institution_id, binding_id, _institutional_actor(auth))
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


@app.get("/v1/institutions/{institution_id}/retention-policies")
def institutional_governance_retention_list_route(institution_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.list_retention_policies(institution_id, _institutional_actor(auth))
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


@app.post("/v1/institutions/{institution_id}/retention-policies")
def institutional_governance_retention_create_route(institution_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.create_retention_policy(institution_id, payload, _institutional_actor(auth))
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


@app.get("/v1/team-workspaces/{workspace_id}/institutional-governance")
def institutional_governance_workspace_get_route(workspace_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.get_workspace_governance(workspace_id, _institutional_actor(auth))
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


@app.post("/v1/team-workspaces/{workspace_id}/institutional-governance")
def institutional_governance_workspace_configure_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.govern_workspace(workspace_id, payload, _institutional_actor(auth))
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


@app.post("/v1/team-workspaces/{workspace_id}/institutional-governance/evaluate")
def institutional_governance_evaluate_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.evaluate(workspace_id, payload, _institutional_actor(auth))
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


@app.get("/v1/team-workspaces/{workspace_id}/governance-approvals")
def institutional_governance_approvals_list_route(workspace_id: str, status: str = Query(""), auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.list_approvals(workspace_id, _institutional_actor(auth), status)
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


@app.post("/v1/team-workspaces/{workspace_id}/governance-approvals")
def institutional_governance_approval_create_route(workspace_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.create_approval(workspace_id, payload, _institutional_actor(auth))
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


@app.post("/v1/team-workspaces/{workspace_id}/governance-approvals/{request_id}/decisions")
def institutional_governance_approval_decide_route(workspace_id: str, request_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.decide_approval(workspace_id, request_id, payload, _institutional_actor(auth))
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


@app.get("/v1/institutions/{institution_id}/governance-dashboard")
def institutional_governance_dashboard_route(institution_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.dashboard(institution_id, _institutional_actor(auth))
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


@app.get("/v1/institutions/{institution_id}/governance-timeline")
def institutional_governance_timeline_route(institution_id: str, limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return institutional_governance.timeline(institution_id, _institutional_actor(auth), limit)
    except InstitutionalGovernanceError as exc:
        raise _institutional_governance_http_error(exc) from exc


# v0.39.1 Security, Privacy, Secrets, and Audit Hardening

def _security_privacy_http_error(exc: SecurityHardeningError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)


@app.get("/v1/security-privacy/health")
def security_privacy_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    body = security_privacy.health()
    body["serviceVersion"] = settings.version
    return body


@app.get("/v1/security-privacy/policies")
def security_privacy_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return security_privacy_policies(settings.security_privacy_persistent_disk_mounted, security_privacy.vault_locked)


@app.post("/v1/security-privacy/privacy-scan")
def security_privacy_scan_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return privacy_scan(payload)


@app.post("/v1/security-privacy/privacy-redact")
def security_privacy_redact_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return {"ok": True, "version": "0.39.1", "redacted": privacy_redact(payload), "scan": privacy_scan(payload)}


@app.get("/v1/institutions/{institution_id}/secrets")
def security_privacy_secrets_list_route(institution_id: str, name: str = Query(""), auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return security_privacy.list_secrets(institution_id, _institutional_actor(auth), name)
    except SecurityHardeningError as exc:
        raise _security_privacy_http_error(exc) from exc


@app.post("/v1/institutions/{institution_id}/secrets")
def security_privacy_secret_put_route(institution_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return security_privacy.put_secret(institution_id, payload, _institutional_actor(auth))
    except SecurityHardeningError as exc:
        raise _security_privacy_http_error(exc) from exc


@app.post("/v1/institutions/{institution_id}/secrets/{name}/verify")
def security_privacy_secret_verify_route(institution_id: str, name: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return security_privacy.verify_secret(institution_id, name, str(payload.get("candidate") or ""), _institutional_actor(auth))
    except SecurityHardeningError as exc:
        raise _security_privacy_http_error(exc) from exc


@app.get("/v1/institutions/{institution_id}/service-credentials")
def security_privacy_credentials_list_route(institution_id: str, principal_id: str = Query("", alias="principalId"), auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return security_privacy.list_credentials(institution_id, _institutional_actor(auth), principal_id)
    except SecurityHardeningError as exc:
        raise _security_privacy_http_error(exc) from exc


@app.post("/v1/institutions/{institution_id}/principals/{principal_id}/service-credentials")
def security_privacy_credential_issue_route(institution_id: str, principal_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return security_privacy.issue_credential(institution_id, principal_id, payload, _institutional_actor(auth))
    except SecurityHardeningError as exc:
        raise _security_privacy_http_error(exc) from exc


@app.delete("/v1/institutions/{institution_id}/service-credentials/{credential_id}")
def security_privacy_credential_revoke_route(institution_id: str, credential_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return security_privacy.revoke_credential(institution_id, credential_id, _institutional_actor(auth))
    except SecurityHardeningError as exc:
        raise _security_privacy_http_error(exc) from exc


@app.post("/v1/service-credentials/verify")
def security_privacy_credential_verify_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return security_privacy.verify_credential(str(payload.get("token") or ""), str(payload.get("requiredScope") or ""))


@app.get("/v1/institutions/{institution_id}/privacy-requests")
def security_privacy_requests_list_route(institution_id: str, status: str = Query(""), auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return security_privacy.list_privacy_requests(institution_id, _institutional_actor(auth), status)
    except SecurityHardeningError as exc:
        raise _security_privacy_http_error(exc) from exc


@app.post("/v1/institutions/{institution_id}/privacy-requests")
def security_privacy_request_create_route(institution_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return security_privacy.create_privacy_request(institution_id, payload, _institutional_actor(auth))
    except SecurityHardeningError as exc:
        raise _security_privacy_http_error(exc) from exc


@app.post("/v1/institutions/{institution_id}/privacy-requests/{request_id}/resolve")
def security_privacy_request_resolve_route(institution_id: str, request_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return security_privacy.resolve_privacy_request(institution_id, request_id, payload, _institutional_actor(auth))
    except SecurityHardeningError as exc:
        raise _security_privacy_http_error(exc) from exc


@app.get("/v1/institutions/{institution_id}/security-audit")
def security_privacy_audit_route(institution_id: str, limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return security_privacy.audit_timeline(institution_id, _institutional_actor(auth), limit)
    except SecurityHardeningError as exc:
        raise _security_privacy_http_error(exc) from exc


@app.get("/v1/institutions/{institution_id}/security-audit/verify")
def security_privacy_audit_verify_route(institution_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return security_privacy.verify_audit_chain(institution_id, _institutional_actor(auth))
    except SecurityHardeningError as exc:
        raise _security_privacy_http_error(exc) from exc

def _multi_instance_http_error(exc: OperationsError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)


@app.get("/v1/multi-instance-operations/health")
def multi_instance_operations_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return multi_instance_operations.health()


@app.get("/v1/multi-instance-operations/policies")
def multi_instance_operations_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return multi_instance_operations_policies(settings.multi_instance_persistent_disk_mounted)


@app.get("/v1/multi-instance-operations/instance")
def multi_instance_operations_instance_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return {"ok": True, "version": "0.39.2", "instance": multi_instance_operations.instance_manifest()}


@app.get("/v1/multi-instance-operations/instances")
def multi_instance_operations_instances_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return multi_instance_operations.list_instances()


@app.post("/v1/multi-instance-operations/instances")
def multi_instance_operations_register_instance_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return multi_instance_operations.register_peer(payload, _institutional_actor(auth))
    except OperationsError as exc:
        raise _multi_instance_http_error(exc) from exc


@app.get("/v1/multi-instance-operations/backups")
def multi_instance_operations_backups_route(limit: int = Query(200, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return multi_instance_operations.list_backups(limit)


@app.post("/v1/multi-instance-operations/backups")
def multi_instance_operations_create_backup_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return multi_instance_operations.create_backup(payload, _institutional_actor(auth))
    except OperationsError as exc:
        raise _multi_instance_http_error(exc) from exc


@app.post("/v1/multi-instance-operations/backups/{backup_id}/verify")
def multi_instance_operations_verify_backup_route(backup_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return multi_instance_operations.verify_backup(backup_id, _institutional_actor(auth))
    except OperationsError as exc:
        raise _multi_instance_http_error(exc) from exc


@app.post("/v1/multi-instance-operations/backups/{backup_id}/restore")
def multi_instance_operations_restore_route(backup_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return multi_instance_operations.stage_restore(backup_id, payload, _institutional_actor(auth))
    except OperationsError as exc:
        raise _multi_instance_http_error(exc) from exc


@app.post("/v1/multi-instance-operations/migrations")
def multi_instance_operations_migration_plan_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return multi_instance_operations.create_migration_plan(payload, _institutional_actor(auth))
    except OperationsError as exc:
        raise _multi_instance_http_error(exc) from exc


@app.post("/v1/multi-instance-operations/migrations/{migration_id}/execute")
def multi_instance_operations_migration_execute_route(migration_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return multi_instance_operations.execute_migration(migration_id, payload, _institutional_actor(auth))
    except OperationsError as exc:
        raise _multi_instance_http_error(exc) from exc


@app.post("/v1/multi-instance-operations/transfers")
def multi_instance_operations_transfer_create_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return multi_instance_operations.create_transfer(payload, _institutional_actor(auth))
    except OperationsError as exc:
        raise _multi_instance_http_error(exc) from exc


@app.post("/v1/multi-instance-operations/transfers/{transfer_id}/verify")
def multi_instance_operations_transfer_verify_route(transfer_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return multi_instance_operations.verify_transfer(transfer_id, _institutional_actor(auth))
    except OperationsError as exc:
        raise _multi_instance_http_error(exc) from exc


@app.post("/v1/multi-instance-operations/transfers/import")
def multi_instance_operations_transfer_import_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return multi_instance_operations.import_transfer(payload, _institutional_actor(auth))
    except OperationsError as exc:
        raise _multi_instance_http_error(exc) from exc


@app.post("/v1/multi-instance-operations/recovery-drills")
def multi_instance_operations_recovery_drill_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return multi_instance_operations.run_recovery_drill(payload, _institutional_actor(auth))
    except OperationsError as exc:
        raise _multi_instance_http_error(exc) from exc


@app.get("/v1/multi-instance-operations/dashboard")
def multi_instance_operations_dashboard_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return multi_instance_operations.dashboard()


def _performance_validation_http_error(exc: PerformanceValidationError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)


@app.get("/v1/performance-validation/health")
def performance_validation_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return performance_validation.health()


@app.get("/v1/performance-validation/policies")
def performance_validation_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return performance_validation_policies(settings.performance_validation_persistent_disk_mounted)


@app.get("/v1/performance-validation/catalog")
def performance_validation_catalog_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return performance_validation.catalog()


@app.get("/v1/performance-validation/runs")
def performance_validation_runs_route(kind: str = "", limit: int = Query(200, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return performance_validation.list_runs(kind, limit)
    except PerformanceValidationError as exc:
        raise _performance_validation_http_error(exc) from exc


@app.get("/v1/performance-validation/runs/{run_id}")
def performance_validation_run_route(run_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return performance_validation.get_run(run_id)
    except PerformanceValidationError as exc:
        raise _performance_validation_http_error(exc) from exc


@app.post("/v1/performance-validation/load-runs")
def performance_validation_load_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return performance_validation.run_load(payload, _institutional_actor(auth))
    except PerformanceValidationError as exc:
        raise _performance_validation_http_error(exc) from exc


@app.post("/v1/performance-validation/chaos-runs")
def performance_validation_chaos_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return performance_validation.run_chaos(payload, _institutional_actor(auth))
    except PerformanceValidationError as exc:
        raise _performance_validation_http_error(exc) from exc


@app.post("/v1/performance-validation/capacity-reports")
def performance_validation_capacity_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return performance_validation.capacity_report(payload, _institutional_actor(auth))
    except PerformanceValidationError as exc:
        raise _performance_validation_http_error(exc) from exc


@app.get("/v1/performance-validation/dashboard")
def performance_validation_dashboard_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return performance_validation.dashboard()

def _platform_beta_http_error(exc: BetaPlatformError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)


def _platform_beta_component_snapshot() -> dict[str, Any]:
    probes = {
        "interoperability": research_interoperability.health,
        "typedHandoffs": typed_cross_product_handoffs.health,
        "publicIntegrations": public_research_integrations.health,
        "governance": institutional_governance.health,
        "securityPrivacy": security_privacy.health,
        "recovery": multi_instance_operations.health,
        "performanceValidation": performance_validation.health,
    }
    snapshot: dict[str, Any] = {}
    for key, probe in probes.items():
        try:
            snapshot[key] = probe()
        except Exception as exc:
            snapshot[key] = {"ok": False, "status": "error", "detail": str(exc)}
    return snapshot


@app.get("/v1/platform-beta/health")
def platform_beta_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return connected_platform_beta.health()


@app.get("/v1/platform-beta/policies")
def platform_beta_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return connected_platform_beta_policies(settings.platform_beta_persistent_disk_mounted, settings.platform_beta_telemetry_enabled)


@app.get("/v1/platform-beta/catalog")
def platform_beta_catalog_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return connected_platform_beta.catalog()


@app.get("/v1/platform-beta/cohorts")
def platform_beta_cohorts_route(limit: int = Query(200, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return connected_platform_beta.list_cohorts(limit)


@app.post("/v1/platform-beta/cohorts")
def platform_beta_cohort_create_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return connected_platform_beta.create_cohort(payload, _institutional_actor(auth))
    except BetaPlatformError as exc: raise _platform_beta_http_error(exc) from exc


@app.get("/v1/platform-beta/onboarding")
def platform_beta_onboarding_route(limit: int = Query(200, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return connected_platform_beta.list_onboarding(limit)


@app.post("/v1/platform-beta/onboarding")
def platform_beta_onboarding_start_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return connected_platform_beta.start_onboarding(payload, _institutional_actor(auth))
    except BetaPlatformError as exc: raise _platform_beta_http_error(exc) from exc


@app.post("/v1/platform-beta/onboarding/{record_id}/advance")
def platform_beta_onboarding_advance_route(record_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return connected_platform_beta.advance_onboarding(record_id, payload, _institutional_actor(auth))
    except BetaPlatformError as exc: raise _platform_beta_http_error(exc) from exc


@app.get("/v1/platform-beta/project-templates")
def platform_beta_templates_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return {"ok": True, "version": "0.40.0", "projectTemplates": connected_platform_beta.catalog()["projectTemplates"]}


@app.get("/v1/platform-beta/projects")
def platform_beta_projects_route(limit: int = Query(200, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return connected_platform_beta.list_projects(limit)


@app.post("/v1/platform-beta/projects")
def platform_beta_project_create_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return connected_platform_beta.create_project(payload, _institutional_actor(auth))
    except BetaPlatformError as exc: raise _platform_beta_http_error(exc) from exc


@app.get("/v1/platform-beta/projects/{project_id}")
def platform_beta_project_get_route(project_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return connected_platform_beta.get_project(project_id)
    except BetaPlatformError as exc: raise _platform_beta_http_error(exc) from exc


@app.post("/v1/platform-beta/projects/{project_id}/advance")
def platform_beta_project_advance_route(project_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return connected_platform_beta.advance_project(project_id, payload, _institutional_actor(auth))
    except BetaPlatformError as exc: raise _platform_beta_http_error(exc) from exc


@app.post("/v1/platform-beta/telemetry")
def platform_beta_telemetry_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return connected_platform_beta.record_telemetry(payload, _institutional_actor(auth))
    except BetaPlatformError as exc: raise _platform_beta_http_error(exc) from exc


@app.get("/v1/platform-beta/telemetry/summary")
def platform_beta_telemetry_summary_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return connected_platform_beta.telemetry_summary()


@app.get("/v1/platform-beta/feedback")
def platform_beta_feedback_route(limit: int = Query(200, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return connected_platform_beta.list_feedback(limit)


@app.post("/v1/platform-beta/feedback")
def platform_beta_feedback_create_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return connected_platform_beta.create_feedback(payload, _institutional_actor(auth))
    except BetaPlatformError as exc: raise _platform_beta_http_error(exc) from exc


@app.post("/v1/platform-beta/feedback/{issue_id}")
def platform_beta_feedback_update_route(issue_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return connected_platform_beta.update_feedback(issue_id, payload, _institutional_actor(auth))
    except BetaPlatformError as exc: raise _platform_beta_http_error(exc) from exc


@app.get("/v1/platform-beta/limitations")
def platform_beta_limitations_route(limit: int = Query(200, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return connected_platform_beta.list_limitations(limit)


@app.post("/v1/platform-beta/limitations")
def platform_beta_limitation_create_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return connected_platform_beta.create_limitation(payload, _institutional_actor(auth))
    except BetaPlatformError as exc: raise _platform_beta_http_error(exc) from exc


@app.post("/v1/platform-beta/limitations/{issue_id}")
def platform_beta_limitation_update_route(issue_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return connected_platform_beta.update_limitation(issue_id, payload, _institutional_actor(auth))
    except BetaPlatformError as exc: raise _platform_beta_http_error(exc) from exc


@app.get("/v1/platform-beta/support-cases")
def platform_beta_support_route(limit: int = Query(200, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return connected_platform_beta.list_support_cases(limit)


@app.post("/v1/platform-beta/support-cases")
def platform_beta_support_create_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return connected_platform_beta.create_support_case(payload, _institutional_actor(auth))
    except BetaPlatformError as exc: raise _platform_beta_http_error(exc) from exc


@app.post("/v1/platform-beta/support-cases/{issue_id}")
def platform_beta_support_update_route(issue_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return connected_platform_beta.update_support_case(issue_id, payload, _institutional_actor(auth))
    except BetaPlatformError as exc: raise _platform_beta_http_error(exc) from exc


@app.get("/v1/platform-beta/readiness-reports")
def platform_beta_readiness_reports_route(limit: int = Query(100, ge=1, le=1000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return connected_platform_beta.list_readiness_reports(limit)


@app.post("/v1/platform-beta/readiness-reports")
def platform_beta_readiness_create_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return connected_platform_beta.evaluate_readiness(payload, _institutional_actor(auth), _platform_beta_component_snapshot())
    except BetaPlatformError as exc: raise _platform_beta_http_error(exc) from exc


@app.get("/v1/platform-beta/timeline")
def platform_beta_timeline_route(limit: int = Query(200, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return connected_platform_beta.timeline(limit)


@app.get("/v1/platform-beta/timeline/verify")
def platform_beta_timeline_verify_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return connected_platform_beta.verify_timeline()


@app.get("/v1/platform-beta/dashboard")
def platform_beta_dashboard_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return connected_platform_beta.dashboard()


def _interface_finalization_http_error(exc: InterfaceFinalizationError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)


@app.get("/v1/interface-finalization/health")
def interface_finalization_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return interface_finalization.health()


@app.get("/v1/interface-finalization/policies")
def interface_finalization_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return interface_finalization_policies(settings.interface_finalization_persistent_disk_mounted, settings.interface_offline_shell_enabled, settings.interface_max_snapshot_assets, settings.interface_max_queue_records)


@app.get("/v1/interface-finalization/catalog")
def interface_finalization_catalog_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return interface_finalization.catalog()


@app.get("/v1/interface-finalization/audits")
def interface_finalization_audits_route(limit: int = Query(200, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return interface_finalization.list_audits(limit)


@app.post("/v1/interface-finalization/audits")
def interface_finalization_audit_create_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return interface_finalization.create_audit(payload, _institutional_actor(auth))
    except InterfaceFinalizationError as exc: raise _interface_finalization_http_error(exc) from exc


@app.get("/v1/interface-finalization/preferences/{profile_id}")
def interface_finalization_preferences_get_route(profile_id: str, auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return interface_finalization.get_preferences(profile_id)
    except InterfaceFinalizationError as exc: raise _interface_finalization_http_error(exc) from exc


@app.put("/v1/interface-finalization/preferences/{profile_id}")
def interface_finalization_preferences_save_route(profile_id: str, payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return interface_finalization.save_preferences(profile_id, payload, _institutional_actor(auth))
    except InterfaceFinalizationError as exc: raise _interface_finalization_http_error(exc) from exc


@app.get("/v1/interface-finalization/offline-snapshots")
def interface_finalization_snapshots_route(limit: int = Query(200, ge=1, le=2000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return interface_finalization.list_snapshots(limit)


@app.post("/v1/interface-finalization/offline-snapshots")
def interface_finalization_snapshot_create_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return interface_finalization.create_snapshot(payload, _institutional_actor(auth))
    except InterfaceFinalizationError as exc: raise _interface_finalization_http_error(exc) from exc


@app.get("/v1/interface-finalization/offline-operations")
def interface_finalization_operations_route(status: str = "", limit: int = Query(500, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try: return interface_finalization.list_operations(status, limit)
    except InterfaceFinalizationError as exc: raise _interface_finalization_http_error(exc) from exc


@app.post("/v1/interface-finalization/offline-operations")
def interface_finalization_operation_create_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return interface_finalization.queue_operation(payload, _institutional_actor(auth))
    except InterfaceFinalizationError as exc: raise _interface_finalization_http_error(exc) from exc


@app.post("/v1/interface-finalization/reconcile")
def interface_finalization_reconcile_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try: return interface_finalization.reconcile(payload, _institutional_actor(auth))
    except InterfaceFinalizationError as exc: raise _interface_finalization_http_error(exc) from exc


@app.get("/v1/interface-finalization/timeline/verify")
def interface_finalization_timeline_verify_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return interface_finalization.verify_timeline()


@app.get("/v1/interface-finalization/dashboard")
def interface_finalization_dashboard_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return interface_finalization.dashboard()


def _public_release_hardening_http_error(exc: PublicReleaseHardeningError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/public-release-hardening/health")
def public_release_hardening_health_route(auth: dict[str,str]=Depends(require_compute_auth)):
    del auth; return public_release_hardening.health()

@app.get("/v1/public-release-hardening/policies")
def public_release_hardening_policies_route(auth: dict[str,str]=Depends(require_compute_auth)):
    del auth; return public_release_hardening_policies(settings.public_release_hardening_persistent_disk_mounted, settings.public_release_migration_execution_enabled)

@app.get("/v1/public-release-hardening/catalog")
def public_release_hardening_catalog_route(auth: dict[str,str]=Depends(require_compute_auth)):
    del auth; return public_release_hardening.catalog()

@app.get("/v1/public-release-hardening/records/{kind}")
def public_release_hardening_records_route(kind:str,limit:int=Query(200,ge=1,le=5000),auth:dict[str,str]=Depends(require_compute_auth)):
    del auth
    try: return public_release_hardening.list_records(kind,limit)
    except PublicReleaseHardeningError as exc: raise _public_release_hardening_http_error(exc) from exc

@app.post("/v1/public-release-hardening/compatibility-matrices")
def public_release_hardening_compatibility_route(payload:dict[str,Any],auth:dict[str,str]=Depends(require_compute_auth)):
    try: return public_release_hardening.create_compatibility_matrix(payload,_institutional_actor(auth))
    except PublicReleaseHardeningError as exc: raise _public_release_hardening_http_error(exc) from exc

@app.post("/v1/public-release-hardening/migration-assessments")
def public_release_hardening_migration_route(payload:dict[str,Any],auth:dict[str,str]=Depends(require_compute_auth)):
    try: return public_release_hardening.assess_migration(payload,_institutional_actor(auth))
    except PublicReleaseHardeningError as exc: raise _public_release_hardening_http_error(exc) from exc

@app.post("/v1/public-release-hardening/deprecations")
def public_release_hardening_deprecation_route(payload:dict[str,Any],auth:dict[str,str]=Depends(require_compute_auth)):
    try: return public_release_hardening.register_deprecation(payload,_institutional_actor(auth))
    except PublicReleaseHardeningError as exc: raise _public_release_hardening_http_error(exc) from exc

@app.post("/v1/public-release-hardening/clean-install-reports")
def public_release_hardening_clean_install_route(payload:dict[str,Any],auth:dict[str,str]=Depends(require_compute_auth)):
    try: return public_release_hardening.create_clean_install_report(payload,_institutional_actor(auth))
    except PublicReleaseHardeningError as exc: raise _public_release_hardening_http_error(exc) from exc

@app.post("/v1/public-release-hardening/rollback-plans")
def public_release_hardening_rollback_route(payload:dict[str,Any],auth:dict[str,str]=Depends(require_compute_auth)):
    try: return public_release_hardening.create_rollback_plan(payload,_institutional_actor(auth))
    except PublicReleaseHardeningError as exc: raise _public_release_hardening_http_error(exc) from exc

@app.post("/v1/public-release-hardening/release-candidates")
def public_release_hardening_candidate_route(payload:dict[str,Any],auth:dict[str,str]=Depends(require_compute_auth)):
    try: return public_release_hardening.create_release_candidate(payload,_institutional_actor(auth))
    except PublicReleaseHardeningError as exc: raise _public_release_hardening_http_error(exc) from exc

@app.get("/v1/public-release-hardening/timeline/verify")
def public_release_hardening_timeline_route(auth:dict[str,str]=Depends(require_compute_auth)):
    del auth; return public_release_hardening.verify_timeline()

@app.get("/v1/public-release-hardening/dashboard")
def public_release_hardening_dashboard_route(auth:dict[str,str]=Depends(require_compute_auth)):
    del auth; return public_release_hardening.dashboard()



def _connected_platform_http_error(exc: ConnectedPlatformError) -> HTTPException:
    return HTTPException(status_code=exc.status_code, detail=exc.detail)


@app.get("/v1/connected-platform/health")
def connected_platform_health_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return connected_platform.health()


@app.get("/v1/connected-platform/policies")
def connected_platform_policies_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return connected_platform_policies(settings.stable_platform_persistent_disk_mounted)


@app.get("/v1/connected-platform/catalog")
def connected_platform_catalog_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return connected_platform.catalog()


@app.get("/v1/connected-platform/records/{kind}")
def connected_platform_records_route(kind: str, limit: int = Query(200, ge=1, le=5000), auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    try:
        return connected_platform.list_records(kind, limit)
    except ConnectedPlatformError as exc:
        raise _connected_platform_http_error(exc) from exc


@app.post("/v1/connected-platform/contracts")
def connected_platform_contract_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return connected_platform.register_contract(payload, _institutional_actor(auth))
    except ConnectedPlatformError as exc:
        raise _connected_platform_http_error(exc) from exc


@app.post("/v1/connected-platform/support-lifecycles")
def connected_platform_support_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return connected_platform.declare_support_lifecycle(payload, _institutional_actor(auth))
    except ConnectedPlatformError as exc:
        raise _connected_platform_http_error(exc) from exc


@app.post("/v1/connected-platform/upgrade-certifications")
def connected_platform_upgrade_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return connected_platform.certify_upgrade(payload, _institutional_actor(auth))
    except ConnectedPlatformError as exc:
        raise _connected_platform_http_error(exc) from exc


@app.post("/v1/connected-platform/production-attestations")
def connected_platform_production_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return connected_platform.attest_production_readiness(payload, _institutional_actor(auth))
    except ConnectedPlatformError as exc:
        raise _connected_platform_http_error(exc) from exc


@app.post("/v1/connected-platform/incident-readiness")
def connected_platform_incident_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return connected_platform.attest_incident_readiness(payload, _institutional_actor(auth))
    except ConnectedPlatformError as exc:
        raise _connected_platform_http_error(exc) from exc


@app.post("/v1/connected-platform/general-availability")
def connected_platform_ga_route(payload: dict[str, Any], auth: dict[str, str] = Depends(require_compute_auth)):
    try:
        return connected_platform.certify_general_availability(payload, _institutional_actor(auth))
    except ConnectedPlatformError as exc:
        raise _connected_platform_http_error(exc) from exc


@app.get("/v1/connected-platform/timeline/verify")
def connected_platform_timeline_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return connected_platform.verify_timeline()


@app.get("/v1/connected-platform/dashboard")
def connected_platform_dashboard_route(auth: dict[str, str] = Depends(require_compute_auth)):
    del auth
    return connected_platform.dashboard()

@app.get("/v1/visualization/v0800/health")
def spatial_v0800_health_route(): return spatial_health_v0800()

@app.get("/v1/visualization/v0800/policies")
def spatial_v0800_policies_route(): return spatial_policies_v0800()

@app.post("/v1/visualization/v0800/crs/normalize")
def spatial_v0800_crs_route(payload: dict[str, Any]):
    try: return {"ok": True, "crs": normalize_crs_v0800(payload.get("crs") or payload)}
    except SpatialVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/visualization/v0800/viewports/normalize")
def spatial_v0800_viewport_route(payload: dict[str, Any]):
    try: return {"ok": True, "viewport": normalize_viewport_v0800(payload.get("viewport") or payload)}
    except SpatialVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/visualization/v0800/vectors/normalize")
def spatial_v0800_vector_route(payload: dict[str, Any]):
    try: return {"ok": True, "layer": normalize_vector_layer_v0800(payload.get("layer") or payload)}
    except SpatialVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/visualization/v0800/rasters/normalize")
def spatial_v0800_raster_route(payload: dict[str, Any]):
    try: return {"ok": True, "raster": normalize_raster_v0800(payload.get("raster") or payload)}
    except SpatialVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/visualization/v0800/selections/bbox")
def spatial_v0800_bbox_route(payload: dict[str, Any]):
    try: return bbox_select_v0800(payload)
    except SpatialVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/visualization/v0800/figures/build")
def spatial_v0800_figure_route(payload: dict[str, Any]):
    try: return build_spatial_figure_v0800(payload.get("figure") or payload)
    except SpatialVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/visualization/v0800/workspaces/build")
def spatial_v0800_workspace_route(payload: dict[str, Any]):
    try: return build_spatial_workspace_v0800(payload)
    except SpatialVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/visualization/v0810/health")
def scientific_markup_v0810_health_route(): return scientific_markup_health_v0810()

@app.get("/v1/visualization/v0810/policies")
def scientific_markup_v0810_policies_route(): return scientific_markup_policies_v0810()

@app.post("/v1/visualization/v0810/annotations/normalize")
def scientific_markup_v0810_annotation_route(payload: dict[str, Any]):
    try: return {"ok": True, "annotation": normalize_annotation_v0810(payload.get("annotation") or payload)}
    except ScientificMarkupError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/visualization/v0810/measurements/compute")
def scientific_markup_v0810_measurement_route(payload: dict[str, Any]):
    try: return {"ok": True, "measurement": compute_measurement_v0810(payload.get("measurement") or payload)}
    except ScientificMarkupError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/visualization/v0810/layers/normalize")
def scientific_markup_v0810_layer_route(payload: dict[str, Any]):
    try: return {"ok": True, "layer": normalize_markup_layer_v0810(payload.get("layer") or payload)}
    except ScientificMarkupError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/visualization/v0810/figures/attach")
def scientific_markup_v0810_figure_route(payload: dict[str, Any]):
    try: return attach_markup_v0810(payload)
    except ScientificMarkupError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/visualization/v0810/workspaces/build")
def scientific_markup_v0810_workspace_route(payload: dict[str, Any]):
    try: return build_markup_workspace_v0810(payload)
    except ScientificMarkupError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)


@app.get("/v1/visualization/v0820/health")
def uncertainty_v0820_health_route(): return uncertainty_health_v0820()

@app.get("/v1/visualization/v0820/policies")
def uncertainty_v0820_policies_route(): return uncertainty_policies_v0820()

@app.post("/v1/visualization/v0820/uncertainty/normalize")
def uncertainty_v0820_series_route(payload: dict[str, Any]):
    try: return {"ok": True, "uncertainty": normalize_uncertainty_v0820(payload.get("uncertainty") or payload)}
    except UncertaintyVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0820/distributions/normalize")
def uncertainty_v0820_distribution_route(payload: dict[str, Any]):
    try: return {"ok": True, "distribution": normalize_distribution_v0820(payload.get("distribution") or payload)}
    except UncertaintyVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0820/ensembles/normalize")
def uncertainty_v0820_ensemble_route(payload: dict[str, Any]):
    try: return {"ok": True, "ensemble": normalize_ensemble_v0820(payload.get("ensemble") or payload)}
    except UncertaintyVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0820/layers/normalize")
def uncertainty_v0820_layer_route(payload: dict[str, Any]):
    try: return {"ok": True, "layer": normalize_uncertainty_layer_v0820(payload.get("layer") or payload)}
    except UncertaintyVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0820/figures/attach")
def uncertainty_v0820_figure_route(payload: dict[str, Any]):
    try: return attach_uncertainty_v0820(payload)
    except UncertaintyVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0820/workspaces/build")
def uncertainty_v0820_workspace_route(payload: dict[str, Any]):
    try: return build_uncertainty_workspace_v0820(payload)
    except UncertaintyVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc


@app.get("/v1/visualization/v0830/health")
def provenance_v0830_health_route(): return provenance_health_v0830()

@app.get("/v1/visualization/v0830/policies")
def provenance_v0830_policies_route(): return provenance_policies_v0830()

@app.post("/v1/visualization/v0830/provenance/normalize")
def provenance_v0830_normalize_route(payload: dict[str, Any]):
    try: return {"ok": True, "provenance": normalize_provenance_v0830(payload.get("provenance") or payload)}
    except FigureProvenanceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0830/figures/attach")
def provenance_v0830_attach_route(payload: dict[str, Any]):
    try: return {"ok": True, "figure": attach_provenance_v0830(payload)}
    except FigureProvenanceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0830/figures/verify")
def provenance_v0830_verify_route(payload: dict[str, Any]):
    try: return verify_provenance_v0830(payload)
    except FigureProvenanceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0830/exports/manifest")
def provenance_v0830_export_route(payload: dict[str, Any]):
    try: return build_export_manifest_v0830(payload)
    except FigureProvenanceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0830/workspaces/build")
def provenance_v0830_workspace_route(payload: dict[str, Any]):
    try: return build_provenance_workspace_v0830(payload)
    except FigureProvenanceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc


@app.get("/v1/visualization/v0840/health")
def gpu_renderer_v0840_health_route(): return gpu_renderer_health_v0840()

@app.get("/v1/visualization/v0840/policies")
def gpu_renderer_v0840_policies_route(): return gpu_renderer_policies_v0840()

@app.get("/v1/visualization/v0840/renderers/registry")
def gpu_renderer_v0840_registry_route(): return {"ok": True, "registry": renderer_registry_v0840()}

@app.post("/v1/visualization/v0840/capabilities/normalize")
def gpu_renderer_v0840_capabilities_route(payload: dict[str, Any]):
    try: return {"ok": True, "capabilities": normalize_browser_capabilities_v0840(payload.get("browserCapabilities") or payload)}
    except GPURendererArchitectureError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0840/renderers/negotiate")
def gpu_renderer_v0840_negotiate_route(payload: dict[str, Any]):
    try: return {"ok": True, "negotiation": negotiate_renderer_v0840(payload)}
    except GPURendererArchitectureError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0840/buffers/plan")
def gpu_renderer_v0840_buffer_route(payload: dict[str, Any]):
    try: return {"ok": True, "bufferPlan": plan_gpu_buffer_v0840(payload)}
    except GPURendererArchitectureError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0840/shaders/normalize")
def gpu_renderer_v0840_shader_route(payload: dict[str, Any]):
    try: return {"ok": True, "shader": normalize_shader_v0840(payload)}
    except GPURendererArchitectureError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0840/picking/normalize")
def gpu_renderer_v0840_picking_route(payload: dict[str, Any]):
    try: return {"ok": True, "picking": normalize_picking_v0840(payload)}
    except GPURendererArchitectureError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0840/diagnostics/build")
def gpu_renderer_v0840_diagnostics_route(payload: dict[str, Any]):
    try: return build_renderer_diagnostics_v0840(payload)
    except GPURendererArchitectureError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0840/workspaces/build")
def gpu_renderer_v0840_workspace_route(payload: dict[str, Any]):
    try: return build_gpu_workspace_v0840(payload)
    except GPURendererArchitectureError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc


@app.get("/v1/visualization/v0850/health")
def webgl2_v0850_health_route(): return webgl2_health_v0850()

@app.get("/v1/visualization/v0850/policies")
def webgl2_v0850_policies_route(): return webgl2_policies_v0850()

@app.get("/v1/visualization/v0850/renderers/webgl2")
def webgl2_v0850_descriptor_route(): return {"ok": True, "renderer": webgl2_renderer_descriptor_v0850()}

@app.post("/v1/visualization/v0850/cameras/normalize")
def webgl2_v0850_camera_route(payload: dict[str, Any]):
    try: return {"ok": True, "camera": normalize_webgl2_camera_v0850(payload.get("camera") or payload)}
    except WebGL2ScientificRendererError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0850/draw-calls/normalize")
def webgl2_v0850_draw_route(payload: dict[str, Any]):
    try: return {"ok": True, "drawCall": normalize_webgl2_draw_call_v0850(payload.get("drawCall") or payload)}
    except WebGL2ScientificRendererError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0850/render-plans/build")
def webgl2_v0850_plan_route(payload: dict[str, Any]):
    try: return {"ok": True, "renderPlan": build_webgl2_render_plan_v0850(payload)}
    except WebGL2ScientificRendererError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0850/picking/normalize")
def webgl2_v0850_picking_route(payload: dict[str, Any]):
    try: return {"ok": True, "picking": normalize_webgl2_picking_v0850(payload)}
    except WebGL2ScientificRendererError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/visualization/v0850/workspaces/build")
def webgl2_v0850_workspace_route(payload: dict[str, Any]):
    try: return build_webgl2_workspace_v0850(payload)
    except WebGL2ScientificRendererError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc




@app.get("/v1/carbon-nature/soc/v0600/health")
def soil_organic_carbon_v0600_health_route():
    return soil_organic_carbon_health_v0600()


@app.get("/v1/carbon-nature/soc/v0600/schema")
def soil_organic_carbon_v0600_schema_route():
    return soil_organic_carbon_schema_v0600()


@app.get("/v1/carbon-nature/soc/v0600/policies")
def soil_organic_carbon_v0600_policies_route():
    return soil_organic_carbon_policies_v0600()


@app.post("/v1/carbon-nature/soc/v0600/layer-stock", dependencies=[Depends(require_compute_auth)])
def soil_organic_carbon_v0600_layer_stock_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "result": calculate_soc_layer_stock_v0600(payload.get("layer") or payload)}
    except SoilOrganicCarbonV0600Error as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc


@app.post("/v1/carbon-nature/soc/v0600/profile-stock", dependencies=[Depends(require_compute_auth)])
def soil_organic_carbon_v0600_profile_stock_route(payload: dict[str, Any]):
    try:
        return {"ok": True, "result": calculate_soc_profile_stock_v0600(payload)}
    except SoilOrganicCarbonV0600Error as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc


@app.post("/v1/carbon-nature/soc/v0600/project-packet", dependencies=[Depends(require_compute_auth)])
def soil_organic_carbon_v0600_project_packet_route(payload: dict[str, Any]):
    try:
        return build_soc_project_packet_v0600(payload)
    except SoilOrganicCarbonV0600Error as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc



@app.get("/v1/carbon-nature/soc/v0700/sampling/health")
def soc_sampling_v0700_health_route():
    return soc_sampling_health_v0700()


@app.get("/v1/carbon-nature/soc/v0700/sampling/schema")
def soc_sampling_v0700_schema_route():
    return soc_sampling_schema_v0700()


@app.get("/v1/carbon-nature/soc/v0700/sampling/policies")
def soc_sampling_v0700_policies_route():
    return soc_sampling_policies_v0700()


def _soc_sampling_call(fn, payload):
    try:
        return fn(payload)
    except SoilCarbonSamplingV0700Error as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc


@app.post("/v1/carbon-nature/soc/v0700/sampling/sample/normalize", dependencies=[Depends(require_compute_auth)])
def soc_sampling_v0700_sample_route(payload: dict[str, Any]):
    return {"ok": True, "sample": _soc_sampling_call(normalize_soc_sample_v0700, payload.get("sample") or payload)}


@app.post("/v1/carbon-nature/soc/v0700/sampling/samples/normalize", dependencies=[Depends(require_compute_auth)])
def soc_sampling_v0700_batch_route(payload: dict[str, Any]):
    return _soc_sampling_call(normalize_soc_sample_batch_v0700, payload)


@app.post("/v1/carbon-nature/soc/v0700/sampling/bulk-density", dependencies=[Depends(require_compute_auth)])
def soc_sampling_v0700_bulk_density_route(payload: dict[str, Any]):
    return _soc_sampling_call(calculate_soc_bulk_density_v0700, payload)


@app.post("/v1/carbon-nature/soc/v0700/sampling/design", dependencies=[Depends(require_compute_auth)])
def soc_sampling_v0700_design_route(payload: dict[str, Any]):
    return _soc_sampling_call(build_soc_sampling_design_v0700, payload)


@app.post("/v1/carbon-nature/soc/v0700/sampling/profile-handoff", dependencies=[Depends(require_compute_auth)])
def soc_sampling_v0700_profile_handoff_route(payload: dict[str, Any]):
    return _soc_sampling_call(build_soc_profile_handoff_v0700, payload)


@app.post("/v1/carbon-nature/soc/v0700/sampling/field-packet", dependencies=[Depends(require_compute_auth)])
def soc_sampling_v0700_field_packet_route(payload: dict[str, Any]):
    return _soc_sampling_call(build_soc_field_packet_v0700, payload)


@app.get("/v1/carbon-nature/soc/v0800/change/health")
def soc_change_v0800_health_route():
    return soc_change_health_v0800()


@app.get("/v1/carbon-nature/soc/v0800/change/schema")
def soc_change_v0800_schema_route():
    return soc_change_schema_v0800()


@app.get("/v1/carbon-nature/soc/v0800/change/policies")
def soc_change_v0800_policies_route():
    return soc_change_policies_v0800()


def _soc_change_call(fn, payload):
    try:
        return fn(payload)
    except SoilCarbonChangeV0800Error as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc


@app.post("/v1/carbon-nature/soc/v0800/change/compare", dependencies=[Depends(require_compute_auth)])
def soc_change_v0800_compare_route(payload: dict[str, Any]):
    return _soc_change_call(compare_soc_profiles_v0800, payload)


@app.post("/v1/carbon-nature/soc/v0800/change/series", dependencies=[Depends(require_compute_auth)])
def soc_change_v0800_series_route(payload: dict[str, Any]):
    return _soc_change_call(build_soc_change_series_v0800, payload)


@app.post("/v1/carbon-nature/soc/v0800/change/project-packet", dependencies=[Depends(require_compute_auth)])
def soc_change_v0800_project_packet_route(payload: dict[str, Any]):
    return _soc_change_call(build_soc_change_project_packet_v0800, payload)


@app.get("/v1/carbon-nature/soc/v0900/uncertainty/health")
def soc_uncertainty_v0900_health_route():
    return soc_uncertainty_health_v0900()


@app.get("/v1/carbon-nature/soc/v0900/uncertainty/schema")
def soc_uncertainty_v0900_schema_route():
    return soc_uncertainty_schema_v0900()


@app.get("/v1/carbon-nature/soc/v0900/uncertainty/policies")
def soc_uncertainty_v0900_policies_route():
    return soc_uncertainty_policies_v0900()


def _soc_uncertainty_call(fn, payload):
    try:
        return fn(payload)
    except SoilCarbonUncertaintyV0900Error as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc


@app.post("/v1/carbon-nature/soc/v0900/uncertainty/replicates", dependencies=[Depends(require_compute_auth)])
def soc_uncertainty_v0900_replicates_route(payload: dict[str, Any]):
    return _soc_uncertainty_call(summarize_soc_replicates_v0900, payload)


@app.post("/v1/carbon-nature/soc/v0900/uncertainty/stratified", dependencies=[Depends(require_compute_auth)])
def soc_uncertainty_v0900_stratified_route(payload: dict[str, Any]):
    return _soc_uncertainty_call(calculate_soc_stratified_estimate_v0900, payload)


@app.post("/v1/carbon-nature/soc/v0900/uncertainty/change", dependencies=[Depends(require_compute_auth)])
def soc_uncertainty_v0900_change_route(payload: dict[str, Any]):
    return _soc_uncertainty_call(calculate_soc_change_uncertainty_v0900, payload)


@app.post("/v1/carbon-nature/soc/v0900/uncertainty/layer-propagation", dependencies=[Depends(require_compute_auth)])
def soc_uncertainty_v0900_layer_route(payload: dict[str, Any]):
    return _soc_uncertainty_call(propagate_soc_layer_uncertainty_v0900, payload)


@app.post("/v1/carbon-nature/soc/v0900/uncertainty/project-packet", dependencies=[Depends(require_compute_auth)])
def soc_uncertainty_v0900_project_packet_route(payload: dict[str, Any]):
    return _soc_uncertainty_call(build_soc_uncertainty_project_packet_v0900, payload)


@app.get("/v1/carbon-nature/soc/v1000/scenarios/health")
def soc_scenario_v1000_health_route():
    return soc_scenario_health_v1000()


@app.get("/v1/carbon-nature/soc/v1000/scenarios/schema")
def soc_scenario_v1000_schema_route():
    return soc_scenario_schema_v1000()


@app.get("/v1/carbon-nature/soc/v1000/scenarios/policies")
def soc_scenario_v1000_policies_route():
    return soc_scenario_policies_v1000()


def _soc_scenario_call(fn, payload):
    try:
        return fn(payload)
    except SoilCarbonScenarioV01000Error as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc


@app.post("/v1/carbon-nature/soc/v1000/scenarios/project", dependencies=[Depends(require_compute_auth)])
def soc_scenario_v1000_project_route(payload: dict[str, Any]):
    return _soc_scenario_call(project_soc_management_scenario_v1000, payload)


@app.post("/v1/carbon-nature/soc/v1000/scenarios/compare", dependencies=[Depends(require_compute_auth)])
def soc_scenario_v1000_compare_route(payload: dict[str, Any]):
    return _soc_scenario_call(compare_soc_management_scenarios_v1000, payload)


@app.post("/v1/carbon-nature/soc/v1000/scenarios/sensitivity", dependencies=[Depends(require_compute_auth)])
def soc_scenario_v1000_sensitivity_route(payload: dict[str, Any]):
    return _soc_scenario_call(soc_management_sensitivity_v1000, payload)


@app.post("/v1/carbon-nature/soc/v1000/scenarios/project-packet", dependencies=[Depends(require_compute_auth)])
def soc_scenario_v1000_project_packet_route(payload: dict[str, Any]):
    return _soc_scenario_call(build_soc_scenario_project_packet_v1000, payload)


@app.get("/v1/carbon-nature/ghg/v1100/balance/health")
def whole_farm_ghg_v1100_health_route():
    return whole_farm_ghg_health_v1100()


@app.get("/v1/carbon-nature/ghg/v1100/balance/schema")
def whole_farm_ghg_v1100_schema_route():
    return whole_farm_ghg_schema_v1100()


@app.get("/v1/carbon-nature/ghg/v1100/balance/policies")
def whole_farm_ghg_v1100_policies_route():
    return whole_farm_ghg_policies_v1100()


def _whole_farm_ghg_call(fn, payload):
    try:
        return fn(payload)
    except WholeFarmGHGV01100Error as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc


@app.post("/v1/carbon-nature/ghg/v1100/balance/entry", dependencies=[Depends(require_compute_auth)])
def whole_farm_ghg_v1100_entry_route(payload: dict[str, Any]):
    if not isinstance(payload, dict):
        raise HTTPException(status_code=422, detail="JSON object required")
    try:
        gwp_set = normalize_whole_farm_gwp_set_v1100(payload.get("gwp_set"))
        entry = payload.get("entry") if isinstance(payload.get("entry"), dict) else payload
        return normalize_whole_farm_ghg_entry_v1100(entry, gwp_set)
    except WholeFarmGHGV01100Error as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc


@app.post("/v1/carbon-nature/ghg/v1100/balance/calculate", dependencies=[Depends(require_compute_auth)])
def whole_farm_ghg_v1100_calculate_route(payload: dict[str, Any]):
    return _whole_farm_ghg_call(calculate_whole_farm_ghg_balance_v1100, payload)


@app.post("/v1/carbon-nature/ghg/v1100/balance/project-packet", dependencies=[Depends(require_compute_auth)])
def whole_farm_ghg_v1100_project_packet_route(payload: dict[str, Any]):
    return _whole_farm_ghg_call(build_whole_farm_ghg_project_packet_v1100, payload)


@app.get("/v1/carbon-nature/mrv/v1200/registry/health")
def carbon_mrv_v1200_health_route():
    return carbon_mrv_registry_health_v1200()

@app.get("/v1/carbon-nature/mrv/v1200/registry/schema")
def carbon_mrv_v1200_schema_route():
    return carbon_mrv_registry_schema_v1200()

@app.get("/v1/carbon-nature/mrv/v1200/registry/policies")
def carbon_mrv_v1200_policies_route():
    return carbon_mrv_registry_policies_v1200()

@app.get("/v1/carbon-nature/mrv/v1200/registry/methods")
def carbon_mrv_v1200_methods_route(method_type: str | None = None, carbon_pool: str | None = None, gas: str | None = None):
    return list_carbon_mrv_methods_v1200(method_type=method_type, carbon_pool=carbon_pool, gas=gas)

@app.get("/v1/carbon-nature/mrv/v1200/registry/methods/{method_key}")
def carbon_mrv_v1200_method_route(method_key: str):
    try:
        return get_carbon_mrv_method_v1200(method_key)
    except CarbonMRVRegistryV01200Error as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc


def _carbon_mrv_call(fn, payload):
    try:
        return fn(payload)
    except CarbonMRVRegistryV01200Error as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc


@app.post("/v1/carbon-nature/mrv/v1200/registry/compare", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1200_compare_route(payload: dict[str, Any]):
    return _carbon_mrv_call(compare_carbon_mrv_methods_v1200, payload)

@app.post("/v1/carbon-nature/mrv/v1200/registry/readiness", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1200_readiness_route(payload: dict[str, Any]):
    return _carbon_mrv_call(assess_carbon_mrv_readiness_v1200, payload)

@app.post("/v1/carbon-nature/mrv/v1200/registry/project-packet", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1200_project_packet_route(payload: dict[str, Any]):
    return _carbon_mrv_call(build_carbon_mrv_project_packet_v1200, payload)


@app.get("/v1/carbon-nature/mrv/v1300/protocol/health")
def carbon_mrv_v1300_health_route():
    return carbon_mrv_protocol_health_v1300()

@app.get("/v1/carbon-nature/mrv/v1300/protocol/schema")
def carbon_mrv_v1300_schema_route():
    return carbon_mrv_protocol_schema_v1300()

@app.get("/v1/carbon-nature/mrv/v1300/protocol/policies")
def carbon_mrv_v1300_policies_route():
    return carbon_mrv_protocol_policies_v1300()

@app.get("/v1/carbon-nature/mrv/v1300/protocol/template/{method_key}")
def carbon_mrv_v1300_template_route(method_key: str):
    try:
        return carbon_mrv_protocol_template_v1300(method_key)
    except CarbonMRVProtocolV01300Error as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc


def _carbon_mrv_protocol_call(fn, payload):
    try:
        return fn(payload)
    except CarbonMRVProtocolV01300Error as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc


@app.post("/v1/carbon-nature/mrv/v1300/protocol/build", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1300_build_route(payload: dict[str, Any]):
    return _carbon_mrv_protocol_call(build_carbon_mrv_protocol_v1300, payload)

@app.post("/v1/carbon-nature/mrv/v1300/protocol/validate", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1300_validate_route(payload: dict[str, Any]):
    return _carbon_mrv_protocol_call(validate_carbon_mrv_protocol_v1300, payload)

@app.post("/v1/carbon-nature/mrv/v1300/protocol/project-packet", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1300_project_packet_route(payload: dict[str, Any]):
    return _carbon_mrv_protocol_call(build_carbon_mrv_protocol_project_packet_v1300, payload)

@app.get("/v1/carbon-nature/mrv/v1400/monitoring/health")
def carbon_mrv_v1400_health_route():
    return carbon_mrv_monitoring_health_v1400()

@app.get("/v1/carbon-nature/mrv/v1400/monitoring/schema")
def carbon_mrv_v1400_schema_route():
    return carbon_mrv_monitoring_schema_v1400()

@app.get("/v1/carbon-nature/mrv/v1400/monitoring/policies")
def carbon_mrv_v1400_policies_route():
    return carbon_mrv_monitoring_policies_v1400()

@app.get("/v1/carbon-nature/mrv/v1400/monitoring/template/{method_key}")
def carbon_mrv_v1400_template_route(method_key: str):
    try:
        return carbon_mrv_monitoring_template_v1400(method_key)
    except CarbonMRVMonitoringV01400Error as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

def _carbon_mrv_monitoring_call(fn, payload):
    try:
        return fn(payload)
    except CarbonMRVMonitoringV01400Error as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/carbon-nature/mrv/v1400/monitoring/sample-size", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1400_sample_size_route(payload: dict[str, Any]):
    return _carbon_mrv_monitoring_call(carbon_mrv_sample_size_v1400, payload)

@app.post("/v1/carbon-nature/mrv/v1400/monitoring/strata-allocation", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1400_strata_allocation_route(payload: dict[str, Any]):
    return _carbon_mrv_monitoring_call(allocate_carbon_mrv_strata_v1400, payload)

@app.post("/v1/carbon-nature/mrv/v1400/monitoring/build", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1400_build_route(payload: dict[str, Any]):
    return _carbon_mrv_monitoring_call(build_carbon_mrv_monitoring_plan_v1400, payload)

@app.post("/v1/carbon-nature/mrv/v1400/monitoring/validate", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1400_validate_route(payload: dict[str, Any]):
    return _carbon_mrv_monitoring_call(validate_carbon_mrv_monitoring_plan_v1400, payload)

@app.post("/v1/carbon-nature/mrv/v1400/monitoring/project-packet", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1400_project_packet_route(payload: dict[str, Any]):
    return _carbon_mrv_monitoring_call(build_carbon_mrv_monitoring_project_packet_v1400, payload)

@app.get("/v1/carbon-nature/mrv/v1500/uncertainty/health")
def carbon_mrv_v1500_health_route():
    return carbon_mrv_uncertainty_health_v1500()

@app.get("/v1/carbon-nature/mrv/v1500/uncertainty/schema")
def carbon_mrv_v1500_schema_route():
    return carbon_mrv_uncertainty_schema_v1500()

@app.get("/v1/carbon-nature/mrv/v1500/uncertainty/policies")
def carbon_mrv_v1500_policies_route():
    return carbon_mrv_uncertainty_policies_v1500()

def _carbon_mrv_uncertainty_call(fn, payload):
    try:
        return fn(payload)
    except CarbonMRVUncertaintyV01500Error as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/carbon-nature/mrv/v1500/uncertainty/budget", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1500_budget_route(payload: dict[str, Any]):
    return _carbon_mrv_uncertainty_call(carbon_mrv_uncertainty_budget_v1500, payload)

@app.post("/v1/carbon-nature/mrv/v1500/uncertainty/change-detection", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1500_change_detection_route(payload: dict[str, Any]):
    return _carbon_mrv_uncertainty_call(carbon_mrv_change_detection_v1500, payload)

@app.post("/v1/carbon-nature/mrv/v1500/uncertainty/detection-sample-size", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1500_detection_sample_size_route(payload: dict[str, Any]):
    return _carbon_mrv_uncertainty_call(carbon_mrv_detection_sample_size_v1500, payload)

@app.post("/v1/carbon-nature/mrv/v1500/uncertainty/build", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1500_build_route(payload: dict[str, Any]):
    return _carbon_mrv_uncertainty_call(build_carbon_mrv_uncertainty_assessment_v1500, payload)

@app.post("/v1/carbon-nature/mrv/v1500/uncertainty/validate", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1500_validate_route(payload: dict[str, Any]):
    return _carbon_mrv_uncertainty_call(validate_carbon_mrv_uncertainty_assessment_v1500, payload)

@app.post("/v1/carbon-nature/mrv/v1500/uncertainty/project-packet", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1500_project_packet_route(payload: dict[str, Any]):
    return _carbon_mrv_uncertainty_call(build_carbon_mrv_uncertainty_project_packet_v1500, payload)

@app.get("/v1/carbon-nature/mrv/v1600/verification-ledger/health")
def carbon_mrv_v1600_verification_health_route():
    return carbon_mrv_verification_health_v1600()

@app.get("/v1/carbon-nature/mrv/v1600/verification-ledger/schema")
def carbon_mrv_v1600_verification_schema_route():
    return carbon_mrv_verification_schema_v1600()

@app.get("/v1/carbon-nature/mrv/v1600/verification-ledger/policies")
def carbon_mrv_v1600_verification_policies_route():
    return carbon_mrv_verification_policies_v1600()

def _carbon_mrv_verification_call(fn, payload):
    try:
        return fn(payload)
    except CarbonMRVVerificationLedgerV01600Error as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/carbon-nature/mrv/v1600/verification-ledger/evidence-entry", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1600_verification_entry_route(payload: dict[str, Any]):
    return _carbon_mrv_verification_call(normalize_carbon_mrv_evidence_entry_v1600, payload)

@app.post("/v1/carbon-nature/mrv/v1600/verification-ledger/build", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1600_verification_build_route(payload: dict[str, Any]):
    return _carbon_mrv_verification_call(build_carbon_mrv_verification_ledger_v1600, payload)

@app.post("/v1/carbon-nature/mrv/v1600/verification-ledger/validate", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1600_verification_validate_route(payload: dict[str, Any]):
    return _carbon_mrv_verification_call(validate_carbon_mrv_verification_ledger_v1600, payload)

@app.post("/v1/carbon-nature/mrv/v1600/verification-ledger/chain-check", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1600_verification_chain_route(payload: dict[str, Any]):
    return _carbon_mrv_verification_call(verify_carbon_mrv_verification_chain_v1600, payload)

@app.post("/v1/carbon-nature/mrv/v1600/verification-ledger/project-packet", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1600_verification_project_packet_route(payload: dict[str, Any]):
    return _carbon_mrv_verification_call(build_carbon_mrv_verification_project_packet_v1600, payload)

@app.get("/v1/carbon-nature/mrv/v1700/reporting/health")
def carbon_mrv_v1700_reporting_health_route():
    return carbon_mrv_reporting_health_v1700()

@app.get("/v1/carbon-nature/mrv/v1700/reporting/schema")
def carbon_mrv_v1700_reporting_schema_route():
    return carbon_mrv_reporting_schema_v1700()

@app.get("/v1/carbon-nature/mrv/v1700/reporting/policies")
def carbon_mrv_v1700_reporting_policies_route():
    return carbon_mrv_reporting_policies_v1700()

@app.get("/v1/carbon-nature/mrv/v1700/reporting/template/{report_type}")
def carbon_mrv_v1700_reporting_template_route(report_type: str):
    try:
        return carbon_mrv_report_template_v1700(report_type)
    except CarbonMRVReportingV01700Error as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

def _carbon_mrv_reporting_call(fn, payload):
    try:
        return fn(payload)
    except CarbonMRVReportingV01700Error as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/carbon-nature/mrv/v1700/reporting/report/build", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1700_report_build_route(payload: dict[str, Any]):
    return _carbon_mrv_reporting_call(build_carbon_mrv_report_v1700, payload)

@app.post("/v1/carbon-nature/mrv/v1700/reporting/report/validate", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1700_report_validate_route(payload: dict[str, Any]):
    return _carbon_mrv_reporting_call(validate_carbon_mrv_report_v1700, payload)

@app.post("/v1/carbon-nature/mrv/v1700/reporting/audit-packet/build", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1700_audit_packet_build_route(payload: dict[str, Any]):
    return _carbon_mrv_reporting_call(build_carbon_mrv_audit_packet_v1700, payload)

@app.post("/v1/carbon-nature/mrv/v1700/reporting/project-packet", dependencies=[Depends(require_compute_auth)])
def carbon_mrv_v1700_reporting_project_packet_route(payload: dict[str, Any]):
    return _carbon_mrv_reporting_call(build_carbon_mrv_reporting_project_packet_v1700, payload)

@app.get("/v1/model-studio/dynamic-systems/v0860/health")
def system_dynamics_v0860_health_route(): return system_dynamics_health_v0860()

@app.get("/v1/model-studio/dynamic-systems/v0860/policies")
def system_dynamics_v0860_policies_route(): return system_dynamics_policies_v0860()

@app.post("/v1/model-studio/dynamic-systems/v0860/causal-loops/normalize")
def system_dynamics_v0860_causal_normalize_route(payload: dict[str, Any]):
    try: return {"ok": True, "model": normalize_causal_loop_v0860(payload.get("model") or payload)}
    except SystemDynamicsV0860Error as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/model-studio/dynamic-systems/v0860/feedback/analyze")
def system_dynamics_v0860_feedback_route(payload: dict[str, Any]):
    try: return analyze_system_feedback_v0860(payload)
    except SystemDynamicsV0860Error as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/model-studio/dynamic-systems/v0860/stock-flow/normalize")
def system_dynamics_v0860_stock_flow_normalize_route(payload: dict[str, Any]):
    try: return {"ok": True, "model": normalize_stock_flow_v0860(payload.get("model") or payload)}
    except SystemDynamicsV0860Error as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/model-studio/dynamic-systems/v0860/stock-flow/simulate")
def system_dynamics_v0860_simulate_route(payload: dict[str, Any]):
    try: return simulate_stock_flow_v0860(payload)
    except SystemDynamicsV0860Error as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/model-studio/dynamic-systems/v0860/leverage/analyze")
def system_dynamics_v0860_leverage_route(payload: dict[str, Any]):
    try: return analyze_system_leverage_v0860(payload)
    except SystemDynamicsV0860Error as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.post("/v1/model-studio/dynamic-systems/v0860/workspaces/build")
def system_dynamics_v0860_workspace_route(payload: dict[str, Any]):
    try: return build_system_dynamics_workspace_v0860(payload)
    except SystemDynamicsV0860Error as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

@app.get("/v1/visualization/v0870/health")
def webgpu_v0870_health_route(): return webgpu_health_v0870()

@app.get("/v1/visualization/v0870/policies")
def webgpu_v0870_policies_route(): return webgpu_policies_v0870()

@app.get("/v1/visualization/v0870/renderers/webgpu")
def webgpu_v0870_descriptor_route(): return {"ok": True, "renderer": webgpu_renderer_descriptor_v0870()}

@app.post("/v1/visualization/v0870/render-passes/normalize")
def webgpu_v0870_render_pass_route(payload: dict[str, Any]):
    try: return {"ok": True, "renderPass": normalize_webgpu_render_pass_v0870(payload.get("renderPass") or payload)}
    except WebGPUScientificRendererError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/visualization/v0870/render-plans/build")
def webgpu_v0870_render_plan_route(payload: dict[str, Any]):
    try: return {"ok": True, "renderPlan": build_webgpu_render_plan_v0870(payload.get("renderPlan") or payload)}
    except WebGPUScientificRendererError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/visualization/v0870/compute/dispatches/normalize")
def webgpu_v0870_compute_dispatch_route(payload: dict[str, Any]):
    try: return {"ok": True, "dispatch": normalize_webgpu_compute_dispatch_v0870(payload.get("dispatch") or payload)}
    except WebGPUScientificRendererError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/visualization/v0870/compute/plans/build")
def webgpu_v0870_compute_plan_route(payload: dict[str, Any]):
    try: return {"ok": True, "computePlan": build_webgpu_compute_plan_v0870(payload.get("computePlan") or payload)}
    except WebGPUScientificRendererError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/visualization/v0870/workspaces/build")
def webgpu_v0870_workspace_route(payload: dict[str, Any]):
    try: return build_webgpu_workspace_v0870(payload)
    except WebGPUScientificRendererError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/visualization/v0880/health")
def advanced_scene_v0880_health_route(): return advanced_scene_health_v0880()

@app.get("/v1/visualization/v0880/policies")
def advanced_scene_v0880_policies_route(): return advanced_scene_policies_v0880()

@app.get("/v1/visualization/v0880/scene-engine")
def advanced_scene_v0880_descriptor_route(): return {"ok": True, "sceneEngine": advanced_scene_descriptor_v0880()}

@app.post("/v1/visualization/v0880/cameras/normalize")
def advanced_scene_v0880_camera_route(payload: dict[str, Any]):
    try: return {"ok": True, "camera": normalize_advanced_scene_camera_v0880(payload.get("camera") or payload)}
    except AdvancedScientificSceneError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/visualization/v0880/lights/normalize")
def advanced_scene_v0880_light_route(payload: dict[str, Any]):
    try: return {"ok": True, "light": normalize_advanced_scene_light_v0880(payload.get("light") or payload)}
    except AdvancedScientificSceneError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/visualization/v0880/materials/normalize")
def advanced_scene_v0880_material_route(payload: dict[str, Any]):
    try: return {"ok": True, "material": normalize_advanced_scene_material_v0880(payload.get("material") or payload)}
    except AdvancedScientificSceneError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/visualization/v0880/scenes/normalize")
def advanced_scene_v0880_scene_route(payload: dict[str, Any]):
    try: return {"ok": True, "scene": normalize_advanced_scene_v0880(payload.get("scene") or payload)}
    except AdvancedScientificSceneError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/visualization/v0880/render-plans/build")
def advanced_scene_v0880_render_plan_route(payload: dict[str, Any]):
    try: return build_advanced_scene_render_plan_v0880(payload)
    except AdvancedScientificSceneError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/visualization/v0880/workspaces/build")
def advanced_scene_v0880_workspace_route(payload: dict[str, Any]):
    try: return build_advanced_scene_workspace_v0880(payload)
    except AdvancedScientificSceneError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)


# Energy Systems Intelligence target-side runtime consumer.
from .energy_runtime_consumer import router as energy_runtime_consumer_router
app.include_router(energy_runtime_consumer_router)

# Energy Systems Intelligence v1.4.0 — Energy Modeling & Uncertainty.
from .energy_modeling_uncertainty import router as energy_modeling_uncertainty_router
app.include_router(energy_modeling_uncertainty_router)

# Energy Systems v1.6.0 — Grid, Storage & Reliability uncertainty routes.
from .energy_grid_reliability import router as energy_grid_reliability_router
app.include_router(energy_grid_reliability_router)


# Lab v0.110.0 — Platform Core v3 Reproducibility & Scholarly Package Bridge
@app.get("/v1/platform-core-v3-scholarly-packages/health")
def platform_core_v3_scholarly_packages_health():
    return core_v3_scholarly_package_health()

@app.get("/v1/platform-core-v3-scholarly-packages/manifest")
def platform_core_v3_scholarly_packages_manifest():
    return core_v3_scholarly_package_manifest()

@app.get("/v1/platform-core-v3-scholarly-packages/schema")
def platform_core_v3_scholarly_packages_schema():
    return {"ok": True, "version": "0.110.0", "minimum_core_release": "3.0.0", "core_package_binding_endpoint": "/v1/research/unified-runtime/package-bindings", "core_scholarly_contract": "sc.research.scholarly-interoperability-packaging.v1", "automatic_core_submission": False, "automatic_publication": False, "automatic_reproducibility_certification": False}

@app.post("/v1/platform-core-v3-scholarly-packages/packages/normalize")
def platform_core_v3_scholarly_packages_normalize(payload: dict):
    try: return normalize_core_v3_package(payload)
    except PlatformCoreV3ScholarlyPackageError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-scholarly-packages/packages/bind")
def platform_core_v3_scholarly_packages_bind(payload: dict):
    try: return build_core_v3_package_binding(payload)
    except PlatformCoreV3ScholarlyPackageError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-scholarly-packages/scholarly/package")
def platform_core_v3_scholarly_package_envelope(payload: dict):
    try: return build_core_v3_scholarly_package(payload)
    except PlatformCoreV3ScholarlyPackageError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-scholarly-packages/scholarly/plan")
def platform_core_v3_scholarly_package_plan(payload: dict):
    try: return build_core_v3_scholarly_plan(payload)
    except PlatformCoreV3ScholarlyPackageError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-scholarly-packages/citations/bridge")
def platform_core_v3_scholarly_citations(payload: dict):
    try: return bridge_core_v3_citations(payload)
    except PlatformCoreV3ScholarlyPackageError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-scholarly-packages/descriptors/bridge")
def platform_core_v3_scholarly_descriptors(payload: dict):
    try: return bridge_core_v3_descriptors(payload)
    except PlatformCoreV3ScholarlyPackageError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-scholarly-packages/provenance/manifest")
def platform_core_v3_scholarly_provenance(payload: dict):
    try: return build_core_v3_provenance_manifest(payload)
    except PlatformCoreV3ScholarlyPackageError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-scholarly-packages/publications/bind")
def platform_core_v3_scholarly_publication(payload: dict):
    try: return build_core_v3_publication_binding(payload)
    except PlatformCoreV3ScholarlyPackageError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-scholarly-packages/validations/record")
def platform_core_v3_scholarly_validation(payload: dict):
    try: return build_core_v3_scholarly_validation(payload)
    except PlatformCoreV3ScholarlyPackageError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-scholarly-packages/legacy-package/map")
def platform_core_v3_scholarly_legacy_map(payload: dict):
    try: return map_core_v3_legacy_package(payload)
    except PlatformCoreV3ScholarlyPackageError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

# Lab v0.111.0 — Platform Core v3 Scientific Investigation Runtime Integration
@app.get("/v1/platform-core-v3-scientific-investigations/health")
def platform_core_v3_scientific_investigations_health():
    return core_v3_scientific_investigation_health()

@app.get("/v1/platform-core-v3-scientific-investigations/manifest")
def platform_core_v3_scientific_investigations_manifest():
    return core_v3_scientific_investigation_manifest()

@app.get("/v1/platform-core-v3-scientific-investigations/schema")
def platform_core_v3_scientific_investigations_schema():
    return {"ok": True, "version": "0.111.0", "minimum_core_release": "3.0.0", "core_investigation_binding_endpoint": "/v1/research/unified-runtime/investigation-bindings", "automatic_core_submission": False, "automatic_execution": False, "automatic_scientific_certification": False, "automatic_truth_determination": False}

@app.post("/v1/platform-core-v3-scientific-investigations/investigations/normalize")
def platform_core_v3_scientific_investigations_normalize(payload: dict):
    try: return normalize_core_v3_scientific_investigation(payload)
    except PlatformCoreV3ScientificInvestigationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-scientific-investigations/investigations/bind")
def platform_core_v3_scientific_investigations_bind(payload: dict):
    try: return build_core_v3_investigation_binding(payload)
    except PlatformCoreV3ScientificInvestigationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-scientific-investigations/investigations/runtime-plan")
def platform_core_v3_scientific_investigations_plan(payload: dict):
    try: return build_core_v3_investigation_plan(payload)
    except PlatformCoreV3ScientificInvestigationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-scientific-investigations/investigations/evidence-context")
def platform_core_v3_scientific_investigations_evidence(payload: dict):
    try: return bridge_core_v3_investigation_evidence(payload)
    except PlatformCoreV3ScientificInvestigationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-scientific-investigations/investigations/reasoning-context")
def platform_core_v3_scientific_investigations_reasoning(payload: dict):
    try: return bridge_core_v3_investigation_reasoning(payload)
    except PlatformCoreV3ScientificInvestigationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-scientific-investigations/investigations/scientific-assets")
def platform_core_v3_scientific_investigations_assets(payload: dict):
    try: return bridge_core_v3_investigation_assets(payload)
    except PlatformCoreV3ScientificInvestigationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-scientific-investigations/investigations/continuity/check")
def platform_core_v3_scientific_investigations_continuity(payload: dict):
    try: return check_core_v3_investigation_continuity(payload)
    except PlatformCoreV3ScientificInvestigationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-scientific-investigations/legacy-study/map")
def platform_core_v3_scientific_investigations_legacy_study(payload: dict):
    try: return map_core_v3_legacy_scientific_study(payload)
    except PlatformCoreV3ScientificInvestigationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-scientific-investigations/legacy-argumentation/map")
def platform_core_v3_scientific_investigations_legacy_argumentation(payload: dict):
    try: return map_core_v3_legacy_argumentation_case(payload)
    except PlatformCoreV3ScientificInvestigationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)


# Lab v0.112.0 — Platform Core Integration Certification
@app.get("/v1/platform-core-v3-integration-certification/health")
def platform_core_v3_integration_certification_health():
    return core_v3_integration_certification_health()

@app.get("/v1/platform-core-v3-integration-certification/manifest")
def platform_core_v3_integration_certification_manifest():
    return core_v3_integration_certification_manifest()

@app.get("/v1/platform-core-v3-integration-certification/schema")
def platform_core_v3_integration_certification_schema():
    return {"ok": True, "version": "0.112.0", "minimum_core_release": "3.0.0", "core_contract": "sc.research.platform-integration-certification.v1", "certification_scope": "platform_runtime_contract_conformance_only", "automatic_core_submission": False, "automatic_product_invocation": False, "automatic_case_execution": False, "automatic_scientific_certification": False, "automatic_truth_determination": False}

@app.get("/v1/platform-core-v3-integration-certification/catalog")
def platform_core_v3_integration_certification_catalog():
    return core_v3_integration_certification_catalog()

@app.post("/v1/platform-core-v3-integration-certification/suites/plan")
def platform_core_v3_integration_certification_suite(payload: dict):
    try: return build_core_v3_certification_suite(payload)
    except PlatformCoreV3IntegrationCertificationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-integration-certification/products/plan")
def platform_core_v3_integration_certification_product(payload: dict):
    try: return build_core_v3_certification_product(payload)
    except PlatformCoreV3IntegrationCertificationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-integration-certification/cases/plan")
def platform_core_v3_integration_certification_cases(payload: dict):
    try: return build_core_v3_certification_cases(payload)
    except PlatformCoreV3IntegrationCertificationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-integration-certification/runs/plan")
def platform_core_v3_integration_certification_run(payload: dict):
    try: return build_core_v3_certification_run(payload)
    except PlatformCoreV3IntegrationCertificationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-integration-certification/case-results/plan")
def platform_core_v3_integration_certification_case_result(payload: dict):
    try: return build_core_v3_certification_case_result(payload)
    except PlatformCoreV3IntegrationCertificationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-integration-certification/exchange-checks/plan")
def platform_core_v3_integration_certification_exchange(payload: dict):
    try: return build_core_v3_certification_exchange(payload)
    except PlatformCoreV3IntegrationCertificationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-integration-certification/trace-checks/plan")
def platform_core_v3_integration_certification_trace(payload: dict):
    try: return build_core_v3_certification_trace(payload)
    except PlatformCoreV3IntegrationCertificationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-integration-certification/reproduction-checks/plan")
def platform_core_v3_integration_certification_reproduction(payload: dict):
    try: return build_core_v3_certification_reproduction(payload)
    except PlatformCoreV3IntegrationCertificationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-integration-certification/evidence/plan")
def platform_core_v3_integration_certification_evidence(payload: dict):
    try: return build_core_v3_certification_evidence(payload)
    except PlatformCoreV3IntegrationCertificationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-integration-certification/findings/plan")
def platform_core_v3_integration_certification_finding(payload: dict):
    try: return build_core_v3_certification_finding(payload)
    except PlatformCoreV3IntegrationCertificationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-integration-certification/roundtrip/assess")
def platform_core_v3_integration_certification_roundtrip(payload: dict):
    try: return assess_core_v3_integration_roundtrip(payload)
    except PlatformCoreV3IntegrationCertificationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-integration-certification/compatibility/check")
def platform_core_v3_integration_certification_compatibility(payload: dict):
    try: return core_v3_integration_certification_compatibility(payload)
    except PlatformCoreV3IntegrationCertificationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-integration-certification/full-plan")
def platform_core_v3_integration_certification_full(payload: dict):
    try: return build_core_v3_certification_full_plan(payload)
    except PlatformCoreV3IntegrationCertificationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

# Lab v0.113.0 — Unified Research Session Production Runtime
@app.get("/v1/platform-core-v3-production-runtime/health")
def platform_core_v3_production_runtime_health():
    return core_v3_production_runtime_health()

@app.get("/v1/platform-core-v3-production-runtime/manifest")
def platform_core_v3_production_runtime_manifest():
    return core_v3_production_runtime_manifest()

@app.get("/v1/platform-core-v3-production-runtime/schema")
def platform_core_v3_production_runtime_schema():
    return {"ok": True, "version": "0.113.0", "minimum_core_release": "3.0.0", "core_runtime_contract": "sc.research.unified-research-scientific-investigation-runtime.v1", "explicit_submission_plans_only": True, "automatic_core_submission": False, "automatic_retry": False, "automatic_recovery": False, "automatic_scientific_execution": False, "automatic_scientific_certification": False, "automatic_truth_determination": False}

@app.get("/v1/platform-core-v3-production-runtime/catalog")
def platform_core_v3_production_runtime_catalog():
    return core_v3_production_runtime_catalog()

@app.post("/v1/platform-core-v3-production-runtime/operations/normalize")
def platform_core_v3_production_runtime_normalize(payload: dict):
    try: return normalize_core_v3_production_operation(payload)
    except PlatformCoreV3ProductionRuntimeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-production-runtime/submissions/plan")
def platform_core_v3_production_runtime_submission(payload: dict):
    try: return build_core_v3_production_submission_plan(payload)
    except PlatformCoreV3ProductionRuntimeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-production-runtime/idempotency/check")
def platform_core_v3_production_runtime_idempotency(payload: dict):
    try: return check_core_v3_production_idempotency(payload)
    except PlatformCoreV3ProductionRuntimeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-production-runtime/receipts/build")
def platform_core_v3_production_runtime_receipt(payload: dict):
    try: return build_core_v3_production_receipt(payload)
    except PlatformCoreV3ProductionRuntimeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-production-runtime/retries/plan")
def platform_core_v3_production_runtime_retry(payload: dict):
    try: return build_core_v3_production_retry(payload)
    except PlatformCoreV3ProductionRuntimeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-production-runtime/recovery/plan")
def platform_core_v3_production_runtime_recovery(payload: dict):
    try: return build_core_v3_production_recovery(payload)
    except PlatformCoreV3ProductionRuntimeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-production-runtime/checkpoints/build")
def platform_core_v3_production_runtime_checkpoint(payload: dict):
    try: return build_core_v3_production_checkpoint(payload)
    except PlatformCoreV3ProductionRuntimeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-production-runtime/continuity/check")
def platform_core_v3_production_runtime_continuity(payload: dict):
    try: return check_core_v3_production_continuity(payload)
    except PlatformCoreV3ProductionRuntimeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-production-runtime/diagnostics/assess")
def platform_core_v3_production_runtime_diagnostics(payload: dict):
    try: return assess_core_v3_production_diagnostics(payload)
    except PlatformCoreV3ProductionRuntimeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-production-runtime/readiness/check")
def platform_core_v3_production_runtime_readiness(payload: dict):
    try: return check_core_v3_production_readiness(payload)
    except PlatformCoreV3ProductionRuntimeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-production-runtime/roundtrip/plan")
def platform_core_v3_production_runtime_roundtrip_plan(payload: dict):
    try: return build_core_v3_production_roundtrip_plan(payload)
    except PlatformCoreV3ProductionRuntimeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/platform-core-v3-production-runtime/roundtrip/assess")
def platform_core_v3_production_runtime_roundtrip_assess(payload: dict):
    try: return assess_core_v3_production_roundtrip(payload)
    except PlatformCoreV3ProductionRuntimeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)



# Lab v0.114.0 — Scientific Visualization Design System & Publication-Grade Rendering
@app.get("/v1/scientific-visualization-design-system/health")
def scientific_visualization_design_system_health(): return visualization_design_health()

@app.get("/v1/scientific-visualization-design-system/manifest")
def scientific_visualization_design_system_manifest(): return visualization_design_manifest()

@app.get("/v1/scientific-visualization-design-system/tokens")
def scientific_visualization_design_system_tokens(): return visualization_design_tokens()

@app.get("/v1/scientific-visualization-design-system/catalog")
def scientific_visualization_design_system_catalog(): return visualization_design_catalog()

@app.post("/v1/scientific-visualization-design-system/figures/normalize")
def scientific_visualization_design_system_normalize(payload: dict):
    try: return visualization_design_normalize(payload)
    except VisualizationDesignSystemError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/scientific-visualization-design-system/figures/profile")
def scientific_visualization_design_system_profile(payload: dict):
    try: return visualization_design_apply_profile(payload)
    except VisualizationDesignSystemError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/scientific-visualization-design-system/annotations/plan")
def scientific_visualization_design_system_annotations(payload: dict):
    try: return visualization_design_annotations(payload)
    except VisualizationDesignSystemError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/scientific-visualization-design-system/uncertainty/style")
def scientific_visualization_design_system_uncertainty(payload: dict):
    try: return visualization_design_uncertainty(payload)
    except VisualizationDesignSystemError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/scientific-visualization-design-system/small-multiples/compose")
def scientific_visualization_design_system_small_multiples(payload: dict):
    try: return visualization_design_small_multiples(payload)
    except VisualizationDesignSystemError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/scientific-visualization-design-system/accessibility/build")
def scientific_visualization_design_system_accessibility(payload: dict):
    try: return visualization_design_accessibility(payload)
    except VisualizationDesignSystemError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/scientific-visualization-design-system/renderers/plan")
def scientific_visualization_design_system_renderer(payload: dict):
    try: return visualization_design_renderer_plan(payload)
    except VisualizationDesignSystemError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/scientific-visualization-design-system/exports/plan")
def scientific_visualization_design_system_export(payload: dict):
    try: return visualization_design_export_plan(payload)
    except VisualizationDesignSystemError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/scientific-visualization-design-system/audit")
def scientific_visualization_design_system_audit(payload: dict):
    try: return visualization_design_audit(payload)
    except VisualizationDesignSystemError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/scientific-visualization-design-system/publication-figure/build")
def scientific_visualization_design_system_publication(payload: dict):
    try: return visualization_design_publication_figure(payload)
    except VisualizationDesignSystemError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/scientific-visualization-design-system/core-visual/plan")
def scientific_visualization_design_system_core_visual(payload: dict):
    try: return visualization_design_core_visual_plan(payload)
    except VisualizationDesignSystemError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)


# Lab v0.115.0 — Advanced Statistical & Uncertainty Graphics
@app.get("/v1/advanced-statistical-uncertainty-graphics/health")
def advanced_statistical_uncertainty_graphics_health(): return advanced_stats_health()

@app.get("/v1/advanced-statistical-uncertainty-graphics/manifest")
def advanced_statistical_uncertainty_graphics_manifest(): return advanced_stats_manifest()

@app.get("/v1/advanced-statistical-uncertainty-graphics/catalog")
def advanced_statistical_uncertainty_graphics_catalog(): return advanced_stats_catalog()

@app.post("/v1/advanced-statistical-uncertainty-graphics/distributions/normalize")
def advanced_statistical_uncertainty_graphics_distribution_normalize(payload: dict):
    try: return advanced_stats_normalize_distribution(payload)
    except AdvancedStatisticalGraphicsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/advanced-statistical-uncertainty-graphics/distributions/kde")
def advanced_statistical_uncertainty_graphics_kde(payload: dict):
    try: return advanced_stats_kde(payload)
    except AdvancedStatisticalGraphicsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/advanced-statistical-uncertainty-graphics/distributions/figure")
def advanced_statistical_uncertainty_graphics_distribution_figure(payload: dict):
    try: return advanced_stats_distribution_figure(payload)
    except AdvancedStatisticalGraphicsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/advanced-statistical-uncertainty-graphics/intervals/figure")
def advanced_statistical_uncertainty_graphics_interval(payload: dict):
    try: return advanced_stats_interval(payload)
    except AdvancedStatisticalGraphicsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/advanced-statistical-uncertainty-graphics/fan-chart/figure")
def advanced_statistical_uncertainty_graphics_fan_chart(payload: dict):
    try: return advanced_stats_fan_chart(payload)
    except AdvancedStatisticalGraphicsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/advanced-statistical-uncertainty-graphics/posterior/figure")
def advanced_statistical_uncertainty_graphics_posterior(payload: dict):
    try: return advanced_stats_posterior(payload)
    except AdvancedStatisticalGraphicsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/advanced-statistical-uncertainty-graphics/coefficients/forest")
def advanced_statistical_uncertainty_graphics_coefficients(payload: dict):
    try: return advanced_stats_coefficients(payload)
    except AdvancedStatisticalGraphicsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/advanced-statistical-uncertainty-graphics/calibration/reliability")
def advanced_statistical_uncertainty_graphics_calibration(payload: dict):
    try: return advanced_stats_calibration(payload)
    except AdvancedStatisticalGraphicsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/advanced-statistical-uncertainty-graphics/diagnostics/residuals")
def advanced_statistical_uncertainty_graphics_residuals(payload: dict):
    try: return advanced_stats_residuals(payload)
    except AdvancedStatisticalGraphicsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/advanced-statistical-uncertainty-graphics/qq/figure")
def advanced_statistical_uncertainty_graphics_qq(payload: dict):
    try: return advanced_stats_qq(payload)
    except AdvancedStatisticalGraphicsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/advanced-statistical-uncertainty-graphics/sensitivity/figure")
def advanced_statistical_uncertainty_graphics_sensitivity(payload: dict):
    try: return advanced_stats_sensitivity(payload)
    except AdvancedStatisticalGraphicsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/advanced-statistical-uncertainty-graphics/uncertainty/decomposition")
def advanced_statistical_uncertainty_graphics_decomposition(payload: dict):
    try: return advanced_stats_decomposition(payload)
    except AdvancedStatisticalGraphicsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/advanced-statistical-uncertainty-graphics/coverage/figure")
def advanced_statistical_uncertainty_graphics_coverage(payload: dict):
    try: return advanced_stats_coverage(payload)
    except AdvancedStatisticalGraphicsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/advanced-statistical-uncertainty-graphics/small-multiples/compose")
def advanced_statistical_uncertainty_graphics_small_multiples(payload: dict):
    try: return advanced_stats_small_multiples(payload)
    except AdvancedStatisticalGraphicsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/advanced-statistical-uncertainty-graphics/publication/build")
def advanced_statistical_uncertainty_graphics_publication(payload: dict):
    try: return advanced_stats_publication(payload)
    except AdvancedStatisticalGraphicsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)


# Lab v0.116.0 — Interactive Scientific Dashboards & Small Multiples
@app.get("/v1/interactive-scientific-dashboards/health")
def interactive_scientific_dashboards_health(): return interactive_dashboards_health()

@app.get("/v1/interactive-scientific-dashboards/manifest")
def interactive_scientific_dashboards_manifest(): return interactive_dashboards_manifest()

@app.get("/v1/interactive-scientific-dashboards/catalog")
def interactive_scientific_dashboards_catalog(): return interactive_dashboards_catalog()

@app.get("/v1/interactive-scientific-dashboards/schema")
def interactive_scientific_dashboards_schema(): return interactive_dashboards_schema()

@app.post("/v1/interactive-scientific-dashboards/dashboard/normalize")
def interactive_scientific_dashboards_normalize(payload: dict):
    try: return interactive_dashboards_normalize(payload)
    except InteractiveDashboardError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/interactive-scientific-dashboards/controls/normalize")
def interactive_scientific_dashboards_control(payload: dict):
    try: return {"ok":True,"control":interactive_dashboards_normalize_control(payload)}
    except InteractiveDashboardError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/interactive-scientific-dashboards/links/normalize")
def interactive_scientific_dashboards_link(payload: dict):
    try: return {"ok":True,"link":interactive_dashboards_normalize_link(payload)}
    except InteractiveDashboardError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/interactive-scientific-dashboards/small-multiples/compose")
def interactive_scientific_dashboards_small_multiples(payload: dict):
    try: return interactive_dashboards_small_multiples(payload)
    except InteractiveDashboardError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/interactive-scientific-dashboards/interactions/propagate")
def interactive_scientific_dashboards_interaction(payload: dict):
    try: return interactive_dashboards_propagate(payload)
    except InteractiveDashboardError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/interactive-scientific-dashboards/filters/state")
def interactive_scientific_dashboards_filters(payload: dict):
    try: return interactive_dashboards_filter_state(payload)
    except InteractiveDashboardError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/interactive-scientific-dashboards/scales/synchronize")
def interactive_scientific_dashboards_scales(payload: dict):
    try: return interactive_dashboards_sync_scales(payload)
    except InteractiveDashboardError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/interactive-scientific-dashboards/state/snapshot")
def interactive_scientific_dashboards_snapshot(payload: dict):
    try: return interactive_dashboards_snapshot(payload)
    except InteractiveDashboardError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/interactive-scientific-dashboards/state/restore-plan")
def interactive_scientific_dashboards_restore(payload: dict):
    try: return interactive_dashboards_restore_plan(payload)
    except InteractiveDashboardError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/interactive-scientific-dashboards/provenance/trace")
def interactive_scientific_dashboards_provenance(payload: dict):
    try: return interactive_dashboards_provenance(payload)
    except InteractiveDashboardError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/interactive-scientific-dashboards/accessibility/audit")
def interactive_scientific_dashboards_accessibility(payload: dict):
    try: return interactive_dashboards_accessibility(payload)
    except InteractiveDashboardError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/interactive-scientific-dashboards/export/plan")
def interactive_scientific_dashboards_export(payload: dict):
    try: return interactive_dashboards_export_plan(payload)
    except InteractiveDashboardError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/interactive-scientific-dashboards/publication/build")
def interactive_scientific_dashboards_publication(payload: dict):
    try: return interactive_dashboards_publication(payload)
    except InteractiveDashboardError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/interactive-scientific-dashboards/core-visual/plan")
def interactive_scientific_dashboards_core_visual(payload: dict):
    try: return interactive_dashboards_core_visual_plan(payload)
    except InteractiveDashboardError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

# Lab v0.117.0 — Advanced 3D/4D Scientific Visualization
@app.get("/v1/advanced-3d-4d-scientific-visualization/health")
def advanced_3d_4d_visualization_health(): return advanced_3d_health()
@app.get("/v1/advanced-3d-4d-scientific-visualization/manifest")
def advanced_3d_4d_visualization_manifest(): return advanced_3d_manifest()
@app.get("/v1/advanced-3d-4d-scientific-visualization/catalog")
def advanced_3d_4d_visualization_catalog(): return advanced_3d_catalog()
@app.get("/v1/advanced-3d-4d-scientific-visualization/schema")
def advanced_3d_4d_visualization_schema(): return advanced_3d_schema()
@app.post("/v1/advanced-3d-4d-scientific-visualization/scene/normalize")
def advanced_3d_4d_visualization_scene(payload: dict):
    try: return advanced_3d_normalize(payload)
    except AdvancedScientificVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/advanced-3d-4d-scientific-visualization/surface/figure")
def advanced_3d_4d_visualization_surface(payload: dict):
    try: return advanced_3d_surface(payload)
    except AdvancedScientificVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/advanced-3d-4d-scientific-visualization/mesh/figure")
def advanced_3d_4d_visualization_mesh(payload: dict):
    try: return advanced_3d_mesh(payload)
    except AdvancedScientificVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/advanced-3d-4d-scientific-visualization/vector-field/figure")
def advanced_3d_4d_visualization_vector(payload: dict):
    try: return advanced_3d_vector_field(payload)
    except AdvancedScientificVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/advanced-3d-4d-scientific-visualization/scalar-field/figure")
def advanced_3d_4d_visualization_scalar(payload: dict):
    try: return advanced_3d_scalar_field(payload)
    except AdvancedScientificVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/advanced-3d-4d-scientific-visualization/volume/plan")
def advanced_3d_4d_visualization_volume(payload: dict):
    try: return advanced_3d_volume(payload)
    except AdvancedScientificVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/advanced-3d-4d-scientific-visualization/trajectory/figure")
def advanced_3d_4d_visualization_trajectory(payload: dict):
    try: return advanced_3d_trajectory(payload)
    except AdvancedScientificVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/advanced-3d-4d-scientific-visualization/time/frames")
def advanced_3d_4d_visualization_frames(payload: dict):
    try: return advanced_3d_temporal_frames(payload)
    except AdvancedScientificVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/advanced-3d-4d-scientific-visualization/slice/plan")
def advanced_3d_4d_visualization_slice(payload: dict):
    try: return advanced_3d_slice(payload)
    except AdvancedScientificVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/advanced-3d-4d-scientific-visualization/isosurface/plan")
def advanced_3d_4d_visualization_isosurface(payload: dict):
    try: return advanced_3d_isosurface(payload)
    except AdvancedScientificVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/advanced-3d-4d-scientific-visualization/camera/state")
def advanced_3d_4d_visualization_camera(payload: dict):
    try: return {"ok":True,"camera":advanced_3d_camera(payload)}
    except AdvancedScientificVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/advanced-3d-4d-scientific-visualization/uncertainty/geometry")
def advanced_3d_4d_visualization_uncertainty(payload: dict):
    try: return advanced_3d_uncertainty(payload)
    except AdvancedScientificVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/advanced-3d-4d-scientific-visualization/renderer/plan")
def advanced_3d_4d_visualization_renderer(payload: dict):
    try: return advanced_3d_renderer(payload)
    except AdvancedScientificVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/advanced-3d-4d-scientific-visualization/publication/export")
def advanced_3d_4d_visualization_publication(payload: dict):
    try: return advanced_3d_publication(payload)
    except AdvancedScientificVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/advanced-3d-4d-scientific-visualization/dashboard/panel")
def advanced_3d_4d_visualization_dashboard(payload: dict):
    try: return advanced_3d_dashboard_panel(payload)
    except AdvancedScientificVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/advanced-3d-4d-scientific-visualization/core-visual/plan")
def advanced_3d_4d_visualization_core(payload: dict):
    try: return advanced_3d_core_visual(payload)
    except AdvancedScientificVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/advanced-3d-4d-scientific-visualization/accessibility/audit")
def advanced_3d_4d_visualization_accessibility(payload: dict):
    try: return advanced_3d_accessibility(payload)
    except AdvancedScientificVisualizationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)



# Lab v0.118.0 — Visual Research Narrative & Figure Composer
@app.get("/v1/visual-research-narrative-figure-composer/health")
def visual_research_narrative_health(): return narrative_health()
@app.get("/v1/visual-research-narrative-figure-composer/manifest")
def visual_research_narrative_manifest(): return narrative_manifest()
@app.get("/v1/visual-research-narrative-figure-composer/catalog")
def visual_research_narrative_catalog(): return narrative_catalog()
@app.get("/v1/visual-research-narrative-figure-composer/schema")
def visual_research_narrative_schema(): return narrative_schema()
@app.post("/v1/visual-research-narrative-figure-composer/narrative/normalize")
def visual_research_narrative_normalize(payload: dict):
    try: return narrative_normalize(payload)
    except VisualResearchNarrativeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/visual-research-narrative-figure-composer/section/normalize")
def visual_research_narrative_section(payload: dict):
    try: return {"ok":True,"section":narrative_section(payload)}
    except VisualResearchNarrativeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/visual-research-narrative-figure-composer/figure/normalize")
def visual_research_narrative_figure(payload: dict):
    try: return {"ok":True,"figure":narrative_figure(payload)}
    except VisualResearchNarrativeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/visual-research-narrative-figure-composer/figure-plate/build")
def visual_research_narrative_figure_plate(payload: dict):
    try: return narrative_figure_plate(payload)
    except VisualResearchNarrativeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/visual-research-narrative-figure-composer/caption/build")
def visual_research_narrative_caption(payload: dict):
    try: return narrative_caption(payload)
    except VisualResearchNarrativeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/visual-research-narrative-figure-composer/annotations/build")
def visual_research_narrative_annotations(payload: dict):
    try: return narrative_annotations(payload)
    except VisualResearchNarrativeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/visual-research-narrative-figure-composer/research-links/build")
def visual_research_narrative_links(payload: dict):
    try: return narrative_links(payload)
    except VisualResearchNarrativeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/visual-research-narrative-figure-composer/references/build")
def visual_research_narrative_references(payload: dict):
    try: return narrative_references(payload)
    except VisualResearchNarrativeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/visual-research-narrative-figure-composer/provenance/trace")
def visual_research_narrative_provenance(payload: dict):
    try: return narrative_provenance(payload)
    except VisualResearchNarrativeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/visual-research-narrative-figure-composer/layout/plan")
def visual_research_narrative_layout(payload: dict):
    try: return narrative_layout(payload)
    except VisualResearchNarrativeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/visual-research-narrative-figure-composer/export/plan")
def visual_research_narrative_export(payload: dict):
    try: return narrative_export(payload)
    except VisualResearchNarrativeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/visual-research-narrative-figure-composer/publication/package")
def visual_research_narrative_publication(payload: dict):
    try: return narrative_publication(payload)
    except VisualResearchNarrativeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/visual-research-narrative-figure-composer/revision/snapshot")
def visual_research_narrative_snapshot(payload: dict):
    try: return narrative_snapshot(payload)
    except VisualResearchNarrativeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/visual-research-narrative-figure-composer/core-visual/plan")
def visual_research_narrative_core_visual(payload: dict):
    try: return narrative_core_visual(payload)
    except VisualResearchNarrativeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/visual-research-narrative-figure-composer/accessibility/audit")
def visual_research_narrative_accessibility(payload: dict):
    try: return narrative_accessibility(payload)
    except VisualResearchNarrativeError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)


# Lab v0.119.0 — Scientific Figure Intelligence & Automatic Layout
@app.get("/v1/scientific-figure-intelligence-automatic-layout/health")
def scientific_figure_layout_health(): return figure_layout_health()
@app.get("/v1/scientific-figure-intelligence-automatic-layout/manifest")
def scientific_figure_layout_manifest(): return figure_layout_manifest()
@app.get("/v1/scientific-figure-intelligence-automatic-layout/catalog")
def scientific_figure_layout_catalog(): return figure_layout_catalog()
@app.get("/v1/scientific-figure-intelligence-automatic-layout/schema")
def scientific_figure_layout_schema(): return figure_layout_schema()
@app.post("/v1/scientific-figure-intelligence-automatic-layout/figure/normalize")
def scientific_figure_layout_normalize(payload: dict):
    try: return {"ok":True,"figure":figure_layout_normalize(payload)}
    except FigureLayoutIntelligenceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-figure-intelligence-automatic-layout/figure/analyze")
def scientific_figure_layout_analyze_route(payload: dict):
    try: return figure_layout_analyze(payload)
    except FigureLayoutIntelligenceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-figure-intelligence-automatic-layout/figure/intelligence")
def scientific_figure_layout_intelligence_route(payload: dict):
    try: return figure_layout_intelligence(payload)
    except FigureLayoutIntelligenceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-figure-intelligence-automatic-layout/layout/plan")
def scientific_figure_layout_plan_route(payload: dict):
    try: return figure_layout_plan(payload)
    except FigureLayoutIntelligenceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-figure-intelligence-automatic-layout/axes/plan")
def scientific_figure_layout_axes_route(payload: dict):
    try: return figure_layout_axes(payload)
    except FigureLayoutIntelligenceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-figure-intelligence-automatic-layout/legend/plan")
def scientific_figure_layout_legend_route(payload: dict):
    try: return figure_layout_legend(payload)
    except FigureLayoutIntelligenceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-figure-intelligence-automatic-layout/panels/plan")
def scientific_figure_layout_panels_route(payload: dict):
    try: return figure_layout_panels(payload)
    except FigureLayoutIntelligenceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-figure-intelligence-automatic-layout/annotations/plan")
def scientific_figure_layout_annotations_route(payload: dict):
    try: return figure_layout_annotations(payload)
    except FigureLayoutIntelligenceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-figure-intelligence-automatic-layout/collisions/audit")
def scientific_figure_layout_collisions_route(payload: dict):
    try: return figure_layout_collision_audit(payload)
    except FigureLayoutIntelligenceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-figure-intelligence-automatic-layout/responsive/plan")
def scientific_figure_layout_responsive_route(payload: dict):
    try: return figure_layout_responsive(payload)
    except FigureLayoutIntelligenceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-figure-intelligence-automatic-layout/print/plan")
def scientific_figure_layout_print_route(payload: dict):
    try: return figure_layout_print(payload)
    except FigureLayoutIntelligenceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-figure-intelligence-automatic-layout/quality/audit")
def scientific_figure_layout_quality_route(payload: dict):
    try: return figure_layout_quality(payload)
    except FigureLayoutIntelligenceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-figure-intelligence-automatic-layout/accessibility/audit")
def scientific_figure_layout_accessibility_route(payload: dict):
    try: return figure_layout_accessibility(payload)
    except FigureLayoutIntelligenceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-figure-intelligence-automatic-layout/layout/explain")
def scientific_figure_layout_explain_route(payload: dict):
    try: return figure_layout_explain(payload)
    except FigureLayoutIntelligenceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-figure-intelligence-automatic-layout/export/plan")
def scientific_figure_layout_export_route(payload: dict):
    try: return figure_layout_export(payload)
    except FigureLayoutIntelligenceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-figure-intelligence-automatic-layout/snapshot/build")
def scientific_figure_layout_snapshot_route(payload: dict):
    try: return figure_layout_snapshot(payload)
    except FigureLayoutIntelligenceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-figure-intelligence-automatic-layout/core-visual/plan")
def scientific_figure_layout_core_route(payload: dict):
    try: return figure_layout_core_visual(payload)
    except FigureLayoutIntelligenceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)


# Lab v0.120.0 — Exploratory Data Analysis Studio
@app.get("/v1/exploratory-data-analysis-studio/health")
def exploratory_data_analysis_health(): return eda_health()
@app.get("/v1/exploratory-data-analysis-studio/manifest")
def exploratory_data_analysis_manifest(): return eda_manifest()
@app.get("/v1/exploratory-data-analysis-studio/catalog")
def exploratory_data_analysis_catalog(): return eda_catalog()
@app.get("/v1/exploratory-data-analysis-studio/schema")
def exploratory_data_analysis_schema(): return eda_schema()
@app.post("/v1/exploratory-data-analysis-studio/dataset/normalize")
def exploratory_data_analysis_normalize(payload: dict):
    try: return {"ok":True,"dataset":eda_normalize(payload)}
    except EDAStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/exploratory-data-analysis-studio/profile/build")
def exploratory_data_analysis_profile(payload: dict):
    try: return eda_profile(payload)
    except EDAStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/exploratory-data-analysis-studio/missingness/analyze")
def exploratory_data_analysis_missingness(payload: dict):
    try: return eda_missingness(payload)
    except EDAStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/exploratory-data-analysis-studio/distributions/analyze")
def exploratory_data_analysis_distributions(payload: dict):
    try: return eda_distributions(payload)
    except EDAStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/exploratory-data-analysis-studio/correlations/analyze")
def exploratory_data_analysis_correlations(payload: dict):
    try: return eda_correlations(payload)
    except EDAStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/exploratory-data-analysis-studio/groups/compare")
def exploratory_data_analysis_groups(payload: dict):
    try: return eda_groups(payload)
    except EDAStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/exploratory-data-analysis-studio/outliers/analyze")
def exploratory_data_analysis_outliers(payload: dict):
    try: return eda_outliers(payload)
    except EDAStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/exploratory-data-analysis-studio/relationship/analyze")
def exploratory_data_analysis_relationship(payload: dict):
    try: return eda_relationship(payload)
    except EDAStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/exploratory-data-analysis-studio/transformations/plan")
def exploratory_data_analysis_transform_plan(payload: dict):
    try: return eda_transform_plan(payload)
    except EDAStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/exploratory-data-analysis-studio/transformations/preview")
def exploratory_data_analysis_transform_preview(payload: dict):
    try: return eda_transform_preview(payload)
    except EDAStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/exploratory-data-analysis-studio/dimensions/pca")
def exploratory_data_analysis_pca(payload: dict):
    try: return eda_pca(payload)
    except EDAStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/exploratory-data-analysis-studio/visualization/plan")
def exploratory_data_analysis_visualization(payload: dict):
    try: return eda_visualization(payload)
    except EDAStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/exploratory-data-analysis-studio/studio/build")
def exploratory_data_analysis_studio_route(payload: dict):
    try: return eda_studio(payload)
    except EDAStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/exploratory-data-analysis-studio/snapshot/build")
def exploratory_data_analysis_snapshot(payload: dict):
    try: return eda_snapshot(payload)
    except EDAStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/exploratory-data-analysis-studio/export/plan")
def exploratory_data_analysis_export(payload: dict):
    try: return eda_export(payload)
    except EDAStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/exploratory-data-analysis-studio/core-object/plan")
def exploratory_data_analysis_core_object(payload: dict):
    try: return eda_core_object(payload)
    except EDAStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)


# Lab v0.121.0 — Statistical Modeling & Model Diagnostics Studio
@app.get("/v1/statistical-modeling-diagnostics-studio/health")
def statistical_modeling_diagnostics_health(): return model_diag_health()
@app.get("/v1/statistical-modeling-diagnostics-studio/manifest")
def statistical_modeling_diagnostics_manifest(): return model_diag_manifest()
@app.get("/v1/statistical-modeling-diagnostics-studio/catalog")
def statistical_modeling_diagnostics_catalog(): return model_diag_catalog()
@app.get("/v1/statistical-modeling-diagnostics-studio/schema")
def statistical_modeling_diagnostics_schema(): return model_diag_schema()
@app.post("/v1/statistical-modeling-diagnostics-studio/model/normalize")
def statistical_modeling_diagnostics_normalize(payload: dict):
    try: return model_diag_normalize(payload)
    except StatisticalModelingDiagnosticsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/statistical-modeling-diagnostics-studio/model/fit")
def statistical_modeling_diagnostics_fit(payload: dict):
    try: return model_diag_fit(payload)
    except StatisticalModelingDiagnosticsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/statistical-modeling-diagnostics-studio/coefficients/report")
def statistical_modeling_diagnostics_coefficients(payload: dict):
    try: return model_diag_coefficients(payload)
    except StatisticalModelingDiagnosticsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/statistical-modeling-diagnostics-studio/diagnostics/analyze")
def statistical_modeling_diagnostics_analyze(payload: dict):
    try: return model_diag_diagnose(payload)
    except StatisticalModelingDiagnosticsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/statistical-modeling-diagnostics-studio/assumptions/audit")
def statistical_modeling_diagnostics_assumptions(payload: dict):
    try: return model_diag_assumptions(payload)
    except StatisticalModelingDiagnosticsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/statistical-modeling-diagnostics-studio/effects/report")
def statistical_modeling_diagnostics_effects(payload: dict):
    try: return model_diag_effects(payload)
    except StatisticalModelingDiagnosticsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/statistical-modeling-diagnostics-studio/predictions/evaluate")
def statistical_modeling_diagnostics_evaluate(payload: dict):
    try: return model_diag_evaluate(payload)
    except StatisticalModelingDiagnosticsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/statistical-modeling-diagnostics-studio/cross-validation/run")
def statistical_modeling_diagnostics_cross_validation(payload: dict):
    try: return model_diag_cross_validate(payload)
    except StatisticalModelingDiagnosticsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/statistical-modeling-diagnostics-studio/models/compare")
def statistical_modeling_diagnostics_compare(payload: dict):
    try: return model_diag_compare(payload)
    except StatisticalModelingDiagnosticsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/statistical-modeling-diagnostics-studio/visualization/plan")
def statistical_modeling_diagnostics_visualization(payload: dict):
    try: return model_diag_visualization(payload)
    except StatisticalModelingDiagnosticsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/statistical-modeling-diagnostics-studio/studio/build")
def statistical_modeling_diagnostics_studio_route(payload: dict):
    try: return model_diag_studio(payload)
    except StatisticalModelingDiagnosticsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/statistical-modeling-diagnostics-studio/snapshot/build")
def statistical_modeling_diagnostics_snapshot(payload: dict):
    try: return model_diag_snapshot(payload)
    except StatisticalModelingDiagnosticsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/statistical-modeling-diagnostics-studio/export/plan")
def statistical_modeling_diagnostics_export(payload: dict):
    try: return model_diag_export(payload)
    except StatisticalModelingDiagnosticsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/statistical-modeling-diagnostics-studio/core-object/plan")
def statistical_modeling_diagnostics_core_object(payload: dict):
    try: return model_diag_core_object(payload)
    except StatisticalModelingDiagnosticsError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)


# Lab v0.122.0 — Bayesian Analysis Workbench II
@app.get("/v1/bayesian-analysis-workbench/health")
def bayesian_analysis_workbench_health(): return bayes2_health()
@app.get("/v1/bayesian-analysis-workbench/manifest")
def bayesian_analysis_workbench_manifest(): return bayes2_manifest()
@app.get("/v1/bayesian-analysis-workbench/catalog")
def bayesian_analysis_workbench_catalog(): return bayes2_catalog()
@app.get("/v1/bayesian-analysis-workbench/schema")
def bayesian_analysis_workbench_schema(): return bayes2_schema()
@app.post("/v1/bayesian-analysis-workbench/analysis/normalize")
def bayesian_analysis_workbench_normalize(payload: dict):
    try: return bayes2_normalize(payload)
    except BayesianWorkbenchError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/bayesian-analysis-workbench/posterior/fit")
def bayesian_analysis_workbench_fit(payload: dict):
    try: return bayes2_fit(payload)
    except BayesianWorkbenchError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/bayesian-analysis-workbench/prior-posterior/report")
def bayesian_analysis_workbench_prior_posterior(payload: dict):
    try: return bayes2_prior_posterior(payload)
    except BayesianWorkbenchError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/bayesian-analysis-workbench/diagnostics/sampler")
def bayesian_analysis_workbench_diagnostics(payload: dict):
    try: return bayes2_diagnostics(payload)
    except BayesianWorkbenchError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/bayesian-analysis-workbench/convergence/audit")
def bayesian_analysis_workbench_convergence(payload: dict):
    try: return bayes2_convergence(payload)
    except BayesianWorkbenchError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/bayesian-analysis-workbench/posterior-predictive/report")
def bayesian_analysis_workbench_ppc(payload: dict):
    try: return bayes2_ppc(payload)
    except BayesianWorkbenchError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/bayesian-analysis-workbench/probability/report")
def bayesian_analysis_workbench_probability(payload: dict):
    try: return bayes2_probability(payload)
    except BayesianWorkbenchError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/bayesian-analysis-workbench/hierarchical/fit")
def bayesian_analysis_workbench_hierarchical_fit(payload: dict):
    try: return bayes2_hierarchical_fit(payload)
    except BayesianWorkbenchError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/bayesian-analysis-workbench/hierarchical/summary")
def bayesian_analysis_workbench_hierarchical_summary(payload: dict):
    try: return bayes2_hierarchical_summary(payload)
    except BayesianWorkbenchError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/bayesian-analysis-workbench/models/compare")
def bayesian_analysis_workbench_compare(payload: dict):
    try: return bayes2_compare(payload)
    except BayesianWorkbenchError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/bayesian-analysis-workbench/visualization/plan")
def bayesian_analysis_workbench_visualization(payload: dict):
    try: return bayes2_visualization(payload)
    except BayesianWorkbenchError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/bayesian-analysis-workbench/workbench/build")
def bayesian_analysis_workbench_build(payload: dict):
    try: return bayes2_workbench(payload)
    except BayesianWorkbenchError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/bayesian-analysis-workbench/snapshot/build")
def bayesian_analysis_workbench_snapshot(payload: dict):
    try: return bayes2_snapshot(payload)
    except BayesianWorkbenchError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/bayesian-analysis-workbench/reproduction/plan")
def bayesian_analysis_workbench_reproduction(payload: dict):
    try: return bayes2_reproduction(payload)
    except BayesianWorkbenchError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/bayesian-analysis-workbench/export/plan")
def bayesian_analysis_workbench_export(payload: dict):
    try: return bayes2_export(payload)
    except BayesianWorkbenchError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/bayesian-analysis-workbench/core-object/plan")
def bayesian_analysis_workbench_core_object(payload: dict):
    try: return bayes2_core_object(payload)
    except BayesianWorkbenchError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/simulation-monte-carlo-research-studio/health")
def simulation_monte_carlo_health(): return sim_mc_health()
@app.get("/v1/simulation-monte-carlo-research-studio/manifest")
def simulation_monte_carlo_manifest(): return sim_mc_manifest()
@app.get("/v1/simulation-monte-carlo-research-studio/catalog")
def simulation_monte_carlo_catalog(): return sim_mc_catalog()
@app.get("/v1/simulation-monte-carlo-research-studio/schema")
def simulation_monte_carlo_schema(): return sim_mc_schema()
@app.post("/v1/simulation-monte-carlo-research-studio/study/normalize")
def simulation_monte_carlo_normalize(payload: dict):
    try: return sim_mc_normalize(payload)
    except SimulationStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/simulation-monte-carlo-research-studio/sampling/plan")
def simulation_monte_carlo_sampling(payload: dict):
    try: return sim_mc_sampling(payload)
    except SimulationStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/simulation-monte-carlo-research-studio/compute-budget/plan")
def simulation_monte_carlo_budget(payload: dict):
    try: return sim_mc_budget(payload)
    except SimulationStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/simulation-monte-carlo-research-studio/simulation/run")
def simulation_monte_carlo_run(payload: dict):
    try: return sim_mc_run(payload)
    except SimulationStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/simulation-monte-carlo-research-studio/convergence/report")
def simulation_monte_carlo_convergence(payload: dict):
    try: return sim_mc_convergence(payload)
    except SimulationStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/simulation-monte-carlo-research-studio/replication/report")
def simulation_monte_carlo_replication(payload: dict):
    try: return sim_mc_replication(payload)
    except SimulationStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/simulation-monte-carlo-research-studio/parameter-sweep/plan")
def simulation_monte_carlo_sweep_plan(payload: dict):
    try: return sim_mc_sweep_plan(payload)
    except SimulationStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/simulation-monte-carlo-research-studio/parameter-sweep/run")
def simulation_monte_carlo_sweep_run(payload: dict):
    try: return sim_mc_sweep_run(payload)
    except SimulationStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/simulation-monte-carlo-research-studio/scenario-ensemble/run")
def simulation_monte_carlo_scenarios(payload: dict):
    try: return sim_mc_scenarios(payload)
    except SimulationStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/simulation-monte-carlo-research-studio/uncertainty-propagation/run")
def simulation_monte_carlo_uncertainty(payload: dict):
    try: return sim_mc_uncertainty(payload)
    except SimulationStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/simulation-monte-carlo-research-studio/threshold/report")
def simulation_monte_carlo_threshold(payload: dict):
    try: return sim_mc_threshold(payload)
    except SimulationStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/simulation-monte-carlo-research-studio/visualization/plan")
def simulation_monte_carlo_visualization(payload: dict):
    try: return sim_mc_visualization(payload)
    except SimulationStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/simulation-monte-carlo-research-studio/studio/build")
def simulation_monte_carlo_studio(payload: dict):
    try: return sim_mc_studio(payload)
    except SimulationStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/simulation-monte-carlo-research-studio/snapshot/build")
def simulation_monte_carlo_snapshot(payload: dict):
    try: return sim_mc_snapshot(payload)
    except SimulationStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/simulation-monte-carlo-research-studio/reproduction/plan")
def simulation_monte_carlo_reproduction(payload: dict):
    try: return sim_mc_reproduction(payload)
    except SimulationStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/simulation-monte-carlo-research-studio/export/plan")
def simulation_monte_carlo_export(payload: dict):
    try: return sim_mc_export(payload)
    except SimulationStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/simulation-monte-carlo-research-studio/core-object/plan")
def simulation_monte_carlo_core_object(payload: dict):
    try: return sim_mc_core_object(payload)
    except SimulationStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/simulation-monte-carlo-research-studio/execution-lineage/plan")
def simulation_monte_carlo_lineage(payload: dict):
    try: return sim_mc_lineage(payload)
    except SimulationStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/sensitivity-global-uncertainty-analysis-studio/health")
def sensitivity_global_uncertainty_health(): return sens_health()
@app.get("/v1/sensitivity-global-uncertainty-analysis-studio/manifest")
def sensitivity_global_uncertainty_manifest(): return sens_manifest()
@app.get("/v1/sensitivity-global-uncertainty-analysis-studio/catalog")
def sensitivity_global_uncertainty_catalog(): return sens_catalog()
@app.get("/v1/sensitivity-global-uncertainty-analysis-studio/schema")
def sensitivity_global_uncertainty_schema(): return sens_schema()
@app.post("/v1/sensitivity-global-uncertainty-analysis-studio/study/normalize")
def sensitivity_global_uncertainty_normalize(payload: dict):
    try: return sens_normalize(payload)
    except SensitivityStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/sensitivity-global-uncertainty-analysis-studio/sobol/report")
def sensitivity_global_uncertainty_sobol(payload: dict):
    try: return sens_sobol(payload)
    except SensitivityStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/sensitivity-global-uncertainty-analysis-studio/morris/report")
def sensitivity_global_uncertainty_morris(payload: dict):
    try: return sens_morris(payload)
    except SensitivityStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/sensitivity-global-uncertainty-analysis-studio/correlation-screening/report")
def sensitivity_global_uncertainty_correlation(payload: dict):
    try: return sens_correlation(payload)
    except SensitivityStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/sensitivity-global-uncertainty-analysis-studio/variance-decomposition/report")
def sensitivity_global_uncertainty_variance(payload: dict):
    try: return sens_variance(payload)
    except SensitivityStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/sensitivity-global-uncertainty-analysis-studio/interaction-screening/report")
def sensitivity_global_uncertainty_interactions(payload: dict):
    try: return sens_interactions(payload)
    except SensitivityStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/sensitivity-global-uncertainty-analysis-studio/response-surface/plan")
def sensitivity_global_uncertainty_surface_plan(payload: dict):
    try: return sens_surface_plan(payload)
    except SensitivityStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/sensitivity-global-uncertainty-analysis-studio/response-surface/run")
def sensitivity_global_uncertainty_surface_run(payload: dict):
    try: return sens_surface_run(payload)
    except SensitivityStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/sensitivity-global-uncertainty-analysis-studio/convergence/report")
def sensitivity_global_uncertainty_convergence(payload: dict):
    try: return sens_convergence(payload)
    except SensitivityStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/sensitivity-global-uncertainty-analysis-studio/replication/report")
def sensitivity_global_uncertainty_replication(payload: dict):
    try: return sens_replication(payload)
    except SensitivityStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/sensitivity-global-uncertainty-analysis-studio/visualization/plan")
def sensitivity_global_uncertainty_visualization(payload: dict):
    try: return sens_visualization(payload)
    except SensitivityStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/sensitivity-global-uncertainty-analysis-studio/studio/build")
def sensitivity_global_uncertainty_studio(payload: dict):
    try: return sens_studio(payload)
    except SensitivityStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/sensitivity-global-uncertainty-analysis-studio/snapshot/build")
def sensitivity_global_uncertainty_snapshot(payload: dict):
    try: return sens_snapshot(payload)
    except SensitivityStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/sensitivity-global-uncertainty-analysis-studio/reproduction/plan")
def sensitivity_global_uncertainty_reproduction(payload: dict):
    try: return sens_reproduction(payload)
    except SensitivityStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/sensitivity-global-uncertainty-analysis-studio/export/plan")
def sensitivity_global_uncertainty_export(payload: dict):
    try: return sens_export(payload)
    except SensitivityStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/sensitivity-global-uncertainty-analysis-studio/core-object/plan")
def sensitivity_global_uncertainty_core_object(payload: dict):
    try: return sens_core_object(payload)
    except SensitivityStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/sensitivity-global-uncertainty-analysis-studio/execution-lineage/plan")
def sensitivity_global_uncertainty_lineage(payload: dict):
    try: return sens_lineage(payload)
    except SensitivityStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/sensitivity-global-uncertainty-analysis-studio/interpretation-boundaries/report")
def sensitivity_global_uncertainty_boundaries(payload: dict):
    try: return sens_boundaries(payload)
    except SensitivityStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.get("/v1/causal-research-studio/health")
def causal_research_studio_health(): return causal2_health()
@app.get("/v1/causal-research-studio/manifest")
def causal_research_studio_manifest(): return causal2_manifest()
@app.get("/v1/causal-research-studio/catalog")
def causal_research_studio_catalog(): return causal2_catalog()
@app.get("/v1/causal-research-studio/schema")
def causal_research_studio_schema(): return causal2_schema()
@app.post("/v1/causal-research-studio/study/normalize")
def causal_research_studio_normalize(payload: dict):
    try: return causal2_normalize(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/dag/normalize")
def causal_research_studio_dag(payload: dict):
    try: return causal2_dag(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/dag/adjustment-plan")
def causal_research_studio_adjustment(payload: dict):
    try: return causal2_adjustment(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/propensity/fit")
def causal_research_studio_propensity(payload: dict):
    try: return causal2_propensity(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/matching/estimate")
def causal_research_studio_matching(payload: dict):
    try: return causal2_matching(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/weighting/estimate")
def causal_research_studio_weighting(payload: dict):
    try: return causal2_weighting(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/balance/report")
def causal_research_studio_balance(payload: dict):
    try: return causal2_balance(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/overlap/report")
def causal_research_studio_overlap(payload: dict):
    try: return causal2_overlap(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/did/estimate")
def causal_research_studio_did(payload: dict):
    try: return causal2_did(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/its/estimate")
def causal_research_studio_its(payload: dict):
    try: return causal2_its(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/rd/estimate")
def causal_research_studio_rd(payload: dict):
    try: return causal2_rd(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/synthetic-control/estimate")
def causal_research_studio_synthetic(payload: dict):
    try: return causal2_synthetic(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/robustness/plan")
def causal_research_studio_robustness(payload: dict):
    try: return causal2_robustness(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/placebo/plan")
def causal_research_studio_placebo(payload: dict):
    try: return causal2_placebo(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/counterfactual/report")
def causal_research_studio_counterfactual(payload: dict):
    try: return causal2_counterfactual(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/visualization/plan")
def causal_research_studio_visualization(payload: dict):
    try: return causal2_visualization(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/studio/build")
def causal_research_studio_build(payload: dict):
    try: return causal2_studio(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/snapshot/build")
def causal_research_studio_snapshot(payload: dict):
    try: return causal2_snapshot(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/reproduction/plan")
def causal_research_studio_reproduction(payload: dict):
    try: return causal2_reproduction(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/export/plan")
def causal_research_studio_export(payload: dict):
    try: return causal2_export(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/core-object/plan")
def causal_research_studio_core(payload: dict):
    try: return causal2_core_object(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/execution-lineage/plan")
def causal_research_studio_lineage(payload: dict):
    try: return causal2_lineage(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/governance/review")
def causal_research_studio_governance(payload: dict):
    try: return causal2_governance(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/causal-research-studio/interpretation-boundaries/report")
def causal_research_studio_boundaries(payload: dict):
    try: return causal2_boundaries(payload)
    except CausalResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/spatial-spatiotemporal-research-studio/health")
def spatial_spatiotemporal_research_studio_health(): return spatial2_health()
@app.get("/v1/spatial-spatiotemporal-research-studio/manifest")
def spatial_spatiotemporal_research_studio_manifest(): return spatial2_manifest()
@app.get("/v1/spatial-spatiotemporal-research-studio/catalog")
def spatial_spatiotemporal_research_studio_catalog(): return spatial2_catalog()
@app.get("/v1/spatial-spatiotemporal-research-studio/schema")
def spatial_spatiotemporal_research_studio_schema(): return spatial2_schema()
@app.post("/v1/spatial-spatiotemporal-research-studio/study/normalize")
def spatial_spatiotemporal_research_studio_normalize(payload: dict):
    try: return spatial2_normalize(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/crs/audit")
def spatial_spatiotemporal_research_studio_crs(payload: dict):
    try: return spatial2_crs_audit(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/weights/build")
def spatial_spatiotemporal_research_studio_weights(payload: dict):
    try: return spatial2_weights(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/autocorrelation/moran")
def spatial_spatiotemporal_research_studio_moran(payload: dict):
    try: return spatial2_moran(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/autocorrelation/local-moran")
def spatial_spatiotemporal_research_studio_local_moran(payload: dict):
    try: return spatial2_local_moran(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/hotspot/getis-ord")
def spatial_spatiotemporal_research_studio_getis(payload: dict):
    try: return spatial2_gistar(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/nearest-neighbor/report")
def spatial_spatiotemporal_research_studio_nearest(payload: dict):
    try: return spatial2_nearest(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/spatial-lag/estimate")
def spatial_spatiotemporal_research_studio_lag(payload: dict):
    try: return spatial2_lag(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/raster/zonal-stats")
def spatial_spatiotemporal_research_studio_zonal(payload: dict):
    try: return spatial2_zonal(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/raster/change")
def spatial_spatiotemporal_research_studio_raster_change(payload: dict):
    try: return spatial2_raster_change(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/spatial-join/plan")
def spatial_spatiotemporal_research_studio_join(payload: dict):
    try: return spatial2_join(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/spatiotemporal/cube")
def spatial_spatiotemporal_research_studio_cube(payload: dict):
    try: return spatial2_cube(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/spatiotemporal/change-profile")
def spatial_spatiotemporal_research_studio_change_profile(payload: dict):
    try: return spatial2_change_profile(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/spatiotemporal/autocorrelation")
def spatial_spatiotemporal_research_studio_space_time(payload: dict):
    try: return spatial2_space_time(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/trajectory/report")
def spatial_spatiotemporal_research_studio_trajectory(payload: dict):
    try: return spatial2_trajectory(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/hotspot/persistence")
def spatial_spatiotemporal_research_studio_persistence(payload: dict):
    try: return spatial2_persistence(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/visualization/plan")
def spatial_spatiotemporal_research_studio_visualization(payload: dict):
    try: return spatial2_visualization(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/studio/build")
def spatial_spatiotemporal_research_studio_build(payload: dict):
    try: return spatial2_studio(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/snapshot/build")
def spatial_spatiotemporal_research_studio_snapshot(payload: dict):
    try: return spatial2_snapshot(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/reproduction/plan")
def spatial_spatiotemporal_research_studio_reproduction(payload: dict):
    try: return spatial2_reproduction(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/export/plan")
def spatial_spatiotemporal_research_studio_export(payload: dict):
    try: return spatial2_export(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/core-object/plan")
def spatial_spatiotemporal_research_studio_core(payload: dict):
    try: return spatial2_core_object(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/execution-lineage/plan")
def spatial_spatiotemporal_research_studio_lineage(payload: dict):
    try: return spatial2_lineage(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/spatial-spatiotemporal-research-studio/interpretation-boundaries/report")
def spatial_spatiotemporal_research_studio_boundaries(payload: dict):
    try: return spatial2_boundaries(payload)
    except SpatialResearchStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)



@app.get("/v1/scientific-time-series-laboratory/health")
def scientific_time_series_laboratory_health(): return ts_health()
@app.get("/v1/scientific-time-series-laboratory/manifest")
def scientific_time_series_laboratory_manifest(): return ts_manifest()
@app.get("/v1/scientific-time-series-laboratory/catalog")
def scientific_time_series_laboratory_catalog(): return ts_catalog()
@app.get("/v1/scientific-time-series-laboratory/schema")
def scientific_time_series_laboratory_schema(): return ts_schema()
@app.post("/v1/scientific-time-series-laboratory/series/normalize")
def scientific_time_series_laboratory_normalize(payload: dict):
    try: return ts_normalize(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/frequency/audit")
def scientific_time_series_laboratory_frequency(payload: dict):
    try: return ts_frequency(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/decomposition/run")
def scientific_time_series_laboratory_decompose(payload: dict):
    try: return ts_decompose(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/autocorrelation/acf")
def scientific_time_series_laboratory_acf(payload: dict):
    try: return ts_acf(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/autocorrelation/pacf")
def scientific_time_series_laboratory_pacf(payload: dict):
    try: return ts_pacf(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/stationarity/diagnostics")
def scientific_time_series_laboratory_stationarity(payload: dict):
    try: return ts_stationarity(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/transform/difference")
def scientific_time_series_laboratory_difference(payload: dict):
    try: return ts_difference(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/model/ar/fit")
def scientific_time_series_laboratory_ar(payload: dict):
    try: return ts_ar(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/model/arima/fit")
def scientific_time_series_laboratory_arima(payload: dict):
    try: return ts_arima(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/model/exponential-smoothing/fit")
def scientific_time_series_laboratory_ets(payload: dict):
    try: return ts_ets(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/model/state-space/local-linear-trend")
def scientific_time_series_laboratory_state_space(payload: dict):
    try: return ts_state_space(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/forecast/run")
def scientific_time_series_laboratory_forecast(payload: dict):
    try: return ts_forecast(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/evaluation/rolling-origin")
def scientific_time_series_laboratory_rolling(payload: dict):
    try: return ts_rolling(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/diagnostics/residuals")
def scientific_time_series_laboratory_residuals(payload: dict):
    try: return ts_residuals(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/change-point/detect")
def scientific_time_series_laboratory_changepoint(payload: dict):
    try: return ts_changepoint(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/regime/analyze")
def scientific_time_series_laboratory_regime(payload: dict):
    try: return ts_regime(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/anomaly/screen")
def scientific_time_series_laboratory_anomaly(payload: dict):
    try: return ts_anomaly(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/cross-correlation/report")
def scientific_time_series_laboratory_crosscorr(payload: dict):
    try: return ts_crosscorr(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/spectral/analyze")
def scientific_time_series_laboratory_spectral(payload: dict):
    try: return ts_spectral(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/visualization/plan")
def scientific_time_series_laboratory_visualization(payload: dict):
    try: return ts_visualization(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/studio/build")
def scientific_time_series_laboratory_studio(payload: dict):
    try: return ts_studio(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/snapshot/build")
def scientific_time_series_laboratory_snapshot(payload: dict):
    try: return ts_snapshot(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/reproduction/plan")
def scientific_time_series_laboratory_reproduction(payload: dict):
    try: return ts_reproduction(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/export/plan")
def scientific_time_series_laboratory_export(payload: dict):
    try: return ts_export(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/core-object/plan")
def scientific_time_series_laboratory_core(payload: dict):
    try: return ts_core(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/execution-lineage/plan")
def scientific_time_series_laboratory_lineage(payload: dict):
    try: return ts_lineage(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-time-series-laboratory/interpretation-boundaries/report")
def scientific_time_series_laboratory_boundaries(payload: dict):
    try: return ts_boundaries(payload)
    except TimeSeriesLaboratoryError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)


@app.get("/v1/experimental-design-power-analysis/health")
def experimental_design_power_analysis_health(): return exp_health()
@app.get("/v1/experimental-design-power-analysis/manifest")
def experimental_design_power_analysis_manifest(): return exp_manifest()
@app.get("/v1/experimental-design-power-analysis/catalog")
def experimental_design_power_analysis_catalog(): return exp_catalog()
@app.get("/v1/experimental-design-power-analysis/schema")
def experimental_design_power_analysis_schema(): return exp_schema()
@app.post("/v1/experimental-design-power-analysis/design/normalize")
def experimental_design_power_analysis_normalize(payload: dict):
    try: return exp_normalize(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/effect-size/standardize")
def experimental_design_power_analysis_effect(payload: dict):
    try: return exp_effect(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/power/one-sample-mean")
def experimental_design_power_analysis_one_mean(payload: dict):
    try: return exp_one_mean(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/power/two-sample-mean")
def experimental_design_power_analysis_two_mean(payload: dict):
    try: return exp_two_mean(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/power/paired-mean")
def experimental_design_power_analysis_paired(payload: dict):
    try: return exp_paired(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/power/one-proportion")
def experimental_design_power_analysis_one_prop(payload: dict):
    try: return exp_one_prop(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/power/two-proportion")
def experimental_design_power_analysis_two_prop(payload: dict):
    try: return exp_two_prop(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/power/one-way-anova")
def experimental_design_power_analysis_anova(payload: dict):
    try: return exp_anova(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/factorial/plan")
def experimental_design_power_analysis_factorial(payload: dict):
    try: return exp_factorial(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/randomization/blocked-plan")
def experimental_design_power_analysis_blocked(payload: dict):
    try: return exp_blocked(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/cluster/design-effect")
def experimental_design_power_analysis_cluster(payload: dict):
    try: return exp_cluster(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/precision/sample-size")
def experimental_design_power_analysis_precision(payload: dict):
    try: return exp_precision(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/power/simulation")
def experimental_design_power_analysis_simulation(payload: dict):
    try: return exp_simulation(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/sequential/plan")
def experimental_design_power_analysis_sequential(payload: dict):
    try: return exp_sequential(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/adaptive/reestimation-plan")
def experimental_design_power_analysis_adaptive(payload: dict):
    try: return exp_adaptive(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/multiplicity/plan")
def experimental_design_power_analysis_multiplicity(payload: dict):
    try: return exp_multiplicity(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/randomization/schedule")
def experimental_design_power_analysis_randomization(payload: dict):
    try: return exp_randomization(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/design/diagnostics")
def experimental_design_power_analysis_diagnostics(payload: dict):
    try: return exp_diagnostics(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/visualization/plan")
def experimental_design_power_analysis_visualization(payload: dict):
    try: return exp_visualization(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/studio/build")
def experimental_design_power_analysis_studio(payload: dict):
    try: return exp_studio(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/snapshot/build")
def experimental_design_power_analysis_snapshot(payload: dict):
    try: return exp_snapshot(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/reproduction/plan")
def experimental_design_power_analysis_reproduction(payload: dict):
    try: return exp_reproduction(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/export/plan")
def experimental_design_power_analysis_export(payload: dict):
    try: return exp_export(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/core-object/plan")
def experimental_design_power_analysis_core(payload: dict):
    try: return exp_core(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/execution-lineage/plan")
def experimental_design_power_analysis_lineage(payload: dict):
    try: return exp_lineage(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/experimental-design-power-analysis/interpretation-boundaries/report")
def experimental_design_power_analysis_boundaries(payload: dict):
    try: return exp_boundaries(payload)
    except ExperimentalDesignError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)


@app.get("/v1/research-reproduction-replication-studio/health")
def research_reproduction_replication_studio_health(): return rr_health()
@app.get("/v1/research-reproduction-replication-studio/manifest")
def research_reproduction_replication_studio_manifest(): return rr_manifest()
@app.get("/v1/research-reproduction-replication-studio/catalog")
def research_reproduction_replication_studio_catalog(): return rr_catalog()
@app.get("/v1/research-reproduction-replication-studio/schema")
def research_reproduction_replication_studio_schema(): return rr_schema()
@app.post("/v1/research-reproduction-replication-studio/study/normalize")
def research_reproduction_replication_studio_normalize(payload: dict):
    try: return rr_normalize(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/artifacts/inventory")
def research_reproduction_replication_studio_inventory(payload: dict):
    try: return rr_inventory(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/inputs/integrity")
def research_reproduction_replication_studio_integrity(payload: dict):
    try: return rr_integrity(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/environment/lock-plan")
def research_reproduction_replication_studio_environment(payload: dict):
    try: return rr_environment(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/method/reconstruction-plan")
def research_reproduction_replication_studio_methods(payload: dict):
    try: return rr_methods(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/execution/reproduction-plan")
def research_reproduction_replication_studio_execution(payload: dict):
    try: return rr_execution(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/tolerance/profile")
def research_reproduction_replication_studio_tolerance(payload: dict):
    try: return rr_tolerance(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/results/compare")
def research_reproduction_replication_studio_compare(payload: dict):
    try: return rr_compare(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/figures/reproduction-plan")
def research_reproduction_replication_studio_figures(payload: dict):
    try: return rr_figures(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/provenance/reconstruct")
def research_reproduction_replication_studio_provenance(payload: dict):
    try: return rr_provenance(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/deviations/register")
def research_reproduction_replication_studio_deviations(payload: dict):
    try: return rr_deviations(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/replication/protocol")
def research_reproduction_replication_studio_protocol(payload: dict):
    try: return rr_protocol(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/replication/independent-plan")
def research_reproduction_replication_studio_replication_plan(payload: dict):
    try: return rr_replication_plan(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/replication/result-record")
def research_reproduction_replication_studio_replication_result(payload: dict):
    try: return rr_replication_result(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/claims/linkage")
def research_reproduction_replication_studio_claims(payload: dict):
    try: return rr_claims(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/reproducibility/matrix")
def research_reproduction_replication_studio_matrix(payload: dict):
    try: return rr_matrix(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/package/manifest")
def research_reproduction_replication_studio_package(payload: dict):
    try: return rr_package(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/visualization/plan")
def research_reproduction_replication_studio_visualization(payload: dict):
    try: return rr_visualization(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/studio/build")
def research_reproduction_replication_studio_build(payload: dict):
    try: return rr_studio(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/snapshot/build")
def research_reproduction_replication_studio_snapshot(payload: dict):
    try: return rr_snapshot(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/export/plan")
def research_reproduction_replication_studio_export(payload: dict):
    try: return rr_export(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/core-object/plan")
def research_reproduction_replication_studio_core(payload: dict):
    try: return rr_core(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/execution-lineage/plan")
def research_reproduction_replication_studio_lineage(payload: dict):
    try: return rr_lineage(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/readiness/report")
def research_reproduction_replication_studio_readiness(payload: dict):
    try: return rr_readiness(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/status/report")
def research_reproduction_replication_studio_status(payload: dict):
    try: return rr_status(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/research-reproduction-replication-studio/interpretation-boundaries/report")
def research_reproduction_replication_studio_boundaries(payload: dict):
    try: return rr_boundaries(payload)
    except ReproductionReplicationError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.get("/v1/scientific-research-project-studio/health")
def scientific_research_project_studio_health(): return project_health()
@app.get("/v1/scientific-research-project-studio/manifest")
def scientific_research_project_studio_manifest(): return project_manifest()
@app.get("/v1/scientific-research-project-studio/catalog")
def scientific_research_project_studio_catalog(): return project_catalog()
@app.get("/v1/scientific-research-project-studio/schema")
def scientific_research_project_studio_schema(): return project_schema()

def _project_call(fn,payload):
    try: return fn(payload)
    except ResearchProjectStudioError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/scientific-research-project-studio/project/normalize")
def scientific_research_project_studio_normalize(payload: dict): return _project_call(project_normalize,payload)
@app.post("/v1/scientific-research-project-studio/components/register")
def scientific_research_project_studio_components(payload: dict): return _project_call(project_components,payload)
@app.post("/v1/scientific-research-project-studio/relationships/graph")
def scientific_research_project_studio_relationships(payload: dict): return _project_call(project_relationships,payload)
@app.post("/v1/scientific-research-project-studio/workstreams/plan")
def scientific_research_project_studio_workstreams(payload: dict): return _project_call(project_workstreams,payload)
@app.post("/v1/scientific-research-project-studio/milestones/plan")
def scientific_research_project_studio_milestones(payload: dict): return _project_call(project_milestones,payload)
@app.post("/v1/scientific-research-project-studio/state/report")
def scientific_research_project_studio_state(payload: dict): return _project_call(project_state,payload)
@app.post("/v1/scientific-research-project-studio/datasets/index")
def scientific_research_project_studio_datasets(payload: dict): return _project_call(project_datasets,payload)
@app.post("/v1/scientific-research-project-studio/models/index")
def scientific_research_project_studio_models(payload: dict): return _project_call(project_models,payload)
@app.post("/v1/scientific-research-project-studio/executions/index")
def scientific_research_project_studio_executions(payload: dict): return _project_call(project_executions,payload)
@app.post("/v1/scientific-research-project-studio/figures/index")
def scientific_research_project_studio_figures(payload: dict): return _project_call(project_figures,payload)
@app.post("/v1/scientific-research-project-studio/claims-evidence/index")
def scientific_research_project_studio_claims(payload: dict): return _project_call(project_claim_evidence,payload)
@app.post("/v1/scientific-research-project-studio/manuscripts/index")
def scientific_research_project_studio_manuscripts(payload: dict): return _project_call(project_manuscripts,payload)
@app.post("/v1/scientific-research-project-studio/reproduction/index")
def scientific_research_project_studio_reproductions(payload: dict): return _project_call(project_reproductions,payload)
@app.post("/v1/scientific-research-project-studio/investigations/index")
def scientific_research_project_studio_investigations(payload: dict): return _project_call(project_investigations,payload)
@app.post("/v1/scientific-research-project-studio/dependencies/graph")
def scientific_research_project_studio_dependencies(payload: dict): return _project_call(project_dependencies,payload)
@app.post("/v1/scientific-research-project-studio/bindings/cross-product")
def scientific_research_project_studio_bindings(payload: dict): return _project_call(project_bindings,payload)
@app.post("/v1/scientific-research-project-studio/sessions/plan")
def scientific_research_project_studio_session(payload: dict): return _project_call(project_session,payload)
@app.post("/v1/scientific-research-project-studio/handoffs/plan")
def scientific_research_project_studio_handoff(payload: dict): return _project_call(project_handoff,payload)
@app.post("/v1/scientific-research-project-studio/workspace/layout")
def scientific_research_project_studio_workspace(payload: dict): return _project_call(project_workspace,payload)
@app.post("/v1/scientific-research-project-studio/timeline/build")
def scientific_research_project_studio_timeline(payload: dict): return _project_call(project_timeline,payload)
@app.post("/v1/scientific-research-project-studio/provenance/aggregate")
def scientific_research_project_studio_provenance(payload: dict): return _project_call(project_provenance,payload)
@app.post("/v1/scientific-research-project-studio/readiness/report")
def scientific_research_project_studio_readiness(payload: dict): return _project_call(project_readiness,payload)
@app.post("/v1/scientific-research-project-studio/snapshot/build")
def scientific_research_project_studio_snapshot(payload: dict): return _project_call(project_snapshot,payload)
@app.post("/v1/scientific-research-project-studio/revision/plan")
def scientific_research_project_studio_revision(payload: dict): return _project_call(project_revision,payload)
@app.post("/v1/scientific-research-project-studio/export/plan")
def scientific_research_project_studio_export(payload: dict): return _project_call(project_export,payload)
@app.post("/v1/scientific-research-project-studio/core-project/plan")
def scientific_research_project_studio_core(payload: dict): return _project_call(project_core,payload)
@app.post("/v1/scientific-research-project-studio/publication-package/plan")
def scientific_research_project_studio_publication(payload: dict): return _project_call(project_publication,payload)
@app.post("/v1/scientific-research-project-studio/interpretation-boundaries/report")
def scientific_research_project_studio_boundaries(payload: dict): return _project_call(project_boundaries,payload)



@app.get("/v1/research-question-hypothesis-workspace/health")
def research_question_hypothesis_workspace_health(): return rqh_health()
@app.get("/v1/research-question-hypothesis-workspace/manifest")
def research_question_hypothesis_workspace_manifest(): return rqh_manifest()
@app.get("/v1/research-question-hypothesis-workspace/catalog")
def research_question_hypothesis_workspace_catalog(): return rqh_catalog()
@app.get("/v1/research-question-hypothesis-workspace/schema")
def research_question_hypothesis_workspace_schema(): return rqh_schema()

def _rqh_call(fn,payload):
    try: return fn(payload)
    except ResearchQuestionHypothesisError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/research-question-hypothesis-workspace/question/normalize")
def research_question_hypothesis_workspace_question(payload: dict): return _rqh_call(rqh_question,payload)
@app.post("/v1/research-question-hypothesis-workspace/questions/register")
def research_question_hypothesis_workspace_questions(payload: dict): return _rqh_call(rqh_questions,payload)
@app.post("/v1/research-question-hypothesis-workspace/hypotheses/register")
def research_question_hypothesis_workspace_hypotheses(payload: dict): return _rqh_call(rqh_hypotheses,payload)
@app.post("/v1/research-question-hypothesis-workspace/hypotheses/competing-set")
def research_question_hypothesis_workspace_competing(payload: dict): return _rqh_call(rqh_competing,payload)
@app.post("/v1/research-question-hypothesis-workspace/variables/operationalize")
def research_question_hypothesis_workspace_variables(payload: dict): return _rqh_call(rqh_variables,payload)
@app.post("/v1/research-question-hypothesis-workspace/observables/register")
def research_question_hypothesis_workspace_observables(payload: dict): return _rqh_call(rqh_observables,payload)
@app.post("/v1/research-question-hypothesis-workspace/scope/normalize")
def research_question_hypothesis_workspace_scope(payload: dict): return _rqh_call(rqh_scope,payload)
@app.post("/v1/research-question-hypothesis-workspace/assumptions/register")
def research_question_hypothesis_workspace_assumptions(payload: dict): return _rqh_call(rqh_assumptions,payload)
@app.post("/v1/research-question-hypothesis-workspace/expected-observations/matrix")
def research_question_hypothesis_workspace_expectations(payload: dict): return _rqh_call(rqh_expectations,payload)
@app.post("/v1/research-question-hypothesis-workspace/falsification/criteria")
def research_question_hypothesis_workspace_falsification(payload: dict): return _rqh_call(rqh_falsification,payload)
@app.post("/v1/research-question-hypothesis-workspace/disconfirmation/criteria")
def research_question_hypothesis_workspace_disconfirmation(payload: dict): return _rqh_call(rqh_disconfirmation,payload)
@app.post("/v1/research-question-hypothesis-workspace/rival-explanations/register")
def research_question_hypothesis_workspace_rivals(payload: dict): return _rqh_call(rqh_rivals,payload)
@app.post("/v1/research-question-hypothesis-workspace/discriminating-tests/plan")
def research_question_hypothesis_workspace_tests(payload: dict): return _rqh_call(rqh_tests,payload)
@app.post("/v1/research-question-hypothesis-workspace/preregistration/lock")
def research_question_hypothesis_workspace_lock(payload: dict): return _rqh_call(rqh_lock,payload)
@app.post("/v1/research-question-hypothesis-workspace/preregistration/deviation")
def research_question_hypothesis_workspace_deviation(payload: dict): return _rqh_call(rqh_deviation,payload)
@app.post("/v1/research-question-hypothesis-workspace/evidence-links/plan")
def research_question_hypothesis_workspace_evidence(payload: dict): return _rqh_call(rqh_evidence,payload)
@app.post("/v1/research-question-hypothesis-workspace/method-eligibility/plan")
def research_question_hypothesis_workspace_methods(payload: dict): return _rqh_call(rqh_methods,payload)
@app.post("/v1/research-question-hypothesis-workspace/decision-boundaries/plan")
def research_question_hypothesis_workspace_decisions(payload: dict): return _rqh_call(rqh_decisions,payload)
@app.post("/v1/research-question-hypothesis-workspace/status/report")
def research_question_hypothesis_workspace_status(payload: dict): return _rqh_call(rqh_status,payload)
@app.post("/v1/research-question-hypothesis-workspace/readiness/report")
def research_question_hypothesis_workspace_readiness(payload: dict): return _rqh_call(rqh_readiness,payload)
@app.post("/v1/research-question-hypothesis-workspace/provenance/aggregate")
def research_question_hypothesis_workspace_provenance(payload: dict): return _rqh_call(rqh_provenance,payload)
@app.post("/v1/research-question-hypothesis-workspace/project/bind")
def research_question_hypothesis_workspace_project(payload: dict): return _rqh_call(rqh_project,payload)
@app.post("/v1/research-question-hypothesis-workspace/session/bind")
def research_question_hypothesis_workspace_session(payload: dict): return _rqh_call(rqh_session,payload)
@app.post("/v1/research-question-hypothesis-workspace/core-object/plan")
def research_question_hypothesis_workspace_core(payload: dict): return _rqh_call(rqh_core,payload)
@app.post("/v1/research-question-hypothesis-workspace/visualization/plan")
def research_question_hypothesis_workspace_visual(payload: dict): return _rqh_call(rqh_visual,payload)
@app.post("/v1/research-question-hypothesis-workspace/snapshot/build")
def research_question_hypothesis_workspace_snapshot(payload: dict): return _rqh_call(rqh_snapshot,payload)
@app.post("/v1/research-question-hypothesis-workspace/revision/plan")
def research_question_hypothesis_workspace_revision(payload: dict): return _rqh_call(rqh_revision,payload)
@app.post("/v1/research-question-hypothesis-workspace/export/plan")
def research_question_hypothesis_workspace_export(payload: dict): return _rqh_call(rqh_export,payload)
@app.post("/v1/research-question-hypothesis-workspace/interpretation-boundaries/report")
def research_question_hypothesis_workspace_boundaries(payload: dict): return _rqh_call(rqh_boundaries,payload)

@app.get("/v1/method-selection-intelligence/health")
def method_selection_intelligence_health(): return msi_health()
@app.get("/v1/method-selection-intelligence/manifest")
def method_selection_intelligence_manifest(): return msi_manifest()
@app.get("/v1/method-selection-intelligence/catalog")
def method_selection_intelligence_catalog(): return msi_catalog()
@app.get("/v1/method-selection-intelligence/schema")
def method_selection_intelligence_schema(): return msi_schema()

def _msi_call(fn,payload):
    try: return fn(payload)
    except MethodSelectionIntelligenceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/method-selection-intelligence/context/normalize")
def method_selection_intelligence_context(payload: dict): return _msi_call(msi_context,payload)
@app.post("/v1/method-selection-intelligence/methods/registry")
def method_selection_intelligence_methods(payload: dict): return _msi_call(msi_methods,payload)
@app.post("/v1/method-selection-intelligence/data/profile")
def method_selection_intelligence_data(payload: dict): return _msi_call(msi_data,payload)
@app.post("/v1/method-selection-intelligence/estimand/profile")
def method_selection_intelligence_estimand(payload: dict): return _msi_call(msi_estimand,payload)
@app.post("/v1/method-selection-intelligence/design/profile")
def method_selection_intelligence_design(payload: dict): return _msi_call(msi_design,payload)
@app.post("/v1/method-selection-intelligence/sample-structure/profile")
def method_selection_intelligence_sample(payload: dict): return _msi_call(msi_sample,payload)
@app.post("/v1/method-selection-intelligence/temporal-spatial/profile")
def method_selection_intelligence_temporal_spatial(payload: dict): return _msi_call(msi_temporal_spatial,payload)
@app.post("/v1/method-selection-intelligence/uncertainty/profile")
def method_selection_intelligence_uncertainty(payload: dict): return _msi_call(msi_uncertainty,payload)
@app.post("/v1/method-selection-intelligence/candidates/build")
def method_selection_intelligence_candidates(payload: dict): return _msi_call(msi_candidates,payload)
@app.post("/v1/method-selection-intelligence/eligibility/matrix")
def method_selection_intelligence_eligibility(payload: dict): return _msi_call(msi_eligibility,payload)
@app.post("/v1/method-selection-intelligence/assumptions/matrix")
def method_selection_intelligence_assumptions(payload: dict): return _msi_call(msi_assumptions,payload)
@app.post("/v1/method-selection-intelligence/requirements/gaps")
def method_selection_intelligence_gaps(payload: dict): return _msi_call(msi_gaps,payload)
@app.post("/v1/method-selection-intelligence/incompatibilities/report")
def method_selection_intelligence_incompatibilities(payload: dict): return _msi_call(msi_incompatibilities,payload)
@app.post("/v1/method-selection-intelligence/alignment/report")
def method_selection_intelligence_alignment(payload: dict): return _msi_call(msi_alignment,payload)
@app.post("/v1/method-selection-intelligence/comparison/build")
def method_selection_intelligence_comparison(payload: dict): return _msi_call(msi_comparison,payload)
@app.post("/v1/method-selection-intelligence/diagnostics/plan")
def method_selection_intelligence_diagnostics(payload: dict): return _msi_call(msi_diagnostics,payload)
@app.post("/v1/method-selection-intelligence/sensitivity/plan")
def method_selection_intelligence_sensitivity(payload: dict): return _msi_call(msi_sensitivity,payload)
@app.post("/v1/method-selection-intelligence/validation/plan")
def method_selection_intelligence_validation(payload: dict): return _msi_call(msi_validation,payload)
@app.post("/v1/method-selection-intelligence/preanalysis/plan")
def method_selection_intelligence_preanalysis(payload: dict): return _msi_call(msi_preanalysis,payload)
@app.post("/v1/method-selection-intelligence/shortlist/record")
def method_selection_intelligence_shortlist(payload: dict): return _msi_call(msi_shortlist,payload)
@app.post("/v1/method-selection-intelligence/decision/record")
def method_selection_intelligence_decision(payload: dict): return _msi_call(msi_decision,payload)
@app.post("/v1/method-selection-intelligence/handoff/plan")
def method_selection_intelligence_handoff(payload: dict): return _msi_call(msi_handoff,payload)
@app.post("/v1/method-selection-intelligence/visualization/plan")
def method_selection_intelligence_visual(payload: dict): return _msi_call(msi_visual,payload)
@app.post("/v1/method-selection-intelligence/readiness/report")
def method_selection_intelligence_readiness(payload: dict): return _msi_call(msi_readiness,payload)
@app.post("/v1/method-selection-intelligence/status/report")
def method_selection_intelligence_status(payload: dict): return _msi_call(msi_status,payload)
@app.post("/v1/method-selection-intelligence/provenance/aggregate")
def method_selection_intelligence_provenance(payload: dict): return _msi_call(msi_provenance,payload)
@app.post("/v1/method-selection-intelligence/project/bind")
def method_selection_intelligence_project(payload: dict): return _msi_call(msi_project,payload)
@app.post("/v1/method-selection-intelligence/session/bind")
def method_selection_intelligence_session(payload: dict): return _msi_call(msi_session,payload)
@app.post("/v1/method-selection-intelligence/core-object/plan")
def method_selection_intelligence_core(payload: dict): return _msi_call(msi_core,payload)
@app.post("/v1/method-selection-intelligence/snapshot/build")
def method_selection_intelligence_snapshot(payload: dict): return _msi_call(msi_snapshot,payload)
@app.post("/v1/method-selection-intelligence/revision/plan")
def method_selection_intelligence_revision(payload: dict): return _msi_call(msi_revision,payload)
@app.post("/v1/method-selection-intelligence/export/plan")
def method_selection_intelligence_export(payload: dict): return _msi_call(msi_export,payload)
@app.post("/v1/method-selection-intelligence/interpretation-boundaries/report")
def method_selection_intelligence_boundaries(payload: dict): return _msi_call(msi_boundaries,payload)



# Lab v0.133.0 — Statistical Assumption & Diagnostic Intelligence
@app.get("/v1/statistical-assumption-diagnostic-intelligence/health")
def statistical_assumption_diagnostic_intelligence_health(): return sadi_health()
@app.get("/v1/statistical-assumption-diagnostic-intelligence/manifest")
def statistical_assumption_diagnostic_intelligence_manifest(): return sadi_manifest()
@app.get("/v1/statistical-assumption-diagnostic-intelligence/catalog")
def statistical_assumption_diagnostic_intelligence_catalog(): return sadi_catalog()
@app.get("/v1/statistical-assumption-diagnostic-intelligence/schema")
def statistical_assumption_diagnostic_intelligence_schema(): return sadi_schema()

def _sadi_call(fn,payload):
    try: return fn(payload)
    except StatisticalAssumptionDiagnosticIntelligenceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/statistical-assumption-diagnostic-intelligence/context/normalize")
def sadi_context_route(payload:dict): return _sadi_call(sadi_context,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/assumptions/registry")
def sadi_assumptions_route(payload:dict): return _sadi_call(sadi_assumptions,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/diagnostics/registry")
def sadi_diagnostics_route(payload:dict): return _sadi_call(sadi_diagnostics,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/diagnostic-observations/normalize")
def sadi_observations_route(payload:dict): return _sadi_call(sadi_observations,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/assumptions/evaluate")
def sadi_evaluate_route(payload:dict): return _sadi_call(sadi_evaluate,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/assumptions/matrix")
def sadi_matrix_route(payload:dict): return _sadi_call(sadi_matrix,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/violations/report")
def sadi_violations_route(payload:dict): return _sadi_call(sadi_violations,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/uncertainty/report")
def sadi_uncertainty_route(payload:dict): return _sadi_call(sadi_uncertainty,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/untested/report")
def sadi_untested_route(payload:dict): return _sadi_call(sadi_untested,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/dependencies/graph")
def sadi_dependencies_route(payload:dict): return _sadi_call(sadi_dependencies,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/method-impact/report")
def sadi_method_impact_route(payload:dict): return _sadi_call(sadi_method_impact,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/remediation/options")
def sadi_remediation_route(payload:dict): return _sadi_call(sadi_remediation,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/residuals/diagnostics")
def sadi_residuals_route(payload:dict): return _sadi_call(sadi_residuals,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/distribution/diagnostics")
def sadi_distribution_route(payload:dict): return _sadi_call(sadi_distribution,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/independence/diagnostics")
def sadi_independence_route(payload:dict): return _sadi_call(sadi_independence,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/variance/diagnostics")
def sadi_variance_route(payload:dict): return _sadi_call(sadi_variance,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/multicollinearity/diagnostics")
def sadi_multicollinearity_route(payload:dict): return _sadi_call(sadi_multicollinearity,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/influence/diagnostics")
def sadi_influence_route(payload:dict): return _sadi_call(sadi_influence,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/convergence/diagnostics")
def sadi_convergence_route(payload:dict): return _sadi_call(sadi_convergence,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/causal-overlap/diagnostics")
def sadi_causal_overlap_route(payload:dict): return _sadi_call(sadi_causal_overlap,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/time-series/diagnostics")
def sadi_time_series_route(payload:dict): return _sadi_call(sadi_time_series,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/spatial/diagnostics")
def sadi_spatial_route(payload:dict): return _sadi_call(sadi_spatial,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/missingness/diagnostics")
def sadi_missingness_route(payload:dict): return _sadi_call(sadi_missingness,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/simulation/diagnostics")
def sadi_simulation_route(payload:dict): return _sadi_call(sadi_simulation,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/experimental-design/diagnostics")
def sadi_experimental_route(payload:dict): return _sadi_call(sadi_experimental,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/robustness/plan")
def sadi_robustness_route(payload:dict): return _sadi_call(sadi_robustness,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/sensitivity/plan")
def sadi_sensitivity_route(payload:dict): return _sadi_call(sadi_sensitivity,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/cross-method/synthesis")
def sadi_synthesis_route(payload:dict): return _sadi_call(sadi_synthesis,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/adjudication/record")
def sadi_adjudication_route(payload:dict): return _sadi_call(sadi_adjudication,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/readiness/report")
def sadi_readiness_route(payload:dict): return _sadi_call(sadi_readiness,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/status/report")
def sadi_status_route(payload:dict): return _sadi_call(sadi_status,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/visualization/plan")
def sadi_visual_route(payload:dict): return _sadi_call(sadi_visual,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/provenance/aggregate")
def sadi_provenance_route(payload:dict): return _sadi_call(sadi_provenance,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/project/bind")
def sadi_project_route(payload:dict): return _sadi_call(sadi_project,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/session/bind")
def sadi_session_route(payload:dict): return _sadi_call(sadi_session,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/handoff/plan")
def sadi_handoff_route(payload:dict): return _sadi_call(sadi_handoff,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/core-object/plan")
def sadi_core_route(payload:dict): return _sadi_call(sadi_core,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/snapshot/build")
def sadi_snapshot_route(payload:dict): return _sadi_call(sadi_snapshot,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/revision/plan")
def sadi_revision_route(payload:dict): return _sadi_call(sadi_revision,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/export/plan")
def sadi_export_route(payload:dict): return _sadi_call(sadi_export,payload)
@app.post("/v1/statistical-assumption-diagnostic-intelligence/interpretation-boundaries/report")
def sadi_boundaries_route(payload:dict): return _sadi_call(sadi_boundaries,payload)

# Lab v0.134.0 — Evidence Synthesis Intelligence II
@app.get("/v1/evidence-synthesis-intelligence-ii/health")
def evidence_synthesis_intelligence_ii_health(): return esii_health()
@app.get("/v1/evidence-synthesis-intelligence-ii/manifest")
def evidence_synthesis_intelligence_ii_manifest(): return esii_manifest()
@app.get("/v1/evidence-synthesis-intelligence-ii/catalog")
def evidence_synthesis_intelligence_ii_catalog(): return esii_catalog()
@app.get("/v1/evidence-synthesis-intelligence-ii/schema")
def evidence_synthesis_intelligence_ii_schema(): return esii_schema()

def _esii_call(fn,payload):
    try: return fn(payload)
    except EvidenceSynthesisIntelligenceIIError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)

@app.post("/v1/evidence-synthesis-intelligence-ii/context/normalize")
def esii_context_route(payload:dict): return _esii_call(esii_context,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/protocol/normalize")
def esii_protocol_route(payload:dict): return _esii_call(esii_protocol,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/evidence/normalize")
def esii_evidence_route(payload:dict): return _esii_call(esii_evidence,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/findings/registry")
def esii_findings_route(payload:dict): return _esii_call(esii_findings,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/claims/registry")
def esii_claims_route(payload:dict): return _esii_call(esii_claims,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/studies/characteristics-matrix")
def esii_study_matrix_route(payload:dict): return _esii_call(esii_study_matrix,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/evidence/matrix")
def esii_evidence_matrix_route(payload:dict): return _esii_call(esii_evidence_matrix,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/direction/agreement-report")
def esii_direction_route(payload:dict): return _esii_call(esii_direction,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/contradictions/report")
def esii_contradictions_route(payload:dict): return _esii_call(esii_contradictions,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/heterogeneity/report")
def esii_heterogeneity_route(payload:dict): return _esii_call(esii_heterogeneity,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/quantitative/synthesize")
def esii_quantitative_route(payload:dict): return _esii_call(esii_quantitative,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/subgroups/synthesize")
def esii_subgroup_route(payload:dict): return _esii_call(esii_subgroup,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/leave-one-out/synthesize")
def esii_leave_one_out_route(payload:dict): return _esii_call(esii_leave_one_out,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/replications/synthesize")
def esii_replication_route(payload:dict): return _esii_call(esii_replication,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/qualitative/themes-register")
def esii_themes_route(payload:dict): return _esii_call(esii_themes,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/mixed-methods/plan")
def esii_mixed_methods_route(payload:dict): return _esii_call(esii_mixed_methods,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/uncertainty/synthesize")
def esii_uncertainty_route(payload:dict): return _esii_call(esii_uncertainty,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/coverage/gaps-report")
def esii_coverage_gaps_route(payload:dict): return _esii_call(esii_coverage_gaps,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/publication-bias/diagnostic-plan")
def esii_publication_bias_route(payload:dict): return _esii_call(esii_publication_bias,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/robustness/plan")
def esii_robustness_route(payload:dict): return _esii_call(esii_robustness,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/sensitivity/plan")
def esii_sensitivity_route(payload:dict): return _esii_call(esii_sensitivity,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/assumptions-diagnostics/bridge")
def esii_assumption_bridge_route(payload:dict): return _esii_call(esii_assumption_bridge,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/hypotheses/evidence-map")
def esii_hypothesis_map_route(payload:dict): return _esii_call(esii_hypothesis_map,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/claims/synthesize")
def esii_claim_synthesis_route(payload:dict): return _esii_call(esii_claim_synthesis,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/competing-findings/report")
def esii_competing_findings_route(payload:dict): return _esii_call(esii_competing_findings,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/narrative/plan")
def esii_narrative_route(payload:dict): return _esii_call(esii_narrative,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/adjudication/record")
def esii_adjudication_route(payload:dict): return _esii_call(esii_adjudication,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/status/report")
def esii_status_route(payload:dict): return _esii_call(esii_status,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/readiness/report")
def esii_readiness_route(payload:dict): return _esii_call(esii_readiness,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/provenance/aggregate")
def esii_provenance_route(payload:dict): return _esii_call(esii_provenance,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/lineage/graph")
def esii_lineage_route(payload:dict): return _esii_call(esii_lineage,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/visualization/plan")
def esii_visual_route(payload:dict): return _esii_call(esii_visual,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/project/bind")
def esii_project_route(payload:dict): return _esii_call(esii_project,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/session/bind")
def esii_session_route(payload:dict): return _esii_call(esii_session,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/handoff/plan")
def esii_handoff_route(payload:dict): return _esii_call(esii_handoff,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/core-object/plan")
def esii_core_route(payload:dict): return _esii_call(esii_core,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/snapshot/build")
def esii_snapshot_route(payload:dict): return _esii_call(esii_snapshot,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/revision/plan")
def esii_revision_route(payload:dict): return _esii_call(esii_revision,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/export/plan")
def esii_export_route(payload:dict): return _esii_call(esii_export,payload)
@app.post("/v1/evidence-synthesis-intelligence-ii/interpretation-boundaries/report")
def esii_boundaries_route(payload:dict): return _esii_call(esii_boundaries,payload)



# Lab v0.135.0 — Competing Model & Hypothesis Analysis
@app.get("/v1/competing-model-hypothesis-analysis/health")
def competing_model_hypothesis_analysis_health(): return cmha_health()
@app.get("/v1/competing-model-hypothesis-analysis/manifest")
def competing_model_hypothesis_analysis_manifest(): return cmha_manifest()
@app.get("/v1/competing-model-hypothesis-analysis/catalog")
def competing_model_hypothesis_analysis_catalog(): return cmha_catalog()
@app.get("/v1/competing-model-hypothesis-analysis/schema")
def competing_model_hypothesis_analysis_schema(): return cmha_schema()

def _cmha_call(fn,payload):
    try: return fn(payload)
    except CompetingModelHypothesisAnalysisError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/competing-model-hypothesis-analysis/context/normalize")
def cmha_route_0(payload:dict): return _cmha_call(cmha_context,payload)
@app.post("/v1/competing-model-hypothesis-analysis/hypotheses/registry")
def cmha_route_1(payload:dict): return _cmha_call(cmha_hypotheses,payload)
@app.post("/v1/competing-model-hypothesis-analysis/models/registry")
def cmha_route_2(payload:dict): return _cmha_call(cmha_models,payload)
@app.post("/v1/competing-model-hypothesis-analysis/observations/registry")
def cmha_route_3(payload:dict): return _cmha_call(cmha_observations,payload)
@app.post("/v1/competing-model-hypothesis-analysis/predictions/registry")
def cmha_route_4(payload:dict): return _cmha_call(cmha_predictions,payload)
@app.post("/v1/competing-model-hypothesis-analysis/prediction-observation/matrix")
def cmha_route_5(payload:dict): return _cmha_call(cmha_prediction_matrix,payload)
@app.post("/v1/competing-model-hypothesis-analysis/evidence/alignment-matrix")
def cmha_route_6(payload:dict): return _cmha_call(cmha_evidence_alignment,payload)
@app.post("/v1/competing-model-hypothesis-analysis/diagnostics/bridge")
def cmha_route_7(payload:dict): return _cmha_call(cmha_diagnostic_bridge,payload)
@app.post("/v1/competing-model-hypothesis-analysis/synthesis/bridge")
def cmha_route_8(payload:dict): return _cmha_call(cmha_synthesis_bridge,payload)
@app.post("/v1/competing-model-hypothesis-analysis/replication/bridge")
def cmha_route_9(payload:dict): return _cmha_call(cmha_replication_bridge,payload)
@app.post("/v1/competing-model-hypothesis-analysis/contradictions/matrix")
def cmha_route_10(payload:dict): return _cmha_call(cmha_contradictions,payload)
@app.post("/v1/competing-model-hypothesis-analysis/scope/compatibility")
def cmha_route_11(payload:dict): return _cmha_call(cmha_scope_compatibility,payload)
@app.post("/v1/competing-model-hypothesis-analysis/assumptions/compatibility")
def cmha_route_12(payload:dict): return _cmha_call(cmha_assumption_compatibility,payload)
@app.post("/v1/competing-model-hypothesis-analysis/model-fit/evidence-report")
def cmha_route_13(payload:dict): return _cmha_call(cmha_model_fit,payload)
@app.post("/v1/competing-model-hypothesis-analysis/predictive-performance/report")
def cmha_route_14(payload:dict): return _cmha_call(cmha_predictive_performance,payload)
@app.post("/v1/competing-model-hypothesis-analysis/calibration/report")
def cmha_route_15(payload:dict): return _cmha_call(cmha_calibration,payload)
@app.post("/v1/competing-model-hypothesis-analysis/complexity/report")
def cmha_route_16(payload:dict): return _cmha_call(cmha_complexity,payload)
@app.post("/v1/competing-model-hypothesis-analysis/parsimony/plan")
def cmha_route_17(payload:dict): return _cmha_call(cmha_parsimony,payload)
@app.post("/v1/competing-model-hypothesis-analysis/discriminating-observations/plan")
def cmha_route_18(payload:dict): return _cmha_call(cmha_discriminating_observation,payload)
@app.post("/v1/competing-model-hypothesis-analysis/discriminating-tests/plan")
def cmha_route_19(payload:dict): return _cmha_call(cmha_discriminating_test,payload)
@app.post("/v1/competing-model-hypothesis-analysis/pairwise/compare")
def cmha_route_20(payload:dict): return _cmha_call(cmha_pairwise,payload)
@app.post("/v1/competing-model-hypothesis-analysis/comparison/matrix")
def cmha_route_21(payload:dict): return _cmha_call(cmha_matrix,payload)
@app.post("/v1/competing-model-hypothesis-analysis/sensitivity/evidence-exclusion")
def cmha_route_22(payload:dict): return _cmha_call(cmha_evidence_sensitivity,payload)
@app.post("/v1/competing-model-hypothesis-analysis/sensitivity/assumption-state")
def cmha_route_23(payload:dict): return _cmha_call(cmha_assumption_sensitivity,payload)
@app.post("/v1/competing-model-hypothesis-analysis/robustness/plan")
def cmha_route_24(payload:dict): return _cmha_call(cmha_robustness,payload)
@app.post("/v1/competing-model-hypothesis-analysis/counterexamples/registry")
def cmha_route_25(payload:dict): return _cmha_call(cmha_counterexamples,payload)
@app.post("/v1/competing-model-hypothesis-analysis/unexplained-observations/report")
def cmha_route_26(payload:dict): return _cmha_call(cmha_unexplained,payload)
@app.post("/v1/competing-model-hypothesis-analysis/hypotheses/revision-plan")
def cmha_route_27(payload:dict): return _cmha_call(cmha_hypothesis_revision,payload)
@app.post("/v1/competing-model-hypothesis-analysis/models/revision-plan")
def cmha_route_28(payload:dict): return _cmha_call(cmha_model_revision,payload)
@app.post("/v1/competing-model-hypothesis-analysis/researcher/shortlist")
def cmha_route_29(payload:dict): return _cmha_call(cmha_shortlist,payload)
@app.post("/v1/competing-model-hypothesis-analysis/researcher/adjudication")
def cmha_route_30(payload:dict): return _cmha_call(cmha_adjudication,payload)
@app.post("/v1/competing-model-hypothesis-analysis/decision-rules/plan")
def cmha_route_31(payload:dict): return _cmha_call(cmha_decision_rules,payload)
@app.post("/v1/competing-model-hypothesis-analysis/status/report")
def cmha_route_32(payload:dict): return _cmha_call(cmha_status,payload)
@app.post("/v1/competing-model-hypothesis-analysis/readiness/report")
def cmha_route_33(payload:dict): return _cmha_call(cmha_readiness,payload)
@app.post("/v1/competing-model-hypothesis-analysis/visualization/plan")
def cmha_route_34(payload:dict): return _cmha_call(cmha_visual,payload)
@app.post("/v1/competing-model-hypothesis-analysis/provenance/aggregate")
def cmha_route_35(payload:dict): return _cmha_call(cmha_provenance,payload)
@app.post("/v1/competing-model-hypothesis-analysis/lineage/graph")
def cmha_route_36(payload:dict): return _cmha_call(cmha_lineage,payload)
@app.post("/v1/competing-model-hypothesis-analysis/project/bind")
def cmha_route_37(payload:dict): return _cmha_call(cmha_project,payload)
@app.post("/v1/competing-model-hypothesis-analysis/session/bind")
def cmha_route_38(payload:dict): return _cmha_call(cmha_session,payload)
@app.post("/v1/competing-model-hypothesis-analysis/handoff/plan")
def cmha_route_39(payload:dict): return _cmha_call(cmha_handoff,payload)
@app.post("/v1/competing-model-hypothesis-analysis/core-object/plan")
def cmha_route_40(payload:dict): return _cmha_call(cmha_core,payload)
@app.post("/v1/competing-model-hypothesis-analysis/snapshot/build")
def cmha_route_41(payload:dict): return _cmha_call(cmha_snapshot,payload)
@app.post("/v1/competing-model-hypothesis-analysis/revision/plan")
def cmha_route_42(payload:dict): return _cmha_call(cmha_revision,payload)
@app.post("/v1/competing-model-hypothesis-analysis/export/plan")
def cmha_route_43(payload:dict): return _cmha_call(cmha_export,payload)
@app.post("/v1/competing-model-hypothesis-analysis/interpretation-boundaries/report")
def cmha_route_44(payload:dict): return _cmha_call(cmha_boundaries,payload)

# Lab v0.135.1 — Scientific Visualization Experience Overhaul
@app.get("/v1/scientific-visualization-experience/health")
def scientific_visualization_experience_health(): return sve1351_health()
@app.get("/v1/scientific-visualization-experience/manifest")
def scientific_visualization_experience_manifest(): return sve1351_manifest()
@app.get("/v1/scientific-visualization-experience/catalog")
def scientific_visualization_experience_catalog(): return sve1351_catalog()
@app.get("/v1/scientific-visualization-experience/schema")
def scientific_visualization_experience_schema(): return sve1351_schema()

def _sve1351_call(fn,payload):
    try: return fn(payload)
    except ScientificVisualizationExperienceError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/scientific-visualization-experience/experience/normalize")
def sve1351_route_0(payload:dict): return _sve1351_call(sve1351_normalize,payload)
@app.post("/v1/scientific-visualization-experience/capabilities/map")
def sve1351_route_1(payload:dict): return _sve1351_call(sve1351_capabilities,payload)
@app.post("/v1/scientific-visualization-experience/workspace/compose")
def sve1351_route_2(payload:dict): return _sve1351_call(sve1351_workspace,payload)
@app.post("/v1/scientific-visualization-experience/primary-figure/plan")
def sve1351_route_3(payload:dict): return _sve1351_call(sve1351_primary,payload)
@app.post("/v1/scientific-visualization-experience/analytical-panels/plan")
def sve1351_route_4(payload:dict): return _sve1351_call(sve1351_panels,payload)
@app.post("/v1/scientific-visualization-experience/model-architecture/graph")
def sve1351_route_5(payload:dict): return _sve1351_call(sve1351_model_graph,payload)
@app.post("/v1/scientific-visualization-experience/provenance/pipeline")
def sve1351_route_6(payload:dict): return _sve1351_call(sve1351_pipeline,payload)
@app.post("/v1/scientific-visualization-experience/linked-views/plan")
def sve1351_route_7(payload:dict): return _sve1351_call(sve1351_linked,payload)
@app.post("/v1/scientific-visualization-experience/evidence-assumptions/panel")
def sve1351_route_8(payload:dict): return _sve1351_call(sve1351_evidence,payload)
@app.post("/v1/scientific-visualization-experience/renderer-stack/plan")
def sve1351_route_9(payload:dict): return _sve1351_call(sve1351_renderer,payload)
@app.post("/v1/scientific-visualization-experience/presentation/profile")
def sve1351_route_10(payload:dict): return _sve1351_call(sve1351_profile,payload)
@app.post("/v1/scientific-visualization-experience/responsive-layout/plan")
def sve1351_route_11(payload:dict): return _sve1351_call(sve1351_responsive,payload)
@app.post("/v1/scientific-visualization-experience/interaction/contract")
def sve1351_route_12(payload:dict): return _sve1351_call(sve1351_interaction,payload)
@app.post("/v1/scientific-visualization-experience/accessibility/audit")
def sve1351_route_13(payload:dict): return _sve1351_call(sve1351_accessibility,payload)
@app.post("/v1/scientific-visualization-experience/quality/audit")
def sve1351_route_14(payload:dict): return _sve1351_call(sve1351_quality,payload)
@app.post("/v1/scientific-visualization-experience/visualization/plan")
def sve1351_route_15(payload:dict): return _sve1351_call(sve1351_visual,payload)
@app.post("/v1/scientific-visualization-experience/project/bind")
def sve1351_route_16(payload:dict): return _sve1351_call(sve1351_project,payload)
@app.post("/v1/scientific-visualization-experience/session/bind")
def sve1351_route_17(payload:dict): return _sve1351_call(sve1351_session,payload)
@app.post("/v1/scientific-visualization-experience/handoff/plan")
def sve1351_route_18(payload:dict): return _sve1351_call(sve1351_handoff,payload)
@app.post("/v1/scientific-visualization-experience/export/plan")
def sve1351_route_19(payload:dict): return _sve1351_call(sve1351_export,payload)
@app.post("/v1/scientific-visualization-experience/provenance/aggregate")
def sve1351_route_20(payload:dict): return _sve1351_call(sve1351_provenance,payload)
@app.post("/v1/scientific-visualization-experience/snapshot/build")
def sve1351_route_21(payload:dict): return _sve1351_call(sve1351_snapshot,payload)
@app.post("/v1/scientific-visualization-experience/revision/plan")
def sve1351_route_22(payload:dict): return _sve1351_call(sve1351_revision,payload)
@app.post("/v1/scientific-visualization-experience/core-visual-object/plan")
def sve1351_route_23(payload:dict): return _sve1351_call(sve1351_core,payload)
@app.post("/v1/scientific-visualization-experience/readiness/report")
def sve1351_route_24(payload:dict): return _sve1351_call(sve1351_readiness,payload)
@app.post("/v1/scientific-visualization-experience/status/report")
def sve1351_route_25(payload:dict): return _sve1351_call(sve1351_status,payload)
@app.post("/v1/scientific-visualization-experience/interpretation-boundaries/report")
def sve1351_route_26(payload:dict): return _sve1351_call(sve1351_boundaries,payload)
@app.post("/v1/scientific-visualization-experience/experience/snapshot")
def sve1351_route_27(payload:dict): return _sve1351_call(sve1351_snapshot,payload)


# Lab v0.135.2 — Model Architecture & Computational Provenance Graphs
@app.get("/v1/model-architecture-provenance-graphs/health")
def model_architecture_provenance_graphs_health(): return macpg1352_health()
@app.get("/v1/model-architecture-provenance-graphs/manifest")
def model_architecture_provenance_graphs_manifest(): return macpg1352_manifest()
@app.get("/v1/model-architecture-provenance-graphs/catalog")
def model_architecture_provenance_graphs_catalog(): return macpg1352_catalog()
@app.get("/v1/model-architecture-provenance-graphs/schema")
def model_architecture_provenance_graphs_schema(): return macpg1352_schema()
def _macpg1352_call(fn,payload):
    try: return fn(payload)
    except ModelArchitectureProvenanceGraphError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail)
@app.post("/v1/model-architecture-provenance-graphs/graph/normalize")
def macpg1352_route_0(payload:dict): return _macpg1352_call(macpg1352_normalize,payload)
@app.post("/v1/model-architecture-provenance-graphs/architecture/graph")
def macpg1352_route_1(payload:dict): return _macpg1352_call(macpg1352_architecture,payload)
@app.post("/v1/model-architecture-provenance-graphs/provenance/graph")
def macpg1352_route_2(payload:dict): return _macpg1352_call(macpg1352_provenance,payload)
@app.post("/v1/model-architecture-provenance-graphs/execution/trace")
def macpg1352_route_3(payload:dict): return _macpg1352_call(macpg1352_execution,payload)
@app.post("/v1/model-architecture-provenance-graphs/uncertainty/graph")
def macpg1352_route_4(payload:dict): return _macpg1352_call(macpg1352_uncertainty,payload)
@app.post("/v1/model-architecture-provenance-graphs/evidence/lineage")
def macpg1352_route_5(payload:dict): return _macpg1352_call(macpg1352_evidence,payload)
@app.post("/v1/model-architecture-provenance-graphs/core/lineage")
def macpg1352_route_6(payload:dict): return _macpg1352_call(macpg1352_core_lineage,payload)
@app.post("/v1/model-architecture-provenance-graphs/dependency/graph")
def macpg1352_route_7(payload:dict): return _macpg1352_call(macpg1352_dependency,payload)
@app.post("/v1/model-architecture-provenance-graphs/subgraph/build")
def macpg1352_route_8(payload:dict): return _macpg1352_call(macpg1352_subgraph,payload)
@app.post("/v1/model-architecture-provenance-graphs/node/detail")
def macpg1352_route_9(payload:dict): return _macpg1352_call(macpg1352_node,payload)
@app.post("/v1/model-architecture-provenance-graphs/edge/detail")
def macpg1352_route_10(payload:dict): return _macpg1352_call(macpg1352_edge,payload)
@app.post("/v1/model-architecture-provenance-graphs/path/trace")
def macpg1352_route_11(payload:dict): return _macpg1352_call(macpg1352_path,payload)
@app.post("/v1/model-architecture-provenance-graphs/upstream/trace")
def macpg1352_route_12(payload:dict): return _macpg1352_call(macpg1352_upstream,payload)
@app.post("/v1/model-architecture-provenance-graphs/downstream/trace")
def macpg1352_route_13(payload:dict): return _macpg1352_call(macpg1352_downstream,payload)
@app.post("/v1/model-architecture-provenance-graphs/impact/path")
def macpg1352_route_14(payload:dict): return _macpg1352_call(macpg1352_impact,payload)
@app.post("/v1/model-architecture-provenance-graphs/orphans/report")
def macpg1352_route_15(payload:dict): return _macpg1352_call(macpg1352_orphans,payload)
@app.post("/v1/model-architecture-provenance-graphs/cycles/audit")
def macpg1352_route_16(payload:dict): return _macpg1352_call(macpg1352_cycles,payload)
@app.post("/v1/model-architecture-provenance-graphs/statistics/report")
def macpg1352_route_17(payload:dict): return _macpg1352_call(macpg1352_stats,payload)
@app.post("/v1/model-architecture-provenance-graphs/layout/layered")
def macpg1352_route_18(payload:dict): return _macpg1352_call(macpg1352_layered,payload)
@app.post("/v1/model-architecture-provenance-graphs/layout/swimlane")
def macpg1352_route_19(payload:dict): return _macpg1352_call(macpg1352_swimlane,payload)
@app.post("/v1/model-architecture-provenance-graphs/layout/radial")
def macpg1352_route_20(payload:dict): return _macpg1352_call(macpg1352_radial,payload)
@app.post("/v1/model-architecture-provenance-graphs/visual-encoding/plan")
def macpg1352_route_21(payload:dict): return _macpg1352_call(macpg1352_encoding,payload)
@app.post("/v1/model-architecture-provenance-graphs/interaction/contract")
def macpg1352_route_22(payload:dict): return _macpg1352_call(macpg1352_interaction,payload)
@app.post("/v1/model-architecture-provenance-graphs/linked-selection/plan")
def macpg1352_route_23(payload:dict): return _macpg1352_call(macpg1352_linked,payload)
@app.post("/v1/model-architecture-provenance-graphs/timeline-overlay/plan")
def macpg1352_route_24(payload:dict): return _macpg1352_call(macpg1352_timeline,payload)
@app.post("/v1/model-architecture-provenance-graphs/assumption-overlay/plan")
def macpg1352_route_25(payload:dict): return _macpg1352_call(macpg1352_assumptions,payload)
@app.post("/v1/model-architecture-provenance-graphs/diagnostic-overlay/plan")
def macpg1352_route_26(payload:dict): return _macpg1352_call(macpg1352_diagnostics,payload)
@app.post("/v1/model-architecture-provenance-graphs/evidence-overlay/plan")
def macpg1352_route_27(payload:dict): return _macpg1352_call(macpg1352_evidence_overlay,payload)
@app.post("/v1/model-architecture-provenance-graphs/project/bind")
def macpg1352_route_28(payload:dict): return _macpg1352_call(macpg1352_project,payload)
@app.post("/v1/model-architecture-provenance-graphs/session/bind")
def macpg1352_route_29(payload:dict): return _macpg1352_call(macpg1352_session,payload)
@app.post("/v1/model-architecture-provenance-graphs/handoff/plan")
def macpg1352_route_30(payload:dict): return _macpg1352_call(macpg1352_handoff,payload)
@app.post("/v1/model-architecture-provenance-graphs/export/plan")
def macpg1352_route_31(payload:dict): return _macpg1352_call(macpg1352_export,payload)
@app.post("/v1/model-architecture-provenance-graphs/snapshot/build")
def macpg1352_route_32(payload:dict): return _macpg1352_call(macpg1352_snapshot,payload)
@app.post("/v1/model-architecture-provenance-graphs/revision/plan")
def macpg1352_route_33(payload:dict): return _macpg1352_call(macpg1352_revision,payload)
@app.post("/v1/model-architecture-provenance-graphs/core-object/plan")
def macpg1352_route_34(payload:dict): return _macpg1352_call(macpg1352_core,payload)
@app.post("/v1/model-architecture-provenance-graphs/readiness/report")
def macpg1352_route_35(payload:dict): return _macpg1352_call(macpg1352_readiness,payload)


# Lab v0.135.3 — Multi-View Scientific Analysis Canvas
@app.get("/v1/multi-view-scientific-analysis-canvas/health")
def multi_view_scientific_analysis_canvas_health(): return mvac1353_health()
@app.get("/v1/multi-view-scientific-analysis-canvas/manifest")
def multi_view_scientific_analysis_canvas_manifest(): return mvac1353_manifest()
@app.get("/v1/multi-view-scientific-analysis-canvas/catalog")
def multi_view_scientific_analysis_canvas_catalog(): return mvac1353_catalog()
@app.get("/v1/multi-view-scientific-analysis-canvas/schema")
def multi_view_scientific_analysis_canvas_schema(): return mvac1353_schema()
def _mvac1353_call(fn,payload):
    try: return fn(payload)
    except MultiViewScientificAnalysisCanvasError as exc: raise HTTPException(status_code=exc.status_code,detail=exc.detail)
@app.post("/v1/multi-view-scientific-analysis-canvas/canvas/normalize")
def mvac1353_route_0(payload:dict): return _mvac1353_call(mvac1353_normalize,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/canvas/compose")
def mvac1353_route_1(payload:dict): return _mvac1353_call(mvac1353_compose,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/layout/analysis")
def mvac1353_route_2(payload:dict): return _mvac1353_call(mvac1353_analysis_layout,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/layout/comparison")
def mvac1353_route_3(payload:dict): return _mvac1353_call(mvac1353_comparison_layout,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/layout/diagnostics")
def mvac1353_route_4(payload:dict): return _mvac1353_call(mvac1353_diagnostics_layout,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/layout/evidence")
def mvac1353_route_5(payload:dict): return _mvac1353_call(mvac1353_evidence_layout,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/layout/presentation")
def mvac1353_route_6(payload:dict): return _mvac1353_call(mvac1353_presentation_layout,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/panel/plan")
def mvac1353_route_7(payload:dict): return _mvac1353_call(mvac1353_panel,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/panel/primary-figure")
def mvac1353_route_8(payload:dict): return _mvac1353_call(mvac1353_primary,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/panel/diagnostics")
def mvac1353_route_9(payload:dict): return _mvac1353_call(mvac1353_diagnostics_panel,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/panel/uncertainty")
def mvac1353_route_10(payload:dict): return _mvac1353_call(mvac1353_uncertainty_panel,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/panel/evidence")
def mvac1353_route_11(payload:dict): return _mvac1353_call(mvac1353_evidence_panel,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/panel/provenance")
def mvac1353_route_12(payload:dict): return _mvac1353_call(mvac1353_provenance_panel,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/panel/model-graph")
def mvac1353_route_13(payload:dict): return _mvac1353_call(mvac1353_model_graph,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/panel/data-table")
def mvac1353_route_14(payload:dict): return _mvac1353_call(mvac1353_table,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/panel/narrative")
def mvac1353_route_15(payload:dict): return _mvac1353_call(mvac1353_narrative,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/selection/normalize")
def mvac1353_route_16(payload:dict): return _mvac1353_call(mvac1353_selection_normalize,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/selection/link")
def mvac1353_route_17(payload:dict): return _mvac1353_call(mvac1353_selection_link,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/selection/clear")
def mvac1353_route_18(payload:dict): return _mvac1353_call(mvac1353_selection_clear,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/filter/normalize")
def mvac1353_route_19(payload:dict): return _mvac1353_call(mvac1353_filter_normalize,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/filter/apply")
def mvac1353_route_20(payload:dict): return _mvac1353_call(mvac1353_filter_apply,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/filter/share")
def mvac1353_route_21(payload:dict): return _mvac1353_call(mvac1353_filter_share,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/domain/sync")
def mvac1353_route_22(payload:dict): return _mvac1353_call(mvac1353_domain,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/cursor/sync")
def mvac1353_route_23(payload:dict): return _mvac1353_call(mvac1353_cursor,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/brush/plan")
def mvac1353_route_24(payload:dict): return _mvac1353_call(mvac1353_brush,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/focus/plan")
def mvac1353_route_25(payload:dict): return _mvac1353_call(mvac1353_focus,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/annotation/plan")
def mvac1353_route_26(payload:dict): return _mvac1353_call(mvac1353_annotation,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/context/evidence")
def mvac1353_route_27(payload:dict): return _mvac1353_call(mvac1353_evidence_context,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/context/diagnostics")
def mvac1353_route_28(payload:dict): return _mvac1353_call(mvac1353_diagnostic_context,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/context/uncertainty")
def mvac1353_route_29(payload:dict): return _mvac1353_call(mvac1353_uncertainty_context,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/context/provenance")
def mvac1353_route_30(payload:dict): return _mvac1353_call(mvac1353_provenance_context,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/snapshot/build")
def mvac1353_route_31(payload:dict): return _mvac1353_call(mvac1353_snapshot,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/revision/plan")
def mvac1353_route_32(payload:dict): return _mvac1353_call(mvac1353_revision,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/export/plan")
def mvac1353_route_33(payload:dict): return _mvac1353_call(mvac1353_export,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/handoff/plan")
def mvac1353_route_34(payload:dict): return _mvac1353_call(mvac1353_handoff,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/core-object/plan")
def mvac1353_route_35(payload:dict): return _mvac1353_call(mvac1353_core,payload)
@app.post("/v1/multi-view-scientific-analysis-canvas/readiness/report")
def mvac1353_route_36(payload:dict): return _mvac1353_call(mvac1353_readiness,payload)

# Lab v0.135.4 — Interactive Scientific Scene & Visual Drill-Down
@app.get("/v1/interactive-scientific-scene-drilldown/health")
def interactive_scientific_scene_drilldown_health(): return scene1354.health()
@app.get("/v1/interactive-scientific-scene-drilldown/manifest")
def interactive_scientific_scene_drilldown_manifest(): return scene1354.manifest()
@app.get("/v1/interactive-scientific-scene-drilldown/catalog")
def interactive_scientific_scene_drilldown_catalog(): return scene1354.catalog()
@app.get("/v1/interactive-scientific-scene-drilldown/schema")
def interactive_scientific_scene_drilldown_schema(): return scene1354.schema_info()
def _scene1354_call(fn,payload):
    try: return fn(payload)
    except InteractiveScientificSceneDrillDownError as exc: raise HTTPException(status_code=exc.status_code,detail=exc.detail)
_SCENE1354_ROUTES = [
("scene/normalize",scene1354.normalize_scene),("scene/compose",scene1354.compose_scene),("layer/plan",scene1354.layer_plan),
("layer/figure",scene1354.figure_layer_plan),("layer/data",scene1354.data_layer_plan),("layer/uncertainty",scene1354.uncertainty_layer_plan),
("layer/evidence",scene1354.evidence_layer_plan),("layer/provenance",scene1354.provenance_layer_plan),("layer/spatial",scene1354.spatial_layer_plan),
("layer/temporal",scene1354.temporal_layer_plan),("drill/target",scene1354.drill_target),("drill/path",scene1354.drill_path),
("drill/children",scene1354.drill_children),("drill/breadcrumb",scene1354.breadcrumb_plan),("drill/detail-panel",scene1354.detail_panel_plan),
("selection/bridge",scene1354.selection_bridge),("context/linked",scene1354.linked_context_plan),("mapping/validate",scene1354.mapping_validation),
("viewport/plan",scene1354.viewport_plan),("camera/plan",scene1354.camera_plan),("time/scrub",scene1354.time_scrub_plan),
("slice/plan",scene1354.slice_plan),("isolate/plan",scene1354.isolate_plan),("compare/plan",scene1354.compare_plan),
("trace/plan",scene1354.trace_plan),("lod/plan",scene1354.lod_plan),("density/plan",scene1354.density_plan),
("context/evidence",scene1354.evidence_context_plan),("context/uncertainty",scene1354.uncertainty_context_plan),("context/diagnostics",scene1354.diagnostic_context_plan),
("context/provenance",scene1354.provenance_context_plan),("context/spatial",scene1354.spatial_context_plan),("animation/plan",scene1354.animation_plan),
("measurement/plan",scene1354.measurement_plan),("annotation/focus",scene1354.annotation_focus_plan),("snapshot/build",scene1354.build_snapshot),
("revision/plan",scene1354.revision_plan),("export/plan",scene1354.export_plan),("handoff/plan",scene1354.handoff_plan),
("core-object/plan",scene1354.core_object_plan),("readiness/report",scene1354.readiness_report),
]
for _i,(_path,_fn) in enumerate(_SCENE1354_ROUTES):
    def _make_scene1354_route(fn):
        async def _route(payload:dict): return _scene1354_call(fn,payload)
        return _route
    _route=_make_scene1354_route(_fn); _route.__name__=f"scene1354_route_{_i}"
    app.post(f"/v1/interactive-scientific-scene-drilldown/{_path}")(_route)


# Lab v0.135.5 — Scientific Scene Linking, Comparative Inspection & Context Preservation
@app.get("/v1/scientific-scene-linking-comparative-context/health")
def scientific_scene_linking_comparative_context_health(): return scene1355.health()
@app.get("/v1/scientific-scene-linking-comparative-context/manifest")
def scientific_scene_linking_comparative_context_manifest(): return scene1355.manifest()
@app.get("/v1/scientific-scene-linking-comparative-context/catalog")
def scientific_scene_linking_comparative_context_catalog(): return scene1355.catalog()
@app.get("/v1/scientific-scene-linking-comparative-context/schema")
def scientific_scene_linking_comparative_context_schema(): return scene1355.schema_info()
def _scene1355_call(fn,payload):
    try: return fn(payload)
    except ScientificSceneLinkingComparativeContextError as exc: raise HTTPException(status_code=exc.status_code,detail=exc.detail)
_SCENE1355_ROUTES = [
("link/normalize",scene1355.normalize_link),("link/validate",scene1355.validate_link),("context/normalize",scene1355.normalize_context_capsule),
("context/preserve",scene1355.preserve_context),("context/restore",scene1355.restore_context),("pin/normalize",scene1355.normalize_pin),
("pin/add",scene1355.pin_object),("pin/remove",scene1355.unpin_object),("pin/set",scene1355.pin_set),
("comparison/normalize",scene1355.normalize_comparison_set),("comparison/plan",scene1355.comparison_plan),("comparison/side-by-side",scene1355.side_by_side_plan),
("comparison/overlay",scene1355.overlay_plan),("comparison/synchronized",scene1355.synchronized_plan),("comparison/provenance",scene1355.provenance_compare_plan),
("comparison/difference-metadata",scene1355.difference_metadata_plan),("comparison/small-multiple",scene1355.small_multiple_plan),("focus/synchronize",scene1355.synchronized_focus_plan),
("bridge/context",scene1355.context_bridge),("bridge/canvas",scene1355.canvas_bridge),("bridge/scene",scene1355.scene_bridge),
("bridge/provenance",scene1355.provenance_bridge),("bridge/uncertainty",scene1355.uncertainty_bridge),("bridge/evidence",scene1355.evidence_bridge),
("bridge/diagnostics",scene1355.diagnostics_bridge),("bridge/spatial",scene1355.spatial_bridge),("bridge/temporal",scene1355.temporal_bridge),
("mapping/validate",scene1355.mapping_validation),("guard/equivalence",scene1355.equivalence_guard),("guard/compare-targets",scene1355.compare_target_guard),
("link/cross-scene",scene1355.cross_scene_link_plan),("navigation/context",scene1355.navigation_context_plan),("drill/context",scene1355.drill_context_plan),
("breadcrumb/context",scene1355.breadcrumb_context_plan),("context/diff",scene1355.context_diff),("context/snapshot",scene1355.context_snapshot),
("snapshot/build",scene1355.build_snapshot),("snapshot/restore",scene1355.restore_snapshot),("context/audit",scene1355.audit_context_integrity),
("export/plan",scene1355.export_plan),("handoff/plan",scene1355.handoff_plan),("core-object/plan",scene1355.core_object_plan),
("boundaries/report",scene1355.interpretation_boundaries_report),("readiness/report",scene1355.readiness_report),
]
for _i,(_path,_fn) in enumerate(_SCENE1355_ROUTES):
    def _make_scene1355_route(fn):
        async def _route(payload:dict): return _scene1355_call(fn,payload)
        return _route
    _route=_make_scene1355_route(_fn); _route.__name__=f"scene1355_route_{_i}"
    app.post(f"/v1/scientific-scene-linking-comparative-context/{_path}")(_route)

# Lab v0.135.6 — Reproducible Visual Analysis Sessions & Interaction Lineage
@app.get("/v1/reproducible-visual-analysis-sessions/health")
def reproducible_visual_analysis_sessions_health(): return session1356.health()
@app.get("/v1/reproducible-visual-analysis-sessions/manifest")
def reproducible_visual_analysis_sessions_manifest(): return session1356.manifest()
@app.get("/v1/reproducible-visual-analysis-sessions/catalog")
def reproducible_visual_analysis_sessions_catalog(): return session1356.catalog()
@app.get("/v1/reproducible-visual-analysis-sessions/schema")
def reproducible_visual_analysis_sessions_schema(): return session1356.schema_info()
def _session1356_call(fn,payload):
    try: return fn(payload)
    except ReproducibleVisualAnalysisSessionError as exc: raise HTTPException(status_code=exc.status_code,detail=exc.detail)
_SESSION1356_ROUTES = [
("session/normalize",session1356.normalize_session),("session/create",session1356.create_session),("session/close",session1356.close_session),("session/clone",session1356.clone_session),
("event/normalize",session1356.normalize_event),("event/append",session1356.append_event),("event/batch",session1356.append_event_batch),("event/list",session1356.list_events),
("lineage/build",session1356.build_lineage),("lineage/audit",session1356.audit_lineage),("lineage/path",session1356.lineage_path),("lineage/upstream",session1356.lineage_upstream),("lineage/downstream",session1356.lineage_downstream),
("checkpoint/normalize",session1356.normalize_checkpoint),("checkpoint/create",session1356.create_checkpoint),("checkpoint/restore",session1356.restore_checkpoint),("checkpoint/list",session1356.list_checkpoints),
("replay/plan",session1356.replay_plan),("replay/validate",session1356.validate_replay),("replay/window",session1356.replay_window),("replay/step",session1356.replay_step),
("state/current",session1356.current_state),("state/diff",session1356.state_diff),("state/hash",session1356.state_hash),("state/restore-plan",session1356.state_restore_plan),
("bridge/canvas",session1356.canvas_bridge),("bridge/scene",session1356.scene_bridge),("bridge/context",session1356.context_bridge),("bridge/comparison",session1356.comparison_bridge),("bridge/pins",session1356.pins_bridge),("bridge/provenance",session1356.provenance_bridge),("bridge/uncertainty",session1356.uncertainty_bridge),("bridge/evidence",session1356.evidence_bridge),
("export/plan",session1356.export_plan),("import/plan",session1356.import_plan),("reproduction/plan",session1356.reproduction_plan),("core-object/plan",session1356.core_object_plan),("handoff/plan",session1356.handoff_plan),
("guard/sequence",session1356.sequence_guard),("guard/references",session1356.reference_guard),("guard/presentation",session1356.presentation_guard),("guard/scientific-boundary",session1356.scientific_boundary_guard),
("boundaries/report",session1356.interpretation_boundaries_report),("readiness/report",session1356.readiness_report),("integrity/report",session1356.integrity_report),("determinism/report",session1356.determinism_report),
("snapshot/build",session1356.build_snapshot),("snapshot/restore",session1356.restore_snapshot),
]
for _i,(_path,_fn) in enumerate(_SESSION1356_ROUTES):
    def _make_session1356_route(fn):
        async def _route(payload:dict): return _session1356_call(fn,payload)
        return _route
    _route=_make_session1356_route(_fn); _route.__name__=f"session1356_route_{_i}"
    app.post(f"/v1/reproducible-visual-analysis-sessions/{_path}")(_route)

# Lab v0.135.7 — Visual Research Narrative, Findings & Publication Handoff
@app.get("/v1/visual-research-narrative-findings/health")
def visual_research_narrative_findings_health(): return narrative1357.health()
@app.get("/v1/visual-research-narrative-findings/manifest")
def visual_research_narrative_findings_manifest(): return narrative1357.manifest()
@app.get("/v1/visual-research-narrative-findings/catalog")
def visual_research_narrative_findings_catalog(): return narrative1357.catalog()
@app.get("/v1/visual-research-narrative-findings/schema")
def visual_research_narrative_findings_schema(): return narrative1357.schema_info()
def _narrative1357_call(fn,payload):
    try: return fn(payload)
    except VisualResearchNarrativeFindingsError as exc: raise HTTPException(status_code=exc.status_code,detail=exc.detail)
_NARRATIVE1357_ROUTES = [
("narrative/normalize",narrative1357.normalize_narrative),("narrative/create",narrative1357.create_narrative),("narrative/clone",narrative1357.clone_narrative),("narrative/reorder",narrative1357.reorder_narrative),
("block/normalize",narrative1357.normalize_block),("block/add",narrative1357.add_block),("block/update",narrative1357.update_block),("block/remove",narrative1357.remove_block),("block/list",narrative1357.list_blocks),
("figure/bind",narrative1357.bind_figure),("figure/snapshot",narrative1357.snapshot_figure),("figure/caption",narrative1357.caption_figure),("figure/alt-text",narrative1357.alt_text_figure),
("finding/normalize",narrative1357.normalize_finding),("finding/create",narrative1357.create_finding),("finding/status",narrative1357.set_finding_status),("finding/evidence",narrative1357.link_finding_evidence),("finding/uncertainty",narrative1357.link_finding_uncertainty),("finding/limitation",narrative1357.link_finding_limitation),
("annotation/normalize",narrative1357.normalize_annotation),("annotation/add",narrative1357.add_annotation),("annotation/resolve",narrative1357.resolve_annotation),
("citation/normalize",narrative1357.normalize_citation),("citation/plan",narrative1357.citation_plan),("citation/validate",narrative1357.validate_citation),
("context/provenance",narrative1357.context_provenance),("context/evidence",narrative1357.context_evidence),("context/uncertainty",narrative1357.context_uncertainty),("context/diagnostics",narrative1357.context_diagnostics),("context/session",narrative1357.context_session),("context/scene",narrative1357.context_scene),("context/canvas",narrative1357.context_canvas),("context/comparison",narrative1357.context_comparison),
("publication/outline",narrative1357.publication_outline),("publication/section",narrative1357.publication_section),("publication/figure-table",narrative1357.publication_figure_table),("publication/appendix",narrative1357.publication_appendix),("publication/abstract",narrative1357.publication_abstract),("publication/readiness",narrative1357.publication_readiness),
("handoff/plan",narrative1357.handoff_plan),("handoff/core",narrative1357.handoff_core),("handoff/publication",narrative1357.handoff_publication),("handoff/library",narrative1357.handoff_library),("handoff/workspace",narrative1357.handoff_workspace),
("export/plan",narrative1357.export_plan),("export/package",narrative1357.export_package),("export/manifest",narrative1357.export_manifest),
("guard/claims",narrative1357.guard_claims),("guard/evidence",narrative1357.guard_evidence),("guard/presentation",narrative1357.guard_presentation),("guard/references",narrative1357.guard_references),
("boundaries/report",narrative1357.interpretation_boundaries_report),
]
for _i,(_path,_fn) in enumerate(_NARRATIVE1357_ROUTES):
    def _make_narrative1357_route(fn):
        async def _route(payload:dict): return _narrative1357_call(fn,payload)
        return _route
    _route=_make_narrative1357_route(_fn); _route.__name__=f"narrative1357_route_{_i}"
    app.post(f"/v1/visual-research-narrative-findings/{_path}")(_route)



# Lab v0.135.8 — Research Review, Critique & Revision Lineage
@app.get("/v1/research-review-critique-revision/health")
def research_review_critique_revision_health(): return review1358.health()
@app.get("/v1/research-review-critique-revision/manifest")
def research_review_critique_revision_manifest(): return review1358.manifest()
@app.get("/v1/research-review-critique-revision/catalog")
def research_review_critique_revision_catalog(): return review1358.catalog()
@app.get("/v1/research-review-critique-revision/schema")
def research_review_critique_revision_schema(): return review1358.schema_info()
def _review1358_call(fn,payload):
    try: return fn(payload)
    except ResearchReviewCritiqueRevisionError as exc: raise HTTPException(status_code=exc.status_code,detail=exc.detail)
_REVIEW1358_ROUTES = [
("review/normalize",review1358.normalize_review),("review/create",review1358.create_review),("review/clone",review1358.clone_review),("review/open",review1358.open_review),("review/close",review1358.close_review),
("item/normalize",review1358.normalize_item),("item/add",review1358.add_item),("item/update",review1358.update_item),("item/resolve",review1358.resolve_item),("item/list",review1358.list_items),
("critique/normalize",review1358.normalize_critique),("critique/add",review1358.add_critique),("critique/classify",review1358.classify_critique),("critique/link-target",review1358.link_critique_target),("critique/resolve",review1358.resolve_critique),
("response/normalize",review1358.normalize_response),("response/add",review1358.add_response),("response/update",review1358.update_response),("response/status",review1358.set_response_status),
("revision/normalize",review1358.normalize_revision),("revision/create",review1358.create_revision),("revision/diff",review1358.revision_diff),("revision/link-response",review1358.link_revision_response),("revision/supersede",review1358.supersede_revision),("revision/list",review1358.list_revisions),
("decision/normalize",review1358.normalize_decision),("decision/record",review1358.record_decision),
("replication/study",review1358.link_replication_study),("replication/run",review1358.link_replication_run),("replication/result",review1358.link_replication_result),("replication/plan",review1358.replication_plan),
("context/narrative",review1358.context_narrative),("context/finding",review1358.context_finding),("context/evidence",review1358.context_evidence),("context/uncertainty",review1358.context_uncertainty),("context/method",review1358.context_method),("context/session",review1358.context_session),("context/scene",review1358.context_scene),("context/provenance",review1358.context_provenance),
("lineage/build",review1358.build_lineage),("lineage/audit",review1358.audit_lineage),("lineage/path",review1358.lineage_path),("lineage/upstream",review1358.lineage_upstream),("lineage/downstream",review1358.lineage_downstream),
("handoff/plan",review1358.handoff_plan),("handoff/core",review1358.handoff_core),("handoff/publication",review1358.handoff_publication),("handoff/library",review1358.handoff_library),("handoff/workspace",review1358.handoff_workspace),("handoff/replication",review1358.handoff_replication),
("guard/claims",review1358.guard_claims),("guard/evidence",review1358.guard_evidence),("guard/review",review1358.guard_review),("guard/presentation",review1358.guard_presentation),("guard/references",review1358.guard_references),
("boundaries/report",review1358.interpretation_boundaries_report),
]
for _i,(_path,_fn) in enumerate(_REVIEW1358_ROUTES):
    def _make_review1358_route(fn):
        async def _route(payload:dict): return _review1358_call(fn,payload)
        return _route
    _route=_make_review1358_route(_fn); _route.__name__=f"review1358_route_{_i}"
    app.post(f"/v1/research-review-critique-revision/{_path}")(_route)

# Lab v0.135.8.1 — Graph Studio Canonical Runtime & Interface Recovery
@app.get("/v1/graph-studio-canonical-runtime/health")
def graph_studio_canonical_runtime_health(): return canonical13581.health()
@app.get("/v1/graph-studio-canonical-runtime/manifest")
def graph_studio_canonical_runtime_manifest(): return canonical13581.manifest()
@app.get("/v1/graph-studio-canonical-runtime/catalog")
def graph_studio_canonical_runtime_catalog(): return canonical13581.catalog()
@app.get("/v1/graph-studio-canonical-runtime/schema")
def graph_studio_canonical_runtime_schema(): return canonical13581.schema_info()
def _canonical13581_call(fn,payload):
    try: return fn(payload)
    except GraphStudioCanonicalRuntimeError as exc: raise HTTPException(status_code=exc.status_code,detail=exc.detail)
_CANONICAL13581_ROUTES = [
("runtime/normalize",canonical13581.normalize_runtime),("runtime/verify",canonical13581.verify_runtime),
("view/normalize",canonical13581.normalize_view),("view/activate",canonical13581.activate_view),
("lifecycle/plan",canonical13581.lifecycle_plan),("capability/normalize",canonical13581.normalize_capability),("capability/isolate",canonical13581.isolate_capability),
("asset/plan",canonical13581.asset_plan),("asset/audit",canonical13581.asset_audit),("identity/report",canonical13581.identity_report),
("dom/audit",canonical13581.dom_audit),("boundaries/report",canonical13581.boundaries_report),
]
for _i,(_path,_fn) in enumerate(_CANONICAL13581_ROUTES):
    def _make_canonical13581_route(fn):
        async def _route(payload:dict): return _canonical13581_call(fn,payload)
        return _route
    _route=_make_canonical13581_route(_fn); _route.__name__=f"canonical13581_route_{_i}"
    app.post(f"/v1/graph-studio-canonical-runtime/{_path}")(_route)



# v0.135.8.2 Graph Studio Renderer Replacement & Advanced Visual Analysis Workspace
@app.get("/v1/graph-studio-renderer/v013582/health")
def graph_studio_renderer_v013582_health(): return renderer13582.health()

@app.get("/v1/graph-studio-renderer/v013582/manifest")
def graph_studio_renderer_v013582_manifest(): return renderer13582.manifest()

@app.get("/v1/graph-studio-renderer/v013582/renderer-registry")
def graph_studio_renderer_v013582_registry(): return renderer13582.renderer_registry()

@app.get("/v1/graph-studio-renderer/v013582/view-catalog")
def graph_studio_renderer_v013582_view_catalog(): return renderer13582.view_catalog()

@app.get("/v1/graph-studio-renderer/v013582/capability-catalog")
def graph_studio_renderer_v013582_capability_catalog(): return renderer13582.capability_catalog()

@app.get("/v1/graph-studio-renderer/v013582/legacy-audit")
def graph_studio_renderer_v013582_legacy_audit(): return renderer13582.legacy_audit()

@app.get("/v1/graph-studio-renderer/v013582/identity")
def graph_studio_renderer_v013582_identity(): return renderer13582.identity_report()

@app.get("/v1/graph-studio-renderer/v013582/boundaries")
def graph_studio_renderer_v013582_boundaries(): return renderer13582.boundaries_report()

def _renderer13582_call(fn,payload):
    try: return fn(payload)
    except GraphStudioRendererReplacementError as exc: raise HTTPException(status_code=exc.status_code, detail=exc.detail) from exc

for _i,(_path,_fn) in enumerate((
    ("workspace/normalize",renderer13582.normalize_workspace),("renderer/select",renderer13582.renderer_select),
    ("render/plan",renderer13582.render_plan),("analysis/plan",renderer13582.analysis_plan),
    ("provenance/plan",renderer13582.provenance_plan),("scene/plan",renderer13582.scene_plan),
    ("linked-view/plan",renderer13582.linked_view_plan),("compare/plan",renderer13582.compare_plan),
    ("export/plan",renderer13582.export_plan),("lifecycle/plan",renderer13582.lifecycle_plan),
    ("asset/plan",renderer13582.asset_plan),("asset/audit",renderer13582.asset_audit),
    ("dom/audit",renderer13582.dom_audit),("session/plan",renderer13582.session_plan),
    ("narrative/plan",renderer13582.narrative_plan),("review/plan",renderer13582.review_plan),
)):
    def _make_renderer13582_route(fn):
        async def _route(payload:dict): return _renderer13582_call(fn,payload)
        return _route
    _route=_make_renderer13582_route(_fn); _route.__name__=f"renderer13582_route_{_i}"
    app.post(f"/v1/graph-studio-renderer/v013582/{_path}")(_route)


@app.get("/v1/graph-studio-recovery/v013583/health")
def graph_studio_recovery_v013583_health(): return recovery13583.health()
@app.get("/v1/graph-studio-recovery/v013583/manifest")
def graph_studio_recovery_v013583_manifest(): return recovery13583.manifest()
@app.get("/v1/graph-studio-recovery/v013583/lifecycle")
def graph_studio_recovery_v013583_lifecycle(): return recovery13583.lifecycle_report()
@app.get("/v1/graph-studio-recovery/v013583/acceptance")
def graph_studio_recovery_v013583_acceptance(): return recovery13583.acceptance_report()
@app.get("/v1/graph-studio-recovery/v013583/boundaries")
def graph_studio_recovery_v013583_boundaries(): return recovery13583.boundaries_report()
@app.get("/v1/graph-studio-recovery/v013583/compatibility")
def graph_studio_recovery_v013583_compatibility(): return recovery13583.compatibility_report()
@app.post("/v1/graph-studio-recovery/v013583/hydration/plan")
async def graph_studio_recovery_v013583_hydration(payload:dict): return recovery13583.hydration_plan(payload)
@app.post("/v1/graph-studio-recovery/v013583/provenance/plan")
async def graph_studio_recovery_v013583_provenance(payload:dict): return recovery13583.provenance_plan(payload)
@app.post("/v1/graph-studio-recovery/v013583/state/normalize")
async def graph_studio_recovery_v013583_normalize(payload:dict): return recovery13583.normalize_state(payload)

@app.post("/v1/graph-studio-recovery/v013583/dom/audit")
async def graph_studio_recovery_v013583_dom(payload:dict): return recovery13583.dom_audit(payload)
@app.get("/v1/graph-studio-recovery/v013583/hydration/sources")
def graph_studio_recovery_v013583_sources(): return recovery13583.hydration_sources()
@app.get("/v1/graph-studio-recovery/v013583/provenance/layouts")
def graph_studio_recovery_v013583_layouts(): return recovery13583.provenance_layouts()
@app.get("/v1/graph-studio-recovery/v013583/renderer/status")
def graph_studio_recovery_v013583_renderer_status(): return recovery13583.renderer_status()
@app.get("/v1/graph-studio-recovery/v013583/state/status")
def graph_studio_recovery_v013583_state_status(): return recovery13583.state_status()
@app.post("/v1/graph-studio-recovery/v013583/recovery/plan")
async def graph_studio_recovery_v013583_plan(payload:dict): return recovery13583.recovery_plan(payload)
@app.post("/v1/graph-studio-recovery/v013583/ui/acceptance")
async def graph_studio_recovery_v013583_ui_acceptance(payload:dict): return recovery13583.ui_acceptance(payload)


# Lab v0.135.8.3.1 — Graph Studio Bootstrap, Hydration & Legacy Surface Finalization
@app.get("/v1/graph-studio-bootstrap-finalization/v0135831/health")
def graph_studio_bootstrap_finalization_v0135831_health(): return bootstrap135831.health()
@app.get("/v1/graph-studio-bootstrap-finalization/v0135831/manifest")
def graph_studio_bootstrap_finalization_v0135831_manifest(): return bootstrap135831.manifest()
@app.get("/v1/graph-studio-bootstrap-finalization/v0135831/bootstrap")
def graph_studio_bootstrap_finalization_v0135831_bootstrap(): return bootstrap135831.bootstrap_plan()
@app.get("/v1/graph-studio-bootstrap-finalization/v0135831/hydration")
def graph_studio_bootstrap_finalization_v0135831_hydration(): return bootstrap135831.hydration_contract()
@app.get("/v1/graph-studio-bootstrap-finalization/v0135831/legacy")
def graph_studio_bootstrap_finalization_v0135831_legacy(): return bootstrap135831.legacy_contract()
@app.get("/v1/graph-studio-bootstrap-finalization/v0135831/acceptance")
def graph_studio_bootstrap_finalization_v0135831_acceptance(): return bootstrap135831.acceptance_report()
@app.get("/v1/graph-studio-bootstrap-finalization/v0135831/boundaries")
def graph_studio_bootstrap_finalization_v0135831_boundaries(): return bootstrap135831.boundaries()
@app.get("/v1/graph-studio-bootstrap-finalization/v0135831/compatibility")
def graph_studio_bootstrap_finalization_v0135831_compatibility(): return bootstrap135831.compatibility()


# Lab v0.135.8.4 — Live Scientific Object Binding, Project-State Persistence & Advanced Provenance Exploration
@app.get("/v1/graph-studio-live-binding/v013584/health")
def graph_studio_live_binding_v013584_health(): return binding13584.health()
@app.get("/v1/graph-studio-live-binding/v013584/binding")
def graph_studio_live_binding_v013584_binding(): return binding13584.binding_contract()
@app.get("/v1/graph-studio-live-binding/v013584/persistence")
def graph_studio_live_binding_v013584_persistence(): return binding13584.persistence_contract()
@app.get("/v1/graph-studio-live-binding/v013584/provenance")
def graph_studio_live_binding_v013584_provenance(): return binding13584.provenance_contract()
@app.get("/v1/graph-studio-live-binding/v013584/acceptance")
def graph_studio_live_binding_v013584_acceptance(): return binding13584.acceptance_report()
@app.get("/v1/graph-studio-live-binding/v013584/compatibility")
def graph_studio_live_binding_v013584_compatibility(): return binding13584.compatibility()
@app.get("/v1/graph-studio-live-binding/v013584/object-types")
def graph_studio_live_binding_v013584_object_types(): return binding13584.object_types()
@app.get("/v1/graph-studio-live-binding/v013584/relation-types")
def graph_studio_live_binding_v013584_relation_types(): return binding13584.relation_types()
@app.post("/v1/graph-studio-live-binding/v013584/binding/plan")
async def graph_studio_live_binding_v013584_binding_plan(payload:dict): return binding13584.binding_plan(payload)
@app.post("/v1/graph-studio-live-binding/v013584/restore/plan")
async def graph_studio_live_binding_v013584_restore_plan(payload:dict): return binding13584.restore_plan(payload)
@app.post("/v1/graph-studio-live-binding/v013584/provenance/plan")
async def graph_studio_live_binding_v013584_provenance_plan(payload:dict): return binding13584.provenance_plan(payload)
@app.post("/v1/graph-studio-live-binding/v013584/selection/plan")
async def graph_studio_live_binding_v013584_selection_plan(payload:dict): return binding13584.selection_plan(payload)


# Lab v0.135.8.4.1 — Provenance Interaction Lifecycle & Graph Control Recovery
@app.get("/v1/graph-studio-provenance-interaction/v0135841/health")
def graph_studio_provenance_interaction_v0135841_health(): return provenance135841.health()
@app.get("/v1/graph-studio-provenance-interaction/v0135841/lifecycle")
def graph_studio_provenance_interaction_v0135841_lifecycle(): return provenance135841.lifecycle_contract()
@app.get("/v1/graph-studio-provenance-interaction/v0135841/layout")
def graph_studio_provenance_interaction_v0135841_layout(): return provenance135841.layout_contract()
@app.get("/v1/graph-studio-provenance-interaction/v0135841/selection")
def graph_studio_provenance_interaction_v0135841_selection(): return provenance135841.selection_contract()
@app.get("/v1/graph-studio-provenance-interaction/v0135841/exploration")
def graph_studio_provenance_interaction_v0135841_exploration(): return provenance135841.exploration_contract()
@app.get("/v1/graph-studio-provenance-interaction/v0135841/persistence")
def graph_studio_provenance_interaction_v0135841_persistence(): return provenance135841.persistence_contract()
@app.post("/v1/graph-studio-provenance-interaction/v0135841/interaction/plan")
async def graph_studio_provenance_interaction_v0135841_plan(payload:dict): return provenance135841.interaction_plan(payload)
@app.get("/v1/graph-studio-provenance-interaction/v0135841/acceptance")
def graph_studio_provenance_interaction_v0135841_acceptance(): return provenance135841.acceptance_report()

# Lab v0.135.8.5 — Native Provenance State Engine, Interactive Graph Runtime & Browser Hardening
@app.get("/v1/graph-studio-native-provenance/v013585/health")
def graph_studio_native_provenance_v013585_health(): return provenance13585.health()
@app.get("/v1/graph-studio-native-provenance/v013585/runtime")
def graph_studio_native_provenance_v013585_runtime(): return provenance13585.runtime_contract()
@app.get("/v1/graph-studio-native-provenance/v013585/graph")
def graph_studio_native_provenance_v013585_graph(): return provenance13585.graph_contract()
@app.get("/v1/graph-studio-native-provenance/v013585/layout")
def graph_studio_native_provenance_v013585_layout(): return provenance13585.layout_contract()
@app.get("/v1/graph-studio-native-provenance/v013585/traversal")
def graph_studio_native_provenance_v013585_traversal(): return provenance13585.traversal_contract()
@app.get("/v1/graph-studio-native-provenance/v013585/inspector")
def graph_studio_native_provenance_v013585_inspector(): return provenance13585.inspector_contract()
@app.get("/v1/graph-studio-native-provenance/v013585/persistence")
def graph_studio_native_provenance_v013585_persistence(): return provenance13585.persistence_contract()
@app.get("/v1/graph-studio-native-provenance/v013585/browser-hardening")
def graph_studio_native_provenance_v013585_browser_hardening(): return provenance13585.browser_hardening_contract()
@app.post("/v1/graph-studio-native-provenance/v013585/graph/normalize")
async def graph_studio_native_provenance_v013585_normalize(payload:dict): return provenance13585.normalize_graph(payload)
@app.post("/v1/graph-studio-native-provenance/v013585/traverse")
async def graph_studio_native_provenance_v013585_traverse(payload:dict): return provenance13585.traverse(payload)
@app.post("/v1/graph-studio-native-provenance/v013585/interaction/plan")
async def graph_studio_native_provenance_v013585_plan(payload:dict): return provenance13585.interaction_plan(payload)
@app.get("/v1/graph-studio-native-provenance/v013585/diagnostics")
def graph_studio_native_provenance_v013585_diagnostics(): return provenance13585.diagnostics()
@app.get("/v1/graph-studio-native-provenance/v013585/acceptance")
def graph_studio_native_provenance_v013585_acceptance(): return provenance13585.acceptance_report()

# Lab v0.135.8.5.1 — Provenance Runtime Authority, Legacy Interaction Shutdown & Real Browser Certification
@app.get("/v1/graph-studio-provenance-authority/v0135851/health")
def graph_studio_provenance_authority_v0135851_health(): return provenance135851.health()
@app.get("/v1/graph-studio-provenance-authority/v0135851/authority")
def graph_studio_provenance_authority_v0135851_authority(): return provenance135851.authority_contract()
@app.get("/v1/graph-studio-provenance-authority/v0135851/browser")
def graph_studio_provenance_authority_v0135851_browser(): return provenance135851.browser_contract()
@app.get("/v1/graph-studio-provenance-authority/v0135851/diagnostics")
def graph_studio_provenance_authority_v0135851_diagnostics(): return provenance135851.runtime_diagnostics()
@app.get("/v1/graph-studio-provenance-authority/v0135851/acceptance")
def graph_studio_provenance_authority_v0135851_acceptance(): return provenance135851.acceptance_report()
@app.get("/v1/graph-studio-provenance-authority/v0135851/deployment-gate")
def graph_studio_provenance_authority_v0135851_gate(): return provenance135851.deployment_gate()


# Lab v0.135.8.5.2 — Provenance Interaction Responsiveness, Relationship Filtering & Incremental Rendering
@app.get("/v1/graph-studio-incremental-interaction/v0135852/health")
def graph_studio_incremental_interaction_v0135852_health(): return provenance135852.health()
@app.get("/v1/graph-studio-incremental-interaction/v0135852/performance")
def graph_studio_incremental_interaction_v0135852_performance(): return provenance135852.performance_contract()
@app.get("/v1/graph-studio-incremental-interaction/v0135852/relationship-filter")
def graph_studio_incremental_interaction_v0135852_relationship_filter(): return provenance135852.relationship_filter_contract()
@app.get("/v1/graph-studio-incremental-interaction/v0135852/persistence")
def graph_studio_incremental_interaction_v0135852_persistence(): return provenance135852.persistence_contract()
@app.get("/v1/graph-studio-incremental-interaction/v0135852/browser")
def graph_studio_incremental_interaction_v0135852_browser(): return provenance135852.browser_contract()
@app.get("/v1/graph-studio-incremental-interaction/v0135852/diagnostics")
def graph_studio_incremental_interaction_v0135852_diagnostics(): return provenance135852.diagnostics_contract()
@app.get("/v1/graph-studio-incremental-interaction/v0135852/acceptance")
def graph_studio_incremental_interaction_v0135852_acceptance(): return provenance135852.acceptance_report()
# Lab v0.135.8.5.3 — Context-Aware Relationship Filtering & Provenance Interaction Hardening
@app.get("/v1/graph-studio-context-relationships/v0135853/health")
def graph_studio_context_relationships_v0135853_health(): return provenance135853.health()
@app.get("/v1/graph-studio-context-relationships/v0135853/compatibility")
def graph_studio_context_relationships_v0135853_compatibility(): return provenance135853.compatibility_contract()
@app.post("/v1/graph-studio-context-relationships/v0135853/options")
def graph_studio_context_relationships_v0135853_options(payload: dict): return provenance135853.relationship_options(payload)
@app.get("/v1/graph-studio-context-relationships/v0135853/zero-match")
def graph_studio_context_relationships_v0135853_zero_match(): return provenance135853.zero_match_contract()
@app.get("/v1/graph-studio-context-relationships/v0135853/persistence")
def graph_studio_context_relationships_v0135853_persistence(): return provenance135853.persistence_contract()
@app.get("/v1/graph-studio-context-relationships/v0135853/browser")
def graph_studio_context_relationships_v0135853_browser(): return provenance135853.browser_contract()
@app.get("/v1/graph-studio-context-relationships/v0135853/diagnostics")
def graph_studio_context_relationships_v0135853_diagnostics(): return provenance135853.diagnostics_contract()
@app.get("/v1/graph-studio-context-relationships/v0135853/acceptance")
def graph_studio_context_relationships_v0135853_acceptance(): return provenance135853.acceptance_report()


# Lab v0.135.9.0 — Scientific Object Inspector, Provenance Neighborhoods & Cross-Workspace Navigation
@app.get("/v1/graph-studio-object-explorer/v013590/health")
def graph_studio_object_explorer_v013590_health(): return explorer13590.health()
@app.post("/v1/graph-studio-object-explorer/v013590/neighborhood")
def graph_studio_object_explorer_v013590_neighborhood(payload: dict): return explorer13590.neighborhood(payload)
@app.post("/v1/graph-studio-object-explorer/v013590/object-summary")
def graph_studio_object_explorer_v013590_object_summary(payload: dict): return explorer13590.object_summary(payload)
@app.post("/v1/graph-studio-object-explorer/v013590/relation-summary")
def graph_studio_object_explorer_v013590_relation_summary(payload: dict): return explorer13590.relation_summary(payload)
@app.get("/v1/graph-studio-object-explorer/v013590/inspector")
def graph_studio_object_explorer_v013590_inspector(): return explorer13590.inspector_contract()
@app.get("/v1/graph-studio-object-explorer/v013590/navigation")
def graph_studio_object_explorer_v013590_navigation(): return explorer13590.navigation_contract()
@app.get("/v1/graph-studio-object-explorer/v013590/scope")
def graph_studio_object_explorer_v013590_scope(): return explorer13590.scope_contract()
@app.get("/v1/graph-studio-object-explorer/v013590/persistence")
def graph_studio_object_explorer_v013590_persistence(): return explorer13590.persistence_contract()
@app.get("/v1/graph-studio-object-explorer/v013590/browser")
def graph_studio_object_explorer_v013590_browser(): return explorer13590.browser_contract()
@app.get("/v1/graph-studio-object-explorer/v013590/acceptance")
def graph_studio_object_explorer_v013590_acceptance(): return explorer13590.acceptance_report()


# Lab v0.135.10.0 — Provenance Path Analysis, Multi-Object Comparison & Research Context Linking
@app.get("/v1/graph-studio-path-analysis/v0135100/health")
def graph_studio_path_analysis_v0135100_health(): return path135100.health()
@app.post("/v1/graph-studio-path-analysis/v0135100/path")
def graph_studio_path_analysis_v0135100_path(payload: dict): return path135100.shortest_path(payload)
@app.post("/v1/graph-studio-path-analysis/v0135100/compare")
def graph_studio_path_analysis_v0135100_compare(payload: dict): return path135100.compare_objects(payload)
@app.post("/v1/graph-studio-path-analysis/v0135100/context-packet")
def graph_studio_path_analysis_v0135100_context(payload: dict): return path135100.context_packet(payload)
@app.get("/v1/graph-studio-path-analysis/v0135100/path-contract")
def graph_studio_path_analysis_v0135100_contract(): return path135100.path_contract()
@app.get("/v1/graph-studio-path-analysis/v0135100/navigation")
def graph_studio_path_analysis_v0135100_navigation(): return path135100.navigation_contract()
@app.get("/v1/graph-studio-path-analysis/v0135100/browser")
def graph_studio_path_analysis_v0135100_browser(): return path135100.browser_contract()
@app.get("/v1/graph-studio-path-analysis/v0135100/diagnostics")
def graph_studio_path_analysis_v0135100_diagnostics(): return path135100.diagnostics_contract()
@app.get("/v1/graph-studio-path-analysis/v0135100/acceptance")
def graph_studio_path_analysis_v0135100_acceptance(): return path135100.acceptance_report()

# Lab v0.135.11.0 — Provenance Hypothesis Comparison, Competing Paths & Evidence Context
@app.get("/v1/graph-studio-competing-paths/v0135110/health")
def graph_studio_competing_paths_v0135110_health(): return paths135110.health()
@app.post("/v1/graph-studio-competing-paths/v0135110/paths")
def graph_studio_competing_paths_v0135110_paths(payload: dict): return paths135110.enumerate_paths(payload)
@app.post("/v1/graph-studio-competing-paths/v0135110/compare")
def graph_studio_competing_paths_v0135110_compare(payload: dict): return paths135110.compare_paths(payload)
@app.post("/v1/graph-studio-competing-paths/v0135110/evidence-context")
def graph_studio_competing_paths_v0135110_evidence(payload: dict): return paths135110.evidence_context(payload)
@app.post("/v1/graph-studio-competing-paths/v0135110/context-packet")
def graph_studio_competing_paths_v0135110_context(payload: dict): return paths135110.context_packet(payload)
@app.get("/v1/graph-studio-competing-paths/v0135110/path-contract")
def graph_studio_competing_paths_v0135110_path_contract(): return paths135110.path_contract()
@app.get("/v1/graph-studio-competing-paths/v0135110/evidence-contract")
def graph_studio_competing_paths_v0135110_evidence_contract(): return paths135110.evidence_contract()
@app.get("/v1/graph-studio-competing-paths/v0135110/browser")
def graph_studio_competing_paths_v0135110_browser(): return paths135110.browser_contract()
@app.get("/v1/graph-studio-competing-paths/v0135110/diagnostics")
def graph_studio_competing_paths_v0135110_diagnostics(): return paths135110.diagnostics_contract()
@app.get("/v1/graph-studio-competing-paths/v0135110/acceptance")
def graph_studio_competing_paths_v0135110_acceptance(): return paths135110.acceptance_report()

# Lab v0.135.12.0 — Provenance Review Threads, Path Annotations & Claim Linkage
@app.get("/v1/graph-studio-review-threads/v0135120/health")
def graph_studio_review_threads_v0135120_health(): return review135120.health()
@app.post("/v1/graph-studio-review-threads/v0135120/normalize")
def graph_studio_review_threads_v0135120_normalize(payload: dict): return review135120.normalize_thread(payload)
@app.post("/v1/graph-studio-review-threads/v0135120/summary")
def graph_studio_review_threads_v0135120_summary(payload: dict): return review135120.summarize_thread(payload)
@app.post("/v1/graph-studio-review-threads/v0135120/claim-links")
def graph_studio_review_threads_v0135120_claim_links(payload: dict): return review135120.claim_links(payload)
@app.post("/v1/graph-studio-review-threads/v0135120/review-packet")
def graph_studio_review_threads_v0135120_review_packet(payload: dict): return review135120.review_packet(payload)
@app.post("/v1/graph-studio-review-threads/v0135120/fingerprint")
def graph_studio_review_threads_v0135120_fingerprint(payload: dict): return review135120.fingerprint(payload)
@app.get("/v1/graph-studio-review-threads/v0135120/thread-contract")
def graph_studio_review_threads_v0135120_thread_contract(): return review135120.thread_contract()
@app.get("/v1/graph-studio-review-threads/v0135120/annotation-contract")
def graph_studio_review_threads_v0135120_annotation_contract(): return review135120.annotation_contract()
@app.get("/v1/graph-studio-review-threads/v0135120/browser")
def graph_studio_review_threads_v0135120_browser(): return review135120.browser_contract()
@app.get("/v1/graph-studio-review-threads/v0135120/acceptance")
def graph_studio_review_threads_v0135120_acceptance(): return review135120.acceptance_report()

# Lab v0.135.13.0 — Provenance Review Resolution, Revision Actions & Decision Lineage
@app.get("/v1/graph-studio-review-audit/v0135140/health")
def graph_studio_review_audit_v0135140_health(): return reviewaudit135140.health()
@app.post("/v1/graph-studio-review-audit/v0135140/normalize")
def graph_studio_review_audit_v0135140_normalize(payload: dict): return reviewaudit135140.normalize_record(payload)
@app.post("/v1/graph-studio-review-audit/v0135140/states")
def graph_studio_review_audit_v0135140_states(payload: dict): return reviewaudit135140.states(payload)
@app.post("/v1/graph-studio-review-audit/v0135140/transition")
def graph_studio_review_audit_v0135140_transition(payload: dict): return reviewaudit135140.transition(payload)
@app.post("/v1/graph-studio-review-audit/v0135140/verify-action")
def graph_studio_review_audit_v0135140_verify_action(payload: dict): return reviewaudit135140.verify_action(payload)
@app.post("/v1/graph-studio-review-audit/v0135140/audit-trail")
def graph_studio_review_audit_v0135140_audit_trail(payload: dict): return reviewaudit135140.audit_trail(payload)
@app.post("/v1/graph-studio-review-audit/v0135140/resolution-readiness")
def graph_studio_review_audit_v0135140_resolution_readiness(payload: dict): return reviewaudit135140.resolution_readiness(payload)
@app.post("/v1/graph-studio-review-audit/v0135140/integrity")
def graph_studio_review_audit_v0135140_integrity(payload: dict): return reviewaudit135140.integrity(payload)
@app.post("/v1/graph-studio-review-audit/v0135140/audit-packet")
def graph_studio_review_audit_v0135140_audit_packet(payload: dict): return reviewaudit135140.audit_packet(payload)
@app.post("/v1/graph-studio-review-audit/v0135140/fingerprint")
def graph_studio_review_audit_v0135140_fingerprint(payload: dict): return reviewaudit135140.fingerprint(payload)
@app.get("/v1/graph-studio-review-audit/v0135140/contract")
def graph_studio_review_audit_v0135140_contract(): return reviewaudit135140.contract()
@app.get("/v1/graph-studio-review-audit/v0135140/browser")
def graph_studio_review_audit_v0135140_browser(): return reviewaudit135140.browser_contract()
@app.get("/v1/graph-studio-review-audit/v0135140/acceptance")
def graph_studio_review_audit_v0135140_acceptance(): return reviewaudit135140.acceptance_report()

# Lab v0.135.15.0 — Verification Artifact Manifests & Review Evidence Bundles
@app.get("/v1/graph-studio-verification-artifacts/v0135150/health")
def graph_studio_verification_artifacts_v0135150_health(): return verification135150.health()
@app.post("/v1/graph-studio-verification-artifacts/v0135150/normalize-artifact")
def graph_studio_verification_artifacts_v0135150_normalize_artifact(payload: dict):
    try: return {"ok":True,"version":verification135150.VERSION,"artifact":verification135150.normalize_artifact(payload.get("artifact",payload))}
    except ValueError as e: return {"ok":False,"version":verification135150.VERSION,"error":str(e)}
@app.post("/v1/graph-studio-verification-artifacts/v0135150/normalize-bundle")
def graph_studio_verification_artifacts_v0135150_normalize_bundle(payload: dict): return verification135150.normalize_bundle(payload)
@app.post("/v1/graph-studio-verification-artifacts/v0135150/validate")
def graph_studio_verification_artifacts_v0135150_validate(payload: dict): return verification135150.validate_bundle(payload)
@app.post("/v1/graph-studio-verification-artifacts/v0135150/summary")
def graph_studio_verification_artifacts_v0135150_summary(payload: dict): return verification135150.summarize(payload)
@app.post("/v1/graph-studio-verification-artifacts/v0135150/fingerprint")
def graph_studio_verification_artifacts_v0135150_fingerprint(payload: dict): return verification135150.fingerprint(payload)
@app.post("/v1/graph-studio-verification-artifacts/v0135150/integrity")
def graph_studio_verification_artifacts_v0135150_integrity(payload: dict): return verification135150.integrity(payload)
@app.post("/v1/graph-studio-verification-artifacts/v0135150/event-bindings")
def graph_studio_verification_artifacts_v0135150_event_bindings(payload: dict): return verification135150.event_bindings(payload)
@app.post("/v1/graph-studio-verification-artifacts/v0135150/compare")
def graph_studio_verification_artifacts_v0135150_compare(payload: dict): return verification135150.compare(payload)
@app.post("/v1/graph-studio-verification-artifacts/v0135150/verification-packet")
def graph_studio_verification_artifacts_v0135150_verification_packet(payload: dict): return verification135150.verification_packet(payload)
@app.get("/v1/graph-studio-verification-artifacts/v0135150/contract")
def graph_studio_verification_artifacts_v0135150_contract(): return verification135150.contract()
@app.get("/v1/graph-studio-verification-artifacts/v0135150/browser")
def graph_studio_verification_artifacts_v0135150_browser(): return verification135150.browser_contract()
@app.get("/v1/graph-studio-verification-artifacts/v0135150/acceptance")
def graph_studio_verification_artifacts_v0135150_acceptance(): return verification135150.acceptance_report()

@app.get("/v1/graph-studio-review-resolution/v0135130/health")
def graph_studio_review_resolution_v0135130_health(): return resolution135130.health()
@app.post("/v1/graph-studio-review-resolution/v0135130/normalize")
def graph_studio_review_resolution_v0135130_normalize(payload: dict): return resolution135130.normalize_record(payload)
@app.post("/v1/graph-studio-review-resolution/v0135130/summary")
def graph_studio_review_resolution_v0135130_summary(payload: dict): return resolution135130.summarize(payload)
@app.post("/v1/graph-studio-review-resolution/v0135130/pending")
def graph_studio_review_resolution_v0135130_pending(payload: dict): return resolution135130.pending(payload)
@app.post("/v1/graph-studio-review-resolution/v0135130/revision-actions")
def graph_studio_review_resolution_v0135130_revision_actions(payload: dict): return resolution135130.revision_actions(payload)
@app.post("/v1/graph-studio-review-resolution/v0135130/decision-lineage")
def graph_studio_review_resolution_v0135130_decision_lineage(payload: dict): return resolution135130.decision_lineage(payload)
@app.post("/v1/graph-studio-review-resolution/v0135130/resolution-packet")
def graph_studio_review_resolution_v0135130_resolution_packet(payload: dict): return resolution135130.resolution_packet(payload)
@app.post("/v1/graph-studio-review-resolution/v0135130/fingerprint")
def graph_studio_review_resolution_v0135130_fingerprint(payload: dict): return resolution135130.fingerprint(payload)
@app.get("/v1/graph-studio-review-resolution/v0135130/contract")
def graph_studio_review_resolution_v0135130_contract(): return resolution135130.contract()
@app.get("/v1/graph-studio-review-resolution/v0135130/browser")
def graph_studio_review_resolution_v0135130_browser(): return resolution135130.browser_contract()
@app.get("/v1/graph-studio-review-resolution/v0135130/acceptance")
def graph_studio_review_resolution_v0135130_acceptance(): return resolution135130.acceptance_report()

