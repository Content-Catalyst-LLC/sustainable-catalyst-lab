# Sustainable Catalyst Lab v0.28.1

## Dataset Registry and Scientific Data Management

- New Project → Dataset registry workspace
- Structured data dictionaries, units, variable roles, source metadata, licenses, validation state, and dataset lineage
- CSV, JSON, and GeoJSON browser import
- NetCDF metadata-only registration
- Browser-local profiles and optional governed Python profiling
- Dataset bundle export
- Project-scoped derived-from relationships
- No automatic server-backed dataset persistence
