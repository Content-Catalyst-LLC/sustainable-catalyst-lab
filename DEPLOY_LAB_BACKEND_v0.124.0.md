# Deploy Lab backend v0.124.0

Upload `sustainable-catalyst-lab-backend-v0.124.0.zip` and `upgrade_lab_backend_v0_124_0_contabo.sh` to `/tmp`, then execute the upgrade script on the Contabo host. The verifier checks behavior-specific v0.124 fixtures and treats application-wide route totals as informational only.
