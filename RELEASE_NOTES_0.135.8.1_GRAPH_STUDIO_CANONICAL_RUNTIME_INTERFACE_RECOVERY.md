# Release Notes — Lab v0.135.8.1

**Graph Studio Canonical Runtime & Interface Recovery**

This repair release consolidates the accumulated Graph Studio presentation stack into one canonical runtime. The v0.135.1–v0.135.8 research surfaces now appear as eight mutually exclusive tabs instead of a vertically accumulated sequence beneath the same figure. Historical v0.75–v0.88 renderer/capability controls are retained as isolated, collapsed compatibility adapters.

The public Graph Studio identity is aligned to Lab v0.135.8.1 while the legacy 0.26.3.1 routing subsystem remains internal. The backend adds a 16-route canonical-runtime audit/health API for view ownership, lifecycle, capability isolation, asset ownership, DOM audits, identity, and scientific boundaries.

No scientific values, relationships, claims, evidence weight, validity judgments, or Core submissions are inferred or changed by the repair.
