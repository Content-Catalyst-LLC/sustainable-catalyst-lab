# Deploy Sustainable Catalyst Lab Backend v0.132.0

From macOS, upload the backend ZIP and deployment script to `/tmp`, SSH to the Contabo host, then execute `upgrade_lab_backend_v0_132_0_contabo.sh`.

The verifier checks Lab Compute Core, retained v0.131 Research Question & Hypothesis Workspace, Method Selection Intelligence health, Platform Core compatibility >=3.0.0, and real candidate/eligibility/decision/Core fixtures. Exact application-wide FastAPI route count is informational only.
