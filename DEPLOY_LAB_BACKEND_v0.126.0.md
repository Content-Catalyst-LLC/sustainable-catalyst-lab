# Deploy Lab backend v0.126.0

Upload `sustainable-catalyst-lab-backend-v0.126.0.zip` and `upgrade_lab_backend_v0_126_0_contabo.sh` to `/tmp` on the Contabo VPS, then run the upgrade script. The verifier preserves compose-managed data, validates Lab Compute Core, retained v0.125 causal research, v0.126 spatial/spatiotemporal behavior, and Platform Core compatibility `>=3.0.0`.
