(function(W,D){'use strict';const Lab=W.SCLab=W.SCLab||{},VERSION='0.47.0',KEY='scLabNavGroupsV0470';
 function load(){try{return JSON.parse(localStorage.getItem(KEY)||'{}')}catch(_){return{}}}function save(v){try{localStorage.setItem(KEY,JSON.stringify(v))}catch(_){}}
 function init(app){if(!app||app.dataset.interfaceV0470Mounted==='1')return;app.dataset.interfaceV0470Mounted='1';const state=load(),groups=[...app.querySelectorAll('[data-lab-nav-group]')];groups.forEach(group=>{const name=group.dataset.labNavGroup,toggle=group.querySelector('[data-lab-nav-group-toggle]');if(!toggle)return;const collapsed=state[name]===true;group.classList.toggle('is-collapsed',collapsed);toggle.setAttribute('aria-expanded',String(!collapsed));toggle.addEventListener('click',()=>{const next=!group.classList.contains('is-collapsed');group.classList.toggle('is-collapsed',next);toggle.setAttribute('aria-expanded',String(!next));state[name]=next;save(state);});toggle.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();toggle.click();}});group.querySelectorAll('[data-lab-module-button]').forEach(button=>button.addEventListener('click',()=>{if(group.classList.contains('is-collapsed')){group.classList.remove('is-collapsed');toggle.setAttribute('aria-expanded','true');state[name]=false;save(state);}}));});
  app.addEventListener('sc-lab:open-graph-studio',()=>{const g=app.querySelector('[data-lab-nav-group="Visualize"]');if(g?.classList.contains('is-collapsed'))g.querySelector('[data-lab-nav-group-toggle]')?.click();});
 }
 function boot(){D.querySelectorAll('.sc-lab-app').forEach(init)}if(D.readyState==='loading')D.addEventListener('DOMContentLoaded',boot);else boot();new MutationObserver(boot).observe(D.documentElement,{childList:true,subtree:true});Lab.InterfaceReorganizationV0470={version:VERSION,init};
})(window,document);
