(function(W,D){'use strict';
const VERSION='0.135.6'; const root=()=>D.querySelector('[data-lab-module="graph-studio"]'); const host=()=>{const r=root();if(!r)return null;let h=r.querySelector('[data-viz1356-session-root]');if(!h){const prev=r.querySelector('[data-viz1355-context-root]');h=D.createElement('div');h.setAttribute('data-viz1356-session-root','');(prev?.parentNode||r).insertBefore(h,prev?.nextSibling||null);}return h;};
const state={session:'visual-session:local',status:'ACTIVE',events:[],checkpoints:0,replay:'strict'}; const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function event(type,target){state.events.push({sequence:state.events.length,event_type:type,target_ref:target||null}); if(state.events.length>12)state.events=state.events.slice(-12); render();}
function render(){const h=host();if(!h)return;h.innerHTML=`<section class="sc-viz1356-head"><div><span>REPRODUCIBLE VISUAL SESSION · v${VERSION}</span><h4>Replay the analytical path, not just the final picture</h4><p>Capture ordered visual interactions, checkpoints, scene/context links, and reproducible replay plans while keeping UI history separate from scientific evidence.</p></div><div class="actions"><button data-v1356-checkpoint>CHECKPOINT</button><button data-v1356-replay>REPLAY PLAN</button></div></section><section class="sc-viz1356-status"><span>SESSION <b>${esc(state.session)}</b></span><span>STATE <b>${state.status}</b></span><span>EVENTS <b>${state.events.length}</b></span><span>CHECKPOINTS <b>${state.checkpoints}</b></span><span>MODE <b>${state.replay.toUpperCase()}</b></span></section><section class="sc-viz1356-body"><div><h5>Interaction lineage</h5>${state.events.length?`<ol>${state.events.map(e=>`<li><b>#${e.sequence}</b><span>${esc(e.event_type)}</span><em>${esc(e.target_ref||'presentation-state')}</em></li>`).join('')}</ol>`:'<p class="empty">Interact with the v0.135.4/v0.135.5 scene to populate a local lineage preview.</p>'}</div><aside><h5>Reproducibility boundary</h5><p>Interaction order records what the researcher did. It does <b>not</b> establish scientific causality, evidence weight, validity, or truth.</p><p>Replay plans restore declared presentation/reference state only.</p></aside></section>`;}
function mount(){const h=host();if(!h||h.dataset.mounted==='1')return;h.dataset.mounted='1';h.classList.add('sc-viz1356');render();h.addEventListener('click',e=>{if(e.target.closest('[data-v1356-checkpoint]')){state.checkpoints++;event('checkpoint','session:'+state.checkpoints)} if(e.target.closest('[data-v1356-replay]')){state.replay=state.replay==='strict'?'reference-only':state.replay==='reference-only'?'presentation-only':'strict';event('layout','replay:'+state.replay)}});const r=root();r?.addEventListener('click',e=>{const t=e.target.closest('[data-v1355-ref],[data-v1355-mode],[data-v1354-target],[data-v1354-drill]');if(t){const target=t.dataset.v1355Ref||t.dataset.v1355Mode||t.dataset.v1354Target||'scene';event(t.dataset.v1355Mode?'compare':t.dataset.v1355Ref?'focus':'drill',target);}},true);}
function boot(){mount();const r=root();if(r)new MutationObserver(()=>mount()).observe(r,{childList:true,subtree:true});} if(D.readyState==='loading')D.addEventListener('DOMContentLoaded',boot);else boot(); W.SCLabReproducibleVisualAnalysisSessionsV01356={version:VERSION,state,record:event,refresh:render};
})(window,document);
