/* Sustainable Catalyst Lab v0.135.12.0 — Project Workspace provenance review-thread handoff */
(function(W,D){'use strict';const VERSION='0.135.12.0',KEY='scLabGraphStudioReviewThreadV0135120';function root(){return D.querySelector('[data-lab-module="project-workspace"]')}function read(){if(W.SCLabGraphStudioReviewThreadContextV0135120)return W.SCLabGraphStudioReviewThreadContextV0135120;try{return JSON.parse(W.sessionStorage?.getItem(KEY)||'null')}catch(_){return null}}function esc(s){return String(s??'').replace(/[&<>"']/g,'')}function mount(){const r=root(),p=read();if(!r||!p?.thread?.id||r.dataset.gs135120ContextMounted==='1')return false;r.dataset.gs135120ContextMounted='1';const t=p.thread,a=t.annotations||[],note=D.createElement('div');note.className='sc-gs135120-project-context';note.dataset.gs135120ProjectContext='';note.innerHTML=`<strong>Graph Studio provenance review thread</strong><span>${esc(t.title||'Review thread')}</span><small>${esc(t.status||'draft')} · ${a.length} annotation${a.length===1?'':'s'} · ${esc(t.objectA?.label||t.objectA?.id||'A')} ↔ ${esc(t.objectB?.label||t.objectB?.id||'B')}</small>${a.length?`<ol>${a.map(x=>`<li><strong>${esc(x.kind)}</strong> · ${esc(x.scope)} · ${esc(x.targetId)}${x.claimRef?` · ${esc(x.claimRef)}`:''}<br>${esc(x.note)}</li>`).join('')}</ol>`:''}<p>Annotations are explicit reviewer-authored context. No truth ranking, causal inference, or evidence-weight inference is implied.</p><button type="button" data-gs135120-clear-project-context>Clear review context</button>`;const head=r.querySelector('.sc-lab-module-header,.sc-lab-panel-head');(head?.parentNode||r).insertBefore(note,head?.nextSibling||r.firstChild);note.querySelector('button')?.addEventListener('click',()=>{W.SCLabGraphStudioReviewThreadContextV0135120=null;try{W.sessionStorage?.removeItem(KEY)}catch(_){}note.remove()});return true}let n=0;function boot(){if(mount())return;if(++n<40)W.setTimeout(boot,100)}if(D.readyState==='loading')D.addEventListener('DOMContentLoaded',boot);else boot();W.SCLabProjectWorkspaceReviewThreadV0135120={version:VERSION,mount};})(window,document);
