# Deploy Lab backend v0.135.8

Use `sustainable-catalyst-lab-backend-v0.135.8.zip` with `upgrade_lab_backend_v0_135_8_contabo.sh`. The deployer preserves the existing data directory and environment, rebuilds the Lab container, verifies the retained v0.135.2–v0.135.7 visual research stack, validates v0.135.8 health and boundary fixtures, and checks Platform Core v3 compatibility.
