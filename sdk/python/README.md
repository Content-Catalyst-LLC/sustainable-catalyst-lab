# Sustainable Catalyst Lab Python SDK v0.38.2

A standard-library-only client for typed research handoffs, webhooks, and embeds.

```python
from sustainable_catalyst_lab import LabClient
client = LabClient("https://lab.example.org", api_key="...")
print(client.catalog())
```
