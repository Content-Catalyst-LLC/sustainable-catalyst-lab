# Deploy Lab backend v0.127.0

Upload `sustainable-catalyst-lab-backend-v0.127.0.zip` and `upgrade_lab_backend_v0_127_0_contabo.sh` to `/tmp`, then run the upgrade script on Contabo. The verifier checks retained v0.126 spatial health, v0.127 time-series health, Platform Core >= 3.0.0, and real time-series fixtures without gating on an exact application-wide route count.
