"use strict";const assert=require('node:assert/strict'),path=require('node:path');global.window={SCLab:{},setTimeout:()=>0,localStorage:{getItem:()=>null,setItem:()=>{}},location:{hash:''},addEventListener:()=>{}};require(path.resolve(__dirname,'../assets/js/modules/bioprocess-monitoring-control-v0222.js'));const api=window.SCLab.BioprocessMonitoringControl;assert.equal(api.VERSION,'0.22.2');assert.equal(api.contract.channels.length,8);assert.equal(api.contract.controllers.length,6);const rows=api.parseCsv('time,run,phase,dissolved_oxygen\n0,a,growth,50\n1,a,growth,42\n2,a,feed,15\n3,a,feed,45');const a=api.analyze(rows,'dissolved_oxygen',{low:20,high:100,rollingWindow:3});assert.equal(a.summary.actionCount,1);assert.equal(a.summary.validPointCount,4);const p=api.pid({setpoint:40,pv0:20,kp:1,ki:.2,kd:.05,duration:10,dt:.1});assert.ok(p.points.length>90);assert.ok(Number.isFinite(p.summary.finalProcessValue));const c=api.compare(api.parseCsv('time,run,biomass\n0,a,1\n1,a,2\n0,b,1.1\n1,b,2.2'),'biomass');assert.equal(c.summary.runCount,2);console.log('Lab v0.22.2 JS tests passed: 8 channels, 6 controllers, monitoring, PID, and run comparison.');