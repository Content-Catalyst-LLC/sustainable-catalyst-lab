const fs=require('fs'),path=require('path');const root=path.resolve(__dirname,'..');const client=fs.readFileSync(path.join(root,'assets/js/modules/compute-client.js'),'utf8');for(const [name,token] of Object.entries({capabilities:'capabilities()',coreMethods:'coreMethods()',runCore:'runCore(payload)'})){if(!client.includes(token)){throw new Error(`Missing ${name}`)}console.log(`PASS: ${name}`)}
