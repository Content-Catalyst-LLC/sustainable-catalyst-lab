# Deploy Lab backend v0.125.0

Upload `sustainable-catalyst-lab-backend-v0.125.0.zip` and `upgrade_lab_backend_v0_125_0_contabo.sh` to `/tmp` on Contabo, then run the upgrade script. The verifier checks retained v0.124 sensitivity analysis, v0.125 causal health, Platform Core >=3.0.0, required routes, and real causal-method fixtures. Global FastAPI route totals are informational only.
